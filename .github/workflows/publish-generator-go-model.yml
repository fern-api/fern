# Generators are registered within seed.yml, once their tests are passing
name: Publish Go Model Generator

on:
  push:
    branches:
      - main
    paths:
      - "generators/go/model/versions.yml"
  workflow_dispatch:
    inputs:
      version:
        description: "The version of the generator to publish."
        required: true
        type: string

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: automated publish
        if: ${{ github.event_name == 'push' }}
        uses: ./.github/actions/publish-generator
        with:
          generator-name: "go-model"
          version-file: "generators/go/model/versions.yml"
      - name: manual publish
        if: ${{ github.event_name == 'workflow_dispatch' }}
        uses: ./.github/actions/publish-generator
        with:
          generator-name: "go-model"
          version: ${{ inputs.version }}
          manual-trigger: "true"
