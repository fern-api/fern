name: Go Generator

on:
  pull_request:
    paths:
      - "generators/go/**"
    branches:
      - main
  workflow_call:

env:
  DO_NOT_TRACK: "1"

jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v5

      - name: Compile
        working-directory: ./generators/go
        run: go build ./...

  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v5

      - name: Run Tests
        working-directory: ./generators/go
        run: make test
