## FastAPI Generator v1.6.1
Fixed critical issues in Python FastAPI server generation to better match your Fern specification.

```python
# Generated endpoints now correctly handle path parameters
@router.get("/users/{user_id}/posts/{post_id}")
async def get_user_post(user_id: str, post_id: str):
    return {"user_id": user_id, "post_id": post_id}
```

## Enhanced Edge Functions
Optimized URL parameter handling in edge functions for better performance and reliability.

```typescript
// New URLSearchParams handling
const params = new URLSearchParams({
  filter: 'active',
  sort: 'created_at'
}).toString();
```

## Documentation Controls
Control documentation layout with new frontmatter options.

```yaml
---
title: API Reference
hide-toc: true
---
```

_Image suggestion: Screenshot comparing docs layout with and without TOC_