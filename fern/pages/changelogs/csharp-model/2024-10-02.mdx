## 1.0.0
**`(break):`** The C# SDK now uses forward compatible enums which are not compatible with the previously generated enums. Set `enable-forward-compatible-enums` to `false` in the configuration to generate the old enums.

**`(feat):`** Add forward compatible enums

