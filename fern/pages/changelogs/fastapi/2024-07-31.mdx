## 1.2.0
**`(feat):`** The FastAPI `async_handlers` configuration now accepts a list of string or a boolean so that users can turn on async handlers for specific endpoints.

## 1.1.0-rc0
**`(internal):`** The generator now consumes IRv53.

