## 0.11.1
**`(fix):`** Expose the `use_str_enums` flag.

### What's been fixed
- The generator now respects the `use_str_enums` flag, and defaults it to `False`.

## 0.11.0
**`(fix):`** Correctly sanitize parameter descriptions.

### What's been fixed
- The generator now correctly sanitizes parameter descriptions. Previously it was duplicating strings.

