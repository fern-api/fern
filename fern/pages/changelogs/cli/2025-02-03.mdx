## 0.51.19
**`(fix):`** The OpenRPC parser now supports generating code snippets in the API Explorer.


## 0.51.21
**`(feat):`** The `fern init` command now respects JSON formatting when parsing from an external URL. 