## 0.47.2
**`(feat):`** The CLI now supports checking for broken links in your docs. You will see warnings in `fern check` if your docs link to any page that can't be resolved, and
you can also run `fern docs broken-links` to only check for broken links.


