## 0.14.1
**`(fix):`** Generate code snippets for file upload endpoints

**`(fix):`** Create simple jest tests when integration tests not enabled

**`(feat):`** Improve JSR compatibility with direct file imports

**`(fix):`** Fix import for node-fetch in Fetcher.ts for ESM environments

**`(fix):`** Make test job in ci.yml work without integration tests

**`(feat):`** Add allowExtraFields option for serialized requests

