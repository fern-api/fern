---
title: fern ir
description: The command "fern ir" generates an Immediate Representation (IR).
---

Use `fern ir <path-to-output>` generates an Immediate Representation (IR).

## Usage

```bash
fern ir <path-to-output>
```