# yaml-language-server: $schema=https://raw.githubusercontent.com/fern-api/fern/main/fern.schema.json

types:
  Constants:
    properties:
      errors: ErrorConstants
  ErrorConstants:
    properties:
      errorDiscriminant: string
      errorInstanceIdKey: string
      errorContentKey: string
