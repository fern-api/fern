types:
  AIServicesSchema:
    properties:
      provider: ModelProvider
      model: ModelName

  ModelProvider:
    enum:
      - openai
      - anthropic
      - value: aws-bedrock
        name: AwsBedrock

  ModelName: string
