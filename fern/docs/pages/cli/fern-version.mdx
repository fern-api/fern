Use `fern version` to check the currently installed version.

## Usage

```bash
$ fern --version
```

## Example outputs 

```bash
$ fern --version
0.14.3
```

```bash
$ fern --version
0.15.0-rc53
```