---
title: Use fern register to register your API
description: The command "fern register" registers your API so it can be depended on by other APIs.
---

`fern register ` You can register your API so it can be depended on by other APIs. Read more in the Depending on other APIs section.

## Usage

```bash
fern register
```
