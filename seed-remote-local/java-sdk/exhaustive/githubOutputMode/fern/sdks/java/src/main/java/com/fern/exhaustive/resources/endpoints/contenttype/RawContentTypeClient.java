/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.fern.exhaustive.resources.endpoints.contenttype;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fern.exhaustive.core.ClientOptions;
import com.fern.exhaustive.core.FernExhaustiveApiException;
import com.fern.exhaustive.core.FernExhaustiveException;
import com.fern.exhaustive.core.FernExhaustiveHttpResponse;
import com.fern.exhaustive.core.ObjectMappers;
import com.fern.exhaustive.core.RequestOptions;
import com.fern.exhaustive.resources.types.object.types.ObjectWithOptionalField;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.MediaType;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawContentTypeClient {
    protected final ClientOptions clientOptions;

    public RawContentTypeClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public FernExhaustiveHttpResponse<Void> postJsonPatchContentType() {
        return postJsonPatchContentType(ObjectWithOptionalField.builder().build());
    }

    public FernExhaustiveHttpResponse<Void> postJsonPatchContentType(ObjectWithOptionalField request) {
        return postJsonPatchContentType(request, null);
    }

    public FernExhaustiveHttpResponse<Void> postJsonPatchContentType(
            ObjectWithOptionalField request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("foo")
                .addPathSegments("bar")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request),
                    MediaType.parse("application/json-patch+json"));
        } catch (JsonProcessingException e) {
            throw new FernExhaustiveException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json-patch+json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new FernExhaustiveHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new FernExhaustiveApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new FernExhaustiveException("Network error executing HTTP request", e);
        }
    }

    public FernExhaustiveHttpResponse<Void> postJsonPatchContentWithCharsetType() {
        return postJsonPatchContentWithCharsetType(
                ObjectWithOptionalField.builder().build());
    }

    public FernExhaustiveHttpResponse<Void> postJsonPatchContentWithCharsetType(ObjectWithOptionalField request) {
        return postJsonPatchContentWithCharsetType(request, null);
    }

    public FernExhaustiveHttpResponse<Void> postJsonPatchContentWithCharsetType(
            ObjectWithOptionalField request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("foo")
                .addPathSegments("baz")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request),
                    MediaType.parse("application/json-patch+json; charset=utf-8"));
        } catch (JsonProcessingException e) {
            throw new FernExhaustiveException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json-patch+json; charset=utf-8")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new FernExhaustiveHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new FernExhaustiveApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new FernExhaustiveException("Network error executing HTTP request", e);
        }
    }
}
