/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as core from "./core";
import { Hello } from "./api/resources/hello/client/Client";

export declare namespace SmokeTestTestApiClient {
    interface Options {
        environment: core.Supplier<string>;
    }

    interface RequestOptions {
        /** The maximum time to wait for a response in seconds. */
        timeoutInSeconds?: number;
        /** The number of times to retry the request. Defaults to 2. */
        maxRetries?: number;
        /** A hook to abort the request. */
        abortSignal?: AbortSignal;
    }
}

export class SmokeTestTestApiClient {
    constructor(protected readonly _options: SmokeTestTestApiClient.Options) {}

    protected _hello: Hello | undefined;

    public get hello(): Hello {
        return (this._hello ??= new Hello(this._options));
    }
}
