types:
  Organization:
    properties:
      id: string
      name: string

service:
  auth: false
  base-path: ""
  endpoints:
    check:
      path: /organization/{id}
      path-parameters:
        id: string
      method: GET
      auth: true
      response: Organization
