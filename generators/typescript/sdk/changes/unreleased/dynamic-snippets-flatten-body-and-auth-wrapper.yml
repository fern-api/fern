# yaml-language-server: $schema=../../../../../fern-changes-yml.schema.json

- summary: |
    TypeScript dynamic snippets honor `flattenRequestParameters` by spreading referenced object bodies instead of emitting `body: {...}`, and nest multi-auth constructor options under the auth scheme key.
  type: fix
