- summary: |
    Strip SDK-injected auth headers on cross-origin HTTP redirects to prevent
    credential leakage. The fetcher now manually follows redirects and removes
    auth headers when the redirect target differs in scheme, host, or port.
  type: fix
