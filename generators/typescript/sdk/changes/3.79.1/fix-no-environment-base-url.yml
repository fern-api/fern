- summary: |
    Generated snippets now use `baseUrl` instead of `environment` when
    no environments are defined in the API spec, matching the actual
    constructor parameter users should provide.
  type: fix
