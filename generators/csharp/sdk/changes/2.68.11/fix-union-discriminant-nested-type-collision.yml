- summary: |
    Fixed a C# compile error (CS0102) for discriminated unions whose discriminant property name
    collides with one of the union's variant type names. The colliding nested variant type is now
    suffixed with `Inner` (e.g. `EventInner`), consistent with the existing handling for `Value`/`Type`.
  type: fix
