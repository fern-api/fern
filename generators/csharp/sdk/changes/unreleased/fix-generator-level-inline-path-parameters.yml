- summary: |
    When `inline-path-parameters: false` is set in the generator config, the C# generator now
    correctly elides wrapper request classes that would only contain path parameters, matching
    the behavior of the spec-level setting. Wrappers that contained only a body reference alongside
    path parameters are also unwrapped to pass the body type directly.
  type: fix
