- summary: |
    Wire OAuth client-credentials auth into generated CLIs from the Fern IR.
    The generator reads the IR's `oauth` client-credentials scheme (token
    endpoint, client id/secret env vars, and scopes) and emits the runtime
    `OAuth2Auth` builder, so users no longer configure it separately.
  type: feat
