# yaml-language-server: $schema=../../../../fern-changes-yml.schema.json

- summary: |
    Embedded types generation no longer fails on large APIs. The rust-model subprocess
    is no longer killed after a fixed 120 second timeout, and its stderr is now surfaced
    in the error message when it does fail.
  type: fix
