- summary: |
    Write the LICENSE file into the generated crate when a custom license is configured
    (`github.license.custom` or `metadata.license.custom`), matching the other SDK generators.
    `Cargo.toml` now also falls back to the configured license when neither `packageLicense`
    nor `packageLicenseFile` is set, instead of always defaulting to MIT.
  type: fix
