- summary: |
    Make the generated Maven Central publish configuration compatible with Java 8 builds by only resolving and applying the sonatype-central-upload plugin (which requires a Java 11+ runtime) when the build runs on Java 11 or newer.
  type: fix
