# yaml-language-server: $schema=../../../../../fern-changes-yml.schema.json

- summary: |
    Fix two Java SDK compile errors. The raw client no longer generates a request-less
    endpoint overload (calling `Request.builder().build()`) when the wrapped request has a
    required service-level header, and dynamic snippets now fully-qualify class references
    when two types share the same simple name instead of emitting clashing imports.
  type: fix
