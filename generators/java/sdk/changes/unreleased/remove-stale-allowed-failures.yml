# yaml-language-server: $schema=../../../../../fern-changes-yml.schema.json

- summary: |
    Re-enable seed test coverage for the endpoint-security-auth, exhaustive
    publish-to/signed_publish, and pagination-custom fixtures, which now
    generate and compile cleanly.
  type: fix
