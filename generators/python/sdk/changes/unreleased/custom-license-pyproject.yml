# yaml-language-server: $schema=../../../../../fern-changes-yml.schema.json

- summary: |
    Custom licenses (`metadata.license: { custom: "./LICENSE" }`) are now declared in the
    generated `pyproject.toml` via `license = { file = "LICENSE" }` in the `[project]` table,
    complementing the existing `license = "MIT"` / `"Apache-2.0"` support.
  type: feat
