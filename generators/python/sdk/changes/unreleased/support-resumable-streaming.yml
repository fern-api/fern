- summary: |
    Support `resumable` flag in SSE streaming endpoints. When an endpoint has
    `x-fern-streaming.resumable: true`, the generated SDK now passes `resumable=True`
    to `EventSource`, enabling client-side reconnection logic.
  type: feat
