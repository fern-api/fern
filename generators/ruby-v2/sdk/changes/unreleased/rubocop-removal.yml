- summary: |
    Fix Ruby string interpolation injection vulnerability in TypeLiteral string emitter.
    Untrusted API spec default values containing #{} are now properly escaped in generated
    double-quoted Ruby strings.
  type: fix
- summary: |
    Generate fully rubocop-compliant code, eliminating all remaining rubocop offenses
    in generated output. This enables removing rubocop -A auto-correction from the
    generation pipeline. Added rubocop check-only as a CI regression gate.
  type: feat
