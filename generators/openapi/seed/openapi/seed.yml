irVersion: v23
docker: fern-openapi:latest
dockerCommand: docker build -f ./Dockerfile -t fern-openapi:latest .
generatorType: documentation
fixtures:
  imdb:
    - customConfig: null
      outputFolder: no-custom-config
    - customConfig:
        customOverrides:
          info: 
            license: MIT
      outputFolder: custom-overrides
    - customConfig:
        format: json
      outputFolder: json-format
