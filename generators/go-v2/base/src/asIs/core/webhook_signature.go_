// Code generated by Fern. DO NOT EDIT.

package core

import (
	"crypto/hmac"
	"crypto/sha1"
	"crypto/sha256"
	"crypto/sha512"
	"encoding/base64"
	"encoding/hex"
	"fmt"
	"hash"
)

// ComputeHmacSignature computes the HMAC signature of a payload using the given
// secret, algorithm ("sha1", "sha256", "sha384", or "sha512"), and encoding
// ("base64" or "hex").
func ComputeHmacSignature(payload string, secret string, algorithm string, encoding string) (string, error) {
	var newHash func() hash.Hash
	switch algorithm {
	case "sha1":
		newHash = sha1.New
	case "sha256":
		newHash = sha256.New
	case "sha384":
		newHash = sha512.New384
	case "sha512":
		newHash = sha512.New
	default:
		return "", fmt.Errorf("unrecognized HMAC algorithm: %q", algorithm)
	}

	mac := hmac.New(newHash, []byte(secret))
	if _, err := mac.Write([]byte(payload)); err != nil {
		return "", err
	}
	sum := mac.Sum(nil)

	switch encoding {
	case "hex":
		return hex.EncodeToString(sum), nil
	case "base64":
		return base64.StdEncoding.EncodeToString(sum), nil
	default:
		return "", fmt.Errorf("unrecognized webhook signature encoding: %q", encoding)
	}
}

// TimingSafeEqual compares two strings in constant time to avoid leaking
// information through timing.
func TimingSafeEqual(a string, b string) bool {
	return hmac.Equal([]byte(a), []byte(b))
}
