types:
  DummyId: string
services:
  http:
    DummyService:
      auth: false
      base-path: /dummy
      endpoints:
        getDummy:
          method: POST
          path: /
          request: DummyId
        health:
          method: GET
          path: /health