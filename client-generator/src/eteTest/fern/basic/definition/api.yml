name: basic
auth: bearer
environments:
  prod: prod.com
  staging: staging.com
default-environment: prod
headers:
  API-VERSION:
    name: apiVersion
    type: string
    docs: The version of the API that you are on.
variables:
  appName:
    type: string
error-discrimination:
  strategy: status-code