openapi: 3.1.0
info:
  title: webhooks
  version: 0.0.0
paths: {}
components:
  schemas:
    UserCreatedPayload:
      title: UserCreatedPayload
      type: object
      properties:
        userId:
          type: string
        email:
          type: string
        createdAt:
          type: string
      required:
        - userId
        - email
        - createdAt
    OrderCompletedPayload:
      title: OrderCompletedPayload
      type: object
      properties:
        orderId:
          type: string
        total:
          type: number
          format: double
        currency:
          type: string
      required:
        - orderId
        - total
        - currency
    PaymentNotificationPayload:
      title: PaymentNotificationPayload
      type: object
      properties:
        paymentId:
          type: string
        amount:
          type: number
          format: double
        status:
          type: string
      required:
        - paymentId
        - amount
        - status
    RefundProcessedPayload:
      title: RefundProcessedPayload
      type: object
      properties:
        refundId:
          type: string
        amount:
          type: number
          format: double
        reason:
          type:
            - string
            - 'null'
      required:
        - refundId
        - amount
  securitySchemes: {}
