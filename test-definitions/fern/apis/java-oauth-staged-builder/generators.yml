# yaml-language-server: $schema=https://schema.buildwithfern.dev/generators-yml.json
# This test fixture is Java-specific and tests that OAuthTokenSupplier
# generates builder calls in the correct staged builder order when
# the token endpoint uses a referenced request type (not inlined).
groups: {}
