types:
  WeatherReport:
    enum:
      - SUNNY
      - CLOUDY
      - RAINING
      - SNOWING

  # Object with optional enum field to test wire test generation
  # when example doesn't provide a value for the enum
  ObjectWithOptionalEnum:
    properties:
      string: string
      weather: optional<WeatherReport>

errors:
  ErrorWithEnumBody:
    status-code: 400
    type: WeatherReport #does this even make sense? the type of the error body would be enum, and it could only be one of the 4 values?
