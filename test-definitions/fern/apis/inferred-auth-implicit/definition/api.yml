name: inferred-auth-explicit
imports:
  auth: auth.yml

auth: InferredAuthScheme
auth-schemes:
  InferredAuthScheme:
    scheme: inferred
    get-token:
      endpoint: auth.getTokenWithClientCredentials