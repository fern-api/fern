name: go-optional-header-env
auth: Bearer
auth-schemes:
  Bearer:
    scheme: bearer
    token:
      name: apiKey
      env: COURIER_API_KEY
headers:
  X-API-Version:
    name: version
    type: optional<string>
    env: MY_API_VERSION
