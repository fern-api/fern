# yaml-language-server: $schema=https://schema.buildwithfern.dev/generators-yml.json
api:
  specs:
    - openapi: ./openapi.yml
groups:
  go-sdk:
    generators:
      - name: fernapi/fern-go-sdk
        version: latest
        github:
          token: ${GITHUB_TOKEN}
          mode: push
          uri: fern-api/go-sdk-tests
          branch: openapi-pdf-download
