# yaml-language-server: $schema=https://schema.buildwithfern.dev/generators-yml.json
auth-schemes:
  OAuth2:
    scheme: oauth
    type: client-credentials
    client-id-env: ACME_CLIENT_ID
    client-secret-env: ACME_CLIENT_SECRET
    token-header: X-Access-Token
    token-prefix: ""
    scopes:
      - read:pets
    get-token:
      endpoint: POST /token
      request-properties:
        client-id: $request.credentials.client_id
        client-secret: $request.credentials.client_secret
        scopes: $request.credentials.scopes
      response-properties:
        access-token: $response.result.access_token
        expires-in: $response.result.expires_in
        refresh-token: $response.result.refresh_token
    refresh-token:
      endpoint: POST /refresh
      request-properties:
        refresh-token: $request.refresh_token
      response-properties:
        access-token: $response.result.access_token
        expires-in: $response.result.expires_in
        refresh-token: $response.result.refresh_token
api:
  auth: OAuth2
  specs:
    - openapi: ./openapi.yml

groups:
  cli:
    generators:
      - name: fernapi/fern-cli-generator
        version: latest
        ir-version: latest
