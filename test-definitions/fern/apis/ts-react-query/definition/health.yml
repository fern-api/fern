service:
  base-path: /health
  auth: false
  endpoints:
    check:
      method: HEAD
      path: ""
      docs: Health check endpoint
    
    details:
      method: GET
      path: /details
      response: string
      docs: Get health details
