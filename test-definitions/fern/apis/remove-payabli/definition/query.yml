imports:
  root: __package__.yml
  queryTypes: __queryTypes__.yml

service:
  auth: false
  base-path: ''
  endpoints:
    ListBatchDetails:
      auth: true
      display-name: >-
        Get a list of settled and unsettled transactions in batches for a
        paypoint
      docs: |
        Retrieve a list of batches and their details, including settled and
        unsettled transactions for a paypoint. Use filters to limit results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response. 
      examples:
        - name: ListBatchExampleRecord
          path-parameters:
            entry: 8cfec329267
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                - Id: 25048
                  Method: ach
                  WalletType: null
                  SettledAmount: 0.5
                  Type: credit
                  BatchNumber: batch-100-20-2024
                  BatchAmount: 32
                  PaymentTransId: 245-9e4072eef77e45979ea0e49f680000X
                  PaymentTransStatus: 1
                  ScheduleReference: 0
                  GatewayTransId: TRN_XXXXX
                  OrderId: ''
                  TransMethod: ach
                  PaymentData:
                    AccountType: Checking
                    HolderName: Lydia Marshall
                    MaskedAccount: 1XXXXXX5678
                    paymentDetails:
                      categories:
                        - amount: 1000
                          label: Deposit
                      currency: USD
                      serviceFee: 0
                      splitFunding:
                        - {}
                      totalAmount: 2
                  NetAmount: 2
                  Operation: Sale
                  Category: auth
                  Source: api
                  Status: 1
                  TransactionTime: '2024-11-19T15:58:01Z'
                  Customer:
                    AdditionalData: null
                    BillingAddress1: 100 Golden Ridge Drive
                    BillingAddress2: STE 100
                    BillingCity: Mendota
                    BillingCountry: US
                    BillingEmail: lydia@example.com
                    BillingPhone: '+12345678'
                    BillingState: VA
                    BillingZip: '20147'
                    customerId: 2707
                    CustomerNumber: '901102'
                    customerStatus: 1
                    FirstName: Lydia
                    LastName: Marshall
                  SettlementDate: '2024-11-20T00:00:00Z'
                  PaymentSettlementStatus: 1
                  BatchStatus: 1
                  DepositDate: '2024-11-22T00:00:00Z'
                  ExpectedDepositDate: '2024-11-22T00:00:00Z'
                  MaskedAccount: 1XXXXXX5678
                  CreatedAt: '2024-11-19T15:58:01Z'
                  PaypointLegalname: Gruzya Adventure Outfitters, LLC
                  ResponseData:
                    authcode: ''
                    avsresponse_text: ''
                    cvvresponse_text: ''
                    response_code: '100'
                    response_code_text: Operation successful.
                    responsetext: CAPTURED
                    transactionid: TRN_XXXXX
                  PaypointDbaname: Gruzya Adventure Outfitters, LLC
                  ParentOrgName: Pilgrim Planner
                  ParentOrgId: 123
                  PaypointEntryname: 7f1a3816XX
                  DeviceId: ''
                  RetrievalId: 0
                  ChargebackId: 0
                  AchHolderType: personal
                  AchSecCode: PPD
                  ConnectorName: DefaultConnector
                  EntrypageId: 0
                  FeeAmount: 0
                  OrgId: 123
                  PayorId: 2707
                  PaypointId: 123
                  PendingFeeAmount: 0
                  RefundId: 0
                  ReturnedId: 0
                  splitFundingInstructions: []
                  TotalAmount: 2
                  CfeeTransactions: []
                  invoiceData: null
                  TransactionEvents:
                    - EventTime: '2024-11-19T15:57:40Z'
                      TransEvent: Created
                    - EventData:
                        account_id: TRA_XXXXX
                        account_name: '123456'
                        action:
                          app_id: XXXXX
                          app_name: PayAbli
                          id: ACT_XXXXX
                          result_code: SUCCESS
                          time_created: '2024-11-19T20:58:01.583Z'
                          type: AUTHORIZE
                        amount: '200'
                        batch_id: ''
                        capture_mode: AUTO
                        channel: CNP
                        country: US
                        currency: USD
                        fees:
                          amount: '0'
                          rate: '0.00'
                          total_amount: '0'
                        id: TRN_XXXXX
                        merchant_amount: '200'
                        merchant_id: MER_XXXXX
                        merchant_name: Henriette97
                        order:
                          reference: ''
                        payment_method:
                          bank_transfer:
                            account_type: CHECKING
                            bank:
                              name: ''
                            masked_account_number_last4: XXXX5678
                          entry_mode: ECOM
                          message: Success
                          narrative: Lydia Marshall
                          result: '00'
                        reference: 245-XXXXX
                        status: CAPTURED
                        time_created: '2024-11-19T20:58:01.583Z'
                        type: SALE
                      EventTime: '2024-11-19T20:58:01Z'
                      TransEvent: Approved
                    - EventTime: '2024-11-20T03:05:10Z'
                      TransEvent: ClosedBatch
                  externalPaypointID: ext-paypoint-123
                  isHold: 0
              Summary:
                serviceFees: 852.48
                transferAmount: 0
                refunds: -3521.85
                heldAmount: 3.7
                totalRecords: 21872
                totalAmount: 61645.74
                totalNetAmount: 61645.74
                totalPages: 21872
                pageSize: 0
                pageidentifier: null
      method: GET
      path: /Query/batchDetails/{entry}
      path-parameters:
        entry: root.Entry
      request:
        name: ListBatchDetailsRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >

              Collection of field names, conditions, and values used to filter
              the query. 

              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>
              
              
              See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for more information.


              **List of field names accepted:**


              - `settlementDate` (gt, ge, lt, le, eq, ne)

              - `depositDate` (gt, ge, lt, le, eq, ne)

              - `transId`  (ne, eq, ct, nct)

              - `gatewayTransId`  (ne, eq, ct, nct)

              - `method`   (in, nin, eq, ne)

              - `settledAmount`  (gt, ge, lt, le, eq, ne)

              - `operation`    (in, nin, eq, ne)

              - `source`   (in, nin, eq, ne)

              - `batchNumber`  (ct, nct, eq, ne)

              - `payaccountLastfour`   (nct, ct)

              - `payaccountType`   (ne, eq, in, nin)

              - `customerFirstname`   (ct, nct, eq, ne)

              - `customerLastname`    (ct, nct, eq, ne)

              - `customerName`   (ct, nct)

              - `customerId`  (eq, ne)

              - `customerNumber`  (ct, nct, eq, ne)

              - `customerCompanyname`    (ct, nct, eq, ne)

              - `customerAddress` (ct, nct, eq, ne)

              - `customerCity`    (ct, nct, eq, ne)

              - `customerZip` (ct, nct, eq, ne)

              - `customerState` (ct, nct, eq, ne)

              - `customerCountry` (ct, nct, eq, ne)

              - `customerPhone` (ct, nct, eq, ne)

              - `customerEmail` (ct, nct, eq, ne)

              - `customerShippingAddress` (ct, nct, eq, ne)

              - `customerShippingCity`    (ct, nct, eq, ne)

              - `customerShippingZip` (ct, nct, eq, ne)

              - `customerShippingState` (ct, nct, eq, ne)

              - `customerShippingCountry` (ct, nct, eq, ne)

              - `orgId`  (eq) *mandatory when entry=org*

              - `isHold` (eq, ne)

              - `paypointId`  (ne, eq)

              - `paypointLegal`  (ne, eq, ct, nct)

              - `paypointDba`  (ne, eq, ct, nct)

              - `orgName`  (ne, eq, ct, nct)

              - `batchId` (ct, nct, eq, neq)

              - `additional-xxx`  (ne, eq, ct, nct) where xxx is the additional
              field name


              **List of comparison accepted:**

              - `eq` or empty => equal

              - `gt` => greater than

              - `ge` => greater or equal

              - `lt` => less than

              - `le` => less or equal

              - `ne` => not equal

              - `ct` => contains

              - `nct` => not contains

              - `in` => inside array separated by "|"

              - `nin` => not inside array separated by "|"


              **List of parameters accepted:**


              - `limitRecord`: max number of records for query (default="20",
              "0" or negative value for all)

              - `fromRecord`: initial record in query


              Example: `settledAmount(gt)=20` returns all records with a `settledAmount` greater than 20.00.
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: queryTypes.QueryBatchesDetailResponse
      
    ListBatchDetailsOrg:
      auth: true
      display-name: Get list of settled and unsettled transactions in batches for an org
      docs: >-
        Retrieve a list of batches and their details, including settled and
        unsettled transactions for an organization. Use filters to limit
        results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.
      examples:
        - name: Example query response
          path-parameters:
            orgId: 123
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                - BatchAmount: 32
                  BatchNumber: batch-100-20-2024
                  Category: auth
                  CreatedAt: '2024-11-19T15:58:01Z'
                  Customer:
                    AdditionalData: AdditionalData
                    BillingAddress1: 100 Golden Ridge Drive
                    BillingAddress2: STE 100
                    BillingCity: Mendota
                    BillingCountry: US
                    BillingEmail: lydia@example.com
                    BillingPhone: '+12345678'
                    BillingState: VA
                    BillingZip: '20147'
                    customerId: 2707
                    CustomerNumber: '901102'
                    customerStatus: 1
                    FirstName: Lydia
                    LastName: Marshall
                  ExpectedDepositDate: '2024-11-22T00:00:00Z'
                  GatewayTransId: TRN_XXXXX
                  Id: 25048
                  isHold: 0
                  MaskedAccount: 1XXXXXX5678
                  Method: ach
                  NetAmount: 2
                  Operation: Sale
                  OrderId: ''
                  ParentOrgName: Pilgrim Planner
                  PaymentData:
                    AccountType: Checking
                    HolderName: Lydia Marshall
                    MaskedAccount: 1XXXXXX5678
                    paymentDetails:
                      categories:
                        - amount: 1000
                          label: Deposit
                      currency: USD
                      serviceFee: 0
                      splitFunding:
                        - {}
                      totalAmount: 2
                  PaymentTransId: 245-9e4072eef77e45979ea0e49f680000X
                  PaymentTransStatus: 1
                  PaypointDbaname: Gruzya Adventure Outfitters, LLC
                  PaypointEntryname: 7f1a3816XX
                  PaypointLegalname: Gruzya Adventure Outfitters, LLC
                  ResponseData:
                    authcode: ''
                    avsresponse_text: ''
                    cvvresponse_text: ''
                    response_code: '100'
                    response_code_text: Operation successful.
                    responsetext: CAPTURED
                    transactionid: TRN_XXXXX
                  ScheduleReference: 0
                  SettledAmount: 0.5
                  SettlementDate: '2024-11-20T00:00:00Z'
                  Source: api
                  Status: 1
                  TransactionEvents:
                    - EventTime: '2024-11-19T15:57:40Z'
                      TransEvent: Created
                    - EventData:
                        account_id: TRA_XXXXX
                        account_name: '123456'
                        action:
                          app_id: XXXXX
                          app_name: PayAbli
                          id: ACT_XXXXX
                          result_code: SUCCESS
                          time_created: '2024-11-19T20:58:01.583Z'
                          type: AUTHORIZE
                        amount: '200'
                        batch_id: ''
                        capture_mode: AUTO
                        channel: CNP
                        country: US
                        currency: USD
                        fees:
                          amount: '0'
                          rate: '0.00'
                          total_amount: '0'
                        id: TRN_XXXXX
                        merchant_amount: '200'
                        merchant_id: MER_XXXXX
                        merchant_name: Henriette97
                        order:
                          reference: ''
                        payment_method:
                          bank_transfer:
                            account_type: CHECKING
                            bank:
                              name: ''
                            masked_account_number_last4: XXXX5678
                          entry_mode: ECOM
                          message: Success
                          narrative: Lydia Marshall
                          result: '00'
                        reference: 245-XXXXX
                        status: CAPTURED
                        time_created: '2024-11-19T20:58:01.583Z'
                        type: SALE
                      EventTime: '2024-11-19T20:58:01Z'
                      TransEvent: Approved
                    - EventTime: '2024-11-20T03:05:10Z'
                      TransEvent: ClosedBatch
                  TransactionTime: '2024-11-19T15:58:01Z'
                  TransMethod: ach
                  Type: credit
              Summary:
                heldAmount: 3.7
                pageSize: 0
                refunds: -3521.85
                serviceFees: 852.48
                totalAmount: 61645.74
                totalNetAmount: 61645.74
                totalPages: 21872
                totalRecords: 21872
                transferAmount: 0
      method: GET
      path: /Query/batchDetails/org/{orgId}
      path-parameters:
        orgId:
          docs: The numeric identifier for organization, assigned by Payabli.
          type: integer
      request:
        name: ListBatchDetailsOrgRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >

              Collection of field names, conditions, and values used to filter
              the query. 
              
              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>

              See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for more information.


              **List of field names accepted:**


              - `settlementDate` (gt, ge, lt, le, eq, ne)

              - `depositDate` (gt, ge, lt, le, eq, ne)

              - `transId`  (ne, eq, ct, nct)

              - `gatewayTransId`  (ne, eq, ct, nct)

              - `method`   (in, nin, eq, ne)

              - `settledAmount`  (gt, ge, lt, le, eq, ne)

              - `operation`    (in, nin, eq, ne)

              - `source`   (in, nin, eq, ne)

              - `batchNumber`  (ct, nct, eq, ne)

              - `payaccountLastfour`   (nct, ct)

              - `payaccountType`   (ne, eq, in, nin)

              - `customerFirstname`   (ct, nct, eq, ne)

              - `customerLastname`    (ct, nct, eq, ne)

              - `customerName`   (ct, nct)

              - `customerId`  (eq, ne)

              - `customerNumber`  (ct, nct, eq, ne)

              - `customerCompanyname`    (ct, nct, eq, ne)

              - `customerAddress` (ct, nct, eq, ne)

              - `customerCity`    (ct, nct, eq, ne)

              - `customerZip` (ct, nct, eq, ne)

              - `customerState` (ct, nct, eq, ne)

              - `customerCountry` (ct, nct, eq, ne)

              - `customerPhone` (ct, nct, eq, ne)

              - `customerEmail` (ct, nct, eq, ne)

              - `customerShippingAddress` (ct, nct, eq, ne)

              - `customerShippingCity`    (ct, nct, eq, ne)

              - `customerShippingZip` (ct, nct, eq, ne)

              - `customerShippingState` (ct, nct, eq, ne)

              - `customerShippingCountry` (ct, nct, eq, ne)

              - `orgId`  (eq) *mandatory when entry=org*

              - `isHold` (eq, ne)

              - `paypointId`  (ne, eq)

              - `paypointLegal`  (ne, eq, ct, nct)

              - `paypointDba`  (ne, eq, ct, nct)

              - `orgName`  (ne, eq, ct, nct)

              - `batchId` (ct, nct, eq, neq)

              - `additional-xxx`  (ne, eq, ct, nct) where xxx is the additional
              field name


              **List of comparison accepted:**

              - `eq` or empty => equal

              - `gt` => greater than

              - `ge` => greater or equal

              - `lt` => less than

              - `le` => less or equal

              - `ne` => not equal

              - `ct` => contains

              - `nct` => not contains

              - `in` => inside array separated by "|"

              - `nin` => not inside array separated by "|"


              **List of parameters accepted:**


              - `limitRecord`: max number of records for query (default="20",
              "0" or negative value for all)

              - `fromRecord`: initial record in query


              Example: `settledAmount(gt)=20` returns all records with a `settledAmount` greater than 20.00.
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.QueryResponseSettlements
      
    ListBatches:
      auth: true
      display-name: Get list of batches for a paypoint
      docs: |
        Retrieve a list of batches for a paypoint. Use filters to limit results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.
      examples:
        - name: GetBatches
          path-parameters:
            entry: 8cfec329267
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body: $queryTypes.QueryBatchesResponse.ExampleBatchQueryResponse

      method: GET
      path: /Query/batches/{entry}
      path-parameters:
        entry: root.Entry
      request:
        name: ListBatchesRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >
              Collection of field names, conditions, and values used to filter
              the query. 

              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>
              
              
              See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for more information.


              **List of field names accepted:**


              - `batchDate` (gt, ge, lt, le, eq, ne)

              - `batchNumber` (ne, eq)

              - `method` (in, nin, eq, ne)

              - `connectorName` (ne, eq, ct, nct)

              - `batchAmount` (gt, ge, lt, le, eq, ne)

              - `feeBatchAmount` (gt, ge, lt, le, eq, ne)

              - `netBatchAmount` (gt, ge, lt, le, eq, ne)

              - `releaseAmount` (gt, ge, lt, le, eq, ne)

              - `heldAmount` (gt, ge, lt, le, eq, ne)

              - `status` (in, nin, eq, ne)

              - `paypointLegal` (ne, eq, ct, nct)

              - `paypointDba` (ne, eq, ct, nct)

              - `orgName` (ne, eq, ct, nct)

              - `paypointId` (ne, eq)

              - `externalPaypointID` (ct, nct, eq, ne)

              - `expectedDepositDate` (gt, ge, lt, le, eq, ne)

              - `depositDate` (gt, ge, lt, le, eq, ne)

              - `batchRecords` (gt, ge, lt, le, eq, ne)

              - `transferId` (ne, eq)

              - `transferDate` (gt, ge, lt, le, eq, ne)

              - `grossAmount` (gt, ge, lt, le, eq, ne)

              - `chargeBackAmount` (gt, ge, lt, le, eq, ne)

              - `returnedAmount` (gt, ge, lt, le, eq, ne)

              - `billingFeeAmount` (gt, ge, lt, le, eq, ne)

              - `thirdPartyPaidAmount` (gt, ge, lt, le, eq, ne)

              - `netFundedAmount` (gt, ge, lt, le, eq, ne)

              - `adjustmentAmount` (gt, ge, lt, le, eq, ne)

              - `processor` (ne, eq, ct, nct)

              - `transferStatus` (ne, eq, in, nin)


              **List of parameters accepted:**

              - `limitRecord`: max number of records for query (default="20",
              "0" or negative value for all)

              - `fromRecord`: initial record in query


              Example: `batchAmount(gt)=20` returns all records with a `batchAmount`
              greater than 20.00
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: queryTypes.QueryBatchesResponse
      
    ListBatchesOrg:
      auth: true
      display-name: Get list of batches for an organization
      docs: |
        Retrieve a list of batches for an org. Use filters to limit results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.

      examples:
        - name: Example Response
          path-parameters:
            orgId: 123
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body: $queryTypes.QueryBatchesResponse.ExampleBatchQueryResponse
      method: GET
      path: /Query/batches/org/{orgId}
      path-parameters:
        orgId:
          docs: The numeric identifier for organization, assigned by Payabli.
          type: integer
      request:
        name: ListBatchesOrgRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >
              Collection of field names, conditions, and values used to filter
              the query. 

              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>
              
              
              See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for more information.


              **List of field names accepted:**


              - `batchDate` (gt, ge, lt, le, eq, ne)

              - `batchNumber` (ne, eq)

              - `method` (in, nin, eq, ne)

              - `connectorName` (ne, eq, ct, nct)

              - `batchAmount` (gt, ge, lt, le, eq, ne)

              - `feeBatchAmount` (gt, ge, lt, le, eq, ne)

              - `netBatchAmount` (gt, ge, lt, le, eq, ne)

              - `releaseAmount` (gt, ge, lt, le, eq, ne)

              - `heldAmount` (gt, ge, lt, le, eq, ne)

              - `status` (in, nin, eq, ne)

              - `paypointLegal` (ne, eq, ct, nct)

              - `paypointDba` (ne, eq, ct, nct)

              - `orgName` (ne, eq, ct, nct)

              - `paypointId` (ne, eq)

              - `externalPaypointID` (ct, nct, eq, ne)

              - `expectedDepositDate` (gt, ge, lt, le, eq, ne)

              - `depositDate` (gt, ge, lt, le, eq, ne)

              - `batchRecords` (gt, ge, lt, le, eq, ne)

              - `transferId` (ne, eq)

              - `transferDate` (gt, ge, lt, le, eq, ne)

              - `grossAmount` (gt, ge, lt, le, eq, ne)

              - `chargeBackAmount` (gt, ge, lt, le, eq, ne)

              - `returnedAmount` (gt, ge, lt, le, eq, ne)

              - `billingFeeAmount` (gt, ge, lt, le, eq, ne)

              - `thirdPartyPaidAmount` (gt, ge, lt, le, eq, ne)

              - `netFundedAmount` (gt, ge, lt, le, eq, ne)

              - `adjustmentAmount` (gt, ge, lt, le, eq, ne)

              - `processor` (ne, eq, ct, nct)

              - `transferStatus` (ne, eq, in, nin)


              **List of parameters accepted:**

              - `limitRecord`: max number of records for query (default="20",
              "0" or negative value for all)

              - `fromRecord`: initial record in query


              Example: `batchAmount(gt)=20` returns all records with a `batchAmount`
              greater than 20.00      
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: queryTypes.QueryBatchesResponse
      
    ListBatchesOut:
      auth: true
      display-name: Get list of MoneyOut batches for a paypoint
      docs: >-
        Retrieve a list of MoneyOut batches for a paypoint. Use filters to limit
        results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.

      examples:
        - path-parameters:
            entry: 8cfec329267
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                - AchAmount: 0
                  AchRecords: 0
                  AchStatus: 0
                  AchStatusText: AchStatusText
                  BatchAmount: 4
                  BatchCancelledAmount: 0
                  BatchCancelledRecords: 0
                  BatchDate: '2024-01-01T00:00:00Z'
                  BatchNumber: 10-20240101-PAYABLITST
                  BatchPaidAmount: 0
                  BatchPaidRecords: 0
                  BatchProcessedAmount: 0
                  BatchProcessedRecords: 0
                  BatchProcessingAmount: 0
                  BatchProcessingRecords: 0
                  BatchRecords: 1
                  BatchStatus: 1
                  BatchStatusText: Waiting Funds
                  CardAmount: 0
                  CardRecords: 0
                  CardStatus: 0
                  CardStatusText: CardStatusText
                  CheckAmount: 0
                  CheckRecords: 0
                  CheckStatus: 0
                  CheckStatusText: CheckStatusText
                  EntryName: d193cf9a46
                  externalPaypointID: Paypoint-100
                  IdBatch: 239
                  ParentOrgName: FitnessManager
                  PaypointDba: Athlete Factory LLC
                  PaypointId: 123
                  PaypointName: Athlete Factory LLC
                  VcardAmount: 0
                  VcardRecords: 0
                  VcardStatus: 0
                  VcardStatusText: VcardStatusText
                  WireAmount: 0
                  WireRecords: 0
                  WireStatus: 0
                  WireStatusText: WireStatusText
              Summary:
                pageidentifier: 'null'
                pageSize: 20
                totalAmount: 0.01
                totalNetAmount: 0
                totalPages: 1
                totalRecords: 46
      method: GET
      path: /Query/batchesOut/{entry}
      path-parameters:
        entry: root.Entry
      request:
        name: ListBatchesOutRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >

              Collection of field names, conditions, and values used to filter
              the query. See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for more information.


              **List of field names accepted**:


              - `batchDate` (gt, ge, lt, le, eq, ne)

              - `batchNumber` (ne, eq)

              - `batchAmount` (gt, ge, lt, le, eq, ne)

              - `parentOrgId` (ne, eq, nin, in)

              - `status` (in, nin, eq, ne)

              - `orgId` (eq)

              - `paypointLegal` (ne, eq, ct, nct)

              - `paypointDba` (ne, eq, ct, nct)

              - `orgName` (ne, eq, ct, nct)

              - `paypointId` (ne, eq)

              - `externalPaypointID` (ct, nct, eq, ne)
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.QueryBatchesOutResponse
      
    ListBatchesOutOrg:
      auth: true
      display-name: Get list of MoneyOut batches for an org
      docs: >-
        Retrieve a list of MoneyOut batches for an org. Use filters to limit
        results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.

      examples:
        - path-parameters:
            orgId: 123
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                - AchAmount: 0
                  AchRecords: 0
                  AchStatus: 0
                  AchStatusText: AchStatusText
                  BatchAmount: 4
                  BatchCancelledAmount: 0
                  BatchCancelledRecords: 0
                  BatchDate: '2024-01-01T00:00:00Z'
                  BatchNumber: 10-20240101-PAYABLITST
                  BatchPaidAmount: 0
                  BatchPaidRecords: 0
                  BatchProcessedAmount: 0
                  BatchProcessedRecords: 0
                  BatchProcessingAmount: 0
                  BatchProcessingRecords: 0
                  BatchRecords: 1
                  BatchStatus: 1
                  BatchStatusText: Waiting Funds
                  CardAmount: 0
                  CardRecords: 0
                  CardStatus: 0
                  CardStatusText: CardStatusText
                  CheckAmount: 0
                  CheckRecords: 0
                  CheckStatus: 0
                  CheckStatusText: CheckStatusText
                  EntryName: d193cf9a46
                  externalPaypointID: Paypoint-100
                  IdBatch: 239
                  ParentOrgName: FitnessManager
                  PaypointDba: Athlete Factory LLC
                  PaypointId: 123
                  PaypointName: Athlete Factory LLC
                  VcardAmount: 0
                  VcardRecords: 0
                  VcardStatus: 0
                  VcardStatusText: VcardStatusText
                  WireAmount: 0
                  WireRecords: 0
                  WireStatus: 0
                  WireStatusText: WireStatusText
              Summary:
                pageidentifier: 'null'
                pageSize: 20
                totalAmount: 0.01
                totalNetAmount: 0
                totalPages: 1
                totalRecords: 46
      method: GET
      path: /Query/batchesOut/org/{orgId}
      path-parameters:
        orgId:
          docs: The numeric identifier for organization, assigned by Payabli.
          type: integer
      request:
        name: ListBatchesOutOrgRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >

              Collection of field names, conditions, and values used to filter
              the query. 
              
              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>
              
              
              See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for more information.


              **List of field names accepted**:


              - `batchDate` (gt, ge, lt, le, eq, ne)

              - `batchNumber` (ne, eq)

              - `batchAmount` (gt, ge, lt, le, eq, ne)

              - `parentOrgId` (ne, eq, nin, in)

              - `status` (in, nin, eq, ne)

              - `orgId` (eq)

              - `paypointLegal` (ne, eq, ct, nct)

              - `paypointDba` (ne, eq, ct, nct)

              - `orgName` (ne, eq, ct, nct)

              - `paypointId` (ne, eq)

              - `externalPaypointID` (ct, nct, eq, ne)
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.QueryBatchesOutResponse
      
    ListChargebacks:
      auth: true
      display-name: Get list of chargebacks and returned transactions for a paypoint
      docs: >-
        Retrieves a list of chargebacks and returned transactions for a
        paypoint. Use filters to limit results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.
      examples:
        - name: Example1
          path-parameters:
            entry: 8cfec329267
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                - AccountType: 4XXXXXX0003
                  CaseNumber: '00001'
                  ChargebackDate: '2023-02-08T00:00:00Z'
                  CreatedAt: '2023-02-20T15:36:49Z'
                  Customer:
                    AdditionalData: AdditionalData
                    BillingAddress1: 321 Big Sky Road
                    BillingAddress2: ''
                    BillingCity: Helena
                    BillingCountry: US
                    BillingEmail: janis.berzins@example.com
                    BillingPhone: 406-555-0123
                    BillingState: MT
                    BillingZip: '59601'
                    CompanyName: Big Sky Imports
                    customerId: 1324
                    CustomerNumber: '12345'
                    customerStatus: 1
                    FirstName: Janis
                    Identifiers:
                      - firstname
                      - email
                    LastName: Berzins
                    ShippingAddress1: 321 Big Sky Road
                    ShippingAddress2: ''
                    ShippingCity: Helena
                    ShippingCountry: US
                    ShippingState: MT
                    ShippingZip: '59601'
                  externalPaypointID: f743aed24a-10
                  Id: 578
                  LastFour: 4XXXXXX0003
                  Method: card
                  NetAmount: 1.5
                  OrderId: ''
                  ParentOrgName: Par
                  PaymentData:
                    AccountExp: '0330'
                    AccountType: visa
                    HolderName: Janis Berzins
                    MaskedAccount: 4XXXXXX0003
                    paymentDetails:
                      totalAmount: 100
                    StoredId: stored-id-123
                  PaymentTransId: 10-bfcd5a17861d4a8690ca53c00000X
                  PaypointDbaname: Global Factory LLC
                  PaypointEntryname: f743aed24a
                  PaypointLegalname: Global Factory LLC
                  Reason: Testing
                  ReasonCode: '00001'
                  ReferenceNumber: 10-bfcd5a17861d4a8690ca53c00000X
                  ReplyBy: '2023-03-02T15:36:49Z'
                  ScheduleReference: 0
                  Status: 3
                  Transaction:
                    BatchAmount: 0
                    Customer:
                      AdditionalData: AdditionalData
                      BillingAddress1: 321 Big Sky Road
                      BillingAddress2: ''
                      BillingCity: Helena
                      BillingCountry: US
                      BillingEmail: janis.berzins@example.com
                      BillingPhone: 406-555-0123
                      BillingState: MT
                      BillingZip: '59601'
                      CompanyName: Big Sky Imports
                      customerId: 1324
                      CustomerNumber: '12345'
                      customerStatus: 1
                      FirstName: Janis
                      Identifiers:
                        - firstname
                        - email
                      LastName: Berzins
                      ShippingAddress1: 321 Big Sky Road
                      ShippingAddress2: ''
                      ShippingCity: Helena
                      ShippingCountry: US
                      ShippingState: MT
                      ShippingZip: '59601'
                    EntrypageId: 0
                    FeeAmount: 0.06
                    GatewayTransId: '8082800000'
                    Method: card
                    NetAmount: 1.5
                    Operation: Sale
                    OrderId: ''
                    OrgId: 0
                    PaymentData:
                      AccountExp: '0330'
                      AccountType: visa
                      HolderName: Lisandra Sosa
                      MaskedAccount: 4XXXXXX0003
                      paymentDetails:
                        currency: USD
                        serviceFee: 0.06
                        totalAmount: 1.56
                    PaymentTransId: 10-bfcd5a17861d4a8690ca53c00000X
                    PayorId: 0
                    PaypointId: 0
                    RefundId: 0
                    ResponseData:
                      authcode: '123456'
                      avsresponse: 'N'
                      avsresponse_text: No address or ZIP match only
                      cvvresponse: 'N'
                      cvvresponse_text: CVV2/CVC2 no match
                      orderid: 10-bfcd5a17861d4a8690ca53c00000X
                      response_code: '100'
                      response_code_text: Transaction was approved.
                      responsetext: SUCCESS
                      transactionid: '8082800000'
                    ReturnedId: 0
                    ScheduleReference: 0
                    SettlementStatus: 0
                    Source: api
                    TotalAmount: 1.56
                    TransactionEvents:
                      - EventTime: '2023-02-20T15:36:47Z'
                        TransEvent: created
                      - EventData: >-
                          response=1&responsetext=SUCCESS&authcode=123456&transactionid=8082800000&avsresponse=N&cvvresponse=N&orderid=10-bfcd5a17861d4a8690ca53c00000X&type=sale&response_code=100
                        EventTime: '2023-02-20T15:36:49Z'
                        TransEvent: Approved
                    TransactionTime: '2023-02-20T15:36:47Z'
                    TransStatus: 1
                  TransactionTime: '2023-02-20T15:36:47Z'
              Summary:
                pageIdentifier: 'null'
                pageSize: 20
                totalAmount: 1.56
                totalNetAmount: 1.5
                totalPages: 1
                totalRecords: 1
      method: GET
      path: /Query/chargebacks/{entry}
      path-parameters:
        entry: root.Entry
      request:
        name: ListChargebacksRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >
              Collection of field names, conditions, and values used to filter
              the query.
            
              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>

              See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for help.


              **List of field names accepted:**

              - `chargebackDate` (gt, ge, lt, le, eq, ne)

              - `transId`  (ne, eq, ct, nct)

              - `method`   (in, nin, eq, ne)

              - `netAmount`  (gt, ge, lt, le, eq, ne)

              - `reasonCode`   (in, nin, eq, ne)

              - `reason`  (ct, nct, eq, ne)

              - `replyDate` (gt, ge, lt, le, eq, ne)

              - `caseNumber`  (ct, nct, eq, ne)

              - `status`   (in, nin, eq, ne)

              - `accountType`   (in, nin, eq, ne)

              - `payaccountLastfour`   (nct, ct)

              - `payaccountType`   (ne, eq, in, nin)

              - `customerFirstname`   (ct, nct, eq, ne)

              - `customerLastname`    (ct, nct, eq, ne)

              - `customerName`   (ct, nct)

              - `customerId`  (eq, ne)

              - `customerNumber`  (ct, nct, eq, ne)

              - `customerCompanyname`    (ct, nct, eq, ne)

              - `customerAddress` (ct, nct, eq, ne)

              - `customerCity`    (ct, nct, eq, ne)

              - `customerZip` (ct, nct, eq, ne)

              - `customerState` (ct, nct, eq, ne)

              - `customerCountry` (ct, nct, eq, ne)

              - `customerPhone` (ct, nct, eq, ne)

              - `customerEmail` (ct, nct, eq, ne)

              - `customerShippingAddress` (ct, nct, eq, ne)

              - `customerShippingCity`    (ct, nct, eq, ne)

              - `customerShippingZip` (ct, nct, eq, ne)

              - `customerShippingState` (ct, nct, eq, ne)

              - `customerShippingCountry` (ct, nct, eq, ne)

              - `orgId`  (eq) *mandatory when entry=org*

              - `paypointId`  (ne, eq)

              - `paypointLegal`  (ne, eq, ct, nct)

              - `paypointDba`  (ne, eq, ct, nct)

              - `orgName`  (ne, eq, ct, nct)

              - `additional-xxx`  (ne, eq, ct, nct) where xxx is the additional
              field name


              **List of comparison accepted - enclosed between parentheses:**

              - `eq` or empty => equal

              - `gt` => greater than

              - `ge` => greater or equal

              - `lt` => less than

              - `le` => less or equal

              - `ne` => not equal

              - `ct` => contains

              - `nct` => not contains

              - `in` => inside array separated by "|"

              - `nin` => not inside array separated by "|"


              **List of parameters accepted:**

              - `limitRecord`: max number of records for query (default="20",
              "0" or negative value for all)

              - `fromRecord`: initial record in query


              Example: `netAmount(gt)=20` returns all records with a `netAmount`
              greater than 20.00
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.QueryChargebacksResponse
      
    ListChargebacksOrg:
      auth: true
      display-name: Get list of chargebacks and returned transactions for an org
      docs: >-
        Retrieve a list of chargebacks and returned transactions for an org. Use
        filters to limit results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.
      examples:
        - path-parameters:
            orgId: 123
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                - AccountType: 4XXXXXX0003
                  CaseNumber: '00001'
                  ChargebackDate: '2023-02-08T00:00:00Z'
                  CreatedAt: '2022-07-01T15:00:01Z'
                  externalPaypointID: Paypoint-100
                  Id: 578
                  LastFour: 4XXXXXX0003
                  Method: card
                  NetAmount: 1.5
                  OrderId: O-5140
                  pageidentifier: 'null'
                  ParentOrgName: PropertyManager Pro
                  PaymentData:
                    paymentDetails:
                      totalAmount: 100
                  PaymentTransId: 10-bfcd5a17861d4a8690ca53c142ca3810
                  PaypointDbaname: Sunshine Gutters
                  PaypointEntryname: d193cf9a46
                  PaypointLegalname: Sunshine Services, LLC
                  Reason: Testing
                  ReasonCode: '00001'
                  ReferenceNumber: 10-bfcd5a17861d4a8690ca53c142ca3810
                  ReplyBy: '2022-07-11T15:00:01Z'
                  Responses: Responses
                  ScheduleReference: 0
                  Status: 3
                  Transaction:
                    EntrypageId: 0
                    FeeAmount: 1
                    PayorId: 1551
                    PaypointId: 226
                    SettlementStatus: 2
                    TotalAmount: 30.22
                    TransStatus: 1
                  TransactionTime: '2024-01-15T09:30:00Z'
              Summary:
                pageIdentifier: 'null'
                pageSize: 20
                totalAmount: 77.22
                totalNetAmount: 77.22
                totalPages: 2
                totalRecords: 2
      method: GET
      path: /Query/chargebacks/org/{orgId}
      path-parameters:
        orgId:
          docs: The numeric identifier for organization, assigned by Payabli.
          type: integer
      request:
        name: ListChargebacksOrgRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >
              Collection of field names, conditions, and values used to filter
              the query.


              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>
              See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for help.


              **List of field names accepted:**


              - `chargebackDate` (gt, ge, lt, le, eq, ne)

              - `transId`  (ne, eq, ct, nct)

              - `method`   (in, nin, eq, ne)

              - `netAmount`  (gt, ge, lt, le, eq, ne)

              - `reasonCode`   (in, nin, eq, ne)

              - `reason`  (ct, nct, eq, ne)

              - `replyDate` (gt, ge, lt, le, eq, ne)

              - `caseNumber`  (ct, nct, eq, ne)

              - `status`   (in, nin, eq, ne)

              - `accountType`   (in, nin, eq, ne)

              - `payaccountLastfour`   (nct, ct)

              - `payaccountType`   (ne, eq, in, nin)

              - `customerFirstname`   (ct, nct, eq, ne)

              - `customerLastname`    (ct, nct, eq, ne)

              - `customerName`   (ct, nct)

              - `customerId`  (eq, ne)

              - `customerNumber`  (ct, nct, eq, ne)

              - `customerCompanyname`    (ct, nct, eq, ne)

              - `customerAddress` (ct, nct, eq, ne)

              - `customerCity`    (ct, nct, eq, ne)

              - `customerZip` (ct, nct, eq, ne)

              - `customerState` (ct, nct, eq, ne)

              - `customerCountry` (ct, nct, eq, ne)

              - `customerPhone` (ct, nct, eq, ne)

              - `customerEmail` (ct, nct, eq, ne)

              - `customerShippingAddress` (ct, nct, eq, ne)

              - `customerShippingCity`    (ct, nct, eq, ne)

              - `customerShippingZip` (ct, nct, eq, ne)

              - `customerShippingState` (ct, nct, eq, ne)

              - `customerShippingCountry` (ct, nct, eq, ne)

              - `orgId`  (eq) *mandatory when entry=org*

              - `paypointId`  (ne, eq)

              - `paypointLegal`  (ne, eq, ct, nct)

              - `paypointDba`  (ne, eq, ct, nct)

              - `orgName`  (ne, eq, ct, nct)

              - `additional-xxx`  (ne, eq, ct, nct) where xxx is the additional
              field name


              **List of comparison accepted - enclosed between parentheses:**


              - `eq` or empty => equal

              - `gt` => greater than

              - `ge` => greater or equal

              - `lt` => less than

              - `le` => less or equal

              - `ne` => not equal

              - `ct` => contains

              - `nct` => not contains

              - `in` => inside array separated by "|"

              - `nin` => not inside array separated by "|"


              **List of parameters accepted:**

              - `limitRecord`: max number of records for query (default="20",
              "0" or negative value for all)

              - `fromRecord`: initial record in query


              Example: `netAmount(gt)=20` returns all records with a `netAmount`
              greater than 20.00
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.QueryChargebacksResponse
      
    ListCustomers:
      auth: true
      display-name: Get list of customers for a paypoint
      docs: >-
        Retrieves a list of customers for a paypoint. Use filters to limit
        results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.
      examples:
        - name: example1
          path-parameters:
            entry: 8cfec329267
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                - Address: 1234 Bayou Road
                  Address1: Suite 2
                  Balance: 0
                  City: Lafayette
                  Company: Boudreaux's Shop
                  Country: US
                  Created: '2023-12-20T13:07:48Z'
                  customerId: 2876
                  customerNumber: '425436530000'
                  customerStatus: 0
                  customerSummary:
                    numberofTransactions: 30
                    totalAmountTransactions: 1500
                    totalNetAmountTransactions: 1500
                  Email: thibodeaux.hebert@bayoumail.com
                  externalPaypointID: pay-10
                  Firstname: Thibodeaux
                  IdentifierFields:
                    - email
                  Lastname: Hebert
                  LastUpdated: '2023-12-20T13:07:48Z'
                  MFA: false
                  MFAMode: 0
                  ParentOrgName: SupplyPro
                  PaypointDbaname: Global Factory LLC
                  PaypointEntryname: 4872acb376a
                  PaypointLegalname: Global Factory LLC
                  Phone: (504) 823-4566
                  ShippingAddress: 1234 Bayou Road
                  ShippingAddress1: Suite 2
                  ShippingCity: Lafayette
                  ShippingCountry: US
                  ShippingState: LA
                  ShippingZip: '70501'
                  State: LA
                  TimeZone: 0
                  Zip: '70501'
              Summary:
                pageIdentifier: XXXXXXXXXXXXXX
                pageSize: 20
                totalAmount: 0
                totalNetAmount: 0
                totalPages: 26
                totalRecords: 510
      method: GET
      path: /Query/customers/{entry}
      path-parameters:
        entry: root.Entry
      request:
        name: ListCustomersRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >
              Collection of field names, conditions, and values used to filter
              the query. 

              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>
              
              See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for more details.


              **List of Accepted Field Names:**


              - `createdDate` (gt, ge, lt, le, eq, ne)

              - `customernumber` (ne, eq, ct, nct)

              - `firstname` (ne, eq, ct, nct)

              - `lastname` (ne, eq, ct, nct)

              - `name` (ct, nct)

              - `address` (ne, eq, ct, nct)

              - `city` (ne, eq, ct, nct)

              - `country` (ne, eq, ct, nct)

              - `zip` (ne, eq, ct, nct)

              - `state` (ne, eq, ct, nct)

              - `shippingaddress` (ne, eq, ct, nct)

              - `shippingcity` (ne, eq, ct, nct)

              - `shippingcountry` (ne, eq, ct, nct)

              - `shippingzip` (ne, eq, ct, nct)

              - `shippingstate` (ne, eq, ct, nct)

              - `phone` (ne, eq, ct, nct)

              - `email` (ne, eq, ct, nct)

              - `company` (ne, eq, ct, nct)

              - `username` (ne, eq, ct, nct)

              - `balance` (gt, ge, lt, le, eq, ne)

              - `status` (in, nin, eq, ne)

              - `additional-xxx` (ne, eq, ct, nct) where xxx is the additional
              field name

              - `orgId` (eq) *mandatory when entry=org*

              - `paypointId` (ne, eq)

              - `paypointLegal` (ne, eq, ct, nct)

              - `paypointDba` (ne, eq, ct, nct)

              - `orgName` (ne, eq, ct, nct)


              **List of Accepted Comparisons:**


              - `eq` or empty => equal

              - `gt` => greater than

              - `ge` => greater or equal

              - `lt` => less than

              - `le` => less or equal

              - `ne` => not equal

              - `ct` => contains

              - `nct` => not contains

              - `in` => inside array separated by "|"

              - `nin` => not inside array separated by "|"


              **Accepted Parameters:**

              - `limitRecord`: Max number of records for query (default="20",
              "0" or negative value for all)

              - `fromRecord`: Initial record in query


              **Example Usage:**

              `balance(gt)=20` will return all records with a balance greater
              than 20.00.
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.QueryCustomerResponse
      
    ListCustomersOrg:
      auth: true
      display-name: Get list of customers for an org
      docs: Retrieves a list of customers for an org. Use filters to limit results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.
      examples:
        - name: example1
          path-parameters:
            orgId: 123
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                - Address: 1234 Bayou Road
                  Address1: Suite 2
                  Balance: 0
                  City: Lafayette
                  Company: Boudreaux's Shop
                  Country: US
                  Created: '2023-12-20T13:07:48Z'
                  customerId: 2876
                  customerNumber: '425436530000'
                  customerStatus: 0
                  customerSummary:
                    numberofTransactions: 30
                    totalAmountTransactions: 1500
                    totalNetAmountTransactions: 1500
                  Email: thibodeaux.hebert@bayoumail.com
                  externalPaypointID: pay-10
                  Firstname: Thibodeaux
                  IdentifierFields:
                    - email
                  Lastname: Hebert
                  LastUpdated: '2023-12-20T13:07:48Z'
                  MFA: false
                  MFAMode: 0
                  ParentOrgName: SupplyPro
                  PaypointDbaname: Global Factory LLC
                  PaypointEntryname: 4872acb376a
                  PaypointLegalname: Global Factory LLC
                  Phone: (504) 823-4566
                  ShippingAddress: 1234 Bayou Road
                  ShippingAddress1: Suite 2
                  ShippingCity: Lafayette
                  ShippingCountry: US
                  ShippingState: LA
                  ShippingZip: '70501'
                  State: LA
                  TimeZone: 0
                  Zip: '70501'
              Summary:
                pageIdentifier: XXXXXXXXXXXXXX
                pageSize: 20
                totalAmount: 0
                totalNetAmount: 0
                totalPages: 26
                totalRecords: 510
      method: GET
      path: /Query/customers/org/{orgId}
      path-parameters:
        orgId:
          docs: The numeric identifier for organization, assigned by Payabli.
          type: integer
      request:
        name: ListCustomersOrgRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >
              Collection of field names, conditions, and values used to filter
              the query. 
              
              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>
              
              See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for more details.


              **List of Accepted Field Names:**


              - `createdDate` (gt, ge, lt, le, eq, ne)

              - `customernumber` (ne, eq, ct, nct)

              - `firstname` (ne, eq, ct, nct)

              - `lastname` (ne, eq, ct, nct)

              - `name` (ct, nct)

              - `address` (ne, eq, ct, nct)

              - `city` (ne, eq, ct, nct)

              - `country` (ne, eq, ct, nct)

              - `zip` (ne, eq, ct, nct)

              - `state` (ne, eq, ct, nct)

              - `shippingaddress` (ne, eq, ct, nct)

              - `shippingcity` (ne, eq, ct, nct)

              - `shippingcountry` (ne, eq, ct, nct)

              - `shippingzip` (ne, eq, ct, nct)

              - `shippingstate` (ne, eq, ct, nct)

              - `phone` (ne, eq, ct, nct)

              - `email` (ne, eq, ct, nct)

              - `company` (ne, eq, ct, nct)

              - `username` (ne, eq, ct, nct)

              - `balance` (gt, ge, lt, le, eq, ne)

              - `status` (in, nin, eq, ne)

              - `additional-xxx` (ne, eq, ct, nct) where xxx is the additional
              field name

              - `orgId` (eq) *mandatory when entry=org*

              - `paypointId` (ne, eq)

              - `paypointLegal` (ne, eq, ct, nct)

              - `paypointDba` (ne, eq, ct, nct)

              - `orgName` (ne, eq, ct, nct)


              **List of Accepted Comparisons:**


              - `eq` or empty => equal

              - `gt` => greater than

              - `ge` => greater or equal

              - `lt` => less than

              - `le` => less or equal

              - `ne` => not equal

              - `ct` => contains

              - `nct` => not contains

              - `in` => inside array separated by "|"

              - `nin` => not inside array separated by "|"


              **Accepted Parameters:**

              - `limitRecord`: Max number of records for query (default="20",
              "0" or negative value for all)

              - `fromRecord`: Initial record in query


              **Example Usage:**

              `balance(gt)=20` will return all records with a balance greater
              than 20.00.
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.QueryCustomerResponse
      
    ListNotificationReports:
      auth: true
      display-name: Get list of reports generated in last 60 days for entrypoint
      docs: >-
        Returns a list of all reports generated in the last 60 days for a single
        entrypoint. Use filters to limit results.
      examples:
        - name: Example Response
          path-parameters:
            entry: 8cfec329267
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                - createdAt: '2024-02-20T01:48:04Z'
                  id: 4881
                  isDownloadable: true
                  reportName: Transaction-2024-02-20-000000-0-2.csv
              Summary:
                pageIdentifier: 'null'
                pageSize: 20
                totalAmount: 0
                totalNetAmount: 0
                totalPages: 1
                totalRecords: 1
      method: GET
      path: /Query/notificationReports/{entry}
      path-parameters:
        entry: root.Entry
      request:
        name: ListNotificationReportsRequest
        query-parameters:
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >
              Collection of field names, conditions, and values used to filter
              the query

              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>


              See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for help.


              List of field names accepted:

              - `reportName` (ct, nct, eq, ne)

              - `createdAt` (gt, ge, lt, le, eq, ne)


              List of comparison accepted - enclosed between parentheses:

              - eq or empty => equal

              - gt => greater than

              - ge => greater or equal

              - lt => less than

              - le => less or equal

              - ne => not equal

              - ct => contains

              - nct => not contains

              - in => inside array

              - nin => not inside array


              List of parameters accepted:

              - limitRecord : max number of records for query (default="20", "0"
              or negative value for all)

              - fromRecord : initial record in query


              Example: reportName(ct)=tr  return all records containing the
              string "tr"
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.QueryResponseNotificationReports
      
    ListNotificationReportsOrg:
      auth: true
      display-name: Get list of reports generated in last 60 days for organization
      docs: >-
        Returns a list of all reports generated in the last 60 days for an
        organization. Use filters to limit results.
      examples:
        - name: Example Response
          path-parameters:
            orgId: 123
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                - createdAt: '2024-02-20T01:48:04Z'
                  id: 4881
                  isDownloadable: true
                  reportName: Transaction-2024-02-20-000000-0-2.csv
              Summary:
                pageIdentifier: 'null'
                pageSize: 20
                totalAmount: 0
                totalNetAmount: 0
                totalPages: 1
                totalRecords: 1
      method: GET
      path: /Query/notificationReports/org/{orgId}
      path-parameters:
        orgId:
          docs: The numeric identifier for organization, assigned by Payabli.
          type: integer
      request:
        name: ListNotificationReportsOrgRequest
        query-parameters:
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >
              Collection of field names, conditions, and values used to filter
              the query
              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>

              See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for help.


              List of field names accepted:

              - `reportName` (ct, nct, eq, ne)

              - `createdAt` (gt, ge, lt, le, eq, ne)


              List of comparison accepted - enclosed between parentheses:

              - eq or empty => equal

              - gt => greater than

              - ge => greater or equal

              - lt => less than

              - le => less or equal

              - ne => not equal

              - ct => contains

              - nct => not contains

              - in => inside array

              - nin => not inside array


              List of parameters accepted:

              - limitRecord : max number of records for query (default="20", "0"
              or negative value for all)

              - fromRecord : initial record in query


              Example: reportName(ct)=tr  return all records containing the
              string "tr"
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.QueryResponseNotificationReports
      
    ListNotifications:
      auth: true
      display-name: Get list of notifications for an entrypoint
      docs: >-
        Returns a list of notifications for an entrypoint. Use filters to limit
        results.
      examples:
        - name: QueryResponseExample
          path-parameters:
            entry: 8cfec329267
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                - content:
                    eventType: ReceivedChargeBack
                    timeZone: 0
                  createdAt: '2022-06-07T05:00:00Z'
                  frequency: untilcancelled
                  lastUpdated: '2022-06-07T05:00:00Z'
                  method: email
                  notificationId: 88976
                  ownerId: '123'
                  ownerName: Pilgrim Planner
                  ownerType: 2
                  source: api
                  status: 1
                  target: example@example.com
                - content:
                    eventType: ReceivedAchReturn
                    timeZone: 0
                  createdAt: '2022-06-07T05:00:00Z'
                  frequency: untilcancelled
                  lastUpdated: '2022-06-07T05:00:00Z'
                  method: email
                  notificationId: 88975
                  ownerId: '123'
                  ownerName: Pilgrim Planner
                  ownerType: 2
                  source: api
                  status: 1
                  target: example@example.com
              Summary:
                pageSize: 20
                totalAmount: 0
                totalNetAmount: 0
                totalPages: 1
                totalRecords: 2
      method: GET
      path: /Query/notifications/{entry}
      path-parameters:
        entry: root.Entry
      request:
        name: ListNotificationsRequest
        query-parameters:
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >
              Collection of field names, conditions, and values used to filter
              the query

              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>

              See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for help.


              List of field names accepted:

              - `frequency` (in, nin,ne, eq)

              - `method` (in, nin, eq, ne)

              - `event` (in, nin, eq, ne)

              - `target` (ct, nct, eq, ne)

              - `status` (eq, ne)


              List of comparison accepted - enclosed between parentheses:

              - eq or empty => equal

              - gt => greater than

              - ge => greater or equal

              - lt => less than

              - le => less or equal

              - ne => not equal

              - ct => contains

              - nct => not contains

              - in => inside array

              - nin => not inside array


              List of parameters accepted:

              - limitRecord : max number of records for query (default="20", "0"
              or negative value for all)

              - fromRecord : initial record in query


              Example: totalAmount(gt)=20  return all records with totalAmount
              greater than 20.00
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.QueryResponseNotifications
      
    ListNotificationsOrg:
      auth: true
      display-name: Get list of notifications for organization
      docs: >-
        Return a list of notifications for an organization. Use filters to limit
        results.
      examples:
        - name: QueryResponseExample
          path-parameters:
            orgId: 123
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                - content:
                    eventType: ReceivedChargeBack
                    timeZone: 0
                  createdAt: '2022-06-07T05:00:00Z'
                  frequency: untilcancelled
                  lastUpdated: '2022-06-07T05:00:00Z'
                  method: email
                  notificationId: 88976
                  ownerId: '123'
                  ownerName: Pilgrim Planner
                  ownerType: 2
                  source: api
                  status: 1
                  target: example@example.com
                - content:
                    eventType: ReceivedAchReturn
                    timeZone: 0
                  createdAt: '2022-06-07T05:00:00Z'
                  frequency: untilcancelled
                  lastUpdated: '2022-06-07T05:00:00Z'
                  method: email
                  notificationId: 88975
                  ownerId: '123'
                  ownerName: Pilgrim Planner
                  ownerType: 2
                  source: api
                  status: 1
                  target: example@example.com
              Summary:
                pageSize: 20
                totalAmount: 0
                totalNetAmount: 0
                totalPages: 1
                totalRecords: 2
      method: GET
      path: /Query/notifications/org/{orgId}
      path-parameters:
        orgId:
          docs: The numeric identifier for organization, assigned by Payabli.
          type: integer
      request:
        name: ListNotificationsOrgRequest
        query-parameters:
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >
              Collection of field names, conditions, and values used to filter
              the query

              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>

              See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for help.


              List of field names accepted:

              - `frequency` (in, nin,ne, eq)

              - `method` (in, nin, eq, ne)

              - `event` (in, nin, eq, ne)

              - `target` (ct, nct, eq, ne)

              - `status` (eq, ne)


              List of comparison accepted - enclosed between parentheses:

              - eq or empty => equal

              - gt => greater than

              - ge => greater or equal

              - lt => less than

              - le => less or equal

              - ne => not equal

              - ct => contains

              - nct => not contains

              - in => inside array

              - nin => not inside array


              List of parameters accepted:

              - limitRecord : max number of records for query (default="20", "0"
              or negative value for all)

              - fromRecord : initial record in query


              Example: totalAmount(gt)=20  return all records with totalAmount
              greater than 20.00
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.QueryResponseNotifications
      
    ListOrganizations:
      auth: true
      display-name: Get list of organizations for a parent
      docs: >-
        Retrieves a list of an organization's suborganizations and their full
        details such as orgId, users, and settings. Use filters to limit
        results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.
      examples:
        - path-parameters:
            orgId: 123
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                - services:
                    - {}
                  contacts:
                    - {}
                  createdAt: '2022-07-01T15:00:01Z'
                  hasBilling: true
                  hasResidual: true
                  idOrg: 123
                  isRoot: false
                  orgAddress: 123 Walnut Street
                  orgCity: Johnson City
                  orgCountry: US
                  orgEntryName: pilgrim-planner
                  orgId: "I-123"
                  orgName: Pilgrim Planner
                  orgParentId: 236
                  orgParentName: PropertyManager Pro
                  orgState: TN
                  orgTimezone: -5
                  orgType: 0
                  orgWebsite: www.pilgrimageplanner.com
                  orgZip: orgZip
                  recipientEmailNotification: true
                  replyToEmail: example@email.com
                  resumable: false
                  users:
                    - createdAt: '2022-07-01T15:00:01Z'
                      UsrMFAMode: 0
              Summary:
                pageIdentifier: 'null'
                pageSize: 20
                totalAmount: 77.22
                totalNetAmount: 77.22
                totalPages: 2
                totalRecords: 2
      method: GET
      path: /Query/organizations/{orgId}
      path-parameters:
        orgId:
          docs: The numeric identifier for organization, assigned by Payabli.
          type: integer
      request:
        name: ListOrganizationsRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >
              Collection of field names, conditions, and values used to filter
              the query.

              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>

              **List of field names accepted:**


              - `createdAt` (gt, ge, lt, le, eq, ne)

              - `startDate` (gt, ge, lt, le, eq, ne)

              - `dbaname`  (ct, nct)

              - `legalname`  (ct, nct)

              - `ein`  (ct, nct)

              - `address`  (ct, nct)

              - `city`  (ct, nct)

              - `state`  (ct, nct)

              - `phone`  (ct, nct)

              - `mcc`  (ct, nct)

              - `owntype`  (ct, nct)

              - `ownerName`  (ct, nct)

              - `contactName`  (ct, nct)

              - `orgParentname`  (ct, nct)

              - `boardingId` (eq, ne) 

              - `entryName`  (ct, nct)


              **List of comparison accepted - enclosed between parentheses:**


              - `eq` or empty => equal

              - `gt` => greater than

              - `ge` => greater or equal

              - `lt` => less than

              - `le` => less or equal

              - `ne` => not equal

              - `ct` => contains

              - `nct` => not contains

              - `in` => inside array

              - `nin` => not inside array


              **List of parameters accepted:**


              - `limitRecord` : max number of records for query (default="20",
              "0" or negative value for all)

              - `fromRecord` : initial record in query


              Example: `dbaname(ct)=hoa` returns all records with a `dbaname`
              containing "hoa"
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: queryTypes.ListOrganizationsResponse
      
    ListPayout:
      auth: true
      display-name: Get list of money out transactions for paypoint
      docs: >-
        Retrieves a list of money out transactions (payouts) for a paypoint. Use
        filters to limit results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.
      examples:
        - path-parameters:
            entry: 8cfec329267
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body: $root.QueryPayoutTransaction.QueryPayoutTransactionExample
      method: GET
      path: /Query/payouts/{entry}
      path-parameters:
        entry: root.Entry
      request:
        name: ListPayoutRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >
              Collection of field names, conditions, and values used to filter
              the query.

              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>


              List of field names accepted:

                - `status` (in, nin, eq, ne)
                - `transactionDate` (gt, ge, lt, le, eq, ne)
                - `billNumber` (ct, nct)
                - `vendorNumber` (ct, nct, eq, ne)
                - `vendorName` (ct, nct, eq, ne)
                - `paymentMethod` (ct, nct, eq, ne, in, nin)
                - `paymentId` (ct, nct, eq, ne)
                - `parentOrgId` (ne, eq, nin, in)
                - `batchNumber` (ct, nct, eq, ne)
                - `totalAmount` (gt, ge, lt, le, eq, ne)
                - `paypointLegal` (ne, eq, ct, nct)
                - `paypointDba` (ne, eq, ct, nct)
                - `accountId` (ne, eq, ct, nct)
                - `orgName` (ne, eq, ct, nct)
                - `externalPaypointID` (ct, nct, eq, ne)
                - `paypointId` (eq, ne)
                - `vendorId` (eq, ne)
                - `vendorEIN` (ct, nct, eq, ne)
                - `vendorPhone` (ct, nct, eq, ne)
                - `vendorEmail` (ct, nct, eq, ne)
                - `vendorAddress` (ct, nct, eq, ne)
                - `vendorCity` (ct, nct, eq, ne)
                - `vendorState` (ct, nct, eq, ne)
                - `vendorCountry` (ct, nct, eq, ne)
                - `vendorZip` (ct, nct, eq, ne)
                - `vendorMCC` (ct, nct, eq, ne)
                - `vendorLocationCode` (ct, nct, eq, ne)
                - `vendorCustomField1` (ct, nct, eq, ne)
                - `vendorCustomField2` (ct, nct, eq, ne)
                - `comments` (ct, nct)
                - `payaccountCurrency` (ne, eq, in, nin)
                - `remitAddress` (ct, nct)
                - `source` (ct, nct, eq, ne)
                - `updatedOn` (gt, ge, lt, le, eq, ne)
                - `feeAmount` (gt, ge, lt, le, eq, ne)
                - `lotNumber` (ct, nct)
                - `customerVendorAccount` (ct, nct, eq, ne)
                - `batchId` (eq, ne)
                - `payoutProgram`(eq, ne) the options are `managed` or `odp`. For example, `payoutProgram(eq)=managed` returns all records with a `payoutProgram` equal to `managed`. 

                List of comparison accepted - enclosed between parentheses:
                - eq or empty => equal
                - gt => greater than
                - ge => greater or equal
                - lt => less than
                - le => less or equal
                - ne => not equal
                - ct => contains
                - nct => not contains
                - in => inside array separated by \"|\"
                - nin => not inside array separated by \"|\"

                List of parameters accepted:

                - limitRecord : max number of records for query (default=\"20\", \"0\" or negative value for all)
                - fromRecord : initial record in query
                - sortBy : indicate field name and direction to sort the results

                Example: `netAmount(gt)=20` returns all records with a `netAmount` greater than 20.00

                Example: `sortBy=desc(netamount)` returns all records sorted by `netAmount` descending
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.QueryPayoutTransaction
      
    ListPayoutOrg:
      auth: true
      display-name: Get list of money out transactions for organization
      docs: >-
        Retrieves a list of money out transactions (payouts) for an
        organization. Use filters to limit results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.
      examples:
        - name: ListPayoutOrgExample
          path-parameters:
            orgId: 123
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
            
          response:
            body: $root.QueryPayoutTransaction.QueryPayoutTransactionExample
      method: GET
      path: /Query/payouts/org/{orgId}
      path-parameters:
        orgId:
          docs: The numeric identifier for organization, assigned by Payabli.
          type: integer
      request:
        name: ListPayoutOrgRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >
              Collection of field names, conditions, and values used to filter
              the query.

              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>

              List of field names accepted:
                
                - `status` (in, nin, eq, ne)
                - `transactionDate` (gt, ge, lt, le, eq, ne)
                - `billNumber` (ct, nct)
                - `vendorNumber` (ct, nct, eq, ne)
                - `vendorName` (ct, nct, eq, ne)
                - `parentOrgId` (ne, eq, nin, in)
                - `paymentMethod` (ct, nct, eq, ne, in, nin)
                - `paymentId` (ct, nct, eq, ne)
                - `batchNumber` (ct, nct, eq, ne)
                - `totalAmount` (gt, ge, lt, le, eq, ne)
                - `paypointLegal` (ne, eq, ct, nct)
                - `paypointDba` (ne, eq, ct, nct)
                - `accountId` (ne, eq, ct, nct)
                - `orgName` (ne, eq, ct, nct)
                - `externalPaypointID` (ct, nct, eq, ne)
                - `paypointId` (eq, ne)
                - `vendorId` (eq, ne)
                - `vendorEIN` (ct, nct, eq, ne)
                - `vendorPhone` (ct, nct, eq, ne)
                - `vendorEmail` (ct, nct, eq, ne)
                - `vendorAddress` (ct, nct, eq, ne)
                - `vendorCity` (ct, nct, eq, ne)
                - `vendorState` (ct, nct, eq, ne)
                - `vendorCountry` (ct, nct, eq, ne)
                - `vendorZip` (ct, nct, eq, ne)
                - `vendorMCC` (ct, nct, eq, ne)
                - `vendorLocationCode` (ct, nct, eq, ne)
                - `vendorCustomField1` (ct, nct, eq, ne)
                - `vendorCustomField2` (ct, nct, eq, ne)
                - `comments` (ct, nct)
                - `payaccountCurrency` (ne, eq, in, nin)
                - `remitAddress` (ct, nct)
                - `source` (ct, nct, eq, ne)
                - `updatedOn` (gt, ge, lt, le, eq, ne)
                - `feeAmount` (gt, ge, lt, le, eq, ne)
                - `lotNumber` (ct, nct)
                - `customerVendorAccount` (ct, nct, eq, ne)
                - `batchId` (eq, ne)
                - `payoutProgram`(eq, ne) the options are `managed` or `odp`. For example, `payoutProgram(eq)=managed` returns all records with a `payoutProgram` equal to `managed`.

                List of comparison accepted - enclosed between parentheses:
                - eq or empty => equal
                - gt => greater than
                - ge => greater or equal
                - lt => less than
                - le => less or equal
                - ne => not equal
                - ct => contains
                - nct => not contains
                - in => inside array separated by \"|\"
                - nin => not inside array separated by \"|\"

                List of parameters accepted:

                - limitRecord : max number of records for query (default=\"20\", \"0\" or negative value for all)
                - fromRecord : initial record in query
                - sortBy : indicate field name and direction to sort the results

                Example: `netAmount(gt)=20` returns all records with a `netAmount` greater than 20.00

                Example: `sortBy=desc(netamount)` returns all records sorted by `netAmount` descending
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.QueryPayoutTransaction
      
    ListPaypoints:
      auth: true
      display-name: Get list of paypoints for an organization
      docs: >-
        Returns a list of paypoints in an organization. Use filters to limit
        results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.
      examples:
        - path-parameters:
            orgId: 123
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                - AverageMonthlyVolume: 1000
                  AverageTicketAmount: 1000
                  BAddress1: 123 Walnut Street
                  BAddress2: Suite 103
                  BankData:
                    - bankAccountFunction: 0
                      bankAccountHolderName: Gruzya Adventure Outfitters LLC
                      nickname: Business Checking 1234
                  BCity: New Vegas
                  BCountry: US
                  BFax: '5551234567'
                  BinPerson: 60
                  BinPhone: 20
                  BinWeb: 20
                  BoardingId: 340
                  BPhone: '5551234567'
                  BStartdate: 01/01/1990
                  BState: FL
                  BSummary: Brick and mortar store that sells office supplies
                  BTimeZone: -5
                  BZip: '33000'
                  ContactData:
                    - {}
                  CreatedAt: '2022-07-01T15:00:01Z'
                  DbaName: Sunshine Gutters
                  DocumentsRef: DocumentsRef
                  Ein: '123456789'
                  EntryPoints:
                    - {}
                  externalPaypointID: Paypoint-100
                  ExternalProcessorInformation: '[MER_xxxxxxxxxxxxxx]/[NNNNNNNNN]'
                  HighTicketAmount: 1000
                  IdPaypoint: 1000000
                  LastModified: '2022-07-01T15:00:01Z'
                  LegalName: Sunshine Services, LLC
                  License: 2222222FFG
                  LicenseState: CA
                  MAddress1: 123 Walnut Street
                  MAddress2: STE 900
                  Mccid: Mccid
                  MCity: Johnson City
                  MCountry: US
                  MState: TN
                  MZip: '37615'
                  OrgId: 123
                  OrgParentName: PropertyManager Pro
                  OwnerData:
                    - {}
                  OwnType: Limited Liability Company
                  PaypointStatus: 1
                  SalesCode: SalesCode
                  Taxfillname: Sunshine LLC
                  TemplateId: 22
                  WebsiteAddress: www.example.com
                  Whencharged: When Service Provided
                  Whendelivered: 0-7 Days
                  Whenprovided: 30 Days or Less
                  Whenrefund: Exchange Only
              Summary:
                pageIdentifier: 'null'
                pageSize: 20
                totalAmount: 77.22
                totalNetAmount: 77.22
                totalPages: 2
                totalRecords: 2
      method: GET
      path: /Query/paypoints/{orgId}
      path-parameters:
        orgId:
          docs: The numeric identifier for organization, assigned by Payabli.
          type: integer
      request:
        name: ListPaypointsRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >
              Collection of field names, conditions, and values used to filter
              the query

              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>

              **List of field names accepted:**


              - `createdAt` (gt, ge, lt, le, eq, ne)

              - `lastModified` (gt, ge, lt, le, eq, ne)

              - `startDate` (gt, ge, lt, le, eq, ne)

              - `dbaname`  (ct, nct)

              - `status` (eq, ne)

              - `legalname`  (ct, nct)

              - `externalPaypointID` (ct, nct)

              - `ein`  (ct, nct)

              - `address`  (ct, nct)

              - `city`  (ct, nct)

              - `state`  (ct, nct)

              - `phone`  (ct, nct)

              - `mcc`  (ct, nct)

              - `owntype`  (ct, nct)

              - `ownerName`  (ct, nct)

              - `contactName`  (ct, nct)

              - `paypointId` (eq, ne)

              - `orgParentname`  (ct, nct, in, nin)

              - `boardingId` (eq, ne) 

              - `entryName`  (ct, nct)

              - `externalOrgID` (ct, nct)


              **List of comparison accepted - enclosed between parentheses:**


              - `eq` or empty => equal

              - `gt` => greater than

              - `ge` => greater or equal

              - `lt` => less than

              - `le` => less or equal

              - `ne` => not equal

              - `ct` => contains

              - `nct` => not contains

              - `in` => inside array

              - `nin` => not inside array


              **List of parameters accepted:**


              - `limitRecord` : max number of records for query (default="20",
              "0" or negative value for all)

              - `fromRecord` : initial record in query


              Example: `dbaname(ct)=hoa` returns all records with a `dbaname`
              containing "hoa"
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.QueryEntrypointResponse
      
    ListSettlements:
      auth: true
      display-name: Get list of settled transactions for a paypoint
      docs: >-
        Retrieve a list of settled transactions for a paypoint. Use filters to
        limit results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.

      examples:
        - name: Example query response
          path-parameters:
            entry: 8cfec329267
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                - BatchAmount: 32
                  BatchNumber: batch-100-20-2024
                  Category: auth
                  CreatedAt: '2024-11-19T15:58:01Z'
                  Customer:
                    AdditionalData: AdditionalData
                    BillingAddress1: 100 Golden Ridge Drive
                    BillingAddress2: STE 100
                    BillingCity: Mendota
                    BillingCountry: US
                    BillingEmail: lydia@example.com
                    BillingPhone: '+12345678'
                    BillingState: VA
                    BillingZip: '20147'
                    customerId: 2707
                    CustomerNumber: '901102'
                    customerStatus: 1
                    FirstName: Lydia
                    LastName: Marshall
                  ExpectedDepositDate: '2024-11-22T00:00:00Z'
                  GatewayTransId: TRN_XXXXX
                  Id: 25048
                  isHold: 0
                  MaskedAccount: 1XXXXXX5678
                  Method: ach
                  NetAmount: 2
                  Operation: Sale
                  OrderId: ''
                  ParentOrgName: Pilgrim Planner
                  PaymentData:
                    AccountType: Checking
                    HolderName: Lydia Marshall
                    MaskedAccount: 1XXXXXX5678
                    paymentDetails:
                      categories:
                        - amount: 1000
                          label: Deposit
                      currency: USD
                      serviceFee: 0
                      splitFunding:
                        - {}
                      totalAmount: 2
                  PaymentTransId: 245-9e4072eef77e45979ea0e49f680000X
                  PaymentTransStatus: 1
                  PaypointDbaname: Gruzya Adventure Outfitters, LLC
                  PaypointEntryname: 7f1a3816XX
                  PaypointLegalname: Gruzya Adventure Outfitters, LLC
                  ResponseData:
                    authcode: ''
                    avsresponse_text: ''
                    cvvresponse_text: ''
                    response_code: '100'
                    response_code_text: Operation successful.
                    responsetext: CAPTURED
                    transactionid: TRN_XXXXX
                  ScheduleReference: 0
                  SettledAmount: 0.5
                  SettlementDate: '2024-11-20T00:00:00Z'
                  Source: api
                  Status: 1
                  TransactionEvents:
                    - EventTime: '2024-11-19T15:57:40Z'
                      TransEvent: Created
                    - EventData:
                        account_id: TRA_XXXXX
                        account_name: '123456'
                        action:
                          app_id: XXXXX
                          app_name: PayAbli
                          id: ACT_XXXXX
                          result_code: SUCCESS
                          time_created: '2024-11-19T20:58:01.583Z'
                          type: AUTHORIZE
                        amount: '200'
                        batch_id: ''
                        capture_mode: AUTO
                        channel: CNP
                        country: US
                        currency: USD
                        fees:
                          amount: '0'
                          rate: '0.00'
                          total_amount: '0'
                        id: TRN_XXXXX
                        merchant_amount: '200'
                        merchant_id: MER_XXXXX
                        merchant_name: Henriette97
                        order:
                          reference: ''
                        payment_method:
                          bank_transfer:
                            account_type: CHECKING
                            bank:
                              name: ''
                            masked_account_number_last4: XXXX5678
                          entry_mode: ECOM
                          message: Success
                          narrative: Lydia Marshall
                          result: '00'
                        reference: 245-XXXXX
                        status: CAPTURED
                        time_created: '2024-11-19T20:58:01.583Z'
                        type: SALE
                      EventTime: '2024-11-19T20:58:01Z'
                      TransEvent: Approved
                    - EventTime: '2024-11-20T03:05:10Z'
                      TransEvent: ClosedBatch
                  TransactionTime: '2024-11-19T15:58:01Z'
                  TransMethod: ach
                  Type: credit
              Summary:
                heldAmount: 3.7
                pageSize: 0
                refunds: -3521.85
                serviceFees: 852.48
                totalAmount: 61645.74
                totalNetAmount: 61645.74
                totalPages: 21872
                totalRecords: 21872
                transferAmount: 0
      method: GET
      path: /Query/settlements/{entry}
      path-parameters:
        entry: root.Entry
      request:
        name: ListSettlementsRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >

              Collection of field names, conditions, and values used to filter
              the query. 

              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>
              
              
              See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for more information.


              **List of field names accepted:**


              - `settlementDate` (gt, ge, lt, le, eq, ne)

              - `depositDate` (gt, ge, lt, le, eq, ne)

              - `transId`  (ne, eq, ct, nct)

              - `gatewayTransId`  (ne, eq, ct, nct)

              - `method`   (in, nin, eq, ne)

              - `settledAmount`  (gt, ge, lt, le, eq, ne)

              - `operation`    (in, nin, eq, ne)

              - `source`   (in, nin, eq, ne)

              - `batchNumber`  (ct, nct, eq, ne)

              - `payaccountLastfour`   (nct, ct)

              - `payaccountType`   (ne, eq, in, nin)

              - `customerFirstname`   (ct, nct, eq, ne)

              - `customerLastname`    (ct, nct, eq, ne)

              - `customerName`   (ct, nct)

              - `customerId`  (eq, ne)

              - `customerNumber`  (ct, nct, eq, ne)

              - `customerCompanyname`    (ct, nct, eq, ne)

              - `customerAddress` (ct, nct, eq, ne)

              - `customerCity`    (ct, nct, eq, ne)

              - `customerZip` (ct, nct, eq, ne)

              - `customerState` (ct, nct, eq, ne)

              - `customerCountry` (ct, nct, eq, ne)

              - `customerPhone` (ct, nct, eq, ne)

              - `customerEmail` (ct, nct, eq, ne)

              - `customerShippingAddress` (ct, nct, eq, ne)

              - `customerShippingCity`    (ct, nct, eq, ne)

              - `customerShippingZip` (ct, nct, eq, ne)

              - `customerShippingState` (ct, nct, eq, ne)

              - `customerShippingCountry` (ct, nct, eq, ne)

              - `orgId`  (eq) *mandatory when entry=org*

              - `isHold` (eq, ne)

              - `paypointId`  (ne, eq)

              - `paypointLegal`  (ne, eq, ct, nct)

              - `paypointDba`  (ne, eq, ct, nct)

              - `orgName`  (ne, eq, ct, nct)

              - `batchId` (ct, nct, eq, neq)

              - `additional-xxx`  (ne, eq, ct, nct) where xxx is the additional
              field name


              **List of comparison accepted:**

              - `eq` or empty => equal

              - `gt` => greater than

              - `ge` => greater or equal

              - `lt` => less than

              - `le` => less or equal

              - `ne` => not equal

              - `ct` => contains

              - `nct` => not contains

              - `in` => inside array separated by "|"

              - `nin` => not inside array separated by "|"


              **List of parameters accepted:**


              - `limitRecord`: max number of records for query (default="20",
              "0" or negative value for all)

              - `fromRecord`: initial record in query


              Example: `settledAmount(gt)=20` returns all records with a `settledAmount` greater than 20.00.
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.QueryResponseSettlements
      
    ListSettlementsOrg:
      auth: true
      display-name: Get list of settled transactions for an org
      docs: |
        Retrieve a list of settled transactions for an organization. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.
      examples:
        - name: Example query response
          path-parameters:
            orgId: 123
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                - BatchAmount: 32
                  BatchNumber: batch-100-20-2024
                  Category: auth
                  CreatedAt: '2024-11-19T15:58:01Z'
                  Customer:
                    AdditionalData: AdditionalData
                    BillingAddress1: 100 Golden Ridge Drive
                    BillingAddress2: STE 100
                    BillingCity: Mendota
                    BillingCountry: US
                    BillingEmail: lydia@example.com
                    BillingPhone: '+12345678'
                    BillingState: VA
                    BillingZip: '20147'
                    customerId: 2707
                    CustomerNumber: '901102'
                    customerStatus: 1
                    FirstName: Lydia
                    LastName: Marshall
                  ExpectedDepositDate: '2024-11-22T00:00:00Z'
                  GatewayTransId: TRN_XXXXX
                  Id: 25048
                  isHold: 0
                  MaskedAccount: 1XXXXXX5678
                  Method: ach
                  NetAmount: 2
                  Operation: Sale
                  OrderId: ''
                  ParentOrgName: Pilgrim Planner
                  PaymentData:
                    AccountType: Checking
                    HolderName: Lydia Marshall
                    MaskedAccount: 1XXXXXX5678
                    paymentDetails:
                      categories:
                        - amount: 1000
                          label: Deposit
                      currency: USD
                      serviceFee: 0
                      splitFunding:
                        - {}
                      totalAmount: 2
                  PaymentTransId: 245-9e4072eef77e45979ea0e49f680000X
                  PaymentTransStatus: 1
                  PaypointDbaname: Gruzya Adventure Outfitters, LLC
                  PaypointEntryname: 7f1a3816XX
                  PaypointLegalname: Gruzya Adventure Outfitters, LLC
                  ResponseData:
                    authcode: ''
                    avsresponse_text: ''
                    cvvresponse_text: ''
                    response_code: '100'
                    response_code_text: Operation successful.
                    responsetext: CAPTURED
                    transactionid: TRN_XXXXX
                  ScheduleReference: 0
                  SettledAmount: 0.5
                  SettlementDate: '2024-11-20T00:00:00Z'
                  Source: api
                  Status: 1
                  TransactionEvents:
                    - EventTime: '2024-11-19T15:57:40Z'
                      TransEvent: Created
                    - EventData:
                        account_id: TRA_XXXXX
                        account_name: '123456'
                        action:
                          app_id: XXXXX
                          app_name: PayAbli
                          id: ACT_XXXXX
                          result_code: SUCCESS
                          time_created: '2024-11-19T20:58:01.583Z'
                          type: AUTHORIZE
                        amount: '200'
                        batch_id: ''
                        capture_mode: AUTO
                        channel: CNP
                        country: US
                        currency: USD
                        fees:
                          amount: '0'
                          rate: '0.00'
                          total_amount: '0'
                        id: TRN_XXXXX
                        merchant_amount: '200'
                        merchant_id: MER_XXXXX
                        merchant_name: Henriette97
                        order:
                          reference: ''
                        payment_method:
                          bank_transfer:
                            account_type: CHECKING
                            bank:
                              name: ''
                            masked_account_number_last4: XXXX5678
                          entry_mode: ECOM
                          message: Success
                          narrative: Lydia Marshall
                          result: '00'
                        reference: 245-XXXXX
                        status: CAPTURED
                        time_created: '2024-11-19T20:58:01.583Z'
                        type: SALE
                      EventTime: '2024-11-19T20:58:01Z'
                      TransEvent: Approved
                    - EventTime: '2024-11-20T03:05:10Z'
                      TransEvent: ClosedBatch
                  TransactionTime: '2024-11-19T15:58:01Z'
                  TransMethod: ach
                  Type: credit
              Summary:
                heldAmount: 3.7
                pageSize: 0
                refunds: -3521.85
                serviceFees: 852.48
                totalAmount: 61645.74
                totalNetAmount: 61645.74
                totalPages: 21872
                totalRecords: 21872
                transferAmount: 0
      method: GET
      path: /Query/settlements/org/{orgId}
      path-parameters:
        orgId:
          docs: The numeric identifier for organization, assigned by Payabli.
          type: integer
      request:
        name: ListSettlementsOrgRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >

              Collection of field names, conditions, and values used to filter
              the query. 

              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>
              
              
              See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for more information.


              **List of field names accepted:**


              - `settlementDate` (gt, ge, lt, le, eq, ne)

              - `depositDate` (gt, ge, lt, le, eq, ne)

              - `transId`  (ne, eq, ct, nct)

              - `gatewayTransId`  (ne, eq, ct, nct)

              - `method`   (in, nin, eq, ne)

              - `settledAmount`  (gt, ge, lt, le, eq, ne)

              - `operation`    (in, nin, eq, ne)

              - `source`   (in, nin, eq, ne)

              - `batchNumber`  (ct, nct, eq, ne)

              - `payaccountLastfour`   (nct, ct)

              - `payaccountType`   (ne, eq, in, nin)

              - `customerFirstname`   (ct, nct, eq, ne)

              - `customerLastname`    (ct, nct, eq, ne)

              - `customerName`   (ct, nct)

              - `customerId`  (eq, ne)

              - `customerNumber`  (ct, nct, eq, ne)

              - `customerCompanyname`    (ct, nct, eq, ne)

              - `customerAddress` (ct, nct, eq, ne)

              - `customerCity`    (ct, nct, eq, ne)

              - `customerZip` (ct, nct, eq, ne)

              - `customerState` (ct, nct, eq, ne)

              - `customerCountry` (ct, nct, eq, ne)

              - `customerPhone` (ct, nct, eq, ne)

              - `customerEmail` (ct, nct, eq, ne)

              - `customerShippingAddress` (ct, nct, eq, ne)

              - `customerShippingCity`    (ct, nct, eq, ne)

              - `customerShippingZip` (ct, nct, eq, ne)

              - `customerShippingState` (ct, nct, eq, ne)

              - `customerShippingCountry` (ct, nct, eq, ne)

              - `orgId`  (eq) *mandatory when entry=org*

              - `isHold` (eq, ne)

              - `paypointId`  (ne, eq)

              - `paypointLegal`  (ne, eq, ct, nct)

              - `paypointDba`  (ne, eq, ct, nct)

              - `orgName`  (ne, eq, ct, nct)

              - `batchId` (ct, nct, eq, neq)

              - `additional-xxx`  (ne, eq, ct, nct) where xxx is the additional
              field name


              **List of comparison accepted:**

              - `eq` or empty => equal

              - `gt` => greater than

              - `ge` => greater or equal

              - `lt` => less than

              - `le` => less or equal

              - `ne` => not equal

              - `ct` => contains

              - `nct` => not contains

              - `in` => inside array separated by "|"

              - `nin` => not inside array separated by "|"


              **List of parameters accepted:**


              - `limitRecord`: max number of records for query (default="20",
              "0" or negative value for all)

              - `fromRecord`: initial record in query


              Example: `settledAmount(gt)=20` returns all records with a `settledAmount` greater than 20.00.
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.QueryResponseSettlements
      
    ListSubscriptions:
      auth: true
      display-name: Get list of subscriptions for a paypoint
      docs: >-
        Returns a list of subscriptions for a single paypoint. Use filters to
        limit results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.
      examples:
        - name: Example
          path-parameters:
            entry: 8cfec329267
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                CreatedAt: '2023-12-14T08:51:10Z'
                Customer:
                  AdditionalData: AdditionalData
                  BillingAddress1: 68 Golden Drive
                  BillingAddress2: ''
                  BillingCity: Johnson City
                  BillingCountry: US
                  BillingEmail: company@payabli.com
                  BillingPhone: ''
                  BillingState: TN
                  BillingZip: '37612'
                  CompanyName: Sunshine LLC
                  customerId: 1323
                  CustomerNumber: '1234'
                  customerStatus: 1
                  FirstName: Lisandra
                  Identifiers:
                    - \"firstname\"
                    - \"lastname\"
                    - \"email\"
                  LastName: Smitch
                  ShippingAddress1: 68 Golden Drive
                  ShippingCity: Johnson City
                  ShippingCountry: US
                  ShippingState: TN
                  ShippingZip: '37612'
                EndDate: '2026-03-20T00:00:00Z'
                EntrypageId: 0
                ExternalPaypointID: f743aed24a-10
                FeeAmount: 0
                Frequency: monthly
                IdSub: 580
                InvoiceData:
                  AdditionalData: AdditionalData
                  frequency: one-time
                  invoiceAmount: 100
                  invoiceNumber: QA-1702561870
                  invoiceStatus: 1
                  invoiceType: 1
                  items:
                    - itemCost: 10
                      itemDescription: service
                      itemMode: 1
                      itemProductName: Mat replacement
                      itemQty: 5
                      itemTotalAmount: 50
                    - itemCost: 5
                      itemDescription: service
                      itemMode: 1
                      itemProductName: Mat clean
                      itemQty: 10
                      itemTotalAmount: 50
                LastRun: '2024-01-02T14:32:11Z'
                LastUpdated: '2023-12-14T08:51:10Z'
                LeftCycles: 20
                Method: card
                NetAmount: 10
                NextDate: '2024-07-20T00:00:00Z'
                ParentOrgName: FitnessManager
                PaymentData:
                  AccountExp: '0924'
                  AccountType: unknown
                  AccountZip: '90210'
                  binData: $root.BinData.ExampleBinData
                  HolderName: Mr. Michael Abernathy
                  Initiator: payor
                  MaskedAccount: 2222 4XXXXXX0010
                  paymentDetails:
                    currency: USD
                    serviceFee: 0
                    totalAmount: 100
                  Sequence: subsequent
                  StoredMethodUsageType: subscription
                PaypointDbaname: Athlete Factory LLC
                PaypointEntryname: 473ac58b0
                PaypointId: 10
                PaypointLegalname: Athlete Factory LLC
                PlanId: 1
                StartDate: '2024-07-20T00:00:00Z'
                SubEvents:
                  - description: created
                    eventTime: '2023-12-14T13:51:10Z'
                    refData: 00-3470dfe2658b492811630255602f3fb5-d06fe0f72110000-00
                  - description: updated
                    eventTime: '2023-12-15T10:30:00Z'
                    refData: 01-1234abcde6789fghij4567klmnopqr89-abcdefghi12345678-01
                    source: web app
                SubStatus: 1
                TotalAmount: 100
                TotalCycles: 20
                UntilCancelled: false
              Summary:
                pageIdentifier: XXXXXXXXXXXXXXXXXXX
                pageSize: 20
                totalAmount: 150.22
                totalNetAmount: 150.22
                totalPages: 1
                totalRecords: 2
      method: GET
      path: /Query/subscriptions/{entry}
      path-parameters:
        entry: root.Entry
      request:
        name: ListSubscriptionsRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >

              Collection of field names, conditions, and values used to filter
              the query. 
              
              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>
              
              See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for more information.
                    
              **List of field names accepted:**


              - `startDate` (gt, ge, lt, le, eq, ne)

              - `endDate` (gt, ge, lt, le, eq, ne)

              - `nextDate` (gt, ge, lt, le, eq, ne)

              - `frequency` (in, nin, ne, eq)

              - `method` (in, nin, eq, ne)

              - `totalAmount` (gt, ge, lt, le, eq, ne)

              - `netAmount` (gt, ge, lt, le, eq, ne)

              - `feeAmount` (gt, ge, lt, le, eq, ne)

              - `status` (in, nin, eq, ne)

              - `untilcancelled` (eq, ne)

              - `payaccountLastfour` (nct, ct)

              - `payaccountType` (ne, eq, in, nin)

              - `payaccountCurrency` (ne, eq, in, nin)

              - `customerFirstname` (ct, nct, eq, ne)

              - `customerLastname` (ct, nct, eq, ne)

              - `customerName` (ct, nct)

              - `customerId` (eq, ne)

              - `customerNumber` (ct, nct, eq, ne)

              - `customerCompanyname` (ct, nct, eq, ne)

              - `customerAddress` (ct, nct, eq, ne)

              - `customerCity` (ct, nct, eq, ne)

              - `customerZip` (ct, nct, eq, ne)

              - `customerState` (ct, nct, eq, ne)

              - `customerCountry` (ct, nct, eq, ne)

              - `customerPhone` (ct, nct, eq, ne)

              - `customerEmail` (ct, nct, eq, ne)

              - `customerShippingAddress` (ct, nct, eq, ne)

              - `customerShippingCity` (ct, nct, eq, ne)

              - `customerShippingZip` (ct, nct, eq, ne)

              - `customerShippingState` (ct, nct, eq, ne)

              - `customerShippingCountry` (ct, nct, eq, ne)

              - `orgId` (eq)

              - `paypointId` (ne, eq)

              - `paypointLegal` (ne, eq, ct, nct)

              - `paypointDba` (ne, eq, ct, nct)

              - `orgName` (ne, eq, ct, nct)

              - `externalPaypointId` (ct, nct, ne, eq)

              - `subId` (eq, ne)

              - `orderDescription` (ct, nct)

              - `cycles` (eq, ne, gt, ge, lt, le)

              - `leftcycles` (eq, ne, gt, ge, lt, le)

              - `createdAt` (eq, ne, gt, ge, lt, le)

              - `updatedOn` (eq, ne, gt, ge, lt, le)

              - `invoiceNumber` (ct, nct)

              - `additional-xxx` (ne, eq, ct, nct) where xxx is the additional
              field name  


              **List of comparison operators accepted:**

              - `eq` or empty => equal

              - `gt` => greater than

              - `ge` => greater or equal

              - `lt` => less than

              - `le` => less or equal

              - `ne` => not equal

              - `ct` => contains

              - `nct` => not contains

              - `in` => inside array

              - `nin` => not inside array
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.QuerySubscriptionResponse
      
    ListSubscriptionsOrg:
      auth: true
      display-name: Get list of subscriptions for an org
      docs: >-
        Returns a list of subscriptions for a single org. Use filters to limit
        results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.
      examples:
        - name: Example
          path-parameters:
            orgId: 123
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                CreatedAt: '2023-12-14T08:51:10Z'
                Customer:
                  AdditionalData: AdditionalData
                  BillingAddress1: 68 Golden Drive
                  BillingAddress2: ''
                  BillingCity: Johnson City
                  BillingCountry: US
                  BillingEmail: company@payabli.com
                  BillingPhone: ''
                  BillingState: TN
                  BillingZip: '37612'
                  CompanyName: Sunshine LLC
                  customerId: 1323
                  CustomerNumber: '1234'
                  customerStatus: 1
                  FirstName: Lisandra
                  Identifiers:
                    - \"firstname\"
                    - \"lastname\"
                    - \"email\"
                  LastName: Smitch
                  ShippingAddress1: 68 Golden Drive
                  ShippingCity: Johnson City
                  ShippingCountry: US
                  ShippingState: TN
                  ShippingZip: '37612'
                EndDate: '2026-03-20T00:00:00Z'
                EntrypageId: 0
                ExternalPaypointID: f743aed24a-10
                FeeAmount: 0
                Frequency: monthly
                IdSub: 580
                InvoiceData:
                  AdditionalData: AdditionalData
                  frequency: one-time
                  invoiceAmount: 100
                  invoiceNumber: QA-1702561870
                  invoiceStatus: 1
                  invoiceType: 1
                  items:
                    - itemCost: 10
                      itemDescription: service
                      itemMode: 1
                      itemProductName: Mat replacement
                      itemQty: 5
                      itemTotalAmount: 50
                    - itemCost: 5
                      itemDescription: service
                      itemMode: 1
                      itemProductName: Mat clean
                      itemQty: 10
                      itemTotalAmount: 50
                LastRun: '2024-01-02T14:32:11Z'
                LastUpdated: '2023-12-14T08:51:10Z'
                LeftCycles: 20
                Method: card
                NetAmount: 10
                NextDate: '2024-07-20T00:00:00Z'
                ParentOrgName: FitnessManager
                PaymentData:
                  AccountExp: '0924'
                  AccountType: unknow
                  AccountZip: '90210'
                  binData: $root.BinData.ExampleBinData
                  HolderName: Mr. Michael Abernathy
                  Initiator: payor
                  MaskedAccount: 2222 4XXXXXX0010
                  paymentDetails:
                    currency: USD
                    serviceFee: 0
                    totalAmount: 100
                  Sequence: subsequent
                  StoredMethodUsageType: subscription
                PaypointDbaname: Athlete Factory LLC
                PaypointEntryname: 473ac58b0
                PaypointId: 10
                PaypointLegalname: Athlete Factory LLC
                PlanId: 1
                StartDate: '2024-07-20T00:00:00Z'
                SubEvents:
                  - description: created
                    eventTime: '2023-12-14T13:51:10Z'
                    refData: 00-3470dfe2658b492811630255602f3fb5-d06fe0f72110000-00
                  - description: updated
                    eventTime: '2023-12-15T10:30:00Z'
                    refData: 01-1234abcde6789fghij4567klmnopqr89-abcdefghi12345678-01
                    source: web app
                SubStatus: 1
                TotalAmount: 100
                TotalCycles: 20
                UntilCancelled: false
              Summary:
                pageIdentifier: XXXXXXXXXXXXXXXXXXX
                pageSize: 20
                totalAmount: 150.22
                totalNetAmount: 150.22
                totalPages: 1
                totalRecords: 2
      method: GET
      path: /Query/subscriptions/org/{orgId}
      path-parameters:
        orgId:
          docs: The numeric identifier for organization, assigned by Payabli.
          type: integer
      request:
        name: ListSubscriptionsOrgRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >

              Collection of field names, conditions, and values used to filter
              the query. 
              
              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>

              See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for more information.
                    
              **List of field names accepted:**


              - `startDate` (gt, ge, lt, le, eq, ne)

              - `endDate` (gt, ge, lt, le, eq, ne)

              - `nextDate` (gt, ge, lt, le, eq, ne)

              - `frequency` (in, nin, ne, eq)

              - `method` (in, nin, eq, ne)

              - `totalAmount` (gt, ge, lt, le, eq, ne)

              - `netAmount` (gt, ge, lt, le, eq, ne)

              - `feeAmount` (gt, ge, lt, le, eq, ne)

              - `status` (in, nin, eq, ne)

              - `untilcancelled` (eq, ne)

              - `payaccountLastfour` (nct, ct)

              - `payaccountType` (ne, eq, in, nin)

              - `payaccountCurrency` (ne, eq, in, nin)

              - `customerFirstname` (ct, nct, eq, ne)

              - `customerLastname` (ct, nct, eq, ne)

              - `customerName` (ct, nct)

              - `customerId` (eq, ne)

              - `customerNumber` (ct, nct, eq, ne)

              - `customerCompanyname` (ct, nct, eq, ne)

              - `customerAddress` (ct, nct, eq, ne)

              - `customerCity` (ct, nct, eq, ne)

              - `customerZip` (ct, nct, eq, ne)

              - `customerState` (ct, nct, eq, ne)

              - `customerCountry` (ct, nct, eq, ne)

              - `customerPhone` (ct, nct, eq, ne)

              - `customerEmail` (ct, nct, eq, ne)

              - `customerShippingAddress` (ct, nct, eq, ne)

              - `customerShippingCity` (ct, nct, eq, ne)

              - `customerShippingZip` (ct, nct, eq, ne)

              - `customerShippingState` (ct, nct, eq, ne)

              - `customerShippingCountry` (ct, nct, eq, ne)

              - `orgId` (eq)

              - `paypointId` (ne, eq)

              - `paypointLegal` (ne, eq, ct, nct)

              - `paypointDba` (ne, eq, ct, nct)

              - `orgName` (ne, eq, ct, nct)

              - `externalPaypointId` (ct, nct, ne, eq)

              - `subId` (eq, ne)

              - `orderDescription` (ct, nct)

              - `cycles` (eq, ne, gt, ge, lt, le)

              - `leftcycles` (eq, ne, gt, ge, lt, le)

              - `createdAt` (eq, ne, gt, ge, lt, le)

              - `updatedOn` (eq, ne, gt, ge, lt, le)

              - `invoiceNumber` (ct, nct)

              - `additional-xxx` (ne, eq, ct, nct) where xxx is the additional
              field name  


              **List of comparison operators accepted:**

              - `eq` or empty => equal

              - `gt` => greater than

              - `ge` => greater or equal

              - `lt` => less than

              - `le` => less or equal

              - `ne` => not equal

              - `ct` => contains

              - `nct` => not contains

              - `in` => inside array

              - `nin` => not inside array      
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.QuerySubscriptionResponse
      
    ListTransactions:
      auth: true
      display-name: Get list of transactions for a paypoint
      docs: >-
        Retrieve a list of transactions for a paypoint. Use filters to limit
        results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.
        
        By default, this endpoint returns only transactions from the last 60 days. To query transactions outside of this period, include `transactionDate` filters.

        For example, this request parameters filter for transactions between April 01, 2024 and April 09, 2024. 

        ```
        curl --request GET \
          --url https://sandbox.payabli.com/api/Query/transactions/org/1?limitRecord=20&fromRecord=0&transactionDate(ge)=2024-04-01T00:00:00&transactionDate(le)=2024-04-09T23:59:59\
          --header 'requestToken: <api-key>'

          ```
      examples:
        - name: Example
          path-parameters:
            entry: 8cfec329267
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                - BatchAmount: 30.22
                  BatchNumber: '1234567'
                  CfeeTransactions:
                    - cFeeTransid: string
                      feeAmount: 0
                      operation: string
                      refundId: 0
                      settlementStatus: 0
                      transactionTime: '2019-08-24T14:15:22Z'
                      transStatus: 0
                  ConnectorName: gp
                  Customer:
                    AdditionalData: AdditionalData
                    BillingAddress1: 1111 street
                    BillingAddress2: string
                    BillingCity: myCity
                    BillingCountry: US
                    BillingEmail: customer@mail.com
                    BillingPhone: '1234567890'
                    BillingState: CA
                    BillingZip: '45567'
                    CompanyName: Sunshine LLC
                    customerId: 34
                    CustomerNumber: 3456-7645A
                    FirstName: John
                    LastName: Doe
                    ShippingAddress1: string
                    ShippingAddress2: string
                    ShippingCity: string
                    ShippingCountry: string
                    ShippingState: string
                    ShippingZip: string
                  DeviceId: 6c361c7d-674c-44cc-b790-382b75d1xxx
                  EntrypageId: 0
                  ExternalProcessorInformation: ' '
                  FeeAmount: 10.25
                  GatewayTransId: string
                  InvoiceData:
                    attachments:
                      - fContent: TXkgdGVzdCBmaWxlHJ==...
                        filename: my-doc.pdf
                        ftype: pdf
                        furl: https://mysite.com/my-doc.pdf
                    company: string
                    discount: 0
                    dutyAmount: 0
                    firstName: string
                    freightAmount: 10
                    frequency: one-time
                    invoiceAmount: 105
                    invoiceDate: '2026-01-01'
                    invoiceDueDate: '2026-01-15'
                    invoiceEndDate: '2026-01-15'
                    invoiceNumber: INV-2345
                    invoiceStatus: 0
                    invoiceType: 0
                    items:
                      - itemCategories:
                          - string
                        itemCommodityCode: string
                        itemCost: 1
                        itemDescription: string
                        itemMode: 0
                        itemProductCode: string
                        itemProductName: product 01
                        itemQty: 1
                        itemTaxAmount: 0
                        itemTaxRate: 0
                        itemTotalAmount: 0
                        itemUnitOfMeasure: U
                    lastName: string
                    notes: string
                    paymentTerms: PIA
                    purchaseOrder: string
                    shippingAddress1: string
                    shippingAddress2: string
                    shippingCity: string
                    shippingCountry: string
                    shippingEmail: string
                    shippingFromZip: string
                    shippingPhone: string
                    shippingState: string
                    shippingZip: string
                    summaryCommodityCode: string
                    tax: 2.05
                    termsConditions: string
                  Method: card
                  NetAmount: 100
                  Operation: Sale
                  OrderId: '9876543'
                  OrgId: 2
                  ParentOrgName: Payabli
                  PaymentData:
                    AccountExp: '0426'
                    AccountType: visa
                    binData: $root.BinData.PolishExampleBinData
                    HolderName: Billy J Franks
                    Initiator: payor
                    MaskedAccount: 411111XXXXXX1111
                    paymentDetails:
                      totalAmount: 100
                    Sequence: first
                    StoredId: 675b43c1-9867-463c-8dc7-3d6ddb68554b-12812
                    StoredMethodUsageType: unscheduled
                  PaymentTransId: 2345667-ddd-fff
                  PayorId: 55
                  PaypointDbaname: Sunshine LLC
                  PaypointEntryname: 7acda8200
                  PaypointId: 2
                  PaypointLegalname: Sunshine LLC
                  PendingFeeAmount: 2
                  RefundId: 0
                  ResponseData:
                    authcode: '00'
                    response_code: '100'
                    response_code_text: Transaction was approved.
                    responsetext: CAPTURED
                    transactionid: TRN_xwCAjQorWAYX1nAhAoHZVfN8iYHbI0
                  ReturnedId: 0
                  ScheduleReference: 0
                  SettlementStatus: 0
                  Source: vterminal
                  splitFundingInstructions:
                    - {}
                  TotalAmount: 110.25
                  TransactionEvents:
                    - {}
                  TransactionTime: '2019-08-24T14:15:22Z'
                  TransStatus: 1
              Summary:
                pageIdentifier: XXXXXXXXXXXXXXXXXXX
                pageSize: 20
                totalAmount: 177.22
                totalNetAmount: 177.22
                totalPages: 1
                totalRecords: 2
      method: GET
      path: /Query/transactions/{entry}
      path-parameters:
        entry: root.Entry
      request:
        name: ListTransactionsRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >

              Collection of field names, conditions, and values used to filter
              the query. 

              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>

              See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for more information.


              **List of field names accepted:**


              - `transactionDate` (gt, ge, lt, le, eq, ne)

              - `transId` (ne, eq, ct, nct, in, nin)

              - `gatewayTransId` (ne, eq, ct, nct)

              - `orderId` (ne, eq)

              - `scheduleId` (ne, eq)

              - `returnId` (ne, eq)

              - `refundId` (ne, eq)

              - `idTrans` (ne, eq)

              - `orgId` (ne, eq)

              - `paypointId` (ne, eq)

              - `paypointLegal` (ne, eq, ct, nct)

              - `paypointDba` (ne, eq, ct, nct)

              - `orgName` (ne, eq, ct, nct)

              - `externalPaypointId` (ct, nct, eq, ne)

              - `method` (in, nin, eq, ne)

              - `totalAmount` (gt, ge, lt, le, eq, ne)

              - `netAmount` (gt, ge, lt, le, eq, ne)

              - `feeAmount` (gt, ge, lt, le, eq, ne)

              - `operation` (in, nin, eq, ne)

              - `source` (in, nin, eq, ne, ct, nct)

              - `status` (in, nin, eq, ne)

              - `settlementStatus` (in, nin, eq, ne)

              - `batchNumber` (nct, ct)

              - `invoiceNumber` (ct, nct)

              - `authCode` (ct, nct)

              - `orderDescription` (ct, nct)

              - `payaccountLastfour` (nct, ct)

              - `payaccountType` (ne, eq, in, nin)

              - `payaccountCurrency` (ne, eq, in, nin)

              - `customerFirstname` (ct, nct, eq, ne)

              - `customerLastname` (ct, nct, eq, ne)

              - `customerName` (ct, nct)

              - `customerId` (eq, ne)

              - `customerNumber` (ct, nct, eq, ne)

              - `customerCompanyname` (ct, nct, eq, ne)

              - `customerAddress` (ct, nct, eq, ne)

              - `customerCity` (ct, nct, eq, ne)

              - `customerZip` (ct, nct, eq, ne)

              - `customerState` (ct, nct, eq, ne)

              - `customerCountry` (ct, nct, eq, ne)

              - `customerPhone` (ct, nct, eq, ne)

              - `customerEmail` (ct, nct, eq, ne)

              - `customerShippingAddress` (ct, nct, eq, ne)

              - `customerShippingCity` (ct, nct, eq, ne)

              - `customerShippingZip` (ct, nct, eq, ne)

              - `customerShippingState` (ct, nct, eq, ne)

              - `customerShippingCountry` (ct, nct, eq, ne)

              - `deviceId` (ct, nct, in, nin, eq, ne)

              - `AchSecCode` ( ct, nct, in, nin, eq, ne)

              - `AchHolderType` (ct, nct, in, nin, eq, and ne)

              - `additional-xxx` (ne, eq, ct, nct) where xxx is the additional
              field name related to customer data
              
              - 'invoiceAdditional-xxx' (ne, eq, ct, nct) where xxx is the additional
              field name related to invoice data


              **List of comparison operators accepted:**

              - `eq` or empty => equal

              - `gt` => greater than

              - `ge` => greater or equal

              - `lt` => less than

              - `le` => less or equal

              - `ne` => not equal

              - `ct` => contains

              - `nct` => not contains

              - `in` => inside array

              - `nin` => not inside array      
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.QueryResponseTransactions
      
    ListTransactionsOrg:
      auth: true
      display-name: Get list of transactions for an organization
      docs: |

        Retrieve a list of transactions for an organization. Use filters to
        limit results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.


        By default, this endpoint returns only transactions from the last 60 days. To query transactions outside of this period, include `transactionDate` filters.

        For example, this request parameters filter for transactions between April 01, 2024 and April 09, 2024. 

        ```
        curl --request GET \
          --url https://sandbox.payabli.com/api/Query/transactions/org/1?limitRecord=20&fromRecord=0&transactionDate(ge)=2024-04-01T00:00:00&transactionDate(le)=2024-04-09T23:59:59\
          --header 'requestToken: <api-key>'

          ```

      examples:
        - path-parameters:
            orgId: 123
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                - AchHolderType: personal
                  AchSecCode: AchSecCode
                  BatchAmount: 30.22
                  BatchNumber: batch_226_ach_12-30-2023
                  CfeeTransactions:
                    - transactionTime: '2024-01-15T09:30:00Z'
                  ConnectorName: gp
                  DeviceId: 6c361c7d-674c-44cc-b790-382b75d1xxx
                  EntrypageId: 0
                  ExternalProcessorInformation: '[MER_xxxxxxxxxxxxxx]/[NNNNNNNNN]'
                  FeeAmount: 1
                  GatewayTransId: TRN_xwCAjQorWAYX1nAhAoHZVfN8iYHbI0
                  Method: ach
                  NetAmount: 3762.87
                  Operation: Sale
                  OrderId: O-5140
                  OrgId: 123
                  ParentOrgName: PropertyManager Pro
                  PaymentData:
                    paymentDetails:
                      totalAmount: 100
                  PaymentTransId: 226-fe55ec0348e34702bd91b4be198ce7ec
                  PayorId: 1551
                  PaypointDbaname: Sunshine Gutters
                  PaypointEntryname: d193cf9a46
                  PaypointId: 226
                  PaypointLegalname: Sunshine Services, LLC
                  PendingFeeAmount: 2
                  RefundId: 0
                  ResponseData:
                    response_code: XXX
                    response_code_text: Transaction was approved.
                    responsetext: CAPTURED
                  ReturnedId: 0
                  ScheduleReference: 0
                  SettlementStatus: 2
                  Source: api
                  splitFundingInstructions:
                    - {}
                  TotalAmount: 30.22
                  TransactionEvents:
                    - {}
                  TransactionTime: '2025-10-19T00:00:00Z'
                  TransAdditionalData:
                    key: value
                  TransStatus: 1
              Summary:
                pageIdentifier: 'null'
                pageSize: 20
                totalAmount: 77.22
                totalNetAmount: 77.22
                totalPages: 2
                totalRecords: 2
      method: GET
      path: /Query/transactions/org/{orgId}
      path-parameters:
        orgId:
          docs: The numeric identifier for organization, assigned by Payabli.
          type: integer
      request:
        name: ListTransactionsOrgRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >

              Collection of field names, conditions, and values used to filter
              the query. 

              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>

              See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for more information.


              **List of field names accepted:**


              - `transactionDate` (gt, ge, lt, le, eq, ne)

              - `transId` (ne, eq, ct, nct, in, nin)

              - `gatewayTransId` (ne, eq, ct, nct)

              - `orderId` (ne, eq)

              - `scheduleId` (ne, eq)

              - `returnId` (ne, eq)

              - `refundId` (ne, eq)

              - `idTrans` (ne, eq)

              - `orgId` (ne, eq)

              - `paypointId` (ne, eq)

              - `paypointLegal` (ne, eq, ct, nct)

              - `paypointDba` (ne, eq, ct, nct)

              - `orgName` (ne, eq, ct, nct)

              - `externalPaypointId` (ct, nct, eq, ne)

              - `method` (in, nin, eq, ne)

              - `totalAmount` (gt, ge, lt, le, eq, ne)

              - `netAmount` (gt, ge, lt, le, eq, ne)

              - `feeAmount` (gt, ge, lt, le, eq, ne)

              - `operation` (in, nin, eq, ne)

              - `source` (in, nin, eq, ne, ct, nct)

              - `status` (in, nin, eq, ne)

              - `settlementStatus` (in, nin, eq, ne)

              - `batchNumber` (nct, ct)

              - `invoiceNumber` (ct, nct)

              - `authCode` (ct, nct)

              - `orderDescription` (ct, nct)

              - `payaccountLastfour` (nct, ct)

              - `payaccountType` (ne, eq, in, nin)

              - `payaccountCurrency` (ne, eq, in, nin)

              - `customerFirstname` (ct, nct, eq, ne)

              - `customerLastname` (ct, nct, eq, ne)

              - `customerName` (ct, nct)

              - `customerId` (eq, ne)

              - `customerNumber` (ct, nct, eq, ne)

              - `customerCompanyname` (ct, nct, eq, ne)

              - `customerAddress` (ct, nct, eq, ne)

              - `customerCity` (ct, nct, eq, ne)

              - `customerZip` (ct, nct, eq, ne)

              - `customerState` (ct, nct, eq, ne)

              - `customerCountry` (ct, nct, eq, ne)

              - `customerPhone` (ct, nct, eq, ne)

              - `customerEmail` (ct, nct, eq, ne)

              - `customerShippingAddress` (ct, nct, eq, ne)

              - `customerShippingCity` (ct, nct, eq, ne)

              - `customerShippingZip` (ct, nct, eq, ne)

              - `customerShippingState` (ct, nct, eq, ne)

              - `customerShippingCountry` (ct, nct, eq, ne)

              - `deviceId` (ct, nct, in, nin, eq, ne)

              - `AchSecCode` ( ct, nct, in, nin, eq, ne)

              - `AchHolderType`` (ct, nct, in, nin, eq, and ne)

              - `additional-xxx` (ne, eq, ct, nct) where xxx is the additional
              field name related to customer data

              - 'invoiceAdditional-xxx' (ne, eq, ct, nct) where xxx is the additional
              field name related to invoice data


              **List of comparison operators accepted:**

              - `eq` or empty => equal

              - `gt` => greater than

              - `ge` => greater or equal

              - `lt` => less than

              - `le` => less or equal

              - `ne` => not equal

              - `ct` => contains

              - `nct` => not contains

              - `in` => inside array

              - `nin` => not inside array
                
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.QueryResponseTransactions

    ListTransferDetails:
      auth: true
      display-name: Get transfer details for a paypoint
      docs: >-
        Retrieve a list of transfer details records for a paypoint. Use filters to
        limit results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.
      examples:
        - name: ListTransferDetailsPaypoint
          path-parameters:
            entry: 47862acd
            transferId: 123456
          response: 
            body:
              Summary:
                achReturns: -50.00
                adjustments: 10.00
                billingFees: 25.00
                chargebacks: 0.00
                grossTransferAmount: 1000.00
                releaseAmount: 0.00
                thirdPartyPaid: 0.00
                totalNetAmountTransfer: 935.00
                serviceFees: 30.00
                splitAmount: 650.22
                transferAmount: 935.00
                refunds: -20.00
                heldAmount: 0.00
                totalRecords: 1
                totalAmount: 1000.00
                totalNetAmount: 935.00
                netBatchAmount: 935.00
                totalPages: 1
                pageSize: 20
                pageidentifier: "XYZ123ABC456"
              Records:
                - transferDetailId: 654321
                  transferId: 12345
                  transactionId: "txn-4321hg6543fe"
                  #paypointId: 1357
                  type: "credit"
                  category: "sale"
                  grossAmount: 1000.00
                  chargeBackAmount: 0.00
                  returnedAmount: 0.00
                  refundAmount: 20.00
                  holdAmount: 0.00
                  releasedAmount: 0.00
                  billingFeesAmount: 25.00
                  thirdPartyPaidAmount: 0.00
                  adjustmentsAmount: 10.00
                  netTransferAmount: 935.00
                  splitFundingAmount: 0.00
                  ParentOrgName: "GadgetPro"
                  PaypointDbaname: "Global Gadgets"
                  PaypointLegalname: "Global Gadgets, LLC"
                  PaypointEntryname: "48ae10920"
                  PaymentTransId: "txn-4321hg6543fe"
                  ConnectorName: "gp"
                  GatewayTransId: "TRN_K6Nz3JxrNKkaPTF4ExCqfO4UwMW4CM"
                  OrderId: "order789"
                  Method: "ach"
                  BatchNumber: "batch_226_ach_12-30-2023"
                  BatchAmount: 30.22
                  PayorId: 1551
                  PaymentData:
                    MaskedAccount: "411812XXXXXX2357"
                    AccountType: "visa"
                    AccountExp: "08/28"
                    HolderName: "Ara Karapetyan"
                    orderDescription: "Electronics Purchase"
                    StoredId: null
                    Initiator: null
                    StoredMethodUsageType: null
                    Sequence: null
                    accountId: null
                    SignatureData: null
                    binData: $root.BinData.ExampleBinData
                    paymentDetails: null
                  TransStatus: 1
                  TotalAmount: 1000.00
                  NetAmount: 935.00
                  FeeAmount: 1.00
                  SettlementStatus: 2
                  Operation: "Sale"
                  ResponseData:
                    response: "Approved"
                    responsetext: "Transaction successful"
                    authcode: "123456"
                    transactionid: "TRN_K6Nz3JxrNKkaPTF4ExCqfO4OOOOOX"
                    response_code: "100"
                    response_code_text: "Operation successful."
                  Source: "web"
                  ScheduleReference: 0
                  OrgId: 9876
                  RefundId: 0
                  ReturnedId: 0
                  TransactionTime: "2024-01-05T12:15:30.11"
                  Customer:
                    Identifiers: ["customerId", "email"]
                    FirstName: "Ara"
                    LastName: "Karapetyan"
                    CompanyName: "Ara's Electronics"
                    BillingAddress1: "7890 Tech Park Drive"
                    BillingCity: "Baltimore"
                    BillingState: "MD"
                    BillingZip: "21230"
                    BillingCountry: "US"
                    BillingEmail: "ara.karapetyan@electronics.com"
                    CustomerNumber: "0010"
                    customerId: 7890
                  transactionNumber: null
                  billingFeesDetails: []
                  ExternalProcessorInformation: null
                  PaypointId: 1357
                  ChargebackId: null
                  RetrievalId: null
                  TransAdditionalData: null
                  invoiceData: null
                  EntrypageId: null
                  externalPaypointID: null
                  IsValidatedACH: true
                  splitFundingInstructions: []
                  CfeeTransactions: []
                  TransactionEvents: []
                  PendingFeeAmount: 0.00
                  RiskFlagged: false
                  RiskFlaggedOn: null
                  RiskStatus: "approved"
                  RiskReason: null
                  RiskAction: null
                  RiskActionCode: null
                  DeviceId: null
                  AchSecCode: "PPD"
                  AchHolderType: "personal"
                  IpAddress: "192.100.1.100"
                  IsSameDayACH: false
                  WalletType: null
      method: GET
      path: /Query/transferDetails/{entry}/{transferId}
      path-parameters:
        entry: root.Entry
        transferId:
          docs: The numeric identifier for the transfer, assigned by Payabli.
          type: integer
      request:
        name: ListTransfersPaypointRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord: optional<queryTypes.limitRecord>
          parameters:
            docs: |

              Collection of field names, conditions, and values used to filter
              the query. 

              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>

              See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for more information.


              **List of field names accepted:**
  
                - `grossAmount` (gt, ge, lt, le, eq, ne)
                - `chargeBackAmount` (gt, ge, lt, le, eq, ne)
                - `returnedAmount` (gt, ge, lt, le, eq, ne)
                - `billingFeeAmount` (gt, ge, lt, le, eq, ne)
                - `thirdPartyPaidAmount` (gt, ge, lt, le, eq, ne)
                - `netFundedAmount` (gt, ge, lt, le, eq, ne)
                - `adjustmentAmount` (gt, ge, lt, le, eq, ne)
                - `splitFundingAmount` (gt, ge, lt, le, eq, ne)
                - `operation` (in, nin, eq, ne)
                - `transactionId` (eq, ne, in, nin)
                - `category` (eq, ne, ct, nct)
                - `type` (eq, ne, in, nin)
                - `method` (eq, ne, in, nin)
                
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: queryTypes.QueryTransferDetailResponse

    ListTransfers:
      display-name: Get list of transfers for a paypoint
      docs: |
        Retrieve a list of transfers for a paypoint. Use filters to limit results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.
      method: GET
      path: /Query/transfers/{entry}
      path-parameters:
        entry: root.Entry
      request:
        name: ListTransfersRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >
              Collection of field names, conditions, and values used to filter the query. See [Filters and Conditions Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference) for more information.

              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>
        
              List of field names accepted:

                - `transferDate` (gt, ge, lt, le, eq, ne)
                - `grossAmount` (gt, ge, lt, le, eq, ne)
                - `chargeBackAmount` (gt, ge, lt, le, eq, ne)
                - `returnedAmount` (gt, ge, lt, le, eq, ne)
                - `billingFeeAmount` (gt, ge, lt, le, eq, ne)
                - `thirdPartyPaidAmount` (gt, ge, lt, le, eq, ne)
                - `netFundedAmount` (gt, ge, lt, le, eq, ne)
                - `adjustmentAmount` (gt, ge, lt, le, eq, ne)
                - `processor` (ne, eq, ct, nct)
                - `transferStatus` (ne, eq, in, nin)
                - `batchNumber` (ne, eq, ct, nct)
                - `batchId` (ne, eq, in, nin)
                - `transferId` (in, nin, eq, ne)
                - `bankAccountNumber` (ct, nct, ne, eq)
                - `bankRoutingNumber` (ct, nct, ne, eq)
                - `batchCurrency` (in, nin, ne, eq)
                - `parentOrgName` (ct, nct, ne, eq)
                - `parentOrgId` (ct, nct, ne, eq)
                - `externalPaypointID` (ct, nct)

            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.TransferQueryResponse
      examples:
        - name: ListTransfer  
          path-parameters:
            entry: 47862acd
          query-parameters:
            fromRecord: 0
            limitRecord: 20
          response: 
            body: $root.TransferQueryResponse.TransferQueryResponseExample
              
    ListTransfersOrg:
      display-name: Get list of transfers for an organization
      docs: |
        Retrieve a list of transfers for an org. Use filters to limit results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.
      method: GET
      path: /Query/transfers/org/{orgId}
      request:
        name: ListTransfersRequestOrg
        path-parameters:
          orgId: root.Orgid
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >
              Collection of field names, conditions, and values used to filter the query. See [Filters and Conditions Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference) for more information.

              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>
        
              List of field names accepted:

                - `transferDate` (gt, ge, lt, le, eq, ne)
                - `grossAmount` (gt, ge, lt, le, eq, ne)
                - `chargeBackAmount` (gt, ge, lt, le, eq, ne)
                - `returnedAmount` (gt, ge, lt, le, eq, ne)
                - `billingFeeAmount` (gt, ge, lt, le, eq, ne)
                - `thirdPartyPaidAmount` (gt, ge, lt, le, eq, ne)
                - `netFundedAmount` (gt, ge, lt, le, eq, ne)
                - `adjustmentAmount` (gt, ge, lt, le, eq, ne)
                - `processor` (ne, eq, ct, nct)
                - `transferStatus` (ne, eq, in, nin)
                - `batchNumber` (ne, eq, ct, nct)
                - `batchId` (ne, eq, in, nin)
                - `transferId` (in, nin, eq, ne)
                - `bankAccountNumber` (ct, nct, ne, eq)
                - `bankRoutingNumber` (ct, nct, ne, eq)
                - `batchCurrency` (in, nin, ne, eq)

            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.TransferQueryResponse
      examples:
        - name: ListTransferOrg  
          path-parameters:
            orgId: 123
          query-parameters:
            fromRecord: 0
            limitRecord: 20
          response:
            body: $root.TransferQueryResponse.TransferQueryResponseExample

    ListUsersOrg:
      auth: true
      display-name: Get list of users for an org
      docs: >-
        Get list of users for an org. Use filters to limit results.
      examples:
        - path-parameters:
            orgId: 123
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                - Access:
                    - roleValue: true
                  AdditionalData: AdditionalData
                  createdAt: '2022-07-01T15:00:01Z'
                  Email: example@email.com
                  language: en
                  lastAccess: '2022-07-01T15:00:01Z'
                  Name: Sean Smith
                  Phone: '5555555555'
                  Scope:
                    - orgType: 0
                  snData: snData
                  snIdentifier: snIdentifier
                  snProvider: google
                  timeZone: -5
                  userId: 1000000
                  UsrMFA: false
                  UsrMFAMode: 0
                  UsrStatus: 1
              Summary:
                pageIdentifier: 'null'
                pageSize: 20
                totalAmount: 77.22
                totalNetAmount: 77.22
                totalPages: 2
                totalRecords: 2
      method: GET
      path: /Query/users/org/{orgId}
      path-parameters:
        orgId:
          docs: The numeric identifier for organization, assigned by Payabli.
          type: integer
      request:
        name: ListUsersOrgRequest
        query-parameters:
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >
              Collection of field names, conditions, and values used to filter
              the query.

              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>

              See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for help.


              **List of field names accepted:**


              - `createdDate` (gt, ge, lt, le, eq, ne)

              - `name`  (ne, eq, ct, nct)

              - `email`  (ne, eq, ct, nct)

              - `status`   (in, nin, eq, ne)

              - `role.xxx`  (ne, eq, ct, nct) where xxx is the role field:
              `roleLabel` or `roleValue`


              **List of comparison accepted - enclosed between parentheses:**


              - `eq` or empty => equal

              - `gt` => greater than

              - `ge` => greater or equal

              - `lt` => less than

              - `le` => less or equal

              - `ne` => not equal

              - `ct` => contains

              - `nct` => not contains

              - `in` => inside array separated by "|"

              - `nin` => not inside array separated by "|"


              **List of parameters accepted:**

              - `limitRecord`: max number of records for query (default="20",
              "0" or negative value for all)

              - `fromRecord`: initial record in query


              Example: `name(ct)=john`  return all records with name containing
              'john'.
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.QueryUserResponse
      
    ListUsersPaypoint:
      auth: true
      display-name: Get list of users for paypoint
      docs: Get list of users for a paypoint. Use filters to limit results.
      examples:
        - path-parameters:
            entry: 8cfec329267
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                - Access:
                    - roleValue: true
                  AdditionalData: AdditionalData
                  createdAt: '2022-07-01T15:00:01Z'
                  Email: example@email.com
                  language: en
                  lastAccess: '2022-07-01T15:00:01Z'
                  Name: Sean Smith
                  Phone: '5555555555'
                  Scope:
                    - orgType: 0
                  snData: snData
                  snIdentifier: snIdentifier
                  snProvider: google
                  timeZone: -5
                  userId: 1000000
                  UsrMFA: false
                  UsrMFAMode: 0
                  UsrStatus: 1
              Summary:
                pageIdentifier: 'null'
                pageSize: 20
                totalAmount: 77.22
                totalNetAmount: 77.22
                totalPages: 2
                totalRecords: 2
      method: GET
      path: /Query/users/point/{entry}
      path-parameters:
        entry:
          docs: >-
            The paypoint's entrypoint identifier. [Learn
            more](/api-reference/api-overview#entrypoint-vs-entry)
          type: string
      request:
        name: ListUsersPaypointRequest
        query-parameters:
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >
              Collection of field names, conditions, and values used to filter
              the query.

              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>

              See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for help.


              **List of field names accepted:**


              - `createdDate` (gt, ge, lt, le, eq, ne)

              - `name`  (ne, eq, ct, nct)

              - `email`  (ne, eq, ct, nct)

              - `status`   (in, nin, eq, ne)

              - `role.xxx`  (ne, eq, ct, nct) where xxx is the role field:
              `roleLabel` or `roleValue`


              **List of comparison accepted - enclosed between parentheses:**


              - `eq` or empty => equal

              - `gt` => greater than

              - `ge` => greater or equal

              - `lt` => less than

              - `le` => less or equal

              - `ne` => not equal

              - `ct` => contains

              - `nct` => not contains

              - `in` => inside array separated by "|"

              - `nin` => not inside array separated by "|"


              **List of parameters accepted:**

              - `limitRecord`: max number of records for query (default="20",
              "0" or negative value for all)

              - `fromRecord`: initial record in query


              Example: `name(ct)=john`  return all records with name containing
              'john'
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.QueryUserResponse
      
    ListVendors:
      auth: true
      display-name: Get list of vendors for entrypoint
      docs: >-
        Retrieve a list of vendors for an entrypoint. Use filters to limit
        results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.
      examples:
        - name: An example response for vendor queries
          path-parameters:
            entry: 8cfec329267
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                - $root.VendorQueryRecord.VendorQueryRecordExample
              Summary:
                pageIdentifier: 'null'
                pageSize: 20
                totalAmount: 200
                totalNetAmount: 77.22
                totalPages: 1
                totalRecords: 1
      method: GET
      path: /Query/vendors/{entry}
      path-parameters:
        entry:
          docs: >-
            The paypoint's entrypoint identifier. [Learn
            more](/api-reference/api-overview#entrypoint-vs-entry)
          type: string
      request:
        name: ListVendorsRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >
              Collection of field names, conditions, and values used to filter
              the query

              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>

              See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for help.


              List of field names accepted:

              - `method` (in, nin, eq, ne)

              - `enrollmentStatus` (in,nin, eq, ne)

              - `status` (in, nin, eq, ne)

              - `vendorNumber` (ct, nct, eq, ne)

              - `name` (ct, nct, eq, ne)

              - `ein` (ct, nct, eq, ne)

              - `phone` (ct, nct, eq, ne)

              - `email` (ct, nct, eq, ne)

              - `address` (ct, nct, eq, ne)

              - `city` (ct, nct, eq, ne)

              - `state` (ct, nct, eq, ne)

              - `country` (ct, nct, eq, ne)

              - `zip` (ct, nct, eq, ne)

              - `mcc` (ct, nct, eq, ne)

              - `locationCode` (ct, nct, eq, ne)

              - `paypointLegal` (ne, eq, ct, nct)

              - `parentOrgId` (ne, eq, nin, in)

              - `paypointDba` (ne, eq, ct, nct)

              - `orgName` (ne, eq, ct, nct)


              List of comparison accepted - enclosed between parentheses:

              - eq or empty => equal

              - gt => greater than

              - ge => greater or equal

              - lt => less than

              - le => less or equal

              - ne => not equal

              - ct => contains

              - nct => not contains

              - in => inside array separated by "|"

              - nin => not inside array separated by "|"


              List of parameters accepted:

              - limitRecord : max number of records for query (default="20", "0"
              or negative value for all)

              - fromRecord : initial record in query


              Example: `netAmount(gt)=20` returns all records with a `netAmount`
              greater than 20.00
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.QueryResponseVendors
      
    ListVendorsOrg:
      auth: true
      display-name: Get list of vendors for organization
      docs: >-
        Retrieve a list of vendors for an organization. Use filters to limit
        results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.
      examples:
        - name: An example response for vendor queries
          path-parameters:
            orgId: 123
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                - $root.VendorQueryRecord.VendorQueryRecordExample
              Summary:
                pageIdentifier: 'null'
                pageSize: 20
                totalAmount: 200
                totalNetAmount: 77.22
                totalPages: 1
                totalRecords: 1
      method: GET
      path: /Query/vendors/org/{orgId}
      path-parameters:
        orgId:
          docs: The numeric identifier for organization, assigned by Payabli.
          type: integer
      request:
        name: ListVendorsOrgRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >
              Collection of field names, conditions, and values used to filter
              the query

              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>

              See [Filters and Conditions
              Reference](/developers/developer-guides/pay-ops-reporting-engine-overview#filters-and-conditions-reference)
              for help.


              List of field names accepted:

              - `method` (in, nin, eq, ne)

              - `enrollmentStatus` (in,nin, eq, ne)

              - `status` (in, nin, eq, ne)

              - `vendorNumber` (ct, nct, eq, ne)

              - `name` (ct, nct, eq, ne)

              - `ein` (ct, nct, eq, ne)

              - `phone` (ct, nct, eq, ne)

              - `email` (ct, nct, eq, ne)

              - `address` (ct, nct, eq, ne)

              - `city` (ct, nct, eq, ne)

              - `state` (ct, nct, eq, ne)

              - `country` (ct, nct, eq, ne)

              - `zip` (ct, nct, eq, ne)

              - `mcc` (ct, nct, eq, ne)

              - `locationCode` (ct, nct, eq, ne)

              - `paypointLegal` (ne, eq, ct, nct)

              - `paypointDba` (ne, eq, ct, nct)

              - `parentOrgId` (ne, eq, nin, in)

              - `orgName` (ne, eq, ct, nct)


              List of comparison accepted - enclosed between parentheses:

              - eq or empty => equal

              - gt => greater than

              - ge => greater or equal

              - lt => less than

              - le => less or equal

              - ne => not equal

              - ct => contains

              - nct => not contains

              - in => inside array separated by "|"

              - nin => not inside array separated by "|"


              List of parameters accepted:

              - limitRecord : max number of records for query (default="20", "0"
              or negative value for all)

              - fromRecord : initial record in query


              Example: `netAmount(gt)=20` returns all records with a `netAmount`
              greater than 20.00
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.QueryResponseVendors
      
    ListVcards:
      auth: true
      display-name: Get list of virtual cards for entrypoint
      docs: >-
        Retrieve a list of vcards (virtual credit cards) issued for an entrypoint. Use filters to limit results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.
      examples:
        - name: ListVcardsPaypoint
          path-parameters:
            entry: 8cfec329267
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                -  $root.VCardRecord.VCardRecordExample
              Summary:
                pageIdentifier: "XXXXXXXXXXXXXX"
                pageSize: 20
                totalAmount: 2500.00
                totalactive: 5
                totalamounteactive: 2500.00
                totalbalanceactive: 1875.25
                totalPages: 1
                totalRecords: 5
      method: GET
      path: /Query/vcards/{entry}
      path-parameters:
        entry: root.Entry
      request:
        name: ListVcardsRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >
              Collection of field names, conditions, and values used to filter
              the query. 

              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>
              
              List of field names accepted:  

                - `status` (in, nin, eq, ne)  
                - `createdAt` (gt, ge, lt, le, eq, ne)  
                - `cardToken` (ct, nct, eq, ne)  
                - `lastFour` (ct, nct, eq, ne)  
                - `expirationDate` (ct, nct, eq, ne)  
                - `payoutId` (ct, nct, eq, ne, in, nin)  
                - `vendorId` (ct, nct, eq, ne, in, nin)  
                - `miscData1` (ct, nct, eq, ne)  
                - `miscData2` (ct, nct, eq, ne)  
                - `currentUses` (gt, ge, lt, le, eq, ne)  
                - `amount` (gt, ge, lt, le, eq, ne)  
                - `balance` (gt, ge, lt, le, eq, ne)  
                - `paypointLegal` (ne, eq, ct, nct)  
                - `paypointDba` (ne, eq, ct, nct)  
                - `orgName` (ne, eq, ct, nct)  
                - `externalPaypointId` (ct, nct, eq, ne)  
                - `paypointId` (in, nin, eq, ne)  
              
              List of comparison accepted - enclosed between parentheses:  

                - eq or empty => equal  
                - gt => greater than  
                - ge => greater or equal  
                - lt => less than  
                - le => less or equal  
                - ne => not equal  
                - ct => contains  
                - nct => not contains  
                - in => inside array separated by "|"  
                - nin => not inside array separated by "|"
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.VCardQueryResponse

    ListVcardsOrg:
      auth: true
      display-name: Get list of virtual cards for an organization
      docs: >-
        Retrieve a list of vcards (virtual credit cards) issued for an organization. Use filters to limit results. Include the `exportFormat` query parameter to return the results as a file instead of a JSON response.
      examples:
        - name: ListVCardsOrg
          path-parameters:
            orgId: 123
          query-parameters:
            fromRecord: 251
            limitRecord: 0
            sortBy: desc(field_name)
          response:
            body:
              Records:
                -  $root.VCardRecord.VCardRecordExample
              Summary:
                pageIdentifier: "XXXXXXXXXXXXXX"
                pageSize: 20
                totalAmount: 2500.00
                totalactive: 5
                totalamounteactive: 2500.00
                totalbalanceactive: 1875.25
                totalPages: 1
                totalRecords: 5
      method: GET
      path: /Query/vcards/org/{orgId}
      path-parameters:
        orgId:
          docs: The numeric identifier for organization, assigned by Payabli.
          type: integer
      request:
        name: ListVcardsOrgRequest
        query-parameters:
          exportFormat: optional<root.ExportFormat>
          fromRecord:
            default: 0
            docs: >-
              The number of records to skip before starting to collect the
              result set.
            type: optional<integer>
          limitRecord:
            default: 20
            docs: >-
              Max number of records to return for the query. Use `0` or negative
              value to return all records.
            type: optional<integer>
          parameters:
            docs: >
              Collection of field names, conditions, and values used to filter
              the query. 

              <Info>
                **You must remove `parameters=` from the request before you send it, otherwise Payabli will ignore the filters.**

                Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove `parameters=` and run the request in a different client.

                For example:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&limitRecord=20

                should become:

                --url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&limitRecord=20
              </Info>
              
              List of field names accepted:  

                - `status` (in, nin, eq, ne)  
                - `createdAt` (gt, ge, lt, le, eq, ne)  
                - `cardToken` (ct, nct, eq, ne)  
                - `lastFour` (ct, nct, eq, ne)  
                - `expirationDate` (ct, nct, eq, ne)  
                - `payoutId` (ct, nct, eq, ne, in, nin)  
                - `vendorId` (ct, nct, eq, ne, in, nin)  
                - `miscData1` (ct, nct, eq, ne)  
                - `miscData2` (ct, nct, eq, ne)  
                - `currentUses` (gt, ge, lt, le, eq, ne)  
                - `amount` (gt, ge, lt, le, eq, ne)  
                - `balance` (gt, ge, lt, le, eq, ne)  
                - `paypointLegal` (ne, eq, ct, nct)  
                - `paypointDba` (ne, eq, ct, nct)  
                - `orgName` (ne, eq, ct, nct)  
                - `externalPaypointId` (ct, nct, eq, ne)  
                - `paypointId` (in, nin, eq, ne)  
              
              List of comparison accepted - enclosed between parentheses:  

                - eq or empty => equal  
                - gt => greater than  
                - ge => greater or equal  
                - lt => less than  
                - le => less or equal  
                - ne => not equal  
                - ct => contains  
                - nct => not contains  
                - in => inside array separated by "|"  
                - nin => not inside array separated by "|"
            type: optional<map<string, optional<string>>>
          sortBy:
            docs: >-
              The field name to use for sorting results. Use `desc(field_name)`
              to sort descending by `field_name`, and use `asc(field_name)` to
              sort ascending by `field_name`.
            type: optional<string>
      response:
        docs: Success
        status-code: 200
        type: root.VCardQueryResponse