openapi: 3.1.0
info:
  title: java-default-timeout
  version: 0.0.0
paths:
  /user:
    get:
      operationId: getUser
      tags:
      - JavaDefaultTimeout
      parameters: []
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
components:
  schemas:
    User:
      title: User
      type: object
      properties:
        id:
          type: string
        name:
          type: string
      required:
      - id
      - name
  securitySchemes: {}
