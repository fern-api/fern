openapi: 3.2.0
info:
  title: server-sent-events-openapi
  description: >
    Test fixture for SSE handling via OpenAPI 3.2's itemSchema.
    Each endpoint exercises a specific combination of dimensions:
      A: Discriminator style (discriminated with mapping, undiscriminated with const)
      B: Field name collision (event field in both envelope and data payload)
      C: x-fern-discriminator-context (protocol, data, absent)
    All endpoints share the same four event types: heartbeat (no data),
    string literal data, number literal data, and object data. Collision variants
    use ObjectPayloadWithEventField (which has its own "event" property) instead of
    StatusPayload. Endpoint 9 repeats the base case using contentSchema/contentMediaType
    instead of direct typing.
  version: "0.1.0"

paths:
  /stream/discriminated-no-collision-protocol:
    post:
      operationId: streamDiscriminatedNoCollisionProtocol
      summary: Discriminated SSE with protocol context and no field collision
      description: >
        Dimensions: A1-B1-C1.
        Uses discriminator with mapping, x-fern-discriminator-context set to protocol,
        and data fields reference schemas via $ref. The envelope's event field does not
        collide with any field in the data payloads. This is the standard happy-path
        pattern for SSE with typed event discrimination.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StreamRequest"
      responses:
        "200":
          description: SSE stream with discriminated event types
          content:
            text/event-stream:
              itemSchema:
                oneOf:
                  - $ref: "#/components/schemas/DiscriminatedHeartbeat"
                  - $ref: "#/components/schemas/DiscriminatedStringEvent"
                  - $ref: "#/components/schemas/DiscriminatedNumberEvent"
                  - $ref: "#/components/schemas/DiscriminatedObjectEvent"
                discriminator:
                  propertyName: event
                  x-fern-discriminator-context: protocol
                  mapping:
                    heartbeat: "#/components/schemas/DiscriminatedHeartbeat"
                    string_data: "#/components/schemas/DiscriminatedStringEvent"
                    number_data: "#/components/schemas/DiscriminatedNumberEvent"
                    object_data: "#/components/schemas/DiscriminatedObjectEvent"

  /stream/discriminated-collision-protocol:
    post:
      operationId: streamDiscriminatedCollisionProtocol
      summary: Discriminated SSE with protocol context and event field collision
      description: >
        Dimensions: A1-B2-C1.
        The object data payload (ObjectPayloadWithEventField) contains its own "event"
        property, which collides with the SSE envelope's "event" discriminator field.
        Tests whether generators correctly separate the protocol-level discriminant from
        the data-level field when context=protocol is specified.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StreamRequest"
      responses:
        "200":
          description: SSE stream where one data payload has a colliding "event" field
          content:
            text/event-stream:
              itemSchema:
                oneOf:
                  - $ref: "#/components/schemas/DiscriminatedHeartbeat"
                  - $ref: "#/components/schemas/DiscriminatedStringEvent"
                  - $ref: "#/components/schemas/DiscriminatedNumberEvent"
                  - $ref: "#/components/schemas/DiscriminatedCollisionObjectEvent"
                discriminator:
                  propertyName: event
                  x-fern-discriminator-context: protocol
                  mapping:
                    heartbeat: "#/components/schemas/DiscriminatedHeartbeat"
                    string_data: "#/components/schemas/DiscriminatedStringEvent"
                    number_data: "#/components/schemas/DiscriminatedNumberEvent"
                    object_data: "#/components/schemas/DiscriminatedCollisionObjectEvent"

  /stream/discriminated-no-collision-data-context:
    post:
      operationId: streamDiscriminatedNoCollisionDataContext
      summary: Discriminated SSE with explicit data context and no collision
      description: >
        Dimensions: A1-B1-C2.
        x-fern-discriminator-context is explicitly set to "data" (the default value).
        The discriminant should be flattened into the data payload rather than treated
        as a protocol-level SSE envelope field. Verifies that explicitly setting the
        default produces the same behavior as the implicit default.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StreamRequest"
      responses:
        "200":
          description: SSE stream with discriminator context explicitly set to data
          content:
            text/event-stream:
              itemSchema:
                oneOf:
                  - $ref: "#/components/schemas/DiscriminatedHeartbeat"
                  - $ref: "#/components/schemas/DiscriminatedStringEvent"
                  - $ref: "#/components/schemas/DiscriminatedNumberEvent"
                  - $ref: "#/components/schemas/DiscriminatedObjectEvent"
                discriminator:
                  propertyName: event
                  x-fern-discriminator-context: data
                  mapping:
                    heartbeat: "#/components/schemas/DiscriminatedHeartbeat"
                    string_data: "#/components/schemas/DiscriminatedStringEvent"
                    number_data: "#/components/schemas/DiscriminatedNumberEvent"
                    object_data: "#/components/schemas/DiscriminatedObjectEvent"

  /stream/discriminated-collision-data-context:
    post:
      operationId: streamDiscriminatedCollisionDataContext
      summary: Discriminated SSE with explicit data context and event field collision
      description: >
        Dimensions: A1-B2-C2.
        Combines explicit context=data with a data payload that has its own "event" field.
        Since context=data means the discriminant is flattened into the data, the collision
        between the envelope's event field and the payload's event field should surface
        directly in the generated types.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StreamRequest"
      responses:
        "200":
          description: SSE stream with explicit data context and field collision
          content:
            text/event-stream:
              itemSchema:
                oneOf:
                  - $ref: "#/components/schemas/DiscriminatedHeartbeat"
                  - $ref: "#/components/schemas/DiscriminatedStringEvent"
                  - $ref: "#/components/schemas/DiscriminatedNumberEvent"
                  - $ref: "#/components/schemas/DiscriminatedCollisionObjectEvent"
                discriminator:
                  propertyName: event
                  x-fern-discriminator-context: data
                  mapping:
                    heartbeat: "#/components/schemas/DiscriminatedHeartbeat"
                    string_data: "#/components/schemas/DiscriminatedStringEvent"
                    number_data: "#/components/schemas/DiscriminatedNumberEvent"
                    object_data: "#/components/schemas/DiscriminatedCollisionObjectEvent"

  /stream/discriminated-no-collision-no-context:
    post:
      operationId: streamDiscriminatedNoCollisionNoContext
      summary: Discriminated SSE with no context extension and no collision
      description: >
        Dimensions: A1-B1-C3.
        The x-fern-discriminator-context extension is omitted entirely. Tests whether
        Fern correctly infers the default behavior (context=data) when the extension
        is absent. No field collision, so the default should work without issues.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StreamRequest"
      responses:
        "200":
          description: SSE stream with no discriminator context hint
          content:
            text/event-stream:
              itemSchema:
                oneOf:
                  - $ref: "#/components/schemas/DiscriminatedHeartbeat"
                  - $ref: "#/components/schemas/DiscriminatedStringEvent"
                  - $ref: "#/components/schemas/DiscriminatedNumberEvent"
                  - $ref: "#/components/schemas/DiscriminatedObjectEvent"
                discriminator:
                  propertyName: event
                  mapping:
                    heartbeat: "#/components/schemas/DiscriminatedHeartbeat"
                    string_data: "#/components/schemas/DiscriminatedStringEvent"
                    number_data: "#/components/schemas/DiscriminatedNumberEvent"
                    object_data: "#/components/schemas/DiscriminatedObjectEvent"

  /stream/discriminated-collision-no-context:
    post:
      operationId: streamDiscriminatedCollisionNoContext
      summary: Discriminated SSE with event field collision and no context hint
      description: >
        Dimensions: A1-B2-C3.
        Combines the collision scenario (data payload has its own "event" field) with
        the absence of x-fern-discriminator-context. This is the worst case for
        inference — the generator has no hint that the discriminant is protocol-level,
        yet the field collision makes flattening into data incorrect.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StreamRequest"
      responses:
        "200":
          description: SSE stream with field collision and no context hint
          content:
            text/event-stream:
              itemSchema:
                oneOf:
                  - $ref: "#/components/schemas/DiscriminatedHeartbeat"
                  - $ref: "#/components/schemas/DiscriminatedStringEvent"
                  - $ref: "#/components/schemas/DiscriminatedNumberEvent"
                  - $ref: "#/components/schemas/DiscriminatedCollisionObjectEvent"
                discriminator:
                  propertyName: event
                  mapping:
                    heartbeat: "#/components/schemas/DiscriminatedHeartbeat"
                    string_data: "#/components/schemas/DiscriminatedStringEvent"
                    number_data: "#/components/schemas/DiscriminatedNumberEvent"
                    object_data: "#/components/schemas/DiscriminatedCollisionObjectEvent"

  /stream/undiscriminated-no-collision:
    post:
      operationId: streamUndiscriminatedNoCollision
      summary: Undiscriminated oneOf SSE using const on event field, no collision
      description: >
        Dimensions: A2-B1.
        Uses the OAS 3.2 native pattern — oneOf with const constraints on the event
        property instead of a discriminator object. No x-fern-discriminator-context
        since there is no discriminator to attach it to.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StreamRequest"
      responses:
        "200":
          description: SSE stream with undiscriminated event types
          content:
            text/event-stream:
              itemSchema:
                oneOf:
                  - $ref: "#/components/schemas/UndiscriminatedHeartbeat"
                  - $ref: "#/components/schemas/UndiscriminatedStringEvent"
                  - $ref: "#/components/schemas/UndiscriminatedNumberEvent"
                  - $ref: "#/components/schemas/UndiscriminatedObjectEvent"

  /stream/undiscriminated-collision:
    post:
      operationId: streamUndiscriminatedCollision
      summary: Undiscriminated oneOf SSE with event field collision
      description: >
        Dimensions: A2-B2.
        Undiscriminated oneOf with const constraints, but the object data payload
        contains its own "event" field. With no discriminator object and no context
        extension, the generator must handle the collision purely from schema structure.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StreamRequest"
      responses:
        "200":
          description: SSE stream with undiscriminated types and field collision
          content:
            text/event-stream:
              itemSchema:
                oneOf:
                  - $ref: "#/components/schemas/UndiscriminatedHeartbeat"
                  - $ref: "#/components/schemas/UndiscriminatedStringEvent"
                  - $ref: "#/components/schemas/UndiscriminatedNumberEvent"
                  - $ref: "#/components/schemas/UndiscriminatedCollisionObjectEvent"

  /stream/discriminated-content-schema:
    post:
      operationId: streamDiscriminatedContentSchema
      summary: Discriminated SSE using contentSchema and contentMediaType on data
      description: >
        Variant of endpoint 1 (A1-B1-C1) using the OAS 3.2 recommended pattern where
        data is typed as a string with contentMediaType and contentSchema pointing to
        the actual payload schema. This is the spec-compliant way to describe
        JSON-encoded SSE data fields, independent of the main test matrix.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StreamRequest"
      responses:
        "200":
          description: SSE stream using contentSchema for data field typing
          content:
            text/event-stream:
              itemSchema:
                oneOf:
                  - $ref: "#/components/schemas/ContentSchemaHeartbeat"
                  - $ref: "#/components/schemas/ContentSchemaStringEvent"
                  - $ref: "#/components/schemas/ContentSchemaNumberEvent"
                  - $ref: "#/components/schemas/ContentSchemaObjectEvent"
                discriminator:
                  propertyName: event
                  x-fern-discriminator-context: protocol
                  mapping:
                    heartbeat: "#/components/schemas/ContentSchemaHeartbeat"
                    string_data: "#/components/schemas/ContentSchemaStringEvent"
                    number_data: "#/components/schemas/ContentSchemaNumberEvent"
                    object_data: "#/components/schemas/ContentSchemaObjectEvent"

components:
  schemas:
    StreamRequest:
      type: object
      properties:
        query:
          type: string

    StatusPayload:
      type: object
      required:
        - message
        - timestamp
      properties:
        message:
          type: string
        timestamp:
          type: string
          format: date-time

    ObjectPayloadWithEventField:
      type: object
      required:
        - id
        - name
        - event
      properties:
        id:
          type: string
        name:
          type: string
        event:
          type: string
          description: >
            An event type field inside the data payload that collides with
            the SSE envelope's event field used for discrimination.

    # ==========================================================================
    # Discriminated envelope schemas (used by endpoints 1-6)
    # Uses enum on event field, direct typing on data
    # ==========================================================================
    DiscriminatedHeartbeat:
      type: object
      required:
        - event
      properties:
        event:
          type: string
          enum:
            - heartbeat

    DiscriminatedStringEvent:
      type: object
      required:
        - event
        - data
      properties:
        event:
          type: string
          enum:
            - string_data
        data:
          type: string

    DiscriminatedNumberEvent:
      type: object
      required:
        - event
        - data
      properties:
        event:
          type: string
          enum:
            - number_data
        data:
          type: number

    DiscriminatedObjectEvent:
      type: object
      required:
        - event
        - data
      properties:
        event:
          type: string
          enum:
            - object_data
        data:
          $ref: "#/components/schemas/StatusPayload"

    DiscriminatedCollisionObjectEvent:
      type: object
      required:
        - event
        - data
      properties:
        event:
          type: string
          enum:
            - object_data
        data:
          $ref: "#/components/schemas/ObjectPayloadWithEventField"

    # ==========================================================================
    # Undiscriminated envelope schemas (used by endpoints 7-8)
    # Uses const on event field instead of enum, no discriminator object
    # ==========================================================================
    UndiscriminatedHeartbeat:
      type: object
      required:
        - event
      properties:
        event:
          type: string
          const: heartbeat

    UndiscriminatedStringEvent:
      type: object
      required:
        - event
        - data
      properties:
        event:
          type: string
          const: string_data
        data:
          type: string

    UndiscriminatedNumberEvent:
      type: object
      required:
        - event
        - data
      properties:
        event:
          type: string
          const: number_data
        data:
          type: number

    UndiscriminatedObjectEvent:
      type: object
      required:
        - event
        - data
      properties:
        event:
          type: string
          const: object_data
        data:
          $ref: "#/components/schemas/StatusPayload"

    UndiscriminatedCollisionObjectEvent:
      type: object
      required:
        - event
        - data
      properties:
        event:
          type: string
          const: object_data
        data:
          $ref: "#/components/schemas/ObjectPayloadWithEventField"

    # ==========================================================================
    # ContentSchema envelope schemas (used by endpoint 9)
    # Uses contentSchema/contentMediaType instead of direct typing on data
    # ==========================================================================
    ContentSchemaHeartbeat:
      type: object
      required:
        - event
      properties:
        event:
          type: string
          enum:
            - heartbeat

    ContentSchemaStringEvent:
      type: object
      required:
        - event
        - data
      properties:
        event:
          type: string
          enum:
            - string_data
        data:
          type: string

    ContentSchemaNumberEvent:
      type: object
      required:
        - event
        - data
      properties:
        event:
          type: string
          enum:
            - number_data
        data:
          type: string
          contentMediaType: text/plain
          contentSchema:
            type: number

    ContentSchemaObjectEvent:
      type: object
      required:
        - event
        - data
      properties:
        event:
          type: string
          enum:
            - object_data
        data:
          type: string
          contentMediaType: application/json
          contentSchema:
            $ref: "#/components/schemas/StatusPayload"
