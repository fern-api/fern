openapi: 3.1.0
info:
  title: java-optional-query-params-overloads
  version: 0.0.0
paths:
  /api/users/{userId}/insurance:
    get:
      description: Get latest insurance for a user. All query params are optional.
      operationId: getLatestInsurance
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
        - name: includeExpired
          in: query
          description: Include expired insurance policies
          required: false
          schema:
            type:
              - boolean
              - 'null'
        - name: policyType
          in: query
          description: Filter by policy type
          required: false
          schema:
            anyOf:
              - $ref: '#/components/schemas/PolicyType'
              - type: 'null'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InsurancePolicy'
  /api/policies/search:
    get:
      description: Search policies with required query params
      operationId: searchPolicies
      parameters:
        - name: query
          in: query
          description: Required search query
          required: true
          schema:
            type: string
        - name: limit
          in: query
          description: Optional limit
          required: false
          schema:
            type:
              - integer
              - 'null'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/InsurancePolicy'
  /api/policies:
    get:
      description: List all policies
      operationId: listAllPolicies
      parameters: []
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/InsurancePolicy'
components:
  schemas:
    PolicyType:
      title: PolicyType
      type: string
      enum:
        - HEALTH
        - AUTO
        - HOME
        - LIFE
      description: Type of insurance policy
    InsurancePolicy:
      title: InsurancePolicy
      type: object
      description: Insurance policy details
      properties:
        id:
          type: string
        userId:
          type: string
        policyType:
          $ref: '#/components/schemas/PolicyType'
        isExpired:
          type: boolean
        coverageAmount:
          type: number
          format: double
      required:
        - id
        - userId
        - policyType
        - isExpired
        - coverageAmount
  securitySchemes: {}
