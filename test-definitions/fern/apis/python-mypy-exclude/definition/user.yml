types:
  User:
    properties:
      id: string
      name: string

service:
  auth: false
  base-path: /users
  endpoints:
    get:
      method: GET
      path: /{id}
      path-parameters:
        id: string
      response: User
