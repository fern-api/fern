openapi: 3.1.0
info:
  title: go-bytes-request
  version: 0.0.0
paths:
  /upload-content:
    post:
      operationId: service_upload
      tags:
        - Service
      parameters: []
      responses:
        '204':
          description: No content
      requestBody:
        required: true
        content:
          application/octet-stream:
            schema:
              type: string
              format: binary
components:
  schemas: {}
  securitySchemes: {}
