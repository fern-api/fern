# yaml-language-server: $schema=https://schema.buildwithfern.dev/generators-yml.json
api:
  specs:
    - openapi: ./openapi.yml
  auth-schemes:
    token:
      header: Authorization
      type: string
      prefix: "Token"
