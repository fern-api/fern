# yaml-language-server: $schema=https://schema.buildwithfern.dev/generators-yml.json
groups:
  php-sdk:
    generators:
      - name: fernenterprise/fern-php-sdk
        version: latest
        ir-version: v61
        github:
          token: ${GITHUB_TOKEN}
          mode: push
          uri: fern-api/php-sdk-tests
          branch: single-url-environment-no-default
  go-sdk:
    generators:
      - name: fernenterprise/fern-go-sdk
        version: latest
        ir-version: v61
        github:
          token: ${GITHUB_TOKEN}
          mode: push
          uri: fern-api/go-sdk-tests
          branch: single-url-environment-no-default
