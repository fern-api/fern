# yaml-language-server: $schema=https://schema.buildwithfern.dev/generators-yml.json
groups:
  php-sdk:
    generators:
      - name: fernapi/fern-php-sdk
        version: latest
        ir-version: v61
        github:
          token: '{{ GITHUB_TOKEN }}'
          mode: push
          uri: https://github.com/fern-api/php-sdk-tests
          branch: oauth-client-credentials-default
