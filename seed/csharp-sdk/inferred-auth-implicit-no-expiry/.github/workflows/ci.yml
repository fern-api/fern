name: ci

on: [push]

env:
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true
  DOTNET_NOLOGO: true

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.x

      - name: Install tools
        run: dotnet tool restore

      - name: Restore dependencies
        run: dotnet restore src/SeedInferredAuthImplicitNoExpiry/SeedInferredAuthImplicitNoExpiry.csproj

      - name: Build
        id: build
        run: dotnet build src/SeedInferredAuthImplicitNoExpiry/SeedInferredAuthImplicitNoExpiry.csproj --no-restore -c Release

      - name: Restore test dependencies
        run: dotnet restore src/SeedInferredAuthImplicitNoExpiry.Test/SeedInferredAuthImplicitNoExpiry.Test.csproj

      - name: Build tests
        run: dotnet build src/SeedInferredAuthImplicitNoExpiry.Test/SeedInferredAuthImplicitNoExpiry.Test.csproj --no-restore -c Release

      - name: Test
        run: dotnet test src/SeedInferredAuthImplicitNoExpiry.Test/SeedInferredAuthImplicitNoExpiry.Test.csproj --no-restore --no-build -c Release

      - name: Pack
        if: always() && github.event_name == 'push' && contains(github.ref, 'refs/tags/') && steps.build.outcome == 'success'
        run: dotnet pack src/SeedInferredAuthImplicitNoExpiry/SeedInferredAuthImplicitNoExpiry.csproj --no-build --no-restore -c Release

      - name: Publish to NuGet.org
        if: always() && github.event_name == 'push' && contains(github.ref, 'refs/tags/') && steps.build.outcome == 'success'
        env:
          NUGET_API_KEY: ${{ secrets.NUGET_API_TOKEN }}
        run: dotnet nuget push src/SeedInferredAuthImplicitNoExpiry/bin/Release/*.nupkg --api-key $NUGET_API_KEY --source "nuget.org"
