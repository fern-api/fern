# This file was auto-generated by Fern from our API Definition.

import abc
import functools
import inspect
import logging
import typing

import fastapi
from ..core.abstract_fern_service import AbstractFernService
from ..core.exceptions.fern_http_exception import FernHTTPException
from ..core.route_args import get_route_args
from ..types.bulk_update_tasks_response import BulkUpdateTasksResponse
from .bulk_update_tasks_request import BulkUpdateTasksRequest


class AbstractRootService(AbstractFernService):
    """
    AbstractRootService is an abstract class containing the methods that you should implement.

    Each method is associated with an API route, which will be registered
    with FastAPI when you register your implementation using Fern's register()
    function.
    """

    @abc.abstractmethod
    def bulk_update_tasks(
        self,
        *,
        body: BulkUpdateTasksRequest,
        filter_assigned_to: typing.Optional[str] = None,
        filter_is_complete: typing.Optional[str] = None,
        filter_date: typing.Optional[str] = None,
        fields: typing.Optional[str] = None,
    ) -> BulkUpdateTasksResponse: ...

    """
    Below are internal methods used by Fern to register your implementation.
    You can ignore them.
    """

    @classmethod
    def _init_fern(cls, router: fastapi.APIRouter) -> None:
        cls.__init_bulk_update_tasks(router=router)

    @classmethod
    def __init_bulk_update_tasks(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.bulk_update_tasks)
        type_hints = typing.get_type_hints(cls.bulk_update_tasks)

        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            # Get the resolved type hint for this parameter, as fastapi does not handle forward refs in all cases
            resolved_annotation = type_hints.get(parameter_name, parameter.annotation)

            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            elif parameter_name == "body":
                new_parameters.append(
                    parameter.replace(annotation=typing.Annotated[resolved_annotation, fastapi.Body()])
                )
            elif parameter_name == "filter_assigned_to":
                new_parameters.append(
                    parameter.replace(
                        annotation=typing.Annotated[resolved_annotation, fastapi.Query(alias="assigned_to")],
                        default=None,
                    )
                )
            elif parameter_name == "filter_is_complete":
                new_parameters.append(
                    parameter.replace(
                        annotation=typing.Annotated[resolved_annotation, fastapi.Query(alias="is_complete")],
                        default=None,
                    )
                )
            elif parameter_name == "filter_date":
                new_parameters.append(
                    parameter.replace(
                        annotation=typing.Annotated[resolved_annotation, fastapi.Query(alias="date")], default=None
                    )
                )
            elif parameter_name == "fields":
                new_parameters.append(
                    parameter.replace(
                        annotation=typing.Annotated[
                            resolved_annotation,
                            fastapi.Query(
                                alias="_fields",
                                description="Comma-separated list of fields to include in the response.",
                            ),
                        ],
                        default=None,
                    )
                )
            else:
                new_parameters.append(parameter)
        setattr(cls.bulk_update_tasks, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.bulk_update_tasks)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> BulkUpdateTasksResponse:
            try:
                return cls.bulk_update_tasks(*args, **kwargs)
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'bulk_update_tasks' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        router.put(
            path="/task/",
            response_model=None,
            description=AbstractRootService.bulk_update_tasks.__doc__,
            **get_route_args(cls.bulk_update_tasks, default_tag=""),
        )(wrapper)
