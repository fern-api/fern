# This file was auto-generated by Fern from our API Definition.

import abc
import functools
import inspect
import logging
import typing

import fastapi
from ....core.abstract_fern_service import AbstractFernService
from ....core.exceptions.fern_http_exception import FernHTTPException
from ....core.route_args import get_route_args
from ..types.outbound_call_conversations_response import OutboundCallConversationsResponse
from .outbound_call_conversations_request import OutboundCallConversationsRequest


class AbstractConversationsService(AbstractFernService):
    """
    AbstractConversationsService is an abstract class containing the methods that you should implement.

    Each method is associated with an API route, which will be registered
    with FastAPI when you register your implementation using Fern's register()
    function.
    """

    @abc.abstractmethod
    def outbound_call(self, *, body: OutboundCallConversationsRequest) -> OutboundCallConversationsResponse:
        """
        Place an outbound call or validate call setup with dry_run.
        """
        ...

    """
    Below are internal methods used by Fern to register your implementation.
    You can ignore them.
    """

    @classmethod
    def _init_fern(cls, router: fastapi.APIRouter) -> None:
        cls.__init_outbound_call(router=router)

    @classmethod
    def __init_outbound_call(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.outbound_call)
        type_hints = typing.get_type_hints(cls.outbound_call)

        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            # Get the resolved type hint for this parameter, as fastapi does not handle forward refs in all cases
            resolved_annotation = type_hints.get(parameter_name, parameter.annotation)

            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            elif parameter_name == "body":
                new_parameters.append(
                    parameter.replace(annotation=typing.Annotated[resolved_annotation, fastapi.Body()])
                )
            else:
                new_parameters.append(parameter)
        setattr(cls.outbound_call, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.outbound_call)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> OutboundCallConversationsResponse:
            try:
                return cls.outbound_call(*args, **kwargs)
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'outbound_call' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        router.post(
            path="/conversations/outbound-call",
            response_model=None,
            description=AbstractConversationsService.outbound_call.__doc__,
            **get_route_args(cls.outbound_call, default_tag="conversations"),
        )(wrapper)
