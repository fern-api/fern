# This file was auto-generated by Fern from our API Definition.

import abc
import functools
import inspect
import logging
import typing

import fastapi
from ....core.abstract_fern_service import AbstractFernService
from ....core.exceptions.fern_http_exception import FernHTTPException
from ....core.route_args import get_route_args
from ..types.token_response import TokenResponse
from .get_token_request import GetTokenRequest
from .refresh_token_request import RefreshTokenRequest


class AbstractAuthService(AbstractFernService):
    """
    AbstractAuthService is an abstract class containing the methods that you should implement.

    Each method is associated with an API route, which will be registered
    with FastAPI when you register your implementation using Fern's register()
    function.
    """

    @abc.abstractmethod
    def get_token_with_client_credentials(self, *, body: GetTokenRequest, x_api_key: str) -> TokenResponse: ...

    @abc.abstractmethod
    def refresh_token(self, *, body: RefreshTokenRequest, x_api_key: str) -> TokenResponse: ...

    """
    Below are internal methods used by Fern to register your implementation.
    You can ignore them.
    """

    @classmethod
    def _init_fern(cls, router: fastapi.APIRouter) -> None:
        cls.__init_get_token_with_client_credentials(router=router)
        cls.__init_refresh_token(router=router)

    @classmethod
    def __init_get_token_with_client_credentials(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.get_token_with_client_credentials)
        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            elif parameter_name == "body":
                new_parameters.append(parameter.replace(default=fastapi.Body(...)))
            elif parameter_name == "x_api_key":
                new_parameters.append(parameter.replace(default=fastapi.Header(alias="X-Api-Key")))
            else:
                new_parameters.append(parameter)
        setattr(
            cls.get_token_with_client_credentials, "__signature__", endpoint_function.replace(parameters=new_parameters)
        )

        @functools.wraps(cls.get_token_with_client_credentials)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> TokenResponse:
            try:
                return cls.get_token_with_client_credentials(*args, **kwargs)
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'get_token_with_client_credentials' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        # this is necessary for FastAPI to find forward-ref'ed type hints.
        # https://github.com/tiangolo/fastapi/pull/5077
        wrapper.__globals__.update(cls.get_token_with_client_credentials.__globals__)

        router.post(
            path="/token",
            response_model=TokenResponse,
            description=AbstractAuthService.get_token_with_client_credentials.__doc__,
            **get_route_args(cls.get_token_with_client_credentials, default_tag="auth"),
        )(wrapper)

    @classmethod
    def __init_refresh_token(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.refresh_token)
        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            elif parameter_name == "body":
                new_parameters.append(parameter.replace(default=fastapi.Body(...)))
            elif parameter_name == "x_api_key":
                new_parameters.append(parameter.replace(default=fastapi.Header(alias="X-Api-Key")))
            else:
                new_parameters.append(parameter)
        setattr(cls.refresh_token, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.refresh_token)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> TokenResponse:
            try:
                return cls.refresh_token(*args, **kwargs)
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'refresh_token' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        # this is necessary for FastAPI to find forward-ref'ed type hints.
        # https://github.com/tiangolo/fastapi/pull/5077
        wrapper.__globals__.update(cls.refresh_token.__globals__)

        router.post(
            path="/token/refresh",
            response_model=TokenResponse,
            description=AbstractAuthService.refresh_token.__doc__,
            **get_route_args(cls.refresh_token, default_tag="auth"),
        )(wrapper)
