# This file was auto-generated by Fern from our API Definition.

import abc
import functools
import inspect
import logging
import typing

import fastapi
from ....core.abstract_fern_service import AbstractFernService
from ....core.exceptions.fern_http_exception import FernHTTPException
from ....core.route_args import get_route_args
from ..types.list_users_path_pagination_response import ListUsersPathPaginationResponse
from ..types.list_users_uri_pagination_response import ListUsersUriPaginationResponse


class AbstractUsersService(AbstractFernService):
    """
    AbstractUsersService is an abstract class containing the methods that you should implement.

    Each method is associated with an API route, which will be registered
    with FastAPI when you register your implementation using Fern's register()
    function.
    """

    @abc.abstractmethod
    def list_with_uri_pagination(self) -> ListUsersUriPaginationResponse: ...

    @abc.abstractmethod
    def list_with_path_pagination(self) -> ListUsersPathPaginationResponse: ...

    """
    Below are internal methods used by Fern to register your implementation.
    You can ignore them.
    """

    @classmethod
    def _init_fern(cls, router: fastapi.APIRouter) -> None:
        cls.__init_list_with_uri_pagination(router=router)
        cls.__init_list_with_path_pagination(router=router)

    @classmethod
    def __init_list_with_uri_pagination(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.list_with_uri_pagination)
        type_hints = typing.get_type_hints(cls.list_with_uri_pagination)

        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            # Get the resolved type hint for this parameter, as fastapi does not handle forward refs in all cases
            resolved_annotation = type_hints.get(parameter_name, parameter.annotation)

            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            else:
                new_parameters.append(parameter)
        setattr(cls.list_with_uri_pagination, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.list_with_uri_pagination)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> ListUsersUriPaginationResponse:
            try:
                return cls.list_with_uri_pagination(*args, **kwargs)
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'list_with_uri_pagination' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        router.get(
            path="/users/uri",
            response_model=None,
            description=AbstractUsersService.list_with_uri_pagination.__doc__,
            **get_route_args(cls.list_with_uri_pagination, default_tag="users"),
        )(wrapper)

    @classmethod
    def __init_list_with_path_pagination(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.list_with_path_pagination)
        type_hints = typing.get_type_hints(cls.list_with_path_pagination)

        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            # Get the resolved type hint for this parameter, as fastapi does not handle forward refs in all cases
            resolved_annotation = type_hints.get(parameter_name, parameter.annotation)

            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            else:
                new_parameters.append(parameter)
        setattr(cls.list_with_path_pagination, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.list_with_path_pagination)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> ListUsersPathPaginationResponse:
            try:
                return cls.list_with_path_pagination(*args, **kwargs)
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'list_with_path_pagination' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        router.get(
            path="/users/path",
            response_model=None,
            description=AbstractUsersService.list_with_path_pagination.__doc__,
            **get_route_args(cls.list_with_path_pagination, default_tag="users"),
        )(wrapper)
