# This file was auto-generated by Fern from our API Definition.

import abc
import functools
import inspect
import logging
import typing

import fastapi

from ....core.abstract_fern_service import AbstractFernService
from ....core.exceptions.fern_http_exception import FernHTTPException
from ....core.route_args import get_route_args
from ....types.optional_string_response import OptionalStringResponse
from ....types.string_response import StringResponse
from ..types.optional_with_docs import OptionalWithDocs
from ..types.response import Response


class AbstractServiceService(AbstractFernService):
    """
    AbstractServiceService is an abstract class containing the methods that you should implement.

    Each method is associated with an API route, which will be registered
    with FastAPI when you register your implementation using Fern's register()
    function.
    """

    @abc.abstractmethod
    def get_movie(self, *, body: str) -> Response:
        ...

    @abc.abstractmethod
    def get_movie_docs(self, *, body: str) -> Response:
        ...

    @abc.abstractmethod
    def get_movie_name(self, *, body: str) -> StringResponse:
        ...

    @abc.abstractmethod
    def get_movie_metadata(self, *, body: str) -> Response:
        ...

    @abc.abstractmethod
    def get_optional_movie(self, *, body: str) -> typing.Optional[Response]:
        ...

    @abc.abstractmethod
    def get_optional_movie_docs(self, *, body: str) -> OptionalWithDocs:
        ...

    @abc.abstractmethod
    def get_optional_movie_name(self, *, body: str) -> OptionalStringResponse:
        ...

    """
    Below are internal methods used by Fern to register your implementation.
    You can ignore them.
    """

    @classmethod
    def _init_fern(cls, router: fastapi.APIRouter) -> None:
        cls.__init_get_movie(router=router)
        cls.__init_get_movie_docs(router=router)
        cls.__init_get_movie_name(router=router)
        cls.__init_get_movie_metadata(router=router)
        cls.__init_get_optional_movie(router=router)
        cls.__init_get_optional_movie_docs(router=router)
        cls.__init_get_optional_movie_name(router=router)

    @classmethod
    def __init_get_movie(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.get_movie)
        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            elif parameter_name == "body":
                new_parameters.append(parameter.replace(default=fastapi.Body(...)))
            else:
                new_parameters.append(parameter)
        setattr(cls.get_movie, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.get_movie)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> Response:
            try:
                return cls.get_movie(*args, **kwargs)
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'get_movie' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        # this is necessary for FastAPI to find forward-ref'ed type hints.
        # https://github.com/tiangolo/fastapi/pull/5077
        wrapper.__globals__.update(cls.get_movie.__globals__)

        router.post(
            path="//movie",
            response_model=Response,
            description=AbstractServiceService.get_movie.__doc__,
            **get_route_args(cls.get_movie, default_tag="service"),
        )(wrapper)

    @classmethod
    def __init_get_movie_docs(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.get_movie_docs)
        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            elif parameter_name == "body":
                new_parameters.append(parameter.replace(default=fastapi.Body(...)))
            else:
                new_parameters.append(parameter)
        setattr(cls.get_movie_docs, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.get_movie_docs)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> Response:
            try:
                return cls.get_movie_docs(*args, **kwargs)
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'get_movie_docs' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        # this is necessary for FastAPI to find forward-ref'ed type hints.
        # https://github.com/tiangolo/fastapi/pull/5077
        wrapper.__globals__.update(cls.get_movie_docs.__globals__)

        router.post(
            path="//movie",
            response_model=Response,
            description=AbstractServiceService.get_movie_docs.__doc__,
            **get_route_args(cls.get_movie_docs, default_tag="service"),
        )(wrapper)

    @classmethod
    def __init_get_movie_name(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.get_movie_name)
        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            elif parameter_name == "body":
                new_parameters.append(parameter.replace(default=fastapi.Body(...)))
            else:
                new_parameters.append(parameter)
        setattr(cls.get_movie_name, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.get_movie_name)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> StringResponse:
            try:
                return cls.get_movie_name(*args, **kwargs)
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'get_movie_name' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        # this is necessary for FastAPI to find forward-ref'ed type hints.
        # https://github.com/tiangolo/fastapi/pull/5077
        wrapper.__globals__.update(cls.get_movie_name.__globals__)

        router.post(
            path="//movie",
            response_model=StringResponse,
            description=AbstractServiceService.get_movie_name.__doc__,
            **get_route_args(cls.get_movie_name, default_tag="service"),
        )(wrapper)

    @classmethod
    def __init_get_movie_metadata(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.get_movie_metadata)
        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            elif parameter_name == "body":
                new_parameters.append(parameter.replace(default=fastapi.Body(...)))
            else:
                new_parameters.append(parameter)
        setattr(cls.get_movie_metadata, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.get_movie_metadata)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> Response:
            try:
                return cls.get_movie_metadata(*args, **kwargs)
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'get_movie_metadata' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        # this is necessary for FastAPI to find forward-ref'ed type hints.
        # https://github.com/tiangolo/fastapi/pull/5077
        wrapper.__globals__.update(cls.get_movie_metadata.__globals__)

        router.post(
            path="//movie",
            response_model=Response,
            description=AbstractServiceService.get_movie_metadata.__doc__,
            **get_route_args(cls.get_movie_metadata, default_tag="service"),
        )(wrapper)

    @classmethod
    def __init_get_optional_movie(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.get_optional_movie)
        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            elif parameter_name == "body":
                new_parameters.append(parameter.replace(default=fastapi.Body(...)))
            else:
                new_parameters.append(parameter)
        setattr(cls.get_optional_movie, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.get_optional_movie)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> typing.Optional[Response]:
            try:
                return cls.get_optional_movie(*args, **kwargs)
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'get_optional_movie' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        # this is necessary for FastAPI to find forward-ref'ed type hints.
        # https://github.com/tiangolo/fastapi/pull/5077
        wrapper.__globals__.update(cls.get_optional_movie.__globals__)

        router.post(
            path="//movie",
            response_model=typing.Optional[Response],
            description=AbstractServiceService.get_optional_movie.__doc__,
            **get_route_args(cls.get_optional_movie, default_tag="service"),
        )(wrapper)

    @classmethod
    def __init_get_optional_movie_docs(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.get_optional_movie_docs)
        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            elif parameter_name == "body":
                new_parameters.append(parameter.replace(default=fastapi.Body(...)))
            else:
                new_parameters.append(parameter)
        setattr(cls.get_optional_movie_docs, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.get_optional_movie_docs)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> OptionalWithDocs:
            try:
                return cls.get_optional_movie_docs(*args, **kwargs)
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'get_optional_movie_docs' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        # this is necessary for FastAPI to find forward-ref'ed type hints.
        # https://github.com/tiangolo/fastapi/pull/5077
        wrapper.__globals__.update(cls.get_optional_movie_docs.__globals__)

        router.post(
            path="//movie",
            response_model=OptionalWithDocs,
            description=AbstractServiceService.get_optional_movie_docs.__doc__,
            **get_route_args(cls.get_optional_movie_docs, default_tag="service"),
        )(wrapper)

    @classmethod
    def __init_get_optional_movie_name(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.get_optional_movie_name)
        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            elif parameter_name == "body":
                new_parameters.append(parameter.replace(default=fastapi.Body(...)))
            else:
                new_parameters.append(parameter)
        setattr(cls.get_optional_movie_name, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.get_optional_movie_name)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> OptionalStringResponse:
            try:
                return cls.get_optional_movie_name(*args, **kwargs)
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'get_optional_movie_name' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        # this is necessary for FastAPI to find forward-ref'ed type hints.
        # https://github.com/tiangolo/fastapi/pull/5077
        wrapper.__globals__.update(cls.get_optional_movie_name.__globals__)

        router.post(
            path="//movie",
            response_model=OptionalStringResponse,
            description=AbstractServiceService.get_optional_movie_name.__doc__,
            **get_route_args(cls.get_optional_movie_name, default_tag="service"),
        )(wrapper)
