# This file was auto-generated by Fern from our API Definition.

import abc
import functools
import inspect
import logging
import typing

import fastapi
from ..core.abstract_fern_service import AbstractFernService
from ..core.exceptions.fern_http_exception import FernHTTPException
from ..core.route_args import get_route_args
from ..types.create_plant_response import CreatePlantResponse
from ..types.create_plant_with_schema_response import CreatePlantWithSchemaResponse
from ..types.update_plant_response import UpdatePlantResponse
from .create_plant_with_schema_request import CreatePlantWithSchemaRequest


class AbstractRootService(AbstractFernService):
    """
    AbstractRootService is an abstract class containing the methods that you should implement.

    Each method is associated with an API route, which will be registered
    with FastAPI when you register your implementation using Fern's register()
    function.
    """

    @abc.abstractmethod
    def create_plant(self, *, body: typing.Any) -> CreatePlantResponse:
        """
        Creates a plant with example JSON but no request body schema.
        """
        ...

    @abc.abstractmethod
    def update_plant(self, *, body: typing.Any, plant_id: str) -> UpdatePlantResponse:
        """
        Updates a plant with example JSON but no request body schema.
        """
        ...

    @abc.abstractmethod
    def create_plant_with_schema(self, *, body: CreatePlantWithSchemaRequest) -> CreatePlantWithSchemaResponse:
        """
        A control endpoint that has both schema and example defined.
        """
        ...

    """
    Below are internal methods used by Fern to register your implementation.
    You can ignore them.
    """

    @classmethod
    def _init_fern(cls, router: fastapi.APIRouter) -> None:
        cls.__init_create_plant(router=router)
        cls.__init_update_plant(router=router)
        cls.__init_create_plant_with_schema(router=router)

    @classmethod
    def __init_create_plant(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.create_plant)
        type_hints = typing.get_type_hints(cls.create_plant)

        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            # Get the resolved type hint for this parameter, as fastapi does not handle forward refs in all cases
            resolved_annotation = type_hints.get(parameter_name, parameter.annotation)

            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            elif parameter_name == "body":
                new_parameters.append(
                    parameter.replace(annotation=typing.Annotated[resolved_annotation, fastapi.Body()])
                )
            else:
                new_parameters.append(parameter)
        setattr(cls.create_plant, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.create_plant)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> CreatePlantResponse:
            try:
                return cls.create_plant(*args, **kwargs)
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'create_plant' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        router.post(
            path="/plants",
            response_model=None,
            description=AbstractRootService.create_plant.__doc__,
            **get_route_args(cls.create_plant, default_tag=""),
        )(wrapper)

    @classmethod
    def __init_update_plant(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.update_plant)
        type_hints = typing.get_type_hints(cls.update_plant)

        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            # Get the resolved type hint for this parameter, as fastapi does not handle forward refs in all cases
            resolved_annotation = type_hints.get(parameter_name, parameter.annotation)

            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            elif parameter_name == "body":
                new_parameters.append(
                    parameter.replace(annotation=typing.Annotated[resolved_annotation, fastapi.Body()])
                )
            elif parameter_name == "plant_id":
                new_parameters.append(
                    parameter.replace(annotation=typing.Annotated[resolved_annotation, fastapi.Path(alias="plantId")])
                )
            else:
                new_parameters.append(parameter)
        setattr(cls.update_plant, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.update_plant)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> UpdatePlantResponse:
            try:
                return cls.update_plant(*args, **kwargs)
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'update_plant' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        router.put(
            path="/plants/{plant_id}",
            response_model=None,
            description=AbstractRootService.update_plant.__doc__,
            **get_route_args(cls.update_plant, default_tag=""),
        )(wrapper)

    @classmethod
    def __init_create_plant_with_schema(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.create_plant_with_schema)
        type_hints = typing.get_type_hints(cls.create_plant_with_schema)

        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            # Get the resolved type hint for this parameter, as fastapi does not handle forward refs in all cases
            resolved_annotation = type_hints.get(parameter_name, parameter.annotation)

            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            elif parameter_name == "body":
                new_parameters.append(
                    parameter.replace(annotation=typing.Annotated[resolved_annotation, fastapi.Body()])
                )
            else:
                new_parameters.append(parameter)
        setattr(cls.create_plant_with_schema, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.create_plant_with_schema)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> CreatePlantWithSchemaResponse:
            try:
                return cls.create_plant_with_schema(*args, **kwargs)
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'create_plant_with_schema' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        router.post(
            path="/plants/with-schema",
            response_model=None,
            description=AbstractRootService.create_plant_with_schema.__doc__,
            **get_route_args(cls.create_plant_with_schema, default_tag=""),
        )(wrapper)
