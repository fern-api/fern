# This file was auto-generated by Fern from our API Definition.

import abc
import functools
import inspect
import logging
import typing

import fastapi
from ....core.abstract_fern_service import AbstractFernService
from ....core.exceptions.fern_http_exception import FernHTTPException
from ....core.route_args import get_route_args
from ...commons.errors.bad_request_error import BadRequestError
from ...commons.errors.internal_server_error import InternalServerError
from ...commons.errors.not_found_error import NotFoundError
from ..errors.foo_too_little import FooTooLittle
from ..errors.foo_too_much import FooTooMuch
from ..types.foo_request import FooRequest
from ..types.foo_response import FooResponse


class AbstractSimpleService(AbstractFernService):
    """
    AbstractSimpleService is an abstract class containing the methods that you should implement.

    Each method is associated with an API route, which will be registered
    with FastAPI when you register your implementation using Fern's register()
    function.
    """

    @abc.abstractmethod
    def foo_without_endpoint_error(self, *, body: FooRequest) -> FooResponse: ...

    @abc.abstractmethod
    def foo(self, *, body: FooRequest) -> FooResponse: ...

    @abc.abstractmethod
    def foo_with_examples(self, *, body: FooRequest) -> FooResponse: ...

    """
    Below are internal methods used by Fern to register your implementation.
    You can ignore them.
    """

    @classmethod
    def _init_fern(cls, router: fastapi.APIRouter) -> None:
        cls.__init_foo_without_endpoint_error(router=router)
        cls.__init_foo(router=router)
        cls.__init_foo_with_examples(router=router)

    @classmethod
    def __init_foo_without_endpoint_error(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.foo_without_endpoint_error)
        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            elif parameter_name == "body":
                new_parameters.append(
                    parameter.replace(annotation=typing.Annotated[parameter.annotation, fastapi.Body()])
                )
            else:
                new_parameters.append(parameter)
        setattr(cls.foo_without_endpoint_error, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.foo_without_endpoint_error)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> FooResponse:
            try:
                return cls.foo_without_endpoint_error(*args, **kwargs)
            except (NotFoundError, BadRequestError, InternalServerError) as e:
                raise e
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'foo_without_endpoint_error' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        router.post(
            path="/foo1",
            response_model=FooResponse,
            description=AbstractSimpleService.foo_without_endpoint_error.__doc__,
            **get_route_args(cls.foo_without_endpoint_error, default_tag="simple"),
        )(wrapper)

    @classmethod
    def __init_foo(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.foo)
        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            elif parameter_name == "body":
                new_parameters.append(
                    parameter.replace(annotation=typing.Annotated[parameter.annotation, fastapi.Body()])
                )
            else:
                new_parameters.append(parameter)
        setattr(cls.foo, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.foo)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> FooResponse:
            try:
                return cls.foo(*args, **kwargs)
            except (FooTooMuch, FooTooLittle, NotFoundError, BadRequestError, InternalServerError) as e:
                raise e
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'foo' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        router.post(
            path="/foo2",
            response_model=FooResponse,
            description=AbstractSimpleService.foo.__doc__,
            **get_route_args(cls.foo, default_tag="simple"),
        )(wrapper)

    @classmethod
    def __init_foo_with_examples(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.foo_with_examples)
        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            elif parameter_name == "body":
                new_parameters.append(
                    parameter.replace(annotation=typing.Annotated[parameter.annotation, fastapi.Body()])
                )
            else:
                new_parameters.append(parameter)
        setattr(cls.foo_with_examples, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.foo_with_examples)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> FooResponse:
            try:
                return cls.foo_with_examples(*args, **kwargs)
            except (FooTooMuch, FooTooLittle, NotFoundError, BadRequestError, InternalServerError) as e:
                raise e
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'foo_with_examples' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        router.post(
            path="/foo3",
            response_model=FooResponse,
            description=AbstractSimpleService.foo_with_examples.__doc__,
            **get_route_args(cls.foo_with_examples, default_tag="simple"),
        )(wrapper)
