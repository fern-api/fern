# This file was auto-generated by Fern from our API Definition.

import abc
import functools
import inspect
import logging
import typing

import fastapi
from ....core.abstract_fern_service import AbstractFernService
from ....core.exceptions.fern_http_exception import FernHTTPException
from ....core.route_args import get_route_args
from ....types.file_id import FileId


class AbstractFileUploadExampleService(AbstractFernService):
    """
    AbstractFileUploadExampleService is an abstract class containing the methods that you should implement.

    Each method is associated with an API route, which will be registered
    with FastAPI when you register your implementation using Fern's register()
    function.
    """

    @abc.abstractmethod
    def upload_file(self, *, name: str, file: typing.Union[fastapi.UploadFile, None]) -> FileId:
        """
        Upload a file to the database
        """
        ...

    """
    Below are internal methods used by Fern to register your implementation.
    You can ignore them.
    """

    @classmethod
    def _init_fern(cls, router: fastapi.APIRouter) -> None:
        cls.__init_upload_file(router=router)

    @classmethod
    def __init_upload_file(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.upload_file)
        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            elif parameter_name == "name":
                new_parameters.append(parameter.replace(default=fastapi.Form(...)))
            elif parameter_name == "file":
                new_parameters.append(parameter.replace(default=typing.Union[fastapi.UploadFile, None]))
            else:
                new_parameters.append(parameter)
        setattr(cls.upload_file, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.upload_file)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> FileId:
            try:
                return cls.upload_file(*args, **kwargs)
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'upload_file' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        # this is necessary for FastAPI to find forward-ref'ed type hints.
        # https://github.com/tiangolo/fastapi/pull/5077
        wrapper.__globals__.update(cls.upload_file.__globals__)

        router.post(
            path="/upload-file",
            response_model=FileId,
            description=AbstractFileUploadExampleService.upload_file.__doc__,
            **get_route_args(cls.upload_file, default_tag="file_upload_example"),
        )(wrapper)
