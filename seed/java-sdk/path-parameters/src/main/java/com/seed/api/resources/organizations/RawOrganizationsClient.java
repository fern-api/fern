/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.api.resources.organizations;

import com.fasterxml.jackson.core.type.TypeReference;
import com.seed.api.core.ClientOptions;
import com.seed.api.core.ObjectMappers;
import com.seed.api.core.QueryStringMapper;
import com.seed.api.core.RequestOptions;
import com.seed.api.core.SeedApiApiException;
import com.seed.api.core.SeedApiException;
import com.seed.api.core.SeedApiHttpResponse;
import com.seed.api.resources.organizations.requests.OrganizationsGetOrganizationRequest;
import com.seed.api.resources.organizations.requests.OrganizationsGetOrganizationUserRequest;
import com.seed.api.resources.organizations.requests.OrganizationsSearchOrganizationsRequest;
import com.seed.api.types.Organization;
import com.seed.api.types.User;
import java.io.IOException;
import java.util.List;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawOrganizationsClient {
    protected final ClientOptions clientOptions;

    public RawOrganizationsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public SeedApiHttpResponse<Organization> getorganization(String tenantId, String organizationId) {
        return getorganization(
                tenantId,
                organizationId,
                OrganizationsGetOrganizationRequest.builder().build());
    }

    public SeedApiHttpResponse<Organization> getorganization(
            String tenantId, String organizationId, RequestOptions requestOptions) {
        return getorganization(
                tenantId,
                organizationId,
                OrganizationsGetOrganizationRequest.builder().build(),
                requestOptions);
    }

    public SeedApiHttpResponse<Organization> getorganization(
            String tenantId, String organizationId, OrganizationsGetOrganizationRequest request) {
        return getorganization(tenantId, organizationId, request, null);
    }

    public SeedApiHttpResponse<Organization> getorganization(
            String tenantId,
            String organizationId,
            OrganizationsGetOrganizationRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegment(tenantId)
                .addPathSegments("organizations")
                .addPathSegment(organizationId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new SeedApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Organization.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SeedApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new SeedApiException("Network error executing HTTP request", e);
        }
    }

    public SeedApiHttpResponse<User> getorganizationuser(String tenantId, String organizationId, String userId) {
        return getorganizationuser(
                tenantId,
                organizationId,
                userId,
                OrganizationsGetOrganizationUserRequest.builder().build());
    }

    public SeedApiHttpResponse<User> getorganizationuser(
            String tenantId, String organizationId, String userId, RequestOptions requestOptions) {
        return getorganizationuser(
                tenantId,
                organizationId,
                userId,
                OrganizationsGetOrganizationUserRequest.builder().build(),
                requestOptions);
    }

    public SeedApiHttpResponse<User> getorganizationuser(
            String tenantId, String organizationId, String userId, OrganizationsGetOrganizationUserRequest request) {
        return getorganizationuser(tenantId, organizationId, userId, request, null);
    }

    public SeedApiHttpResponse<User> getorganizationuser(
            String tenantId,
            String organizationId,
            String userId,
            OrganizationsGetOrganizationUserRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegment(tenantId)
                .addPathSegments("organizations")
                .addPathSegment(organizationId)
                .addPathSegments("users")
                .addPathSegment(userId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new SeedApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, User.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SeedApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new SeedApiException("Network error executing HTTP request", e);
        }
    }

    public SeedApiHttpResponse<List<Organization>> searchorganizations(String tenantId, String organizationId) {
        return searchorganizations(
                tenantId,
                organizationId,
                OrganizationsSearchOrganizationsRequest.builder().build());
    }

    public SeedApiHttpResponse<List<Organization>> searchorganizations(
            String tenantId, String organizationId, RequestOptions requestOptions) {
        return searchorganizations(
                tenantId,
                organizationId,
                OrganizationsSearchOrganizationsRequest.builder().build(),
                requestOptions);
    }

    public SeedApiHttpResponse<List<Organization>> searchorganizations(
            String tenantId, String organizationId, OrganizationsSearchOrganizationsRequest request) {
        return searchorganizations(tenantId, organizationId, request, null);
    }

    public SeedApiHttpResponse<List<Organization>> searchorganizations(
            String tenantId,
            String organizationId,
            OrganizationsSearchOrganizationsRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegment(tenantId)
                .addPathSegments("organizations")
                .addPathSegment(organizationId)
                .addPathSegments("search");
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new SeedApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, new TypeReference<List<Organization>>() {}),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SeedApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new SeedApiException("Network error executing HTTP request", e);
        }
    }
}
