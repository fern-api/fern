/**
 * This file was auto-generated by Fern from our API Definition.
 */

package com.fern.sdk.resources.endpointspagination;

import com.fern.sdk.core.ClientOptions;
import com.fern.sdk.core.ObjectMappers;
import com.fern.sdk.core.QueryStringMapper;
import com.fern.sdk.core.RequestOptions;
import com.fern.sdk.core.SeedApiApiException;
import com.fern.sdk.core.SeedApiException;
import com.fern.sdk.core.SeedApiHttpResponse;
import com.fern.sdk.resources.endpointspagination.requests.EndpointsPaginationListItemsRequest;
import com.fern.sdk.types.EndpointsPaginatedResponse;
import java.io.IOException;
import java.lang.Object;
import java.lang.String;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawEndpointsPaginationClient {
  protected final ClientOptions clientOptions;

  public RawEndpointsPaginationClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * List items with cursor pagination
   */
  public SeedApiHttpResponse<EndpointsPaginatedResponse> endpointsPaginationListItems() {
    return endpointsPaginationListItems(EndpointsPaginationListItemsRequest.builder().build());
  }

  /**
   * List items with cursor pagination
   */
  public SeedApiHttpResponse<EndpointsPaginatedResponse> endpointsPaginationListItems(
      RequestOptions requestOptions) {
    return endpointsPaginationListItems(EndpointsPaginationListItemsRequest.builder().build(),requestOptions);
  }

  /**
   * List items with cursor pagination
   */
  public SeedApiHttpResponse<EndpointsPaginatedResponse> endpointsPaginationListItems(
      EndpointsPaginationListItemsRequest request) {
    return endpointsPaginationListItems(request,null);
  }

  /**
   * List items with cursor pagination
   */
  public SeedApiHttpResponse<EndpointsPaginatedResponse> endpointsPaginationListItems(
      EndpointsPaginationListItemsRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("pagination");if (request.getCursor().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "cursor", request.getCursor().get(), false);
      }
      if (request.getLimit().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "limit", request.getLimit().get(), false);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new SeedApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, EndpointsPaginatedResponse.class), response);
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new SeedApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (IOException e) {
        throw new SeedApiException("Network error executing HTTP request", e);
      }
    }
  }
