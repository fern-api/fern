/**
 * This file was auto-generated by Fern from our API Definition.
 */

package com.fern.sdk.core;

import com.fern.sdk.resources.oauth.OauthClient;
import com.fern.sdk.resources.oauth.requests.GetTokenRequest;
import com.fern.sdk.resources.oauth.types.OAuthTokenResponse;
import java.lang.String;
import java.time.Instant;
import java.time.temporal.ChronoUnit;
import java.util.function.Supplier;

public final class OAuthTokenSupplier implements Supplier<String> {
  private static final long BUFFER_IN_MINUTES = 2;

  private final String clientId;

  private final String clientSecret;

  private final String grantType;

  private final String scope;

  private final OauthClient authClient;

  private String accessToken;

  private Instant expiresAt;

  public OAuthTokenSupplier(String clientId, String clientSecret, String grantType, String scope,
      OauthClient authClient) {
    this.clientId = clientId;
    this.clientSecret = clientSecret;
    this.grantType = grantType;
    this.scope = scope;
    this.authClient = authClient;
    this.expiresAt = Instant.now();
  }

  public OAuthTokenResponse fetchToken() {
    GetTokenRequest getTokenRequest = GetTokenRequest.builder().grantType(grantType).clientId(clientId).clientSecret(clientSecret).scope(scope).build();
    return authClient.getToken(getTokenRequest);
  }

  @java.lang.Override
  public String get() {
    if (accessToken == null || expiresAt.isBefore(Instant.now())) {
      OAuthTokenResponse authResponse = fetchToken();
      this.accessToken = authResponse.getAccessToken();
      this.expiresAt = getExpiresAt(authResponse.getExpiresIn().orElse(3600L));
    }
    return "Bearer " + accessToken;
  }

  private Instant getExpiresAt(long expiresInSeconds) {
    return Instant.now().plus(expiresInSeconds, ChronoUnit.SECONDS).minus(BUFFER_IN_MINUTES, ChronoUnit.MINUTES);
  }
}
