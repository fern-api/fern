/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.api;

import com.seed.api.core.ClientOptions;
import com.seed.api.core.ObjectMappers;
import com.seed.api.core.QueryStringMapper;
import com.seed.api.core.RequestOptions;
import com.seed.api.core.SeedApiApiException;
import com.seed.api.core.SeedApiException;
import com.seed.api.core.SeedApiHttpResponse;
import com.seed.api.types.EndpointsPaginatedResponse;
import com.seed.api.types.EndpointsPaginationListItemsRequest;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawEndpointsPaginationClient {
    protected final ClientOptions clientOptions;

    public AsyncRawEndpointsPaginationClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * List items with cursor pagination
     */
    public CompletableFuture<SeedApiHttpResponse<EndpointsPaginatedResponse>> endpointsPaginationListItems() {
        return endpointsPaginationListItems(
                EndpointsPaginationListItemsRequest.builder().build());
    }

    /**
     * List items with cursor pagination
     */
    public CompletableFuture<SeedApiHttpResponse<EndpointsPaginatedResponse>> endpointsPaginationListItems(
            RequestOptions requestOptions) {
        return endpointsPaginationListItems(
                EndpointsPaginationListItemsRequest.builder().build(), requestOptions);
    }

    /**
     * List items with cursor pagination
     */
    public CompletableFuture<SeedApiHttpResponse<EndpointsPaginatedResponse>> endpointsPaginationListItems(
            EndpointsPaginationListItemsRequest request) {
        return endpointsPaginationListItems(request, null);
    }

    /**
     * List items with cursor pagination
     */
    public CompletableFuture<SeedApiHttpResponse<EndpointsPaginatedResponse>> endpointsPaginationListItems(
            EndpointsPaginationListItemsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("pagination");
        if (request.getCursor().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "cursor", request.getCursor().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<SeedApiHttpResponse<EndpointsPaginatedResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new SeedApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, EndpointsPaginatedResponse.class),
                                response));
                        return;
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new SeedApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new SeedApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new SeedApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
