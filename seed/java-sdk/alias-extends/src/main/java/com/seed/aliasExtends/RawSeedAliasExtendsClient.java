/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.aliasExtends;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.seed.aliasExtends.core.ClientOptions;
import com.seed.aliasExtends.core.MediaTypes;
import com.seed.aliasExtends.core.ObjectMappers;
import com.seed.aliasExtends.core.RequestOptions;
import com.seed.aliasExtends.core.SeedAliasExtendsApiException;
import com.seed.aliasExtends.core.SeedAliasExtendsException;
import com.seed.aliasExtends.core.SeedAliasExtendsHttpResponse;
import com.seed.aliasExtends.requests.InlinedChildRequest;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawSeedAliasExtendsClient {
    protected final ClientOptions clientOptions;

    public RawSeedAliasExtendsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public SeedAliasExtendsHttpResponse<Void> extendedInlineRequestBody(InlinedChildRequest request) {
        return extendedInlineRequestBody(request, null);
    }

    public SeedAliasExtendsHttpResponse<Void> extendedInlineRequestBody(
            InlinedChildRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("extends")
                .addPathSegments("extended-inline-request-body");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new SeedAliasExtendsException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new SeedAliasExtendsHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SeedAliasExtendsApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new SeedAliasExtendsException("Network error executing HTTP request", e);
        }
    }
}
