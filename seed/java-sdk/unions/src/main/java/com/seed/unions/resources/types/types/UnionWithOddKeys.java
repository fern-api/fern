/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.unions.resources.types.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class UnionWithOddKeys {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private UnionWithOddKeys(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static UnionWithOddKeys abbott(Foo value) {
        return new UnionWithOddKeys(new AbbottValue(value));
    }

    public static UnionWithOddKeys costEllo(Bar value) {
        return new UnionWithOddKeys(new CostElloValue(value));
    }

    public boolean isAbbott() {
        return value instanceof AbbottValue;
    }

    public boolean isCostEllo() {
        return value instanceof CostElloValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<Foo> getAbbott() {
        if (isAbbott()) {
            return Optional.of(((AbbottValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Bar> getCostEllo() {
        if (isCostEllo()) {
            return Optional.of(((CostElloValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitAbbott(Foo abbott);

        T visitCostEllo(Bar costEllo);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(AbbottValue.class), @JsonSubTypes.Type(CostElloValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("abbott")
    @JsonIgnoreProperties("type")
    private static final class AbbottValue implements Value {
        @JsonUnwrapped
        private Foo value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private AbbottValue() {}

        private AbbottValue(Foo value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitAbbott(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof AbbottValue && equalTo((AbbottValue) other);
        }

        private boolean equalTo(AbbottValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "UnionWithOddKeys{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("CostEllo")
    @JsonIgnoreProperties("type")
    private static final class CostElloValue implements Value {
        @JsonUnwrapped
        private Bar value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private CostElloValue() {}

        private CostElloValue(Bar value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitCostEllo(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof CostElloValue && equalTo((CostElloValue) other);
        }

        private boolean equalTo(CostElloValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "UnionWithOddKeys{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "UnionWithOddKeys{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
