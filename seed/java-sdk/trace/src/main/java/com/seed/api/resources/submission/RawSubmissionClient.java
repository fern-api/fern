/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.api.resources.submission;

import com.seed.api.core.ClientOptions;
import com.seed.api.core.ObjectMappers;
import com.seed.api.core.RequestOptions;
import com.seed.api.core.SeedApiApiException;
import com.seed.api.core.SeedApiException;
import com.seed.api.core.SeedApiHttpResponse;
import com.seed.api.resources.submission.requests.SubmissionCreateExecutionSessionRequest;
import com.seed.api.resources.submission.requests.SubmissionGetExecutionSessionRequest;
import com.seed.api.resources.submission.requests.SubmissionStopExecutionSessionRequest;
import com.seed.api.types.ExecutionSessionResponse;
import com.seed.api.types.GetExecutionSessionStateResponse;
import com.seed.api.types.Language;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawSubmissionClient {
    protected final ClientOptions clientOptions;

    public RawSubmissionClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Returns sessionId and execution server URL for session. Spins up server.
     */
    public SeedApiHttpResponse<ExecutionSessionResponse> createexecutionsession(Language language) {
        return createexecutionsession(
                language, SubmissionCreateExecutionSessionRequest.builder().build());
    }

    /**
     * Returns sessionId and execution server URL for session. Spins up server.
     */
    public SeedApiHttpResponse<ExecutionSessionResponse> createexecutionsession(
            Language language, RequestOptions requestOptions) {
        return createexecutionsession(
                language, SubmissionCreateExecutionSessionRequest.builder().build(), requestOptions);
    }

    /**
     * Returns sessionId and execution server URL for session. Spins up server.
     */
    public SeedApiHttpResponse<ExecutionSessionResponse> createexecutionsession(
            Language language, SubmissionCreateExecutionSessionRequest request) {
        return createexecutionsession(language, request, null);
    }

    /**
     * Returns sessionId and execution server URL for session. Spins up server.
     */
    public SeedApiHttpResponse<ExecutionSessionResponse> createexecutionsession(
            Language language, SubmissionCreateExecutionSessionRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("sessions/create-session")
                .addPathSegment(language.toString());
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new SeedApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ExecutionSessionResponse.class),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SeedApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new SeedApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Returns execution server URL for session. Returns empty if session isn't registered.
     */
    public SeedApiHttpResponse<ExecutionSessionResponse> getexecutionsession(String sessionId) {
        return getexecutionsession(
                sessionId, SubmissionGetExecutionSessionRequest.builder().build());
    }

    /**
     * Returns execution server URL for session. Returns empty if session isn't registered.
     */
    public SeedApiHttpResponse<ExecutionSessionResponse> getexecutionsession(
            String sessionId, RequestOptions requestOptions) {
        return getexecutionsession(
                sessionId, SubmissionGetExecutionSessionRequest.builder().build(), requestOptions);
    }

    /**
     * Returns execution server URL for session. Returns empty if session isn't registered.
     */
    public SeedApiHttpResponse<ExecutionSessionResponse> getexecutionsession(
            String sessionId, SubmissionGetExecutionSessionRequest request) {
        return getexecutionsession(sessionId, request, null);
    }

    /**
     * Returns execution server URL for session. Returns empty if session isn't registered.
     */
    public SeedApiHttpResponse<ExecutionSessionResponse> getexecutionsession(
            String sessionId, SubmissionGetExecutionSessionRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("sessions")
                .addPathSegment(sessionId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new SeedApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ExecutionSessionResponse.class),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SeedApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new SeedApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Stops execution session.
     */
    public SeedApiHttpResponse<Void> stopexecutionsession(String sessionId) {
        return stopexecutionsession(
                sessionId, SubmissionStopExecutionSessionRequest.builder().build());
    }

    /**
     * Stops execution session.
     */
    public SeedApiHttpResponse<Void> stopexecutionsession(String sessionId, RequestOptions requestOptions) {
        return stopexecutionsession(
                sessionId, SubmissionStopExecutionSessionRequest.builder().build(), requestOptions);
    }

    /**
     * Stops execution session.
     */
    public SeedApiHttpResponse<Void> stopexecutionsession(
            String sessionId, SubmissionStopExecutionSessionRequest request) {
        return stopexecutionsession(sessionId, request, null);
    }

    /**
     * Stops execution session.
     */
    public SeedApiHttpResponse<Void> stopexecutionsession(
            String sessionId, SubmissionStopExecutionSessionRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("sessions/stop")
                .addPathSegment(sessionId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)));
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new SeedApiHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SeedApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new SeedApiException("Network error executing HTTP request", e);
        }
    }

    public SeedApiHttpResponse<GetExecutionSessionStateResponse> getexecutionsessionsstate() {
        return getexecutionsessionsstate(null);
    }

    public SeedApiHttpResponse<GetExecutionSessionStateResponse> getexecutionsessionsstate(
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("sessions/execution-sessions-state");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new SeedApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetExecutionSessionStateResponse.class),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SeedApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new SeedApiException("Network error executing HTTP request", e);
        }
    }
}
