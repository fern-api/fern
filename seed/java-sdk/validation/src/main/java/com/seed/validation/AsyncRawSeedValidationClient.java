/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.validation;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.seed.validation.core.ClientOptions;
import com.seed.validation.core.MediaTypes;
import com.seed.validation.core.ObjectMappers;
import com.seed.validation.core.QueryStringMapper;
import com.seed.validation.core.RequestOptions;
import com.seed.validation.core.SeedValidationApiException;
import com.seed.validation.core.SeedValidationException;
import com.seed.validation.core.SeedValidationHttpResponse;
import com.seed.validation.requests.CreateRequest;
import com.seed.validation.requests.GetRequest;
import com.seed.validation.types.Type;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawSeedValidationClient {
    protected final ClientOptions clientOptions;

    public AsyncRawSeedValidationClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public CompletableFuture<SeedValidationHttpResponse<Type>> create(CreateRequest request) {
        return create(request, null);
    }

    public CompletableFuture<SeedValidationHttpResponse<Type>> create(
            CreateRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("create")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new SeedValidationException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<SeedValidationHttpResponse<Type>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new SeedValidationHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Type.class), response));
                        return;
                    }
                    future.completeExceptionally(new SeedValidationApiException(
                            "Error with status code " + response.code(),
                            response.code(),
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                            response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(
                            new SeedValidationException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new SeedValidationException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    public CompletableFuture<SeedValidationHttpResponse<Type>> get(GetRequest request) {
        return get(request, null);
    }

    public CompletableFuture<SeedValidationHttpResponse<Type>> get(GetRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl =
                HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder();

        QueryStringMapper.addQueryParameter(httpUrl, "decimal", request.getDecimal(), false);
        QueryStringMapper.addQueryParameter(httpUrl, "even", request.getEven(), false);
        QueryStringMapper.addQueryParameter(httpUrl, "name", request.getName(), false);
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<SeedValidationHttpResponse<Type>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new SeedValidationHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Type.class), response));
                        return;
                    }
                    future.completeExceptionally(new SeedValidationApiException(
                            "Error with status code " + response.code(),
                            response.code(),
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                            response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(
                            new SeedValidationException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new SeedValidationException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
