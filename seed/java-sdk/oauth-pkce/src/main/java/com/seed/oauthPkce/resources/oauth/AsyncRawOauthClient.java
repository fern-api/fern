/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.oauthPkce.resources.oauth;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.seed.oauthPkce.core.ClientOptions;
import com.seed.oauthPkce.core.ObjectMappers;
import com.seed.oauthPkce.core.QueryStringMapper;
import com.seed.oauthPkce.core.RequestOptions;
import com.seed.oauthPkce.core.RetryInterceptor;
import com.seed.oauthPkce.core.SeedOauthPkceApiException;
import com.seed.oauthPkce.core.SeedOauthPkceException;
import com.seed.oauthPkce.core.SeedOauthPkceHttpResponse;
import com.seed.oauthPkce.resources.oauth.requests.AuthorizeRequest;
import com.seed.oauthPkce.types.AuthorizeResponse;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawOauthClient {
    protected final ClientOptions clientOptions;

    public AsyncRawOauthClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Authorization-code grant with PKCE. <code>response_type</code> is a required literal that is
     * hardcoded by the generated method; <code>code_challenge_method</code> is an optional literal
     * that must still be sent on the wire when provided.
     */
    public CompletableFuture<SeedOauthPkceHttpResponse<AuthorizeResponse>> authorize(AuthorizeRequest request) {
        return authorize(request, null);
    }

    /**
     * Authorization-code grant with PKCE. <code>response_type</code> is a required literal that is
     * hardcoded by the generated method; <code>code_challenge_method</code> is an optional literal
     * that must still be sent on the wire when provided.
     */
    public CompletableFuture<SeedOauthPkceHttpResponse<AuthorizeResponse>> authorize(
            AuthorizeRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("oauth/authorize");
        QueryStringMapper.addQueryParameter(httpUrl, "response_type", request.getResponseType(), false);
        QueryStringMapper.addQueryParameter(httpUrl, "client_id", request.getClientId(), false);
        QueryStringMapper.addQueryParameter(httpUrl, "redirect_uri", request.getRedirectUri(), false);
        QueryStringMapper.addQueryParameter(httpUrl, "code_challenge", request.getCodeChallenge(), false);
        if (request.getCodeChallengeMethod().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "code_challenge_method",
                    request.getCodeChallengeMethod().get(),
                    false);
        }
        if (request.getScope().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "scope", request.getScope().get(), false);
        }
        if (request.getState().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "state", request.getState().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        CompletableFuture<SeedOauthPkceHttpResponse<AuthorizeResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new SeedOauthPkceHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, AuthorizeResponse.class),
                                response));
                        return;
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new SeedOauthPkceApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (JsonProcessingException e) {
                    future.completeExceptionally(
                            new SeedOauthPkceException("Failed to deserialize response: " + e.getMessage(), e));
                } catch (IOException e) {
                    future.completeExceptionally(new SeedOauthPkceException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new SeedOauthPkceException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
