/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.api.resources.user;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.seed.api.core.ClientOptions;
import com.seed.api.core.MediaTypes;
import com.seed.api.core.ObjectMappers;
import com.seed.api.core.QueryStringMapper;
import com.seed.api.core.RequestOptions;
import com.seed.api.core.SeedApiApiException;
import com.seed.api.core.SeedApiException;
import com.seed.api.core.SeedApiHttpResponse;
import com.seed.api.resources.user.requests.CreateUsernameBody;
import com.seed.api.resources.user.requests.CreateUsernameBodyOptionalProperties;
import com.seed.api.resources.user.requests.UserCreateUsernameRequest;
import com.seed.api.resources.user.requests.UserGetUsernameRequest;
import com.seed.api.types.User;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawUserClient {
    protected final ClientOptions clientOptions;

    public RawUserClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public SeedApiHttpResponse<Void> createusername(UserCreateUsernameRequest request) {
        return createusername(request, null);
    }

    public SeedApiHttpResponse<Void> createusername(UserCreateUsernameRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("user/username");
        if (request.getTags().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "tags", request.getTags().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new SeedApiHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SeedApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new SeedApiException("Network error executing HTTP request", e);
        }
    }

    public SeedApiHttpResponse<Void> createusernamewithreferencedtype(CreateUsernameBody request) {
        return createusernamewithreferencedtype(request, null);
    }

    public SeedApiHttpResponse<Void> createusernamewithreferencedtype(
            CreateUsernameBody request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("user/username-referenced");
        if (request.getTags().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "tags", request.getTags().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new SeedApiHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SeedApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new SeedApiException("Network error executing HTTP request", e);
        }
    }

    public SeedApiHttpResponse<Void> createusernameoptional() {
        return createusernameoptional(
                CreateUsernameBodyOptionalProperties.builder().build());
    }

    public SeedApiHttpResponse<Void> createusernameoptional(RequestOptions requestOptions) {
        return createusernameoptional(
                CreateUsernameBodyOptionalProperties.builder().build(), requestOptions);
    }

    public SeedApiHttpResponse<Void> createusernameoptional(CreateUsernameBodyOptionalProperties request) {
        return createusernameoptional(request, null);
    }

    public SeedApiHttpResponse<Void> createusernameoptional(
            CreateUsernameBodyOptionalProperties request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("user/username-optional");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new SeedApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new SeedApiHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SeedApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new SeedApiException("Network error executing HTTP request", e);
        }
    }

    public SeedApiHttpResponse<User> getusername(UserGetUsernameRequest request) {
        return getusername(request, null);
    }

    public SeedApiHttpResponse<User> getusername(UserGetUsernameRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("user");
        QueryStringMapper.addQueryParameter(httpUrl, "limit", request.getLimit(), false);
        QueryStringMapper.addQueryParameter(httpUrl, "id", request.getId(), false);
        QueryStringMapper.addQueryParameter(httpUrl, "date", request.getDate(), false);
        QueryStringMapper.addQueryParameter(httpUrl, "deadline", request.getDeadline(), false);
        QueryStringMapper.addQueryParameter(httpUrl, "bytes", request.getBytes(), false);
        QueryStringMapper.addQueryParameter(httpUrl, "user", request.getUser(), false);
        if (request.getOptionalDeadline().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "optionalDeadline", request.getOptionalDeadline().get(), false);
        }
        QueryStringMapper.addQueryParameter(httpUrl, "keyValue", request.getKeyValue(), false);
        if (request.getOptionalString().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "optionalString", request.getOptionalString().get(), false);
        }
        QueryStringMapper.addQueryParameter(httpUrl, "nestedUser", request.getNestedUser(), false);
        if (request.getOptionalUser().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "optionalUser", request.getOptionalUser().get(), false);
        }
        QueryStringMapper.addQueryParameter(httpUrl, "longParam", request.getLongParam(), false);
        QueryStringMapper.addQueryParameter(httpUrl, "bigIntParam", request.getBigIntParam(), false);
        if (request.getUserList().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "userList", request.getUserList().get(), true);
        }
        if (request.getExcludeUser().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "excludeUser", request.getExcludeUser().get(), true);
        }
        if (request.getFilter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "filter", request.getFilter().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new SeedApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, User.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SeedApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new SeedApiException("Network error executing HTTP request", e);
        }
    }
}
