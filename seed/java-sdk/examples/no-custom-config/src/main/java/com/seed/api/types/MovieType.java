/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class MovieType {
    public static final MovieType MOVIE = new MovieType(Value.MOVIE, "movie");

    private final Value value;

    private final String string;

    MovieType(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other) || (other instanceof MovieType && this.string.equals(((MovieType) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case MOVIE:
                return visitor.visitMovie();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static MovieType valueOf(String value) {
        switch (value) {
            case "movie":
                return MOVIE;
            default:
                return new MovieType(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        MOVIE,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitMovie();

        T visitUnknown(String unknownType);
    }
}
