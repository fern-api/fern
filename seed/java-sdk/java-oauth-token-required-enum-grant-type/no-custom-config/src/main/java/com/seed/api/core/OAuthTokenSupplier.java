/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.api.core;

import com.seed.api.resources.authorization.AuthorizationClient;
import com.seed.api.resources.authorization.requests.TokenRequest;
import com.seed.api.resources.authorization.types.TokenRequestGrantType;
import com.seed.api.types.TokenResponse;
import java.time.Instant;
import java.time.temporal.ChronoUnit;
import java.util.Optional;
import java.util.function.Supplier;

public final class OAuthTokenSupplier implements Supplier<String> {
    private static final long BUFFER_IN_MINUTES = 2;

    private final String clientId;

    private final String clientSecret;

    private final Optional<String> scope;

    private final AuthorizationClient authClient;

    private final Object tokenLock = new Object();

    private volatile String accessToken;

    private volatile Instant expiresAt;

    public OAuthTokenSupplier(
            String clientId, String clientSecret, Optional<String> scope, AuthorizationClient authClient) {
        this.clientId = clientId;
        this.clientSecret = clientSecret;
        this.scope = scope;
        this.authClient = authClient;
        this.expiresAt = Instant.now();
    }

    public TokenResponse fetchToken() {
        TokenRequest getTokenRequest = TokenRequest.builder()
                .grantType(TokenRequestGrantType.CLIENT_CREDENTIALS)
                .clientId(clientId)
                .clientSecret(clientSecret)
                .scope(scope)
                .build();
        return authClient.createToken(getTokenRequest);
    }

    @java.lang.Override
    public String get() {
        String cachedToken = this.accessToken;
        Instant cachedExpiresAt = this.expiresAt;
        if (cachedToken != null && cachedExpiresAt != null && !cachedExpiresAt.isBefore(Instant.now())) {
            return "Bearer " + cachedToken;
        }
        synchronized (tokenLock) {
            if (accessToken == null || expiresAt.isBefore(Instant.now())) {
                TokenResponse authResponse = fetchToken();
                this.accessToken = authResponse.getAccessToken();
                this.expiresAt = getExpiresAt(authResponse.getExpiresIn());
            }
            return "Bearer " + accessToken;
        }
    }

    private Instant getExpiresAt(long expiresInSeconds) {
        return Instant.now().plus(expiresInSeconds, ChronoUnit.SECONDS).minus(BUFFER_IN_MINUTES, ChronoUnit.MINUTES);
    }
}
