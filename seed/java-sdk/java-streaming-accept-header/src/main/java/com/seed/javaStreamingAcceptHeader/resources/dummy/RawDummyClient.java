/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.javaStreamingAcceptHeader.resources.dummy;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.seed.javaStreamingAcceptHeader.core.ClientOptions;
import com.seed.javaStreamingAcceptHeader.core.MediaTypes;
import com.seed.javaStreamingAcceptHeader.core.ObjectMappers;
import com.seed.javaStreamingAcceptHeader.core.RequestOptions;
import com.seed.javaStreamingAcceptHeader.core.ResponseBodyReader;
import com.seed.javaStreamingAcceptHeader.core.SeedJavaStreamingAcceptHeaderApiException;
import com.seed.javaStreamingAcceptHeader.core.SeedJavaStreamingAcceptHeaderException;
import com.seed.javaStreamingAcceptHeader.core.SeedJavaStreamingAcceptHeaderHttpResponse;
import com.seed.javaStreamingAcceptHeader.core.Stream;
import com.seed.javaStreamingAcceptHeader.resources.commons.errors.BadRequestError;
import com.seed.javaStreamingAcceptHeader.resources.commons.types.ErrorBody;
import com.seed.javaStreamingAcceptHeader.resources.dummy.requests.GenerateRequest;
import com.seed.javaStreamingAcceptHeader.resources.dummy.requests.GenerateStreamRequest;
import com.seed.javaStreamingAcceptHeader.resources.dummy.types.StreamResponse;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawDummyClient {
    protected final ClientOptions clientOptions;

    public RawDummyClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public SeedJavaStreamingAcceptHeaderHttpResponse<Iterable<StreamResponse>> generateStream(
            GenerateStreamRequest request) {
        return generateStream(request, null);
    }

    public SeedJavaStreamingAcceptHeaderHttpResponse<Iterable<StreamResponse>> generateStream(
            GenerateStreamRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("generate-stream");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((key, value) -> {
                httpUrl.addQueryParameter(key, value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new SeedJavaStreamingAcceptHeaderException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try {
            Response response = client.newCall(okhttpRequest).execute();
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new SeedJavaStreamingAcceptHeaderHttpResponse<>(
                        Stream.fromJson(StreamResponse.class, new ResponseBodyReader(response), "\\n"), response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            try {
                if (response.code() == 400) {
                    throw new BadRequestError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorBody.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SeedJavaStreamingAcceptHeaderApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new SeedJavaStreamingAcceptHeaderException("Network error executing HTTP request", e);
        }
    }

    public SeedJavaStreamingAcceptHeaderHttpResponse<StreamResponse> generate(GenerateRequest request) {
        return generate(request, null);
    }

    public SeedJavaStreamingAcceptHeaderHttpResponse<StreamResponse> generate(
            GenerateRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("generate");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((key, value) -> {
                httpUrl.addQueryParameter(key, value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new SeedJavaStreamingAcceptHeaderException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new SeedJavaStreamingAcceptHeaderHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, StreamResponse.class), response);
            }
            try {
                if (response.code() == 400) {
                    throw new BadRequestError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorBody.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SeedJavaStreamingAcceptHeaderApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new SeedJavaStreamingAcceptHeaderException("Network error executing HTTP request", e);
        }
    }
}
