/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.nurseryApi.resources.package_.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class SipHeaderAction {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private SipHeaderAction(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static SipHeaderAction static_(CustomSipHeader value) {
        return new SipHeaderAction(new StaticValue(value));
    }

    public static SipHeaderAction dynamic(CustomSipHeader value) {
        return new SipHeaderAction(new DynamicValue(value));
    }

    public boolean isStatic() {
        return value instanceof StaticValue;
    }

    public boolean isDynamic() {
        return value instanceof DynamicValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<CustomSipHeader> getStatic() {
        if (isStatic()) {
            return Optional.of(((StaticValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<CustomSipHeader> getDynamic() {
        if (isDynamic()) {
            return Optional.of(((DynamicValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof SipHeaderAction && value.equals(((SipHeaderAction) other).value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public String toString() {
        return value.toString();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitStatic(CustomSipHeader static_);

        T visitDynamic(CustomSipHeader dynamic);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(StaticValue.class), @JsonSubTypes.Type(DynamicValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("static")
    @JsonIgnoreProperties("type")
    private static final class StaticValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private CustomSipHeader value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private StaticValue() {}

        private StaticValue(CustomSipHeader value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitStatic(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof StaticValue && equalTo((StaticValue) other);
        }

        private boolean equalTo(StaticValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "SipHeaderAction{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("dynamic")
    @JsonIgnoreProperties("type")
    private static final class DynamicValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private CustomSipHeader value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private DynamicValue() {}

        private DynamicValue(CustomSipHeader value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitDynamic(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof DynamicValue && equalTo((DynamicValue) other);
        }

        private boolean equalTo(DynamicValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "SipHeaderAction{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "SipHeaderAction{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
