/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.nurseryApi.resources.package_.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class BackupConfig {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private BackupConfig(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static BackupConfig override(BackupOverride value) {
        return new BackupConfig(new OverrideValue(value));
    }

    public static BackupConfig fallback(BackupOverride value) {
        return new BackupConfig(new FallbackValue(value));
    }

    public boolean isOverride() {
        return value instanceof OverrideValue;
    }

    public boolean isFallback() {
        return value instanceof FallbackValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<BackupOverride> getOverride() {
        if (isOverride()) {
            return Optional.of(((OverrideValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<BackupOverride> getFallback() {
        if (isFallback()) {
            return Optional.of(((FallbackValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof BackupConfig && value.equals(((BackupConfig) other).value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public String toString() {
        return value.toString();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitOverride(BackupOverride override);

        T visitFallback(BackupOverride fallback);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(OverrideValue.class), @JsonSubTypes.Type(FallbackValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("override")
    @JsonIgnoreProperties("type")
    private static final class OverrideValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private BackupOverride value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private OverrideValue() {}

        private OverrideValue(BackupOverride value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitOverride(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof OverrideValue && equalTo((OverrideValue) other);
        }

        private boolean equalTo(OverrideValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "BackupConfig{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("fallback")
    @JsonIgnoreProperties("type")
    private static final class FallbackValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private BackupOverride value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private FallbackValue() {}

        private FallbackValue(BackupOverride value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitFallback(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof FallbackValue && equalTo((FallbackValue) other);
        }

        private boolean equalTo(FallbackValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "BackupConfig{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "BackupConfig{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
