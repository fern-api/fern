diff --git a/.fern/metadata.json b/.fern/metadata.json
index 1bdd1da9041..493b9a6745b 100644
--- a/.fern/metadata.json
+++ b/.fern/metadata.json
@@ -2,6 +2,9 @@
   "cliVersion": "DUMMY",
   "generatorName": "fernapi/fern-java-sdk",
   "generatorVersion": "local",
+  "generatorConfig": {
+    "wrapped-aliases": true
+  },
   "originGitCommit": "DUMMY",
   "originGitCommitIsDirty": null,
   "invokedBy": "ci",
diff --git a/src/main/java/com/seed/nullable/core/NullableNonemptyFilter.java b/src/main/java/com/seed/nullable/core/NullableNonemptyFilter.java
index b71460e9793..9bb73de9a65 100644
--- a/src/main/java/com/seed/nullable/core/NullableNonemptyFilter.java
+++ b/src/main/java/com/seed/nullable/core/NullableNonemptyFilter.java
@@ -9,8 +9,9 @@ public final class NullableNonemptyFilter {
     @Override
     public boolean equals(Object o) {
         boolean isOptionalEmpty = isOptionalEmpty(o);
+        boolean isAliasOfOptionalEmpty = o instanceof WrappedAlias && isAliasOfOptionalEmpty((WrappedAlias) o);
 
-        return isOptionalEmpty;
+        return isOptionalEmpty || isAliasOfOptionalEmpty;
     }
 
     private boolean isOptionalEmpty(Object o) {
@@ -19,4 +20,11 @@ public final class NullableNonemptyFilter {
         }
         return false;
     }
+
+    private boolean isAliasOfOptionalEmpty(WrappedAlias o) {
+        if (o.get() instanceof WrappedAlias) {
+            return isAliasOfOptionalEmpty((WrappedAlias) o.get());
+        }
+        return isOptionalEmpty(o.get());
+    }
 }
diff --git a/src/main/java/com/seed/nullable/core/WrappedAlias.java b/src/main/java/com/seed/nullable/core/WrappedAlias.java
new file mode 100644
index 00000000000..fa4ad16788b
--- /dev/null
+++ b/src/main/java/com/seed/nullable/core/WrappedAlias.java
@@ -0,0 +1,8 @@
+/**
+ * This file was auto-generated by Fern from our API Definition.
+ */
+package com.seed.nullable.core;
+
+public interface WrappedAlias {
+    Object get();
+}
diff --git a/src/main/java/com/seed/nullable/resources/nullable/types/Email.java b/src/main/java/com/seed/nullable/resources/nullable/types/Email.java
new file mode 100644
index 00000000000..e4f2fe7b0d1
--- /dev/null
+++ b/src/main/java/com/seed/nullable/resources/nullable/types/Email.java
@@ -0,0 +1,42 @@
+/**
+ * This file was auto-generated by Fern from our API Definition.
+ */
+package com.seed.nullable.resources.nullable.types;
+
+import com.fasterxml.jackson.annotation.JsonCreator;
+import com.fasterxml.jackson.annotation.JsonValue;
+import com.seed.nullable.core.WrappedAlias;
+import java.util.Optional;
+
+public final class Email implements WrappedAlias {
+    private final Optional<String> value;
+
+    private Email(Optional<String> value) {
+        this.value = value;
+    }
+
+    @JsonValue
+    public Optional<String> get() {
+        return this.value;
+    }
+
+    @java.lang.Override
+    public boolean equals(Object other) {
+        return this == other || (other instanceof Email && this.value.equals(((Email) other).value));
+    }
+
+    @java.lang.Override
+    public int hashCode() {
+        return value.hashCode();
+    }
+
+    @java.lang.Override
+    public String toString() {
+        return value.toString();
+    }
+
+    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
+    public static Email of(Optional<String> value) {
+        return new Email(value);
+    }
+}
diff --git a/src/main/java/com/seed/nullable/resources/nullable/types/User.java b/src/main/java/com/seed/nullable/resources/nullable/types/User.java
index bb1fb2e29c9..7469cb6ac26 100644
--- a/src/main/java/com/seed/nullable/resources/nullable/types/User.java
+++ b/src/main/java/com/seed/nullable/resources/nullable/types/User.java
@@ -27,13 +27,13 @@ import org.jetbrains.annotations.NotNull;
 public final class User {
     private final String name;
 
-    private final String id;
+    private final UserId id;
 
     private final Optional<List<String>> tags;
 
     private final Optional<Metadata> metadata;
 
-    private final Optional<String> email;
+    private final Email email;
 
     private final WeirdNumber favoriteNumber;
 
@@ -45,10 +45,10 @@ public final class User {
 
     private User(
             String name,
-            String id,
+            UserId id,
             Optional<List<String>> tags,
             Optional<Metadata> metadata,
-            Optional<String> email,
+            Email email,
             WeirdNumber favoriteNumber,
             Optional<List<Integer>> numbers,
             Optional<Map<String, Object>> strings,
@@ -70,7 +70,7 @@ public final class User {
     }
 
     @JsonProperty("id")
-    public String getId() {
+    public UserId getId() {
         return id;
     }
 
@@ -91,7 +91,10 @@ public final class User {
     }
 
     @JsonIgnore
-    public Optional<String> getEmail() {
+    public Email getEmail() {
+        if (email == null) {
+            return Email.of(Optional.empty());
+        }
         return email;
     }
 
@@ -130,7 +133,7 @@ public final class User {
 
     @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
     @JsonProperty("email")
-    private Optional<String> _getEmail() {
+    private Email _getEmail() {
         return email;
     }
 
@@ -197,7 +200,7 @@ public final class User {
     }
 
     public interface IdStage {
-        FavoriteNumberStage id(@NotNull String id);
+        FavoriteNumberStage id(@NotNull UserId id);
     }
 
     public interface FavoriteNumberStage {
@@ -223,9 +226,7 @@ public final class User {
 
         _FinalStage metadata(Nullable<Metadata> metadata);
 
-        _FinalStage email(Optional<String> email);
-
-        _FinalStage email(String email);
+        _FinalStage email(Nullable<Email> email);
 
         _FinalStage numbers(Optional<List<Integer>> numbers);
 
@@ -244,7 +245,7 @@ public final class User {
     public static final class Builder implements NameStage, IdStage, FavoriteNumberStage, _FinalStage {
         private String name;
 
-        private String id;
+        private UserId id;
 
         private WeirdNumber favoriteNumber;
 
@@ -252,8 +253,6 @@ public final class User {
 
         private Optional<List<Integer>> numbers = Optional.empty();
 
-        private Optional<String> email = Optional.empty();
-
         private Optional<Metadata> metadata = Optional.empty();
 
         private Optional<List<String>> tags = Optional.empty();
@@ -285,7 +284,7 @@ public final class User {
 
         @java.lang.Override
         @JsonSetter("id")
-        public FavoriteNumberStage id(@NotNull String id) {
+        public FavoriteNumberStage id(@NotNull UserId id) {
             this.id = Objects.requireNonNull(id, "id must not be null");
             return this;
         }
@@ -347,16 +346,14 @@ public final class User {
             return this;
         }
 
-        @java.lang.Override
-        public _FinalStage email(String email) {
-            this.email = Optional.ofNullable(email);
-            return this;
-        }
-
-        @java.lang.Override
-        @JsonSetter(value = "email", nulls = Nulls.SKIP)
-        public _FinalStage email(Optional<String> email) {
-            this.email = email;
+        public _FinalStage email(Nullable<Email> email) {
+            if (email.isNull()) {
+                this.email = null;
+            } else if (email.isEmpty()) {
+                this.email = Email.of(Optional.empty());
+            } else {
+                this.email = email.get();
+            }
             return this;
         }
 
diff --git a/src/main/java/com/seed/nullable/resources/nullable/types/UserId.java b/src/main/java/com/seed/nullable/resources/nullable/types/UserId.java
new file mode 100644
index 00000000000..85ac26bcf03
--- /dev/null
+++ b/src/main/java/com/seed/nullable/resources/nullable/types/UserId.java
@@ -0,0 +1,45 @@
+/**
+ * This file was auto-generated by Fern from our API Definition.
+ */
+package com.seed.nullable.resources.nullable.types;
+
+import com.fasterxml.jackson.annotation.JsonCreator;
+import com.fasterxml.jackson.annotation.JsonValue;
+import com.seed.nullable.core.WrappedAlias;
+
+public final class UserId implements WrappedAlias {
+    private final String value;
+
+    private UserId(String value) {
+        this.value = value;
+    }
+
+    @JsonValue
+    public String get() {
+        return this.value;
+    }
+
+    @java.lang.Override
+    public boolean equals(Object other) {
+        return this == other || (other instanceof UserId && this.value.equals(((UserId) other).value));
+    }
+
+    @java.lang.Override
+    public int hashCode() {
+        return value.hashCode();
+    }
+
+    @java.lang.Override
+    public String toString() {
+        return value;
+    }
+
+    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
+    public static UserId of(String value) {
+        return new UserId(value);
+    }
+
+    public static UserId valueOf(String value) {
+        return of(value);
+    }
+}
