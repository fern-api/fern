/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class StreamXFernStreamingUnionStreamRequest {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private StreamXFernStreamingUnionStreamRequest(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static StreamXFernStreamingUnionStreamRequest message(UnionStreamMessageVariant value) {
        return new StreamXFernStreamingUnionStreamRequest(new MessageValue(value));
    }

    public static StreamXFernStreamingUnionStreamRequest interrupt(UnionStreamInterruptVariant value) {
        return new StreamXFernStreamingUnionStreamRequest(new InterruptValue(value));
    }

    public static StreamXFernStreamingUnionStreamRequest compact(UnionStreamCompactVariant value) {
        return new StreamXFernStreamingUnionStreamRequest(new CompactValue(value));
    }

    public boolean isMessage() {
        return value instanceof MessageValue;
    }

    public boolean isInterrupt() {
        return value instanceof InterruptValue;
    }

    public boolean isCompact() {
        return value instanceof CompactValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<UnionStreamMessageVariant> getMessage() {
        if (isMessage()) {
            return Optional.of(((MessageValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<UnionStreamInterruptVariant> getInterrupt() {
        if (isInterrupt()) {
            return Optional.of(((InterruptValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<UnionStreamCompactVariant> getCompact() {
        if (isCompact()) {
            return Optional.of(((CompactValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof StreamXFernStreamingUnionStreamRequest
                && value.equals(((StreamXFernStreamingUnionStreamRequest) other).value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public String toString() {
        return value.toString();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitMessage(UnionStreamMessageVariant message);

        T visitInterrupt(UnionStreamInterruptVariant interrupt);

        T visitCompact(UnionStreamCompactVariant compact);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(MessageValue.class),
        @JsonSubTypes.Type(InterruptValue.class),
        @JsonSubTypes.Type(CompactValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("message")
    @JsonIgnoreProperties("type")
    private static final class MessageValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private UnionStreamMessageVariant value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private MessageValue() {}

        private MessageValue(UnionStreamMessageVariant value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitMessage(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof MessageValue && equalTo((MessageValue) other);
        }

        private boolean equalTo(MessageValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StreamXFernStreamingUnionStreamRequest{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("interrupt")
    @JsonIgnoreProperties("type")
    private static final class InterruptValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private UnionStreamInterruptVariant value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private InterruptValue() {}

        private InterruptValue(UnionStreamInterruptVariant value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitInterrupt(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof InterruptValue && equalTo((InterruptValue) other);
        }

        private boolean equalTo(InterruptValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StreamXFernStreamingUnionStreamRequest{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("compact")
    @JsonIgnoreProperties("type")
    private static final class CompactValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private UnionStreamCompactVariant value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private CompactValue() {}

        private CompactValue(UnionStreamCompactVariant value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitCompact(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof CompactValue && equalTo((CompactValue) other);
        }

        private boolean equalTo(CompactValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StreamXFernStreamingUnionStreamRequest{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StreamXFernStreamingUnionStreamRequest{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
