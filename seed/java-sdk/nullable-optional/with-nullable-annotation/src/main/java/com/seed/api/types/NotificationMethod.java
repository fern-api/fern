/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.api.types;

import com.fasterxml.jackson.annotation.JsonValue;
import com.fasterxml.jackson.core.JsonParseException;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.seed.api.core.ObjectMappers;
import java.io.IOException;
import java.util.Objects;

@JsonDeserialize(using = NotificationMethod.Deserializer.class)
public final class NotificationMethod {
    private final Object value;

    private final int type;

    private NotificationMethod(Object value, int type) {
        this.value = value;
        this.type = type;
    }

    @JsonValue
    public Object get() {
        return this.value;
    }

    @SuppressWarnings("unchecked")
    public <T> T visit(Visitor<T> visitor) {
        if (this.type == 0) {
            return visitor.visit((NotificationMethodZero) this.value);
        } else if (this.type == 1) {
            return visitor.visit((NotificationMethodOne) this.value);
        } else if (this.type == 2) {
            return visitor.visit((NotificationMethodTwo) this.value);
        }
        throw new IllegalStateException("Failed to visit value. This should never happen.");
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof NotificationMethod && equalTo((NotificationMethod) other);
    }

    private boolean equalTo(NotificationMethod other) {
        return value.equals(other.value);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.value);
    }

    @java.lang.Override
    public String toString() {
        return this.value.toString();
    }

    public static NotificationMethod of(NotificationMethodZero value) {
        return new NotificationMethod(value, 0);
    }

    public static NotificationMethod of(NotificationMethodOne value) {
        return new NotificationMethod(value, 1);
    }

    public static NotificationMethod of(NotificationMethodTwo value) {
        return new NotificationMethod(value, 2);
    }

    public interface Visitor<T> {
        T visit(NotificationMethodZero value);

        T visit(NotificationMethodOne value);

        T visit(NotificationMethodTwo value);
    }

    static final class Deserializer extends StdDeserializer<NotificationMethod> {
        Deserializer() {
            super(NotificationMethod.class);
        }

        @java.lang.Override
        public NotificationMethod deserialize(JsonParser p, DeserializationContext context) throws IOException {
            Object value = p.readValueAs(Object.class);
            try {
                return of(ObjectMappers.JSON_MAPPER.convertValue(value, NotificationMethodZero.class));
            } catch (RuntimeException e) {
            }
            try {
                return of(ObjectMappers.JSON_MAPPER.convertValue(value, NotificationMethodOne.class));
            } catch (RuntimeException e) {
            }
            try {
                return of(ObjectMappers.JSON_MAPPER.convertValue(value, NotificationMethodTwo.class));
            } catch (RuntimeException e) {
            }
            throw new JsonParseException(p, "Failed to deserialize");
        }
    }
}
