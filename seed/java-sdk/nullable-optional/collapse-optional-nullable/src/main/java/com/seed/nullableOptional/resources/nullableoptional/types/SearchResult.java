/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.nullableOptional.resources.nullableoptional.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class SearchResult {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private SearchResult(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static SearchResult user(UserResponse value) {
        return new SearchResult(new UserValue(value));
    }

    public static SearchResult organization(Organization value) {
        return new SearchResult(new OrganizationValue(value));
    }

    public static SearchResult document(Document value) {
        return new SearchResult(new DocumentValue(value));
    }

    public boolean isUser() {
        return value instanceof UserValue;
    }

    public boolean isOrganization() {
        return value instanceof OrganizationValue;
    }

    public boolean isDocument() {
        return value instanceof DocumentValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<UserResponse> getUser() {
        if (isUser()) {
            return Optional.of(((UserValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Organization> getOrganization() {
        if (isOrganization()) {
            return Optional.of(((OrganizationValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Document> getDocument() {
        if (isDocument()) {
            return Optional.of(((DocumentValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitUser(UserResponse user);

        T visitOrganization(Organization organization);

        T visitDocument(Document document);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(UserValue.class),
        @JsonSubTypes.Type(OrganizationValue.class),
        @JsonSubTypes.Type(DocumentValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("user")
    @JsonIgnoreProperties("type")
    private static final class UserValue implements Value {
        @JsonUnwrapped
        private UserResponse value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private UserValue() {}

        private UserValue(UserResponse value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitUser(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof UserValue && equalTo((UserValue) other);
        }

        private boolean equalTo(UserValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "SearchResult{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("organization")
    @JsonIgnoreProperties("type")
    private static final class OrganizationValue implements Value {
        @JsonUnwrapped
        private Organization value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private OrganizationValue() {}

        private OrganizationValue(Organization value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitOrganization(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof OrganizationValue && equalTo((OrganizationValue) other);
        }

        private boolean equalTo(OrganizationValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "SearchResult{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("document")
    @JsonIgnoreProperties("type")
    private static final class DocumentValue implements Value {
        @JsonUnwrapped
        private Document value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private DocumentValue() {}

        private DocumentValue(Document value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitDocument(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof DocumentValue && equalTo((DocumentValue) other);
        }

        private boolean equalTo(DocumentValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "SearchResult{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "SearchResult{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
