/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.websocketAuth.resources.auth;

import com.seed.websocketAuth.core.ClientOptions;
import com.seed.websocketAuth.core.MediaTypes;
import com.seed.websocketAuth.core.ObjectMappers;
import com.seed.websocketAuth.core.RequestOptions;
import com.seed.websocketAuth.core.SeedWebsocketAuthApiException;
import com.seed.websocketAuth.core.SeedWebsocketAuthException;
import com.seed.websocketAuth.core.SeedWebsocketAuthHttpResponse;
import com.seed.websocketAuth.resources.auth.requests.GetTokenRequest;
import com.seed.websocketAuth.resources.auth.requests.RefreshTokenRequest;
import com.seed.websocketAuth.resources.auth.types.TokenResponse;
import java.io.IOException;
import java.util.HashMap;
import java.util.Map;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawAuthClient {
    protected final ClientOptions clientOptions;

    public RawAuthClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public SeedWebsocketAuthHttpResponse<TokenResponse> getTokenWithClientCredentials(GetTokenRequest request) {
        return getTokenWithClientCredentials(request, null);
    }

    public SeedWebsocketAuthHttpResponse<TokenResponse> getTokenWithClientCredentials(
            GetTokenRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("token")
                .build();
        Map<String, Object> properties = new HashMap<>();
        properties.put("client_id", request.getClientId());
        properties.put("client_secret", request.getClientSecret());
        properties.put("audience", request.getAudience());
        properties.put("grant_type", request.getGrantType());
        if (request.getScope().isPresent()) {
            properties.put("scope", request.getScope());
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(properties), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("X-Api-Key", request.getXApiKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new SeedWebsocketAuthHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBody.string(), TokenResponse.class), response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            throw new SeedWebsocketAuthApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new SeedWebsocketAuthException("Network error executing HTTP request", e);
        }
    }

    public SeedWebsocketAuthHttpResponse<TokenResponse> refreshToken(RefreshTokenRequest request) {
        return refreshToken(request, null);
    }

    public SeedWebsocketAuthHttpResponse<TokenResponse> refreshToken(
            RefreshTokenRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("token/refresh")
                .build();
        Map<String, Object> properties = new HashMap<>();
        properties.put("client_id", request.getClientId());
        properties.put("client_secret", request.getClientSecret());
        properties.put("refresh_token", request.getRefreshToken());
        properties.put("audience", request.getAudience());
        properties.put("grant_type", request.getGrantType());
        if (request.getScope().isPresent()) {
            properties.put("scope", request.getScope());
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(properties), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("X-Api-Key", request.getXApiKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new SeedWebsocketAuthHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBody.string(), TokenResponse.class), response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            throw new SeedWebsocketAuthApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new SeedWebsocketAuthException("Network error executing HTTP request", e);
        }
    }
}
