/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed._enum.resources.headers;

import com.seed._enum.core.ClientOptions;
import com.seed._enum.core.ObjectMappers;
import com.seed._enum.core.RequestOptions;
import com.seed._enum.core.SeedEnumApiException;
import com.seed._enum.core.SeedEnumException;
import com.seed._enum.core.SeedEnumHttpResponse;
import com.seed._enum.resources.headers.requests.SendEnumAsHeaderRequest;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawHeadersClient {
    protected final ClientOptions clientOptions;

    public RawHeadersClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public SeedEnumHttpResponse<Void> send(SendEnumAsHeaderRequest request) {
        return send(request, null);
    }

    public SeedEnumHttpResponse<Void> send(SendEnumAsHeaderRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("headers")
                .build();
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)));
        _requestBuilder.addHeader("operand", request.getOperand().toString());
        if (request.getMaybeOperand().isPresent()) {
            _requestBuilder.addHeader(
                    "maybeOperand", request.getMaybeOperand().get().toString());
        }
        _requestBuilder.addHeader("operandOrColor", request.getOperandOrColor().toString());
        if (request.getMaybeOperandOrColor().isPresent()) {
            _requestBuilder.addHeader(
                    "maybeOperandOrColor",
                    request.getMaybeOperandOrColor().get().toString());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new SeedEnumHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SeedEnumApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new SeedEnumException("Network error executing HTTP request", e);
        }
    }
}
