/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.javaDefaultTimeout;

import com.seed.javaDefaultTimeout.core.ClientOptions;
import com.seed.javaDefaultTimeout.core.ObjectMappers;
import com.seed.javaDefaultTimeout.core.RequestOptions;
import com.seed.javaDefaultTimeout.core.SeedJavaDefaultTimeoutApiException;
import com.seed.javaDefaultTimeout.core.SeedJavaDefaultTimeoutException;
import com.seed.javaDefaultTimeout.core.SeedJavaDefaultTimeoutHttpResponse;
import com.seed.javaDefaultTimeout.types.User;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawSeedJavaDefaultTimeoutClient {
    protected final ClientOptions clientOptions;

    public AsyncRawSeedJavaDefaultTimeoutClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public CompletableFuture<SeedJavaDefaultTimeoutHttpResponse<User>> getUser() {
        return getUser(null);
    }

    public CompletableFuture<SeedJavaDefaultTimeoutHttpResponse<User>> getUser(RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("user")
                .build();
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<SeedJavaDefaultTimeoutHttpResponse<User>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new SeedJavaDefaultTimeoutHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, User.class), response));
                        return;
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new SeedJavaDefaultTimeoutApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(
                            new SeedJavaDefaultTimeoutException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(
                        new SeedJavaDefaultTimeoutException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
