/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.serverSentEvents.resources.completions.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class StreamEventContextProtocol {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private StreamEventContextProtocol(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static StreamEventContextProtocol completion(CompletionEvent value) {
        return new StreamEventContextProtocol(new CompletionValue(value));
    }

    public static StreamEventContextProtocol error(ErrorEvent value) {
        return new StreamEventContextProtocol(new ErrorValue(value));
    }

    public static StreamEventContextProtocol event(EventEvent value) {
        return new StreamEventContextProtocol(new EventValue(value));
    }

    public boolean isCompletion() {
        return value instanceof CompletionValue;
    }

    public boolean isError() {
        return value instanceof ErrorValue;
    }

    public boolean isEvent() {
        return value instanceof EventValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<CompletionEvent> getCompletion() {
        if (isCompletion()) {
            return Optional.of(((CompletionValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<ErrorEvent> getError() {
        if (isError()) {
            return Optional.of(((ErrorValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<EventEvent> getEvent() {
        if (isEvent()) {
            return Optional.of(((EventValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof StreamEventContextProtocol && value.equals(((StreamEventContextProtocol) other).value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public String toString() {
        return value.toString();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitCompletion(CompletionEvent completion);

        T visitError(ErrorEvent error);

        T visitEvent(EventEvent event);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "event", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(CompletionValue.class),
        @JsonSubTypes.Type(ErrorValue.class),
        @JsonSubTypes.Type(EventValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("completion")
    @JsonIgnoreProperties("event")
    private static final class CompletionValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "event", allowSetters = true)
        private CompletionEvent value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private CompletionValue() {}

        private CompletionValue(CompletionEvent value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitCompletion(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof CompletionValue && equalTo((CompletionValue) other);
        }

        private boolean equalTo(CompletionValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StreamEventContextProtocol{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("error")
    @JsonIgnoreProperties("event")
    private static final class ErrorValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "event", allowSetters = true)
        private ErrorEvent value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ErrorValue() {}

        private ErrorValue(ErrorEvent value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitError(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ErrorValue && equalTo((ErrorValue) other);
        }

        private boolean equalTo(ErrorValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StreamEventContextProtocol{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("event")
    @JsonIgnoreProperties("event")
    private static final class EventValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "event", allowSetters = true)
        private EventEvent value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private EventValue() {}

        private EventValue(EventEvent value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitEvent(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof EventValue && equalTo((EventValue) other);
        }

        private boolean equalTo(EventValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StreamEventContextProtocol{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("event")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StreamEventContextProtocol{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
