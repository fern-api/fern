/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.noRetries.resources.retries;

import com.fasterxml.jackson.core.type.TypeReference;
import com.seed.noRetries.core.ClientOptions;
import com.seed.noRetries.core.ObjectMappers;
import com.seed.noRetries.core.RequestOptions;
import com.seed.noRetries.core.SeedNoRetriesApiException;
import com.seed.noRetries.core.SeedNoRetriesException;
import com.seed.noRetries.core.SeedNoRetriesHttpResponse;
import com.seed.noRetries.resources.retries.types.User;
import java.io.IOException;
import java.util.List;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawRetriesClient {
    protected final ClientOptions clientOptions;

    public RawRetriesClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public SeedNoRetriesHttpResponse<List<User>> getUsers() {
        return getUsers(null);
    }

    public SeedNoRetriesHttpResponse<List<User>> getUsers(RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("users")
                .build();
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new SeedNoRetriesHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, new TypeReference<List<User>>() {}),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SeedNoRetriesApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new SeedNoRetriesException("Network error executing HTTP request", e);
        }
    }
}
