/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.unionQueryParameters.resources.events;

import com.seed.unionQueryParameters.core.ClientOptions;
import com.seed.unionQueryParameters.core.ObjectMappers;
import com.seed.unionQueryParameters.core.QueryStringMapper;
import com.seed.unionQueryParameters.core.RequestOptions;
import com.seed.unionQueryParameters.core.SeedUnionQueryParametersApiException;
import com.seed.unionQueryParameters.core.SeedUnionQueryParametersException;
import com.seed.unionQueryParameters.core.SeedUnionQueryParametersHttpResponse;
import com.seed.unionQueryParameters.resources.events.requests.SubscribeEventsRequest;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawEventsClient {
    protected final ClientOptions clientOptions;

    public RawEventsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Subscribe to events with a oneOf-style query parameter that may be a
     * scalar enum value or a list of enum values.
     */
    public SeedUnionQueryParametersHttpResponse<String> subscribe() {
        return subscribe(SubscribeEventsRequest.builder().build());
    }

    /**
     * Subscribe to events with a oneOf-style query parameter that may be a
     * scalar enum value or a list of enum values.
     */
    public SeedUnionQueryParametersHttpResponse<String> subscribe(RequestOptions requestOptions) {
        return subscribe(SubscribeEventsRequest.builder().build(), requestOptions);
    }

    /**
     * Subscribe to events with a oneOf-style query parameter that may be a
     * scalar enum value or a list of enum values.
     */
    public SeedUnionQueryParametersHttpResponse<String> subscribe(SubscribeEventsRequest request) {
        return subscribe(request, null);
    }

    /**
     * Subscribe to events with a oneOf-style query parameter that may be a
     * scalar enum value or a list of enum values.
     */
    public SeedUnionQueryParametersHttpResponse<String> subscribe(
            SubscribeEventsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("events");
        if (request.getEventType().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "event_type", request.getEventType().get(), false);
        }
        if (request.getTags().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "tags", request.getTags().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new SeedUnionQueryParametersHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, String.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SeedUnionQueryParametersApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new SeedUnionQueryParametersException("Network error executing HTTP request", e);
        }
    }
}
