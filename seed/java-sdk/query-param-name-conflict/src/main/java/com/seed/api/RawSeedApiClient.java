/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.api;

import com.seed.api.core.ClientOptions;
import com.seed.api.core.MediaTypes;
import com.seed.api.core.ObjectMappers;
import com.seed.api.core.QueryStringMapper;
import com.seed.api.core.RequestOptions;
import com.seed.api.core.SeedApiApiException;
import com.seed.api.core.SeedApiException;
import com.seed.api.core.SeedApiHttpResponse;
import com.seed.api.requests.BulkUpdateTasksRequest;
import com.seed.api.types.BulkUpdateTasksResponse;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawSeedApiClient {
    protected final ClientOptions clientOptions;

    public RawSeedApiClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public SeedApiHttpResponse<BulkUpdateTasksResponse> bulkUpdateTasks() {
        return bulkUpdateTasks(BulkUpdateTasksRequest.builder().build());
    }

    public SeedApiHttpResponse<BulkUpdateTasksResponse> bulkUpdateTasks(RequestOptions requestOptions) {
        return bulkUpdateTasks(BulkUpdateTasksRequest.builder().build(), requestOptions);
    }

    public SeedApiHttpResponse<BulkUpdateTasksResponse> bulkUpdateTasks(BulkUpdateTasksRequest request) {
        return bulkUpdateTasks(request, null);
    }

    public SeedApiHttpResponse<BulkUpdateTasksResponse> bulkUpdateTasks(
            BulkUpdateTasksRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("task");
        if (request.getFilterAssignedTo().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "assigned_to", request.getFilterAssignedTo().get(), false);
        }
        if (request.getFilterIsComplete().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "is_complete", request.getFilterIsComplete().get(), false);
        }
        if (request.getFilterDate().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "date", request.getFilterDate().get(), false);
        }
        if (request.getFields().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "_fields", request.getFields().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new SeedApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, BulkUpdateTasksResponse.class),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SeedApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new SeedApiException("Network error executing HTTP request", e);
        }
    }
}
