/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.webhooks.core;

import java.net.URI;
import java.net.URISyntaxException;
import java.net.URLDecoder;
import java.nio.charset.StandardCharsets;
import java.security.GeneralSecurityException;
import java.security.MessageDigest;
import java.util.Base64;

public final class WebhookBodyHash {

    private static final char[] HEX_DIGITS = "0123456789abcdef".toCharArray();

    private WebhookBodyHash() {}

    public static String computeHash(String payload, String algorithm, String encoding) {
        try {
            MessageDigest digest = MessageDigest.getInstance(algorithm);
            byte[] hash = digest.digest(payload.getBytes(StandardCharsets.UTF_8));
            if ("hex".equals(encoding)) {
                return toHex(hash);
            }
            return Base64.getEncoder().encodeToString(hash);
        } catch (GeneralSecurityException exception) {
            throw new IllegalStateException("Failed to compute webhook body hash", exception);
        }
    }

    public static String getQueryParameter(String url, String name) {
        if (url == null || name == null) {
            return null;
        }
        String rawQuery;
        try {
            rawQuery = new URI(url).getRawQuery();
        } catch (URISyntaxException exception) {
            return null;
        }
        if (rawQuery == null || rawQuery.isEmpty()) {
            return null;
        }
        for (String pair : rawQuery.split("&")) {
            int separator = pair.indexOf('=');
            String rawKey = separator >= 0 ? pair.substring(0, separator) : pair;
            if (!name.equals(decode(rawKey))) {
                continue;
            }
            return separator >= 0 ? decode(pair.substring(separator + 1)) : "";
        }
        return null;
    }

    private static String decode(String value) {
        try {
            return URLDecoder.decode(value, StandardCharsets.UTF_8.name());
        } catch (Exception exception) {
            return value;
        }
    }

    private static String toHex(byte[] bytes) {
        char[] hex = new char[bytes.length * 2];
        for (int index = 0; index < bytes.length; index++) {
            int value = bytes[index] & 0xff;
            hex[index * 2] = HEX_DIGITS[value >>> 4];
            hex[index * 2 + 1] = HEX_DIGITS[value & 0x0f];
        }
        return new String(hex);
    }
}
