/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.webhooks.core;

import java.net.URI;
import java.net.URISyntaxException;
import java.net.URLDecoder;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.security.GeneralSecurityException;
import java.security.MessageDigest;
import java.util.ArrayList;
import java.util.Base64;
import java.util.LinkedHashSet;
import java.util.List;
import javax.crypto.Mac;
import javax.crypto.spec.SecretKeySpec;

public final class WebhookSignature {

    private static final char[] HEX_DIGITS = "0123456789abcdef".toCharArray();

    private WebhookSignature() {}

    public static String computeHmacSignature(String payload, String secret, String algorithm, String encoding) {
        try {
            Mac mac = Mac.getInstance(algorithm);
            mac.init(new SecretKeySpec(secret.getBytes(StandardCharsets.UTF_8), algorithm));
            byte[] signature = mac.doFinal(payload.getBytes(StandardCharsets.UTF_8));
            if ("hex".equals(encoding)) {
                return toHex(signature);
            }
            return Base64.getEncoder().encodeToString(signature);
        } catch (GeneralSecurityException exception) {
            throw new IllegalStateException("Failed to compute webhook HMAC signature", exception);
        }
    }

    public static boolean timingSafeEqual(String left, String right) {
        if (left == null || right == null) {
            return false;
        }
        return MessageDigest.isEqual(left.getBytes(StandardCharsets.UTF_8), right.getBytes(StandardCharsets.UTF_8));
    }

    /**
     * Build the list of normalized notification-URL forms to verify a webhook signature against. Some providers (e.g.
     * Twilio) are inconsistent about whether the URL they signed carried a port and how its query string was encoded, so
     * a signature is accepted if it matches the computation over ANY of these candidates.
     *
     * <p>Mirrors twilio's addPort / removePort / buildUrlWithStandardPort / withLegacyQuerystring. Always includes at
     * least the caller-supplied URL and never throws: an unparseable URL yields {@code [url]}.
     */
    public static List<String> notificationUrlCandidates(
            String url, boolean portVariants, boolean legacyQueryEncoding) {
        URI parsed;
        try {
            parsed = new URI(url);
        } catch (URISyntaxException | NullPointerException exception) {
            List<String> fallback = new ArrayList<>();
            fallback.add(url);
            return fallback;
        }

        List<String> portForms = new ArrayList<>();
        if (portVariants) {
            portForms.add(removePort(parsed));
            portForms.add(addPort(parsed));
        } else {
            portForms.add(url);
        }

        // A LinkedHashSet preserves insertion order while collapsing forms that coincide (e.g. a URL that already
        // carries a standard port, or a query-less URL under legacy encoding).
        LinkedHashSet<String> candidates = new LinkedHashSet<>();
        candidates.add(url);
        candidates.addAll(portForms);
        if (legacyQueryEncoding) {
            for (String form : portForms) {
                candidates.add(withLegacyQuerystring(form));
            }
        }
        return new ArrayList<>(candidates);
    }

    private static String buildUrlWithStandardPort(URI parsed) {
        String port = "https".equalsIgnoreCase(parsed.getScheme()) ? ":443" : ":80";
        return rebuild(
                parsed.getScheme(),
                userInfoWithHost(parsed) + port,
                parsed.getRawPath(),
                parsed.getRawQuery(),
                parsed.getRawFragment());
    }

    private static String addPort(URI parsed) {
        if (parsed.getPort() == -1) {
            return buildUrlWithStandardPort(parsed);
        }
        return parsed.toString();
    }

    private static String removePort(URI parsed) {
        return rebuild(
                parsed.getScheme(),
                userInfoWithHost(parsed),
                parsed.getRawPath(),
                parsed.getRawQuery(),
                parsed.getRawFragment());
    }

    private static String withLegacyQuerystring(String url) {
        URI parsed;
        try {
            parsed = new URI(url);
        } catch (URISyntaxException exception) {
            return url;
        }
        String rawQuery = parsed.getRawQuery();
        if (rawQuery == null || rawQuery.isEmpty()) {
            return url;
        }
        // Re-encode the query with legacy form-encoding, reversing percent-encoding differences introduced by URL
        // parsing.
        StringBuilder requoted = new StringBuilder();
        for (String pair : rawQuery.split("&", -1)) {
            int separator = pair.indexOf('=');
            String rawKey = separator >= 0 ? pair.substring(0, separator) : pair;
            String key = formEncode(decode(rawKey));
            if (requoted.length() > 0) {
                requoted.append('&');
            }
            if (separator >= 0) {
                requoted.append(key).append('=').append(formEncode(decode(pair.substring(separator + 1))));
            } else {
                requoted.append(key);
            }
        }
        return rebuild(
                parsed.getScheme(),
                rawAuthority(parsed),
                parsed.getRawPath(),
                requoted.toString(),
                parsed.getRawFragment());
    }

    private static String userInfoWithHost(URI parsed) {
        StringBuilder authority = new StringBuilder();
        String userInfo = parsed.getRawUserInfo();
        if (userInfo != null) {
            authority.append(userInfo).append('@');
        }
        if (parsed.getHost() != null) {
            authority.append(parsed.getHost());
        }
        return authority.toString();
    }

    private static String rawAuthority(URI parsed) {
        String rawAuthority = parsed.getRawAuthority();
        return rawAuthority == null ? "" : rawAuthority;
    }

    private static String rebuild(String scheme, String authority, String path, String query, String fragment) {
        StringBuilder url = new StringBuilder();
        if (scheme != null) {
            url.append(scheme).append("://");
        }
        url.append(authority);
        if (path != null) {
            url.append(path);
        }
        if (query != null) {
            url.append('?').append(query);
        }
        if (fragment != null) {
            url.append('#').append(fragment);
        }
        return url.toString();
    }

    private static String decode(String value) {
        try {
            return URLDecoder.decode(value, StandardCharsets.UTF_8.name());
        } catch (Exception exception) {
            return value;
        }
    }

    private static String formEncode(String value) {
        try {
            return URLEncoder.encode(value, StandardCharsets.UTF_8.name());
        } catch (Exception exception) {
            return value;
        }
    }

    private static String toHex(byte[] bytes) {
        char[] hex = new char[bytes.length * 2];
        for (int index = 0; index < bytes.length; index++) {
            int value = bytes[index] & 0xff;
            hex[index * 2] = HEX_DIGITS[value >>> 4];
            hex[index * 2 + 1] = HEX_DIGITS[value & 0x0f];
        }
        return new String(hex);
    }
}
