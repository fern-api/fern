/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.undiscriminatedUnions.resources.union.types;

import com.fasterxml.jackson.annotation.JsonValue;
import com.fasterxml.jackson.core.JsonParseException;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.seed.undiscriminatedUnions.core.ObjectMappers;
import java.io.IOException;
import java.util.Map;
import java.util.Objects;

@JsonDeserialize(using = AliasedObjectUnion.Deserializer.class)
public final class AliasedObjectUnion {
    private final Object value;

    private final int type;

    private AliasedObjectUnion(Object value, int type) {
        this.value = value;
        this.type = type;
    }

    @JsonValue
    public Object get() {
        return this.value;
    }

    @SuppressWarnings("unchecked")
    public <T> T visit(Visitor<T> visitor) {
        if (this.type == 0) {
            return visitor.visit((LeafObjectA) this.value);
        } else if (this.type == 1) {
            return visitor.visit((LeafObjectB) this.value);
        }
        throw new IllegalStateException("Failed to visit value. This should never happen.");
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof AliasedObjectUnion && equalTo((AliasedObjectUnion) other);
    }

    private boolean equalTo(AliasedObjectUnion other) {
        return value.equals(other.value);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.value);
    }

    @java.lang.Override
    public String toString() {
        return this.value.toString();
    }

    public static AliasedObjectUnion of(LeafObjectA value) {
        return new AliasedObjectUnion(value, 0);
    }

    public static AliasedObjectUnion of(LeafObjectB value) {
        return new AliasedObjectUnion(value, 1);
    }

    public interface Visitor<T> {
        T visit(LeafObjectA value);

        T visit(LeafObjectB value);
    }

    static final class Deserializer extends StdDeserializer<AliasedObjectUnion> {
        Deserializer() {
            super(AliasedObjectUnion.class);
        }

        @java.lang.Override
        public AliasedObjectUnion deserialize(JsonParser p, DeserializationContext context) throws IOException {
            Object value = p.readValueAs(Object.class);
            if (value instanceof Map<?, ?>
                    && ((Map<?, ?>) value).containsKey("onlyInA")
                    && ((Map<?, ?>) value).containsKey("sharedNumber")) {
                try {
                    return of(ObjectMappers.JSON_MAPPER.convertValue(value, LeafObjectA.class));
                } catch (RuntimeException e) {
                }
            }
            if (value instanceof Map<?, ?> && ((Map<?, ?>) value).containsKey("onlyInB")) {
                try {
                    return of(ObjectMappers.JSON_MAPPER.convertValue(value, LeafObjectB.class));
                } catch (RuntimeException e) {
                }
            }
            throw new JsonParseException(p, "Failed to deserialize");
        }
    }
}
