/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.singleProperty.resources.singleproperty;

import com.seed.singleProperty.core.ClientOptions;
import com.seed.singleProperty.core.ObjectMappers;
import com.seed.singleProperty.core.QueryStringMapper;
import com.seed.singleProperty.core.RequestOptions;
import com.seed.singleProperty.core.SeedSinglePropertyApiException;
import com.seed.singleProperty.core.SeedSinglePropertyException;
import com.seed.singleProperty.core.SeedSinglePropertyHttpResponse;
import com.seed.singleProperty.resources.singleproperty.requests.GetThingRequest;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawSinglePropertyClient {
    protected final ClientOptions clientOptions;

    public RawSinglePropertyClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public SeedSinglePropertyHttpResponse<String> doThing(String id) {
        return doThing(id, GetThingRequest.builder().build());
    }

    public SeedSinglePropertyHttpResponse<String> doThing(String id, GetThingRequest request) {
        return doThing(id, request, null);
    }

    public SeedSinglePropertyHttpResponse<String> doThing(
            String id, GetThingRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegment(id);
        if (request.getIncludeRemoteData().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "include-remote-data",
                    request.getIncludeRemoteData().get(),
                    false);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new SeedSinglePropertyHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, String.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SeedSinglePropertyApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new SeedSinglePropertyException("Network error executing HTTP request", e);
        }
    }
}
