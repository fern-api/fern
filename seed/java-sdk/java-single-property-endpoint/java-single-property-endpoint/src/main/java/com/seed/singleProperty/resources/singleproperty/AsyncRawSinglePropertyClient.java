/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.singleProperty.resources.singleproperty;

import com.seed.singleProperty.core.ClientOptions;
import com.seed.singleProperty.core.ObjectMappers;
import com.seed.singleProperty.core.QueryStringMapper;
import com.seed.singleProperty.core.RequestOptions;
import com.seed.singleProperty.core.SeedSinglePropertyApiException;
import com.seed.singleProperty.core.SeedSinglePropertyException;
import com.seed.singleProperty.core.SeedSinglePropertyHttpResponse;
import com.seed.singleProperty.resources.singleproperty.requests.GetThingRequest;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawSinglePropertyClient {
    protected final ClientOptions clientOptions;

    public AsyncRawSinglePropertyClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public CompletableFuture<SeedSinglePropertyHttpResponse<String>> doThing(String id) {
        return doThing(id, GetThingRequest.builder().build());
    }

    public CompletableFuture<SeedSinglePropertyHttpResponse<String>> doThing(String id, GetThingRequest request) {
        return doThing(id, request, null);
    }

    public CompletableFuture<SeedSinglePropertyHttpResponse<String>> doThing(
            String id, GetThingRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegment(id);
        if (request.getIncludeRemoteData().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "include-remote-data",
                    request.getIncludeRemoteData().get(),
                    false);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<SeedSinglePropertyHttpResponse<String>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new SeedSinglePropertyHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, String.class), response));
                        return;
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new SeedSinglePropertyApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(
                            new SeedSinglePropertyException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(
                        new SeedSinglePropertyException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
