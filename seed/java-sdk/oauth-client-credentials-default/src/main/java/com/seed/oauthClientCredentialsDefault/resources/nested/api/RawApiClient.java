/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.oauthClientCredentialsDefault.resources.nested.api;

import com.seed.oauthClientCredentialsDefault.core.ClientOptions;
import com.seed.oauthClientCredentialsDefault.core.ObjectMappers;
import com.seed.oauthClientCredentialsDefault.core.RequestOptions;
import com.seed.oauthClientCredentialsDefault.core.SeedOauthClientCredentialsDefaultApiException;
import com.seed.oauthClientCredentialsDefault.core.SeedOauthClientCredentialsDefaultException;
import com.seed.oauthClientCredentialsDefault.core.SeedOauthClientCredentialsDefaultHttpResponse;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawApiClient {
    protected final ClientOptions clientOptions;

    public RawApiClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public SeedOauthClientCredentialsDefaultHttpResponse<Void> getSomething() {
        return getSomething(null);
    }

    public SeedOauthClientCredentialsDefaultHttpResponse<Void> getSomething(RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("nested")
                .addPathSegments("get-something")
                .build();
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new SeedOauthClientCredentialsDefaultHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SeedOauthClientCredentialsDefaultApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new SeedOauthClientCredentialsDefaultException("Network error executing HTTP request", e);
        }
    }
}
