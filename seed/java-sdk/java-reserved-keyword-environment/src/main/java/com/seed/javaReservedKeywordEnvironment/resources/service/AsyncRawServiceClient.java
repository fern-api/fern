/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.javaReservedKeywordEnvironment.resources.service;

import com.seed.javaReservedKeywordEnvironment.core.ClientOptions;
import com.seed.javaReservedKeywordEnvironment.core.ObjectMappers;
import com.seed.javaReservedKeywordEnvironment.core.RequestOptions;
import com.seed.javaReservedKeywordEnvironment.core.SeedJavaReservedKeywordEnvironmentApiException;
import com.seed.javaReservedKeywordEnvironment.core.SeedJavaReservedKeywordEnvironmentException;
import com.seed.javaReservedKeywordEnvironment.core.SeedJavaReservedKeywordEnvironmentHttpResponse;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawServiceClient {
    protected final ClientOptions clientOptions;

    public AsyncRawServiceClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public CompletableFuture<SeedJavaReservedKeywordEnvironmentHttpResponse<String>> get() {
        return get(null);
    }

    public CompletableFuture<SeedJavaReservedKeywordEnvironmentHttpResponse<String>> get(
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("dummy");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((key, value) -> {
                httpUrl.addQueryParameter(key, value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<SeedJavaReservedKeywordEnvironmentHttpResponse<String>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new SeedJavaReservedKeywordEnvironmentHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, String.class), response));
                        return;
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new SeedJavaReservedKeywordEnvironmentApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(
                            new SeedJavaReservedKeywordEnvironmentException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(
                        new SeedJavaReservedKeywordEnvironmentException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
