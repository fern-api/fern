/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.propertyAccess;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.seed.propertyAccess.core.ClientOptions;
import com.seed.propertyAccess.core.MediaTypes;
import com.seed.propertyAccess.core.ObjectMappers;
import com.seed.propertyAccess.core.RequestOptions;
import com.seed.propertyAccess.core.SeedPropertyAccessApiException;
import com.seed.propertyAccess.core.SeedPropertyAccessException;
import com.seed.propertyAccess.core.SeedPropertyAccessHttpResponse;
import com.seed.propertyAccess.types.User;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawSeedPropertyAccessClient {
    protected final ClientOptions clientOptions;

    public RawSeedPropertyAccessClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public SeedPropertyAccessHttpResponse<User> createUser(User request) {
        return createUser(request, null);
    }

    public SeedPropertyAccessHttpResponse<User> createUser(User request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("users")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new SeedPropertyAccessException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new SeedPropertyAccessHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, User.class), response);
            }
            throw new SeedPropertyAccessApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new SeedPropertyAccessException("Network error executing HTTP request", e);
        }
    }
}
