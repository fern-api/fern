/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class UserOrAdminDiscriminatedAdminType {
    public static final UserOrAdminDiscriminatedAdminType ADMIN =
            new UserOrAdminDiscriminatedAdminType(Value.ADMIN, "admin");

    private final Value value;

    private final String string;

    UserOrAdminDiscriminatedAdminType(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof UserOrAdminDiscriminatedAdminType
                        && this.string.equals(((UserOrAdminDiscriminatedAdminType) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case ADMIN:
                return visitor.visitAdmin();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static UserOrAdminDiscriminatedAdminType valueOf(String value) {
        switch (value) {
            case "admin":
                return ADMIN;
            default:
                return new UserOrAdminDiscriminatedAdminType(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        ADMIN,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitAdmin();

        T visitUnknown(String unknownType);
    }
}
