/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.inferredAuthImplicitApiKey.resources.nestednoauth.api;

import com.seed.inferredAuthImplicitApiKey.core.ClientOptions;
import com.seed.inferredAuthImplicitApiKey.core.ObjectMappers;
import com.seed.inferredAuthImplicitApiKey.core.RequestOptions;
import com.seed.inferredAuthImplicitApiKey.core.SeedInferredAuthImplicitApiKeyApiException;
import com.seed.inferredAuthImplicitApiKey.core.SeedInferredAuthImplicitApiKeyException;
import com.seed.inferredAuthImplicitApiKey.core.SeedInferredAuthImplicitApiKeyHttpResponse;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawApiClient {
    protected final ClientOptions clientOptions;

    public AsyncRawApiClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public CompletableFuture<SeedInferredAuthImplicitApiKeyHttpResponse<Void>> getSomething() {
        return getSomething(null);
    }

    public CompletableFuture<SeedInferredAuthImplicitApiKeyHttpResponse<Void>> getSomething(
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("nested-no-auth")
                .addPathSegments("get-something");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<SeedInferredAuthImplicitApiKeyHttpResponse<Void>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    if (response.isSuccessful()) {
                        future.complete(new SeedInferredAuthImplicitApiKeyHttpResponse<>(null, response));
                        return;
                    }
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new SeedInferredAuthImplicitApiKeyApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(
                            new SeedInferredAuthImplicitApiKeyException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(
                        new SeedInferredAuthImplicitApiKeyException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
