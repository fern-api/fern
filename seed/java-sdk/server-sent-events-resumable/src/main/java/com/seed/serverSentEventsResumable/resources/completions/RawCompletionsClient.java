/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.serverSentEventsResumable.resources.completions;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.seed.serverSentEventsResumable.core.ClientOptions;
import com.seed.serverSentEventsResumable.core.MediaTypes;
import com.seed.serverSentEventsResumable.core.ObjectMappers;
import com.seed.serverSentEventsResumable.core.RequestOptions;
import com.seed.serverSentEventsResumable.core.ResponseBodyReader;
import com.seed.serverSentEventsResumable.core.SeedServerSentEventsResumableApiException;
import com.seed.serverSentEventsResumable.core.SeedServerSentEventsResumableException;
import com.seed.serverSentEventsResumable.core.SeedServerSentEventsResumableHttpResponse;
import com.seed.serverSentEventsResumable.core.Stream;
import com.seed.serverSentEventsResumable.resources.completions.requests.StreamCompletionRequest;
import com.seed.serverSentEventsResumable.resources.completions.requests.StreamCompletionRequestNonResumable;
import com.seed.serverSentEventsResumable.resources.completions.types.StreamedCompletion;
import java.io.IOException;
import java.util.concurrent.TimeUnit;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawCompletionsClient {
    protected final ClientOptions clientOptions;

    public RawCompletionsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public SeedServerSentEventsResumableHttpResponse<Iterable<StreamedCompletion>> stream(
            StreamCompletionRequest request) {
        return stream(request, null);
    }

    public SeedServerSentEventsResumableHttpResponse<Iterable<StreamedCompletion>> stream(
            StreamCompletionRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("stream");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new SeedServerSentEventsResumableException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        client = client.newBuilder().callTimeout(0, TimeUnit.SECONDS).build();
        try {
            Response response = client.newCall(okhttpRequest).execute();
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new SeedServerSentEventsResumableHttpResponse<>(
                        Stream.fromSse(StreamedCompletion.class, new ResponseBodyReader(response), "[[DONE]]"),
                        response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SeedServerSentEventsResumableApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new SeedServerSentEventsResumableException("Network error executing HTTP request", e);
        }
    }

    public SeedServerSentEventsResumableHttpResponse<Iterable<StreamedCompletion>> streamNonResumable(
            StreamCompletionRequestNonResumable request) {
        return streamNonResumable(request, null);
    }

    public SeedServerSentEventsResumableHttpResponse<Iterable<StreamedCompletion>> streamNonResumable(
            StreamCompletionRequestNonResumable request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("stream-non-resumable");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new SeedServerSentEventsResumableException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        client = client.newBuilder().callTimeout(0, TimeUnit.SECONDS).build();
        try {
            Response response = client.newCall(okhttpRequest).execute();
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new SeedServerSentEventsResumableHttpResponse<>(
                        Stream.fromSse(StreamedCompletion.class, new ResponseBodyReader(response), "[[DONE]]"),
                        response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SeedServerSentEventsResumableApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new SeedServerSentEventsResumableException("Network error executing HTTP request", e);
        }
    }
}
