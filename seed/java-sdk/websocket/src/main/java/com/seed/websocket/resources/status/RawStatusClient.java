/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.websocket.resources.status;

import com.seed.websocket.core.ClientOptions;
import com.seed.websocket.core.ObjectMappers;
import com.seed.websocket.core.RequestOptions;
import com.seed.websocket.core.SeedWebsocketApiException;
import com.seed.websocket.core.SeedWebsocketException;
import com.seed.websocket.core.SeedWebsocketHttpResponse;
import com.seed.websocket.resources.status.types.StatusResponse;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawStatusClient {
    protected final ClientOptions clientOptions;

    public RawStatusClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public SeedWebsocketHttpResponse<StatusResponse> getStatus() {
        return getStatus(null);
    }

    public SeedWebsocketHttpResponse<StatusResponse> getStatus(RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("status");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new SeedWebsocketHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, StatusResponse.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SeedWebsocketApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new SeedWebsocketException("Network error executing HTTP request", e);
        }
    }
}
