/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.api.types;

import com.fasterxml.jackson.annotation.JsonValue;
import com.fasterxml.jackson.core.JsonParseException;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.seed.api.core.ObjectMappers;
import com.seed.api.core.XmlReader;
import com.seed.api.core.XmlSerializable;
import java.io.IOException;
import java.util.Objects;
import org.w3c.dom.Element;

@JsonDeserialize(using = ResponseChildrenItem.Deserializer.class)
public final class ResponseChildrenItem implements XmlSerializable {
    private final Object value;

    private final int type;

    private ResponseChildrenItem(Object value, int type) {
        this.value = value;
        this.type = type;
    }

    @JsonValue
    public Object get() {
        return this.value;
    }

    @SuppressWarnings("unchecked")
    public <T> T visit(Visitor<T> visitor) {
        if (this.type == 0) {
            return visitor.visit((Say) this.value);
        } else if (this.type == 1) {
            return visitor.visit((Dial) this.value);
        } else if (this.type == 2) {
            return visitor.visit((Pause) this.value);
        } else if (this.type == 3) {
            return visitor.visit((Hangup) this.value);
        }
        throw new IllegalStateException("Failed to visit value. This should never happen.");
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof ResponseChildrenItem && equalTo((ResponseChildrenItem) other);
    }

    private boolean equalTo(ResponseChildrenItem other) {
        return value.equals(other.value);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.value);
    }

    @java.lang.Override
    public String toString() {
        return this.value.toString();
    }

    public static ResponseChildrenItem of(Say value) {
        return new ResponseChildrenItem(value, 0);
    }

    public static ResponseChildrenItem of(Dial value) {
        return new ResponseChildrenItem(value, 1);
    }

    public static ResponseChildrenItem of(Pause value) {
        return new ResponseChildrenItem(value, 2);
    }

    public static ResponseChildrenItem of(Hangup value) {
        return new ResponseChildrenItem(value, 3);
    }

    @Override
    public String toXml() {
        return ((XmlSerializable) this.value).toXml();
    }

    @Override
    public String toXml(boolean xmlDeclaration) {
        return ((XmlSerializable) this.value).toXml(xmlDeclaration);
    }

    public static ResponseChildrenItem fromXml(Element element) {
        String name = XmlReader.localName(element);
        switch (name) {
            case "Say":
                return of(Say.fromXml(element));
            case "Dial":
                return of(Dial.fromXml(element));
            case "Pause":
                return of(Pause.fromXml(element));
            case "Hangup":
                return of(Hangup.fromXml(element));
            default:
                throw new IllegalArgumentException("Unexpected element <" + name + "> in ResponseChildrenItem");
        }
    }

    public interface Visitor<T> {
        T visit(Say value);

        T visit(Dial value);

        T visit(Pause value);

        T visit(Hangup value);
    }

    static final class Deserializer extends StdDeserializer<ResponseChildrenItem> {
        Deserializer() {
            super(ResponseChildrenItem.class);
        }

        @java.lang.Override
        public ResponseChildrenItem deserialize(JsonParser p, DeserializationContext context) throws IOException {
            Object value = p.readValueAs(Object.class);
            try {
                return of(ObjectMappers.JSON_MAPPER.convertValue(value, Say.class));
            } catch (RuntimeException e) {
            }
            try {
                return of(ObjectMappers.JSON_MAPPER.convertValue(value, Dial.class));
            } catch (RuntimeException e) {
            }
            try {
                return of(ObjectMappers.JSON_MAPPER.convertValue(value, Pause.class));
            } catch (RuntimeException e) {
            }
            try {
                return of(ObjectMappers.JSON_MAPPER.convertValue(value, Hangup.class));
            } catch (RuntimeException e) {
            }
            throw new JsonParseException(p, "Failed to deserialize");
        }
    }
}
