/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seed.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class DialRecordItem {
    public static final DialRecordItem RECORD_FROM_ANSWER =
            new DialRecordItem(Value.RECORD_FROM_ANSWER, "record-from-answer");

    public static final DialRecordItem RECORD_FROM_RINGING =
            new DialRecordItem(Value.RECORD_FROM_RINGING, "record-from-ringing");

    private final Value value;

    private final String string;

    DialRecordItem(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof DialRecordItem && this.string.equals(((DialRecordItem) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case RECORD_FROM_ANSWER:
                return visitor.visitRecordFromAnswer();
            case RECORD_FROM_RINGING:
                return visitor.visitRecordFromRinging();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static DialRecordItem valueOf(String value) {
        switch (value) {
            case "record-from-answer":
                return RECORD_FROM_ANSWER;
            case "record-from-ringing":
                return RECORD_FROM_RINGING;
            default:
                return new DialRecordItem(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        RECORD_FROM_ANSWER,

        RECORD_FROM_RINGING,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitRecordFromAnswer();

        T visitRecordFromRinging();

        T visitUnknown(String unknownType);
    }
}
