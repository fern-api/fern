// Auto-generated by @fern-api/cli-generator's copySpecs step.
// Edit the SDK template / generator if you need to change the shape.

mod custom;
mod sdk;

use fern_cli_sdk::app::CliApp;
use fern_cli_sdk::openapi::OpenApiBinding;
use fern_cli_sdk::auth::{OAuth2Auth, OAuth2Endpoint, OAuth2RequestProperty, OAuth2RequestValue};

fn main() {
    let app = CliApp::new("oauth-client-credentials-openapi")
        .auth(OAuth2Auth::new("CustomAuth").client_id_env("OAUTH_CLIENT_CREDENTIALS_OPENAPI_CLIENT_ID").client_secret_env("OAUTH_CLIENT_CREDENTIALS_OPENAPI_CLIENT_SECRET").token_header("X-Custom-Token").token_prefix("Token").token_endpoint(OAuth2Endpoint::new("/identity/token", "/identity/token").method("POST").use_base_url_override().json_body("application/json").request_property(OAuth2RequestProperty::body(["username"], OAuth2RequestValue::ClientId)).request_property(OAuth2RequestProperty::body(["password"], OAuth2RequestValue::ClientSecret)).access_token_path(["access_token"]).expires_in_path(["expires_in"]).refresh_token_path(["refresh_token"])))
        .binding(
            OpenApiBinding::new()
                .spec(include_str!("openapi0.json"))
        );

    let app = custom::register(app);

    app.run()
}
