// Auto-generated by @fern-api/cli-generator's copySpecs step.
// Edit the SDK template / generator if you need to change the shape.

mod custom;
mod sdk;

use fern_cli_sdk::app::CliApp;
use fern_cli_sdk::openapi::OpenApiBinding;
use fern_cli_sdk::auth::{OAuth2Auth, OAuth2Endpoint, OAuth2RequestProperty, OAuth2RequestValue};

fn main() {
    let app = CliApp::new("oauth-api")
        .auth(OAuth2Auth::new("OAuth").client_id_env("OAUTH_API_CLIENT_ID").client_secret_env("OAUTH_API_CLIENT_SECRET").token_header("Authorization").token_prefix("Bearer").token_endpoint(OAuth2Endpoint::new("/oauth/token", "/oauth/token").method("POST").use_base_url_override().form_body().request_property(OAuth2RequestProperty::body(["client_id"], OAuth2RequestValue::ClientId)).request_property(OAuth2RequestProperty::body(["client_secret"], OAuth2RequestValue::ClientSecret)).access_token_path(["access_token"]).expires_in_path(["expires_in"])))
        .binding(
            OpenApiBinding::new()
                .spec_under("oauth", include_str!("openapi0.json"))
                .spec(include_str!("openapi1.json"))
        );

    let app = custom::register(app);

    app.run()
}
