# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import httpx
from .core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from .core.logging import LogConfig, Logger
from .core.request_options import RequestOptions
from .environment import SeedApiEnvironment
from .raw_client import AsyncRawSeedApi, RawSeedApi
from .types.combined_entity import CombinedEntity
from .types.organization import Organization
from .types.plant_post_sun_exposure import PlantPostSunExposure
from .types.plant_post_watering_frequency import PlantPostWateringFrequency
from .types.plant_strict import PlantStrict
from .types.rule_create_request_execution_context import RuleCreateRequestExecutionContext
from .types.rule_response import RuleResponse
from .types.rule_type_search_response import RuleTypeSearchResponse
from .types.tree_record import TreeRecord
from .types.user_search_response import UserSearchResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class SeedApi:
    """
    Use this class to access the different functions within the SDK. You can instantiate any number of clients with different configuration that will propagate to these functions.

    Parameters
    ----------
    base_url : typing.Optional[str]
        The base url to use for requests from the client.

    environment : SeedApiEnvironment
        The environment to use for requests from the client. from .environment import SeedApiEnvironment



        Defaults to SeedApiEnvironment.DEFAULT



    headers : typing.Optional[typing.Dict[str, str]]
        Additional headers to send with every request.

    timeout : typing.Optional[float]
        The timeout to be used, in seconds, for requests. By default the timeout is 60 seconds, unless a custom httpx client is used, in which case this default is not enforced.

    max_retries : typing.Optional[int]
        The default maximum number of retries for failed requests. Defaults to 2. Per-request `max_retries` in `request_options` takes precedence over this value.

    follow_redirects : typing.Optional[bool]
        Whether the default httpx client follows redirects or not, this is irrelevant if a custom httpx client is passed in.

    httpx_client : typing.Optional[httpx.Client]
        The httpx client to use for making requests, a preconfigured client is used by default, however this is useful should you want to pass in any custom httpx configuration.

    logging : typing.Optional[typing.Union[LogConfig, Logger]]
        Configure logging for the SDK. Accepts a LogConfig dict with 'level' (debug/info/warn/error), 'logger' (custom logger implementation), and 'silent' (boolean, defaults to True) fields. You can also pass a pre-configured Logger instance.

    Examples
    --------
    from seed import SeedApi

    client = SeedApi()
    """

    def __init__(
        self,
        *,
        base_url: typing.Optional[str] = None,
        environment: SeedApiEnvironment = SeedApiEnvironment.DEFAULT,
        headers: typing.Optional[typing.Dict[str, str]] = None,
        timeout: typing.Optional[float] = None,
        max_retries: typing.Optional[int] = None,
        follow_redirects: typing.Optional[bool] = True,
        httpx_client: typing.Optional[httpx.Client] = None,
        logging: typing.Optional[typing.Union[LogConfig, Logger]] = None,
    ):
        _defaulted_timeout = (
            timeout if timeout is not None else 60 if httpx_client is None else httpx_client.timeout.read
        )
        _defaulted_max_retries = max_retries if max_retries is not None else 2
        self._client_wrapper = SyncClientWrapper(
            base_url=_get_base_url(base_url=base_url, environment=environment),
            headers=headers,
            httpx_client=httpx_client
            if httpx_client is not None
            else httpx.Client(timeout=_defaulted_timeout, follow_redirects=follow_redirects)
            if follow_redirects is not None
            else httpx.Client(timeout=_defaulted_timeout),
            timeout=_defaulted_timeout,
            max_retries=_defaulted_max_retries,
            logging=logging,
        )
        self._raw_client = RawSeedApi(client_wrapper=self._client_wrapper)

    @property
    def with_raw_response(self) -> RawSeedApi:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawSeedApi
        """
        return self._raw_client

    def search_rule_types(
        self, *, query: typing.Optional[str] = None, request_options: typing.Optional[RequestOptions] = None
    ) -> RuleTypeSearchResponse:
        """
        Parameters
        ----------
        query : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RuleTypeSearchResponse
            Paginated list of rule types

        Examples
        --------
        from seed import SeedApi

        client = SeedApi()
        client.search_rule_types()
        """
        _response = self._raw_client.search_rule_types(query=query, request_options=request_options)
        return _response.data

    def create_rule(
        self,
        *,
        name: str,
        execution_context: RuleCreateRequestExecutionContext,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> RuleResponse:
        """
        Parameters
        ----------
        name : str

        execution_context : RuleCreateRequestExecutionContext
            Execution context for the rule, excluding the prod environment.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RuleResponse
            Created rule

        Examples
        --------
        from seed import SeedApi

        client = SeedApi()
        client.create_rule(
            name="name",
            execution_context="prod",
        )
        """
        _response = self._raw_client.create_rule(
            name=name, execution_context=execution_context, request_options=request_options
        )
        return _response.data

    def list_users(self, *, request_options: typing.Optional[RequestOptions] = None) -> UserSearchResponse:
        """
        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UserSearchResponse
            Paginated list of users

        Examples
        --------
        from seed import SeedApi

        client = SeedApi()
        client.list_users()
        """
        _response = self._raw_client.list_users(request_options=request_options)
        return _response.data

    def get_entity(self, *, request_options: typing.Optional[RequestOptions] = None) -> CombinedEntity:
        """
        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CombinedEntity
            An entity with properties from multiple parents

        Examples
        --------
        from seed import SeedApi

        client = SeedApi()
        client.get_entity()
        """
        _response = self._raw_client.get_entity(request_options=request_options)
        return _response.data

    def get_organization(self, *, request_options: typing.Optional[RequestOptions] = None) -> Organization:
        """
        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Organization
            An organization whose metadata is merged from two parents

        Examples
        --------
        from seed import SeedApi

        client = SeedApi()
        client.get_organization()
        """
        _response = self._raw_client.get_organization(request_options=request_options)
        return _response.data

    def create_plant(
        self,
        *,
        species: str,
        family: str,
        genus: str,
        common_name: str,
        watering_frequency: PlantPostWateringFrequency,
        sun_exposure: PlantPostSunExposure,
        planted_at: typing.Optional[dt.date] = OMIT,
        soil_type: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PlantStrict:
        """
        Tests three-level allOf chain where a parent schema itself uses allOf with $ref elements. The grandparent's properties must be resolved through the nested $ref.

        Parameters
        ----------
        species : str
            The botanical species name.

        family : str
            The botanical family.

        genus : str
            The botanical genus.

        common_name : str
            The common name of the plant.

        watering_frequency : PlantPostWateringFrequency

        sun_exposure : PlantPostSunExposure
            Required sun exposure level.

        planted_at : typing.Optional[dt.date]
            Date the plant was planted.

        soil_type : typing.Optional[str]
            Preferred soil type.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlantStrict
            Created plant

        Examples
        --------
        from seed import SeedApi

        client = SeedApi()
        client.create_plant(
            species="species",
            family="family",
            genus="genus",
            common_name="commonName",
            watering_frequency="daily",
            sun_exposure="full",
        )
        """
        _response = self._raw_client.create_plant(
            species=species,
            family=family,
            genus=genus,
            common_name=common_name,
            watering_frequency=watering_frequency,
            sun_exposure=sun_exposure,
            planted_at=planted_at,
            soil_type=soil_type,
            request_options=request_options,
        )
        return _response.data

    def create_tree(
        self,
        *,
        id: str,
        tree_name: str,
        tree_species: str,
        tree_description: typing.Optional[str] = OMIT,
        height_in_feet: typing.Optional[float] = OMIT,
        planted_date: typing.Optional[dt.date] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> TreeRecord:
        """
        Tests that when a parent's allOf contains multiple $ref entries, all of them are resolved and their properties merged.

        Parameters
        ----------
        id : str
            Unique tree identifier.

        tree_name : str
            Display name of the tree.

        tree_species : str
            The species of tree.

        tree_description : typing.Optional[str]
            A description of the tree.

        height_in_feet : typing.Optional[float]
            Height of the tree in feet.

        planted_date : typing.Optional[dt.date]
            Date the tree was planted.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        TreeRecord
            Created tree

        Examples
        --------
        from seed import SeedApi

        client = SeedApi()
        client.create_tree(
            id="id",
            tree_name="treeName",
            tree_species="treeSpecies",
        )
        """
        _response = self._raw_client.create_tree(
            id=id,
            tree_name=tree_name,
            tree_species=tree_species,
            tree_description=tree_description,
            height_in_feet=height_in_feet,
            planted_date=planted_date,
            request_options=request_options,
        )
        return _response.data


def _make_default_async_client(
    timeout: typing.Optional[float],
    follow_redirects: typing.Optional[bool],
) -> httpx.AsyncClient:
    try:
        import httpx_aiohttp  # type: ignore[import-not-found]
    except ImportError:
        pass
    else:
        if follow_redirects is not None:
            return httpx_aiohttp.HttpxAiohttpClient(timeout=timeout, follow_redirects=follow_redirects)
        return httpx_aiohttp.HttpxAiohttpClient(timeout=timeout)

    if follow_redirects is not None:
        return httpx.AsyncClient(timeout=timeout, follow_redirects=follow_redirects)
    return httpx.AsyncClient(timeout=timeout)


class AsyncSeedApi:
    """
    Use this class to access the different functions within the SDK. You can instantiate any number of clients with different configuration that will propagate to these functions.

    Parameters
    ----------
    base_url : typing.Optional[str]
        The base url to use for requests from the client.

    environment : SeedApiEnvironment
        The environment to use for requests from the client. from .environment import SeedApiEnvironment



        Defaults to SeedApiEnvironment.DEFAULT



    headers : typing.Optional[typing.Dict[str, str]]
        Additional headers to send with every request.

    timeout : typing.Optional[float]
        The timeout to be used, in seconds, for requests. By default the timeout is 60 seconds, unless a custom httpx client is used, in which case this default is not enforced.

    max_retries : typing.Optional[int]
        The default maximum number of retries for failed requests. Defaults to 2. Per-request `max_retries` in `request_options` takes precedence over this value.

    follow_redirects : typing.Optional[bool]
        Whether the default httpx client follows redirects or not, this is irrelevant if a custom httpx client is passed in.

    httpx_client : typing.Optional[httpx.AsyncClient]
        The httpx client to use for making requests, a preconfigured client is used by default, however this is useful should you want to pass in any custom httpx configuration.

    logging : typing.Optional[typing.Union[LogConfig, Logger]]
        Configure logging for the SDK. Accepts a LogConfig dict with 'level' (debug/info/warn/error), 'logger' (custom logger implementation), and 'silent' (boolean, defaults to True) fields. You can also pass a pre-configured Logger instance.

    Examples
    --------
    from seed import AsyncSeedApi

    client = AsyncSeedApi()
    """

    def __init__(
        self,
        *,
        base_url: typing.Optional[str] = None,
        environment: SeedApiEnvironment = SeedApiEnvironment.DEFAULT,
        headers: typing.Optional[typing.Dict[str, str]] = None,
        timeout: typing.Optional[float] = None,
        max_retries: typing.Optional[int] = None,
        follow_redirects: typing.Optional[bool] = True,
        httpx_client: typing.Optional[httpx.AsyncClient] = None,
        logging: typing.Optional[typing.Union[LogConfig, Logger]] = None,
    ):
        _defaulted_timeout = (
            timeout if timeout is not None else 60 if httpx_client is None else httpx_client.timeout.read
        )
        _defaulted_max_retries = max_retries if max_retries is not None else 2
        self._client_wrapper = AsyncClientWrapper(
            base_url=_get_base_url(base_url=base_url, environment=environment),
            headers=headers,
            httpx_client=httpx_client
            if httpx_client is not None
            else _make_default_async_client(timeout=_defaulted_timeout, follow_redirects=follow_redirects),
            timeout=_defaulted_timeout,
            max_retries=_defaulted_max_retries,
            logging=logging,
        )
        self._raw_client = AsyncRawSeedApi(client_wrapper=self._client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawSeedApi:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawSeedApi
        """
        return self._raw_client

    async def search_rule_types(
        self, *, query: typing.Optional[str] = None, request_options: typing.Optional[RequestOptions] = None
    ) -> RuleTypeSearchResponse:
        """
        Parameters
        ----------
        query : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RuleTypeSearchResponse
            Paginated list of rule types

        Examples
        --------
        import asyncio

        from seed import AsyncSeedApi

        client = AsyncSeedApi()


        async def main() -> None:
            await client.search_rule_types()


        asyncio.run(main())
        """
        _response = await self._raw_client.search_rule_types(query=query, request_options=request_options)
        return _response.data

    async def create_rule(
        self,
        *,
        name: str,
        execution_context: RuleCreateRequestExecutionContext,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> RuleResponse:
        """
        Parameters
        ----------
        name : str

        execution_context : RuleCreateRequestExecutionContext
            Execution context for the rule, excluding the prod environment.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RuleResponse
            Created rule

        Examples
        --------
        import asyncio

        from seed import AsyncSeedApi

        client = AsyncSeedApi()


        async def main() -> None:
            await client.create_rule(
                name="name",
                execution_context="prod",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_rule(
            name=name, execution_context=execution_context, request_options=request_options
        )
        return _response.data

    async def list_users(self, *, request_options: typing.Optional[RequestOptions] = None) -> UserSearchResponse:
        """
        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UserSearchResponse
            Paginated list of users

        Examples
        --------
        import asyncio

        from seed import AsyncSeedApi

        client = AsyncSeedApi()


        async def main() -> None:
            await client.list_users()


        asyncio.run(main())
        """
        _response = await self._raw_client.list_users(request_options=request_options)
        return _response.data

    async def get_entity(self, *, request_options: typing.Optional[RequestOptions] = None) -> CombinedEntity:
        """
        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CombinedEntity
            An entity with properties from multiple parents

        Examples
        --------
        import asyncio

        from seed import AsyncSeedApi

        client = AsyncSeedApi()


        async def main() -> None:
            await client.get_entity()


        asyncio.run(main())
        """
        _response = await self._raw_client.get_entity(request_options=request_options)
        return _response.data

    async def get_organization(self, *, request_options: typing.Optional[RequestOptions] = None) -> Organization:
        """
        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Organization
            An organization whose metadata is merged from two parents

        Examples
        --------
        import asyncio

        from seed import AsyncSeedApi

        client = AsyncSeedApi()


        async def main() -> None:
            await client.get_organization()


        asyncio.run(main())
        """
        _response = await self._raw_client.get_organization(request_options=request_options)
        return _response.data

    async def create_plant(
        self,
        *,
        species: str,
        family: str,
        genus: str,
        common_name: str,
        watering_frequency: PlantPostWateringFrequency,
        sun_exposure: PlantPostSunExposure,
        planted_at: typing.Optional[dt.date] = OMIT,
        soil_type: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PlantStrict:
        """
        Tests three-level allOf chain where a parent schema itself uses allOf with $ref elements. The grandparent's properties must be resolved through the nested $ref.

        Parameters
        ----------
        species : str
            The botanical species name.

        family : str
            The botanical family.

        genus : str
            The botanical genus.

        common_name : str
            The common name of the plant.

        watering_frequency : PlantPostWateringFrequency

        sun_exposure : PlantPostSunExposure
            Required sun exposure level.

        planted_at : typing.Optional[dt.date]
            Date the plant was planted.

        soil_type : typing.Optional[str]
            Preferred soil type.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlantStrict
            Created plant

        Examples
        --------
        import asyncio

        from seed import AsyncSeedApi

        client = AsyncSeedApi()


        async def main() -> None:
            await client.create_plant(
                species="species",
                family="family",
                genus="genus",
                common_name="commonName",
                watering_frequency="daily",
                sun_exposure="full",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_plant(
            species=species,
            family=family,
            genus=genus,
            common_name=common_name,
            watering_frequency=watering_frequency,
            sun_exposure=sun_exposure,
            planted_at=planted_at,
            soil_type=soil_type,
            request_options=request_options,
        )
        return _response.data

    async def create_tree(
        self,
        *,
        id: str,
        tree_name: str,
        tree_species: str,
        tree_description: typing.Optional[str] = OMIT,
        height_in_feet: typing.Optional[float] = OMIT,
        planted_date: typing.Optional[dt.date] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> TreeRecord:
        """
        Tests that when a parent's allOf contains multiple $ref entries, all of them are resolved and their properties merged.

        Parameters
        ----------
        id : str
            Unique tree identifier.

        tree_name : str
            Display name of the tree.

        tree_species : str
            The species of tree.

        tree_description : typing.Optional[str]
            A description of the tree.

        height_in_feet : typing.Optional[float]
            Height of the tree in feet.

        planted_date : typing.Optional[dt.date]
            Date the tree was planted.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        TreeRecord
            Created tree

        Examples
        --------
        import asyncio

        from seed import AsyncSeedApi

        client = AsyncSeedApi()


        async def main() -> None:
            await client.create_tree(
                id="id",
                tree_name="treeName",
                tree_species="treeSpecies",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_tree(
            id=id,
            tree_name=tree_name,
            tree_species=tree_species,
            tree_description=tree_description,
            height_in_feet=height_in_feet,
            planted_date=planted_date,
            request_options=request_options,
        )
        return _response.data


def _get_base_url(*, base_url: typing.Optional[str] = None, environment: SeedApiEnvironment) -> str:
    if base_url is not None:
        return base_url
    elif environment is not None:
        return environment.value
    else:
        raise Exception("Please pass in either base_url or environment to construct the client")
