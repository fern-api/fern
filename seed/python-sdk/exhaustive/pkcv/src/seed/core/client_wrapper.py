# This file was auto-generated by Fern from our API Definition.

import typing

import httpx
from .http_client import AsyncHttpClient, HttpClient
from .pkcv import AsyncPKCVTransport, PKCVTransport


class BaseClientWrapper:
    def __init__(
        self,
        *,
        token: typing.Optional[typing.Union[str, typing.Callable[[], str]]] = None,
        account_sid: typing.Optional[str] = None,
        api_key_sid: typing.Optional[str] = None,
        credential_sid: typing.Optional[str] = None,
        private_key: typing.Optional[str] = None,
        headers: typing.Optional[typing.Dict[str, str]] = None,
        base_url: str,
        timeout: typing.Optional[float] = None,
    ):
        self._token = token
        self._pkcv_account_sid = account_sid
        self._pkcv_api_key_sid = api_key_sid
        self._pkcv_credential_sid = credential_sid
        self._pkcv_private_key = private_key
        self._headers = headers
        self._base_url = base_url
        self._timeout = timeout

    def get_headers(self) -> typing.Dict[str, str]:
        import platform

        headers: typing.Dict[str, str] = {
            "User-Agent": "fern_exhaustive/0.0.1",
            "X-Fern-Language": "Python",
            "X-Fern-Runtime": f"python/{platform.python_version()}",
            "X-Fern-Platform": f"{platform.system().lower()}/{platform.release()}",
            "X-Fern-SDK-Name": "fern_exhaustive",
            "X-Fern-SDK-Version": "0.0.1",
            **(self.get_custom_headers() or {}),
        }
        token = self._get_token()
        if token is not None:
            headers["Authorization"] = f"Bearer {token}"
        return headers

    def _get_token(self) -> typing.Optional[str]:
        if isinstance(self._token, str) or self._token is None:
            return self._token
        else:
            return self._token()

    def get_custom_headers(self) -> typing.Optional[typing.Dict[str, str]]:
        return self._headers

    def get_base_url(self) -> str:
        return self._base_url

    def get_timeout(self) -> typing.Optional[float]:
        return self._timeout


class SyncClientWrapper(BaseClientWrapper):
    def __init__(
        self,
        *,
        token: typing.Optional[typing.Union[str, typing.Callable[[], str]]] = None,
        account_sid: typing.Optional[str] = None,
        api_key_sid: typing.Optional[str] = None,
        credential_sid: typing.Optional[str] = None,
        private_key: typing.Optional[str] = None,
        headers: typing.Optional[typing.Dict[str, str]] = None,
        base_url: str,
        timeout: typing.Optional[float] = None,
        httpx_client: httpx.Client,
    ):
        super().__init__(
            token=token,
            account_sid=account_sid,
            api_key_sid=api_key_sid,
            credential_sid=credential_sid,
            private_key=private_key,
            headers=headers,
            base_url=base_url,
            timeout=timeout,
        )
        _resolved_account_sid = self._pkcv_account_sid
        if (
            _resolved_account_sid is not None
            and self._pkcv_api_key_sid is not None
            and self._pkcv_credential_sid is not None
            and self._pkcv_private_key is not None
        ):
            _pkcv_httpx_client = httpx.Client(
                transport=PKCVTransport(
                    transport=httpx_client._transport,
                    account_sid=_resolved_account_sid,
                    api_key_sid=self._pkcv_api_key_sid,
                    credential_sid=self._pkcv_credential_sid,
                    private_key=self._pkcv_private_key,
                ),
                timeout=httpx_client.timeout,
            )
        else:
            _pkcv_httpx_client = httpx_client
        self.httpx_client = HttpClient(
            httpx_client=_pkcv_httpx_client,
            base_headers=self.get_headers,
            base_timeout=self.get_timeout,
            base_url=self.get_base_url,
        )


class AsyncClientWrapper(BaseClientWrapper):
    def __init__(
        self,
        *,
        token: typing.Optional[typing.Union[str, typing.Callable[[], str]]] = None,
        account_sid: typing.Optional[str] = None,
        api_key_sid: typing.Optional[str] = None,
        credential_sid: typing.Optional[str] = None,
        private_key: typing.Optional[str] = None,
        headers: typing.Optional[typing.Dict[str, str]] = None,
        base_url: str,
        timeout: typing.Optional[float] = None,
        async_token: typing.Optional[typing.Callable[[], typing.Awaitable[str]]] = None,
        httpx_client: httpx.AsyncClient,
    ):
        super().__init__(
            token=token,
            account_sid=account_sid,
            api_key_sid=api_key_sid,
            credential_sid=credential_sid,
            private_key=private_key,
            headers=headers,
            base_url=base_url,
            timeout=timeout,
        )
        self._async_token = async_token
        _resolved_account_sid = self._pkcv_account_sid
        if (
            _resolved_account_sid is not None
            and self._pkcv_api_key_sid is not None
            and self._pkcv_credential_sid is not None
            and self._pkcv_private_key is not None
        ):
            _pkcv_httpx_client = httpx.AsyncClient(
                transport=AsyncPKCVTransport(
                    transport=httpx_client._transport,
                    account_sid=_resolved_account_sid,
                    api_key_sid=self._pkcv_api_key_sid,
                    credential_sid=self._pkcv_credential_sid,
                    private_key=self._pkcv_private_key,
                ),
                timeout=httpx_client.timeout,
            )
        else:
            _pkcv_httpx_client = httpx_client
        self.httpx_client = AsyncHttpClient(
            httpx_client=_pkcv_httpx_client,
            base_headers=self.get_headers,
            base_timeout=self.get_timeout,
            base_url=self.get_base_url,
            async_base_headers=self.async_get_headers,
        )

    async def async_get_headers(self) -> typing.Dict[str, str]:
        headers = self.get_headers()
        if self._async_token is not None:
            token = await self._async_token()
            headers["Authorization"] = f"Bearer {token}"
        return headers
