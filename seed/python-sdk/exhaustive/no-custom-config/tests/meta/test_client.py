# This file was auto-generated by Fern from our API Definition.

from seed import SeedExhaustive


def test_client_can_access_all_subclients() -> None:
    client = SeedExhaustive(base_url="https://api.example.com")
    assert client.endpoints is not None
    assert client.endpoints.container is not None
    assert client.endpoints.content_type is not None
    assert client.endpoints.enum is not None
    assert client.endpoints.http_methods is not None
    assert client.endpoints.object is not None
    assert client.endpoints.params is not None
    assert client.endpoints.primitive is not None
    assert client.endpoints.put is not None
    assert client.endpoints.union is not None
    assert client.endpoints.urls is not None
    assert client.inlined_requests is not None
    assert client.no_auth is not None
    assert client.no_req_body is not None
    assert client.req_with_headers is not None
    services = {attr for attr in dir(client) if not attr.startswith("_") and not callable(getattr(client, attr, None))}
    assert len(services) == 5
