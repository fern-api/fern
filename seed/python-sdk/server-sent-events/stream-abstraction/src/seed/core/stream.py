# This file was auto-generated by Fern from our API Definition.

# This file was auto-generated by Fern from our API Definition.

import types
import typing
from dataclasses import dataclass

T = typing.TypeVar("T")


@dataclass(frozen=True)
class StreamEvent(typing.Generic[T]):
    """A single event of a stream, exposing the server-sent event metadata alongside the parsed payload.

    `id`, `event` and `retry` are only populated for server-sent event streams; for JSON and text
    streams, which have no event metadata, they are always `None`.
    """

    data: T
    id: typing.Optional[str] = None
    event: typing.Optional[str] = None
    retry: typing.Optional[int] = None


class Stream(typing.Generic[T]):
    """A stream of parsed events.

    Iterating the stream yields the parsed payloads, while `with_metadata()` yields them wrapped in a
    `StreamEvent`, which also exposes the server-sent event `id`, `event` and `retry` fields:

        for chunk in client.service.stream(...):
            ...

        for event in client.service.stream(...).with_metadata():
            print(event.id, event.data)

    The HTTP request is only issued once the stream is first iterated. The response is released when
    the stream is exhausted, when iterating raises, when `close()` is called, when the stream is used
    as a context manager, and when the stream is garbage collected (closing the stream closes the
    underlying generator, which in turn closes the response).
    """

    def __init__(self, *, events: typing.Callable[[], typing.Generator[StreamEvent[T], None, None]]):
        self._open_events = events
        self._events: typing.Optional[typing.Generator[StreamEvent[T], None, None]] = None
        self._closed = False

    def with_metadata(self) -> typing.Iterator[StreamEvent[T]]:
        return self._events_generator()

    def close(self) -> None:
        self._closed = True
        events = self._events
        self._events = None
        if events is not None:
            events.close()

    def __iter__(self) -> "Stream[T]":
        return self

    def __next__(self) -> T:
        return next(self._events_generator()).data

    def __enter__(self) -> "Stream[T]":
        return self

    def __exit__(
        self,
        exc_type: typing.Optional[typing.Type[BaseException]],
        exc_value: typing.Optional[BaseException],
        traceback: typing.Optional[types.TracebackType],
    ) -> None:
        self.close()

    def _events_generator(self) -> typing.Generator[StreamEvent[T], None, None]:
        if self._closed:
            raise RuntimeError("The stream has already been closed.")
        if self._events is None:
            self._events = self._open_events()
        return self._events


class AsyncStream(typing.Generic[T]):
    """An asynchronous stream of parsed events.

    Iterating the stream yields the parsed payloads, while `with_metadata()` yields them wrapped in a
    `StreamEvent`, which also exposes the server-sent event `id`, `event` and `retry` fields:

        async for chunk in client.service.stream(...):
            ...

        async for event in (await client.service.stream(...)).with_metadata():
            print(event.id, event.data)

    The HTTP request is only issued once the stream is first iterated. The response is released when
    the stream is exhausted, when iterating raises, when `close()` is called, when the stream is used
    as an asynchronous context manager, and when the event loop finalizes the underlying asynchronous
    generator.
    """

    def __init__(self, *, events: typing.Callable[[], typing.AsyncGenerator[StreamEvent[T], None]]):
        self._open_events = events
        self._events: typing.Optional[typing.AsyncGenerator[StreamEvent[T], None]] = None
        self._closed = False

    def with_metadata(self) -> typing.AsyncIterator[StreamEvent[T]]:
        return self._events_generator()

    async def close(self) -> None:
        self._closed = True
        events = self._events
        self._events = None
        if events is not None:
            await events.aclose()

    async def aclose(self) -> None:
        await self.close()

    def __aiter__(self) -> "AsyncStream[T]":
        return self

    async def __anext__(self) -> T:
        event = await self._events_generator().__anext__()
        return event.data

    async def __aenter__(self) -> "AsyncStream[T]":
        return self

    async def __aexit__(
        self,
        exc_type: typing.Optional[typing.Type[BaseException]],
        exc_value: typing.Optional[BaseException],
        traceback: typing.Optional[types.TracebackType],
    ) -> None:
        await self.close()

    def __await__(self) -> typing.Generator[None, None, "AsyncStream[T]"]:
        """Returns the stream itself, so that it can be awaited before being iterated."""
        yield from ()
        return self

    def _events_generator(self) -> typing.AsyncGenerator[StreamEvent[T], None]:
        if self._closed:
            raise RuntimeError("The stream has already been closed.")
        if self._events is None:
            self._events = self._open_events()
        return self._events
