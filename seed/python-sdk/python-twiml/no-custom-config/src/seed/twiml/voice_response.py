# This file was auto-generated by Fern from our API Definition.

from typing import Any, List, Optional

from . import TwiML, format_value


class VoiceResponse(TwiML):
    """<Response> TwiML for Voice"""

    name = "Response"

    def __init__(self, **kwargs: Any) -> None:
        """Create a new <Response> element.

        :param kwargs: additional attributes"""
        super().__init__(**kwargs)

    def say(
        self,
        message: Optional[str] = None,
        *,
        voice: Optional[str] = None,
        loop: Optional[int] = None,
        language: Optional[str] = None,
        **kwargs: Any,
    ) -> "Say":
        """Create a <Say> element and append it as a child.

        :param message: Message to say
        :param voice: Voice to use
        :param loop: Times to loop message
        :param language: Message language
        :param kwargs: additional attributes

        :returns: the newly created <Say> element"""
        return self.nest(Say(message, voice=voice, loop=loop, language=language, **kwargs))

    def dial(
        self,
        number: Optional[str] = None,
        *,
        action: Optional[str] = None,
        method: Optional[str] = None,
        timeout: Optional[int] = None,
        hangup_on_star: Optional[bool] = None,
        caller_id: Optional[str] = None,
        record: Optional[str] = None,
        **kwargs: Any,
    ) -> "Dial":
        """Create a <Dial> element and append it as a child.

        :param number: Phone number to dial
        :param action: Action URL
        :param method: Action URL method
        :param timeout: Time to wait for answer
        :param hangup_on_star: Hangup call on star press
        :param caller_id: Caller ID to display
        :param record: Record the call
        :param kwargs: additional attributes

        :returns: the newly created <Dial> element"""
        return self.nest(
            Dial(
                number,
                action=action,
                method=method,
                timeout=timeout,
                hangup_on_star=hangup_on_star,
                caller_id=caller_id,
                record=record,
                **kwargs,
            )
        )

    def pause(self, *, length: Optional[int] = None, **kwargs: Any) -> "Pause":
        """Create a <Pause> element and append it as a child.

        :param length: Length in seconds to pause
        :param kwargs: additional attributes

        :returns: the newly created <Pause> element"""
        return self.nest(Pause(length=length, **kwargs))

    def gather(
        self,
        *,
        input: Optional[List[str]] = None,
        action: Optional[str] = None,
        num_digits: Optional[int] = None,
        **kwargs: Any,
    ) -> "Gather":
        """Create a <Gather> element and append it as a child.

        :param input: Input type Twilio should accept
        :param action: Action URL
        :param num_digits: Number of digits to collect
        :param kwargs: additional attributes

        :returns: the newly created <Gather> element"""
        return self.nest(Gather(input=input, action=action, num_digits=num_digits, **kwargs))

    def redirect(
        self,
        url: str,
        *,
        method: Optional[str] = None,
        **kwargs: Any,
    ) -> "Redirect":
        """Create a <Redirect> element and append it as a child.

        :param url: Redirect URL
        :param method: Redirect URL method
        :param kwargs: additional attributes

        :returns: the newly created <Redirect> element"""
        return self.nest(Redirect(url, method=method, **kwargs))


class Dial(TwiML):
    """<Dial> TwiML Verb"""

    name = "Dial"

    def __init__(
        self,
        number: Optional[str] = None,
        *,
        action: Optional[str] = None,
        method: Optional[str] = None,
        timeout: Optional[int] = None,
        hangup_on_star: Optional[bool] = None,
        caller_id: Optional[str] = None,
        record: Optional[str] = None,
        **kwargs: Any,
    ) -> None:
        """Create a new <Dial> element.

        :param number: Phone number to dial
        :param action: Action URL
        :param method: Action URL method
        :param timeout: Time to wait for answer
        :param hangup_on_star: Hangup call on star press
        :param caller_id: Caller ID to display
        :param record: Record the call
        :param kwargs: additional attributes"""
        super().__init__(
            action=action,
            method=method,
            timeout=timeout,
            hangup_on_star=hangup_on_star,
            caller_id=caller_id,
            record=record,
            **kwargs,
        )
        if number is not None:
            self.value = format_value(number)

    def number(
        self,
        phone_number: Optional[str] = None,
        *,
        send_digits: Optional[str] = None,
        status_callback_event: Optional[List[str]] = None,
        byoc: Optional[str] = None,
        **kwargs: Any,
    ) -> "Number":
        """Create a <Number> element and append it as a child.

        :param phone_number: Phone Number to dial
        :param send_digits: DTMF tones to play when the call is answered
        :param status_callback_event: Events to call status callback
        :param byoc: BYOC trunk SID
        :param kwargs: additional attributes

        :returns: the newly created <Number> element"""
        return self.nest(
            Number(
                phone_number, send_digits=send_digits, status_callback_event=status_callback_event, byoc=byoc, **kwargs
            )
        )


class Gather(TwiML):
    """<Gather> TwiML Verb"""

    name = "Gather"

    def __init__(
        self,
        *,
        input: Optional[List[str]] = None,
        action: Optional[str] = None,
        num_digits: Optional[int] = None,
        **kwargs: Any,
    ) -> None:
        """Create a new <Gather> element.

        :param input: Input type Twilio should accept
        :param action: Action URL
        :param num_digits: Number of digits to collect
        :param kwargs: additional attributes"""
        super().__init__(input=input, action=action, num_digits=num_digits, **kwargs)

    def say(
        self,
        message: Optional[str] = None,
        *,
        voice: Optional[str] = None,
        loop: Optional[int] = None,
        language: Optional[str] = None,
        **kwargs: Any,
    ) -> "Say":
        """Create a <Say> element and append it as a child.

        :param message: Message to say
        :param voice: Voice to use
        :param loop: Times to loop message
        :param language: Message language
        :param kwargs: additional attributes

        :returns: the newly created <Say> element"""
        return self.nest(Say(message, voice=voice, loop=loop, language=language, **kwargs))

    def pause(self, *, length: Optional[int] = None, **kwargs: Any) -> "Pause":
        """Create a <Pause> element and append it as a child.

        :param length: Length in seconds to pause
        :param kwargs: additional attributes

        :returns: the newly created <Pause> element"""
        return self.nest(Pause(length=length, **kwargs))

    def prompt(
        self,
        *,
        for_: Optional[str] = None,
        attempt: Optional[List[int]] = None,
        **kwargs: Any,
    ) -> "Prompt":
        """Create a <Prompt> element and append it as a child.

        :param for_: Name of the payment source data element
        :param attempt: Current attempt count
        :param kwargs: additional attributes

        :returns: the newly created <Prompt> element"""
        return self.nest(Prompt(for_=for_, attempt=attempt, **kwargs))


class Number(TwiML):
    """<Number> TwiML Noun"""

    name = "Number"

    def __init__(
        self,
        phone_number: Optional[str] = None,
        *,
        send_digits: Optional[str] = None,
        status_callback_event: Optional[List[str]] = None,
        byoc: Optional[str] = None,
        **kwargs: Any,
    ) -> None:
        """Create a new <Number> element.

        :param phone_number: Phone Number to dial
        :param send_digits: DTMF tones to play when the call is answered
        :param status_callback_event: Events to call status callback
        :param byoc: BYOC trunk SID
        :param kwargs: additional attributes"""
        super().__init__(send_digits=send_digits, status_callback_event=status_callback_event, byoc=byoc, **kwargs)
        if phone_number is not None:
            self.value = format_value(phone_number)


class Pause(TwiML):
    """<Pause> TwiML Verb"""

    name = "Pause"

    def __init__(self, *, length: Optional[int] = None, **kwargs: Any) -> None:
        """Create a new <Pause> element.

        :param length: Length in seconds to pause
        :param kwargs: additional attributes"""
        super().__init__(length=length, **kwargs)


class Prompt(TwiML):
    """<Prompt> Twiml Verb"""

    name = "Prompt"

    def __init__(
        self,
        *,
        for_: Optional[str] = None,
        attempt: Optional[List[int]] = None,
        **kwargs: Any,
    ) -> None:
        """Create a new <Prompt> element.

        :param for_: Name of the payment source data element
        :param attempt: Current attempt count
        :param kwargs: additional attributes"""
        super().__init__(for_=for_, attempt=attempt, **kwargs)

    def say(
        self,
        message: Optional[str] = None,
        *,
        voice: Optional[str] = None,
        loop: Optional[int] = None,
        language: Optional[str] = None,
        **kwargs: Any,
    ) -> "Say":
        """Create a <Say> element and append it as a child.

        :param message: Message to say
        :param voice: Voice to use
        :param loop: Times to loop message
        :param language: Message language
        :param kwargs: additional attributes

        :returns: the newly created <Say> element"""
        return self.nest(Say(message, voice=voice, loop=loop, language=language, **kwargs))

    def pause(self, *, length: Optional[int] = None, **kwargs: Any) -> "Pause":
        """Create a <Pause> element and append it as a child.

        :param length: Length in seconds to pause
        :param kwargs: additional attributes

        :returns: the newly created <Pause> element"""
        return self.nest(Pause(length=length, **kwargs))


class Redirect(TwiML):
    """<Redirect> TwiML Verb"""

    name = "Redirect"

    def __init__(
        self,
        url: str,
        *,
        method: Optional[str] = None,
        **kwargs: Any,
    ) -> None:
        """Create a new <Redirect> element.

        :param url: Redirect URL
        :param method: Redirect URL method
        :param kwargs: additional attributes"""
        super().__init__(method=method, **kwargs)
        self.value = format_value(url)


class Say(TwiML):
    """<Say> TwiML Verb"""

    name = "Say"

    def __init__(
        self,
        message: Optional[str] = None,
        *,
        voice: Optional[str] = None,
        loop: Optional[int] = None,
        language: Optional[str] = None,
        **kwargs: Any,
    ) -> None:
        """Create a new <Say> element.

        :param message: Message to say
        :param voice: Voice to use
        :param loop: Times to loop message
        :param language: Message language
        :param kwargs: additional attributes"""
        super().__init__(voice=voice, loop=loop, language=language, **kwargs)
        if message is not None:
            self.value = format_value(message)

    def break_(
        self,
        *,
        strength: Optional[str] = None,
        time: Optional[str] = None,
        **kwargs: Any,
    ) -> "SsmlBreak":
        """Create a <break> element and append it as a child.

        :param strength: Set a pause based on strength
        :param time: Set a pause to a specific length of time in seconds or milliseconds
        :param kwargs: additional attributes

        :returns: the newly created <break> element"""
        return self.nest(SsmlBreak(strength=strength, time=time, **kwargs))

    def emphasis(
        self,
        words: str,
        *,
        level: Optional[str] = None,
        **kwargs: Any,
    ) -> "SsmlEmphasis":
        """Create a <emphasis> element and append it as a child.

        :param words: Words to emphasize
        :param level: Specify the degree of emphasis
        :param kwargs: additional attributes

        :returns: the newly created <emphasis> element"""
        return self.nest(SsmlEmphasis(words, level=level, **kwargs))


class SsmlBreak(TwiML):
    """Adding a Pause in <Say>"""

    name = "break"

    def __init__(
        self,
        *,
        strength: Optional[str] = None,
        time: Optional[str] = None,
        **kwargs: Any,
    ) -> None:
        """Create a new <break> element.

        :param strength: Set a pause based on strength
        :param time: Set a pause to a specific length of time in seconds or milliseconds
        :param kwargs: additional attributes"""
        super().__init__(strength=strength, time=time, **kwargs)


class SsmlEmphasis(TwiML):
    """Emphasizing Words in <Say>"""

    name = "emphasis"

    def __init__(
        self,
        words: str,
        *,
        level: Optional[str] = None,
        **kwargs: Any,
    ) -> None:
        """Create a new <emphasis> element.

        :param words: Words to emphasize
        :param level: Specify the degree of emphasis
        :param kwargs: additional attributes"""
        super().__init__(level=level, **kwargs)
        self.value = format_value(words)

    def break_(
        self,
        *,
        strength: Optional[str] = None,
        time: Optional[str] = None,
        **kwargs: Any,
    ) -> "SsmlBreak":
        """Create a <break> element and append it as a child.

        :param strength: Set a pause based on strength
        :param time: Set a pause to a specific length of time in seconds or milliseconds
        :param kwargs: additional attributes

        :returns: the newly created <break> element"""
        return self.nest(SsmlBreak(strength=strength, time=time, **kwargs))

    def emphasis(
        self,
        words: str,
        *,
        level: Optional[str] = None,
        **kwargs: Any,
    ) -> "SsmlEmphasis":
        """Create a <emphasis> element and append it as a child.

        :param words: Words to emphasize
        :param level: Specify the degree of emphasis
        :param kwargs: additional attributes

        :returns: the newly created <emphasis> element"""
        return self.nest(SsmlEmphasis(words, level=level, **kwargs))
