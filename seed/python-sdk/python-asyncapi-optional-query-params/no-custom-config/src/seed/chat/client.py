# This file was auto-generated by Fern from our API Definition.

import typing
from contextlib import asynccontextmanager, contextmanager

import httpx
import websockets.exceptions
import websockets.sync.client as websockets_sync_client
from ..core.api_error import ApiError
from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..core.serialization import convert_and_respect_annotation_metadata
from ..types.session_settings import SessionSettings
from .raw_client import AsyncRawChatClient, RawChatClient
from .socket_client import AsyncChatSocketClient, ChatSocketClient

try:
    from websockets.legacy.client import connect as websockets_client_connect  # type: ignore
except ImportError:
    from websockets import connect as websockets_client_connect  # type: ignore


class ChatClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawChatClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawChatClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawChatClient
        """
        return self._raw_client

    @contextmanager
    def connect(
        self,
        *,
        simple_param: typing.Optional[str] = None,
        optional_bool: typing.Optional[bool] = None,
        no_default_string: typing.Optional[str] = None,
        complex_param: SessionSettings,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Iterator[ChatSocketClient]:
        """
        Test WebSocket channel with optional query parameters

        Parameters
        ----------
        simple_param : typing.Optional[str]
            A simple string parameter with a default value

        optional_bool : typing.Optional[bool]
            An optional boolean parameter with a default value

        no_default_string : typing.Optional[str]
            A string parameter without a default value - should still be optional

        complex_param : SessionSettings
            Session settings object

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ChatSocketClient
        """
        ws_url = self._raw_client._client_wrapper.get_base_url() + "/chat"
        query_params = httpx.QueryParams()
        if simple_param is not None:
            query_params = query_params.add("simple_param", simple_param)
        if optional_bool is not None:
            query_params = query_params.add("optional_bool", optional_bool)
        if no_default_string is not None:
            query_params = query_params.add("no_default_string", no_default_string)
        if (
            convert_and_respect_annotation_metadata(
                object_=complex_param, annotation=SessionSettings, direction="write"
            )
            is not None
        ):
            query_params = query_params.add(
                "complex_param",
                convert_and_respect_annotation_metadata(
                    object_=complex_param, annotation=SessionSettings, direction="write"
                ),
            )
        ws_url = ws_url + f"?{query_params}"
        headers = self._raw_client._client_wrapper.get_headers()
        if request_options and "additional_headers" in request_options:
            headers.update(request_options["additional_headers"])
        try:
            with websockets_sync_client.connect(ws_url, additional_headers=headers) as protocol:
                yield ChatSocketClient(websocket=protocol)
        except websockets.exceptions.InvalidStatusCode as exc:
            status_code: int = exc.status_code
            if status_code == 401:
                raise ApiError(
                    status_code=status_code,
                    headers=dict(headers),
                    body="Websocket initialized with invalid credentials.",
                )
            raise ApiError(
                status_code=status_code,
                headers=dict(headers),
                body="Unexpected error when initializing websocket connection.",
            )


class AsyncChatClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawChatClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawChatClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawChatClient
        """
        return self._raw_client

    @asynccontextmanager
    async def connect(
        self,
        *,
        simple_param: typing.Optional[str] = None,
        optional_bool: typing.Optional[bool] = None,
        no_default_string: typing.Optional[str] = None,
        complex_param: SessionSettings,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.AsyncIterator[AsyncChatSocketClient]:
        """
        Test WebSocket channel with optional query parameters

        Parameters
        ----------
        simple_param : typing.Optional[str]
            A simple string parameter with a default value

        optional_bool : typing.Optional[bool]
            An optional boolean parameter with a default value

        no_default_string : typing.Optional[str]
            A string parameter without a default value - should still be optional

        complex_param : SessionSettings
            Session settings object

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncChatSocketClient
        """
        ws_url = self._raw_client._client_wrapper.get_base_url() + "/chat"
        query_params = httpx.QueryParams()
        if simple_param is not None:
            query_params = query_params.add("simple_param", simple_param)
        if optional_bool is not None:
            query_params = query_params.add("optional_bool", optional_bool)
        if no_default_string is not None:
            query_params = query_params.add("no_default_string", no_default_string)
        if (
            convert_and_respect_annotation_metadata(
                object_=complex_param, annotation=SessionSettings, direction="write"
            )
            is not None
        ):
            query_params = query_params.add(
                "complex_param",
                convert_and_respect_annotation_metadata(
                    object_=complex_param, annotation=SessionSettings, direction="write"
                ),
            )
        ws_url = ws_url + f"?{query_params}"
        headers = self._raw_client._client_wrapper.get_headers()
        if request_options and "additional_headers" in request_options:
            headers.update(request_options["additional_headers"])
        try:
            async with websockets_client_connect(ws_url, extra_headers=headers) as protocol:
                yield AsyncChatSocketClient(websocket=protocol)
        except websockets.exceptions.InvalidStatusCode as exc:
            status_code: int = exc.status_code
            if status_code == 401:
                raise ApiError(
                    status_code=status_code,
                    headers=dict(headers),
                    body="Websocket initialized with invalid credentials.",
                )
            raise ApiError(
                status_code=status_code,
                headers=dict(headers),
                body="Unexpected error when initializing websocket connection.",
            )
