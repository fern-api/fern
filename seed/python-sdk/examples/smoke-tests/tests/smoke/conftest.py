# This file was auto-generated by Fern from our API Definition.

import os

import pytest

from seed import AsyncSeedExamples, SeedExamples

"""Pytest fixtures for smoke tests that run against a production environment."""


@pytest.fixture
def client() -> SeedExamples:
    return SeedExamples(
        token=os.getenv("SMOKE_TEST_TOKEN", "token"), base_url=os.getenv("SMOKE_TEST_BASE_URL", "base_url")
    )


@pytest.fixture
def async_client() -> AsyncSeedExamples:
    return AsyncSeedExamples(
        token=os.getenv("SMOKE_TEST_TOKEN", "token"), base_url=os.getenv("SMOKE_TEST_BASE_URL", "base_url")
    )
