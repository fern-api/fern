# This file was auto-generated by Fern from our API Definition.

import time

from ..core.webhook_signature import compute_hmac_signature, timing_safe_equal

TIMESTAMP_TOLERANCE_SECONDS = 300
SIGNATURE_PREFIX = "sha256="


class WebhooksHelper:
    """
    Verify an HMAC webhook signature.

    Extract the signature from the "x-webhook-signature" header and pass it as the signature_header parameter.
    Extract the timestamp from the "x-webhook-timestamp" header and pass it as the timestamp_header parameter.
    """

    @staticmethod
    def verify_signature(
        *,
        request_body: str,
        signature_header: str,
        signature_key: str,
        timestamp_header: str,
    ) -> bool:
        if request_body is None or signature_header is None or signature_key is None:
            return False

        if timestamp_header is None or timestamp_header == "":
            return False

        try:
            timestamp_value = int(timestamp_header)
        except ValueError:
            return False
        timestamp_ms = timestamp_value * 1000

        if abs(time.time() * 1000 - timestamp_ms) > TIMESTAMP_TOLERANCE_SECONDS * 1000:
            return False

        sig = (
            signature_header[len(SIGNATURE_PREFIX) :]
            if signature_header.startswith(SIGNATURE_PREFIX)
            else signature_header
        )

        payload = ".".join([timestamp_header, request_body])

        expected = compute_hmac_signature(
            payload=payload,
            secret=signature_key,
            algorithm="sha256",
            encoding="hex",
        )

        return timing_safe_equal(sig, expected)
