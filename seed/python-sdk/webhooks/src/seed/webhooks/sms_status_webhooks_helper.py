# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.webhook_signature import (
    compute_hash,
    compute_hmac_signature,
    get_webhook_query_parameter,
    notification_url_candidates,
    timing_safe_equal,
)


class SmsStatusWebhooksHelper:
    """
    Verify an HMAC webhook signature.

    Extract the signature from the "x-twilio-signature" header and pass it as the signature_header parameter.
    The request_body parameter accepts either a raw string or a mapping of POST body parameters.
    When a mapping is provided, keys are sorted and each key's values are deduped and sorted, then concatenated as key-value pairs before signing.
    This helper verifies both classic form-encoded and JSON requests: it branches at runtime on whether the body-hash query parameter is present on the notification URL.
    For a JSON request the raw body is verified against that separately-transmitted hash and the signature is checked over the notification URL only.
    Pass the exact raw body as request_body and the verbatim notification URL as notification_url.
    The signature is verified against several normalized forms of the notification URL, succeeding if any candidate matches.
    """

    @staticmethod
    def verify_signature(
        *,
        request_body: typing.Union[str, typing.Mapping[str, typing.Union[str, typing.Sequence[str]]]],
        signature_header: str,
        signature_key: str,
        notification_url: str,
    ) -> bool:
        if request_body is None or signature_header is None or signature_key is None:
            return False

        transmitted_body_hash = get_webhook_query_parameter(notification_url, "bodySHA256")
        if transmitted_body_hash is not None:
            expected_body_hash = compute_hash(
                payload=typing.cast(str, request_body),
                algorithm="sha256",
                encoding="hex",
            )
            if not timing_safe_equal(expected_body_hash, transmitted_body_hash):
                return False
        body_string = (
            request_body
            if isinstance(request_body, str)
            else "".join(
                "".join(key + v for v in sorted(set([value] if isinstance(value, str) else list(value))))
                for key, value in sorted(request_body.items(), key=lambda item: item[0])
            )
        )
        candidates = notification_url_candidates(notification_url, port_variants=True, legacy_query_encoding=True)
        for candidate_url in candidates:
            payload = candidate_url if transmitted_body_hash is not None else "".join([candidate_url, body_string])
            expected = compute_hmac_signature(
                payload=payload,
                secret=signature_key,
                algorithm="sha1",
                encoding="base64",
            )
            if timing_safe_equal(signature_header, expected):
                return True
        return False
