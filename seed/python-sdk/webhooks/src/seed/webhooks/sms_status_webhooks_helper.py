# This file was auto-generated by Fern from our API Definition.


from ..core.webhook_signature import (
    compute_hash,
    compute_hmac_signature,
    get_webhook_query_parameter,
    timing_safe_equal,
)


class SmsStatusWebhooksHelper:
    """
    Verify an HMAC webhook signature.

    Extract the signature from the "x-twilio-signature" header and pass it as the signature_header parameter.
    The raw request_body is hashed and compared against the "bodySHA256" query parameter on the notification_url before the HMAC signature is verified.
    """

    @staticmethod
    def verify_signature(
        *,
        request_body: str,
        signature_header: str,
        signature_key: str,
        notification_url: str,
    ) -> bool:
        if request_body is None or signature_header is None or signature_key is None:
            raise ValueError("Missing required parameters for webhook signature verification")

        expected_body_hash = compute_hash(
            payload=request_body,
            algorithm="sha256",
            encoding="hex",
        )
        transmitted_body_hash = get_webhook_query_parameter(notification_url, "bodySHA256")
        if transmitted_body_hash is None or not timing_safe_equal(expected_body_hash, transmitted_body_hash):
            return False

        payload = "".join([notification_url])

        expected = compute_hmac_signature(
            payload=payload,
            secret=signature_key,
            algorithm="sha1",
            encoding="base64",
        )

        return timing_safe_equal(signature_header, expected)
