# This file was auto-generated by Fern from our API Definition.

import base64
import hashlib
import hmac
import time
from typing import Dict, Union


def _compute_hmac_signature(
    *,
    payload: str,
    secret: str,
    algorithm: str,
    encoding: str,
) -> str:
    """
    Compute an HMAC signature for the given payload.

    Parameters
    ----------
    payload : str
        The string payload to sign.

    secret : str
        The secret key used for HMAC computation.

    algorithm : str
        The hash algorithm (e.g., "sha1", "sha256", "sha384", "sha512").

    encoding : str
        The encoding for the computed signature ("hex" or "base64").

    Returns
    -------
    str
        The computed HMAC signature.
    """
    hash_func = getattr(hashlib, algorithm, None)
    if hash_func is None:
        raise ValueError(f"Unsupported hash algorithm: {algorithm}")

    computed = hmac.new(
        secret.encode("utf-8"),
        payload.encode("utf-8"),
        hash_func,
    )

    if encoding == "base64":
        return base64.b64encode(computed.digest()).decode("utf-8")
    elif encoding == "hex":
        return computed.hexdigest()
    else:
        raise ValueError(f"Unsupported encoding: {encoding}")


def _timing_safe_equals(a: str, b: str) -> bool:
    """Compare two strings in constant time to prevent timing attacks."""
    return hmac.compare_digest(a, b)


def _parse_timestamp_ms(timestamp_header: str, format: str) -> int:
    """
    Parse a timestamp header value into milliseconds since epoch.

    Parameters
    ----------
    timestamp_header : str
        The raw timestamp header value.

    format : str
        One of "unix_seconds", "unix_millis", or "iso8601".

    Returns
    -------
    int
        Milliseconds since epoch.
    """
    if format == "unix_seconds":
        try:
            return int(timestamp_header) * 1000
        except ValueError:
            raise ValueError("Invalid timestamp format: expected unix seconds")
    elif format == "unix_millis":
        try:
            return int(timestamp_header)
        except ValueError:
            raise ValueError("Invalid timestamp format: expected unix milliseconds")
    elif format == "iso8601":
        import datetime

        try:
            dt = datetime.datetime.fromisoformat(timestamp_header.replace("Z", "+00:00"))
            return int(dt.timestamp() * 1000)
        except ValueError:
            raise ValueError("Invalid timestamp format: expected ISO 8601 date string")
    else:
        raise ValueError(f"Unsupported timestamp format: {format}")


def _get_current_time_ms() -> int:
    """Get current time in milliseconds since epoch."""
    return int(time.time() * 1000)


def _build_body_string(request_body: Union[str, Dict[str, str]], body_sort: bool) -> str:
    """Convert request body to a string, optionally sorting dict keys."""
    if isinstance(request_body, dict):
        if body_sort:
            return "".join(f"{k}{v}" for k, v in sorted(request_body.items()))
        return "".join(f"{k}{v}" for k, v in request_body.items())
    return request_body
