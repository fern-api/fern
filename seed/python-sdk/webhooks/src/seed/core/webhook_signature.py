# This file was auto-generated by Fern from our API Definition.

import base64
import hashlib
import hmac
import typing
from urllib.parse import parse_qs, parse_qsl, urlencode, urlsplit, urlunsplit

HmacAlgorithm = typing.Literal["sha1", "sha256", "sha384", "sha512"]
SignatureEncoding = typing.Literal["base64", "hex"]

_HASH_CONSTRUCTORS: typing.Dict[str, typing.Callable[[], "hashlib._Hash"]] = {
    "sha1": hashlib.sha1,
    "sha256": hashlib.sha256,
    "sha384": hashlib.sha384,
    "sha512": hashlib.sha512,
}


def compute_hmac_signature(
    *,
    payload: str,
    secret: str,
    algorithm: HmacAlgorithm,
    encoding: SignatureEncoding,
) -> str:
    """
    Compute the HMAC signature of a payload using the given secret, algorithm, and encoding.
    """
    digestmod = _HASH_CONSTRUCTORS[algorithm]
    mac = hmac.new(secret.encode("utf-8"), payload.encode("utf-8"), digestmod)
    if encoding == "hex":
        return mac.hexdigest()
    return base64.b64encode(mac.digest()).decode("utf-8")


def compute_hash(
    *,
    payload: str,
    algorithm: HmacAlgorithm,
    encoding: SignatureEncoding,
) -> str:
    """
    Compute an unkeyed digest of a payload using the given algorithm and encoding.

    This is distinct from compute_hmac_signature: no secret is involved. It is used to
    recompute the hash of a raw request body so it can be compared against a hash that a
    webhook provider transmits out-of-band (e.g. as a query parameter on the notification URL).
    """
    digestmod = _HASH_CONSTRUCTORS[algorithm]
    digest = digestmod()
    digest.update(payload.encode("utf-8"))
    if encoding == "hex":
        return digest.hexdigest()
    return base64.b64encode(digest.digest()).decode("utf-8")


def get_webhook_query_parameter(url: str, name: str) -> typing.Optional[str]:
    """
    Read a single query parameter from a URL without mutating or reordering the URL.

    Returns the first value for the parameter, or None if the URL cannot be parsed or the
    parameter is absent. This is read-only: it never reconstructs or normalizes the URL.
    """
    try:
        query = urlsplit(url).query
    except ValueError:
        return None
    values = parse_qs(query).get(name)
    if not values:
        return None
    return values[0]


def timing_safe_equal(a: str, b: str) -> bool:
    """
    Compare two strings in constant time to avoid leaking information through timing.
    """
    return hmac.compare_digest(a.encode("utf-8"), b.encode("utf-8"))


def notification_url_candidates(
    url: str,
    *,
    port_variants: bool,
    legacy_query_encoding: bool,
) -> typing.List[str]:
    """
    Build the list of normalized notification-URL forms to verify a webhook signature
    against. Some providers (e.g. Twilio) are inconsistent about whether the URL they
    signed carried a port and how its query string was encoded, so a signature is
    accepted if it matches the computation over ANY of these candidates.

    Mirrors twilio's addPort / removePort / buildUrlWithStandardPort /
    withLegacyQuerystring. Always includes at least the caller-supplied URL and never
    raises: an unparseable URL yields [url].
    """
    try:
        parsed = urlsplit(url)
        # urlsplit is lenient; force it to surface a malformed port here so an
        # unparseable URL falls back to [url] rather than raising later.
        _ = parsed.port
    except ValueError:
        return [url]

    port_forms = [_remove_port(url), _add_port(url)] if port_variants else [url]

    # A dict preserves insertion order while collapsing forms that coincide (e.g. a URL
    # that already carries a standard port, or a query-less URL under legacy encoding).
    candidates: typing.Dict[str, None] = {}
    candidates[url] = None
    for form in port_forms:
        candidates[form] = None
    if legacy_query_encoding:
        for form in port_forms:
            candidates[_with_legacy_querystring(form)] = None
    return list(candidates.keys())


def _build_url_with_standard_port(parsed: "typing.Any") -> str:
    port = ":443" if parsed.scheme == "https" else ":80"
    hostname = parsed.hostname or ""
    userinfo = ""
    if parsed.username:
        userinfo += parsed.username
    if parsed.password:
        userinfo += ":" + parsed.password
    if parsed.username or parsed.password:
        userinfo += "@"
    netloc = userinfo + hostname + port if hostname else userinfo
    return urlunsplit((parsed.scheme, netloc, parsed.path, parsed.query, parsed.fragment))


def _add_port(url: str) -> str:
    parsed = urlsplit(url)
    if not parsed.port:
        return _build_url_with_standard_port(parsed)
    return url


def _remove_port(url: str) -> str:
    parsed = urlsplit(url)
    hostname = parsed.hostname or ""
    userinfo = ""
    if parsed.username:
        userinfo += parsed.username
    if parsed.password:
        userinfo += ":" + parsed.password
    if parsed.username or parsed.password:
        userinfo += "@"
    netloc = userinfo + hostname
    return urlunsplit((parsed.scheme, netloc, parsed.path, parsed.query, parsed.fragment))


def _with_legacy_querystring(url: str) -> str:
    parsed = urlsplit(url)
    if parsed.query:
        # Re-encode the query with legacy form-encoding, reversing percent-encoding
        # differences introduced by WHATWG URL parsing.
        pairs = parse_qsl(parsed.query, keep_blank_values=True)
        requoted = urlencode(pairs)
        return urlunsplit((parsed.scheme, parsed.netloc, parsed.path, requoted, parsed.fragment))
    return url
