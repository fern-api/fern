# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import os
import typing

import httpx
from .core.api_error import ApiError
from .core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from .core.logging import LogConfig, Logger
from .core.oauth_token_provider import AsyncOAuthTokenProvider, OAuthTokenProvider
from .environment import SeedApiEnvironment

if typing.TYPE_CHECKING:
    from .auth.client import AsyncAuthClient, AuthClient
    from .pets.client import AsyncPetsClient, PetsClient
    from .system.client import AsyncSystemClient, SystemClient


class SeedApi:
    """
    Use this class to access the different functions within the SDK. You can instantiate any number of clients with different configuration that will propagate to these functions.

    Parameters
    ----------

    base_url : typing.Optional[str]
        The base url to use for requests from the client.

    client_id : str
        The client identifier used for authentication.

    client_secret : str
        The client secret used for authentication.

    scopes : str
    tenant : str
    timeout : typing.Optional[float]
        The timeout to be used, in seconds, for requests. By default the timeout is 60 seconds, unless a custom httpx client is used, in which case this default is not enforced.

    max_retries : typing.Optional[int]
        The default maximum number of retries for failed requests. Defaults to 2. Per-request `max_retries` in `request_options` takes precedence over this value.

    follow_redirects : typing.Optional[bool]
        Whether the default httpx client follows redirects or not, this is irrelevant if a custom httpx client is passed in.

    httpx_client : typing.Optional[httpx.Client]
        The httpx client to use for making requests, a preconfigured client is used by default, however this is useful should you want to pass in any custom httpx configuration.

    # or ...

    base_url : typing.Optional[str]
        The base url to use for requests from the client.

    token : typing.Callable[[], str]
        Authenticate by providing a callable that returns a pre-generated bearer token. In this mode, OAuth client credentials are not required.

    timeout : typing.Optional[float]
        The timeout to be used, in seconds, for requests. By default the timeout is 60 seconds, unless a custom httpx client is used, in which case this default is not enforced.

    max_retries : typing.Optional[int]
        The default maximum number of retries for failed requests. Defaults to 2. Per-request `max_retries` in `request_options` takes precedence over this value.

    follow_redirects : typing.Optional[bool]
        Whether the default httpx client follows redirects or not, this is irrelevant if a custom httpx client is passed in.

    httpx_client : typing.Optional[httpx.Client]
        The httpx client to use for making requests, a preconfigured client is used by default, however this is useful should you want to pass in any custom httpx configuration.

    Examples
    --------
    from seed import SeedApi

    client = SeedApi(
        client_id="YOUR_CLIENT_ID",
        client_secret="YOUR_CLIENT_SECRET",
    )

    # or ...

    from seed import SeedApi

    client = SeedApi(
        base_url="https://yourhost.com/path/to/api",
        token="YOUR_BEARER_TOKEN",
    )
    """

    @typing.overload
    def __init__(
        self,
        *,
        base_url: typing.Optional[str] = None,
        environment: SeedApiEnvironment = SeedApiEnvironment.DEFAULT,
        headers: typing.Optional[typing.Dict[str, str]] = None,
        timeout: typing.Optional[float] = None,
        max_retries: typing.Optional[int] = None,
        stream_reconnection_enabled: typing.Optional[bool] = None,
        max_stream_reconnection_attempts: typing.Optional[int] = None,
        follow_redirects: typing.Optional[bool] = True,
        httpx_client: typing.Optional[httpx.Client] = None,
        logging: typing.Optional[typing.Union[LogConfig, Logger]] = None,
        client_id: typing.Optional[str] = os.getenv("ACME_CLIENT_ID"),
        client_secret: typing.Optional[str] = os.getenv("ACME_CLIENT_SECRET"),
        scopes: str,
        tenant: str,
    ): ...
    @typing.overload
    def __init__(
        self,
        *,
        base_url: typing.Optional[str] = None,
        environment: SeedApiEnvironment = SeedApiEnvironment.DEFAULT,
        headers: typing.Optional[typing.Dict[str, str]] = None,
        timeout: typing.Optional[float] = None,
        max_retries: typing.Optional[int] = None,
        stream_reconnection_enabled: typing.Optional[bool] = None,
        max_stream_reconnection_attempts: typing.Optional[int] = None,
        follow_redirects: typing.Optional[bool] = True,
        httpx_client: typing.Optional[httpx.Client] = None,
        logging: typing.Optional[typing.Union[LogConfig, Logger]] = None,
        token: typing.Callable[[], str],
    ): ...
    def __init__(
        self,
        *,
        base_url: typing.Optional[str] = None,
        environment: SeedApiEnvironment = SeedApiEnvironment.DEFAULT,
        headers: typing.Optional[typing.Dict[str, str]] = None,
        client_id: typing.Optional[str] = os.getenv("ACME_CLIENT_ID"),
        client_secret: typing.Optional[str] = os.getenv("ACME_CLIENT_SECRET"),
        scopes: typing.Optional[str] = None,
        tenant: typing.Optional[str] = None,
        token: typing.Optional[typing.Callable[[], str]] = None,
        _token_getter_override: typing.Optional[typing.Callable[[], str]] = None,
        timeout: typing.Optional[float] = None,
        max_retries: typing.Optional[int] = None,
        stream_reconnection_enabled: typing.Optional[bool] = None,
        max_stream_reconnection_attempts: typing.Optional[int] = None,
        follow_redirects: typing.Optional[bool] = True,
        httpx_client: typing.Optional[httpx.Client] = None,
        logging: typing.Optional[typing.Union[LogConfig, Logger]] = None,
    ):
        _defaulted_timeout = timeout if timeout is not None else 60 if httpx_client is None else None
        _defaulted_max_retries = max_retries if max_retries is not None else 2
        if token is not None:
            self._client_wrapper = SyncClientWrapper(
                base_url=_get_base_url(base_url=base_url, environment=environment),
                headers=headers,
                httpx_client=httpx_client
                if httpx_client is not None
                else httpx.Client(timeout=_defaulted_timeout, follow_redirects=follow_redirects)
                if follow_redirects is not None
                else httpx.Client(timeout=_defaulted_timeout),
                timeout=_defaulted_timeout,
                max_retries=_defaulted_max_retries,
                stream_reconnection_enabled=stream_reconnection_enabled,
                max_stream_reconnection_attempts=max_stream_reconnection_attempts,
                logging=logging,
                token=_token_getter_override if _token_getter_override is not None else token,
            )
        elif client_id is not None and client_secret is not None:
            if scopes is None:
                raise ApiError(body="The 'scopes' parameter is required when using 'client_id' and 'client_secret'")
            if tenant is None:
                raise ApiError(body="The 'tenant' parameter is required when using 'client_id' and 'client_secret'")
            oauth_token_provider = OAuthTokenProvider(
                client_id=client_id,
                client_secret=client_secret,
                scopes=scopes,
                tenant=tenant,
                client_wrapper=SyncClientWrapper(
                    base_url=_get_base_url(base_url=base_url, environment=environment),
                    headers=headers,
                    httpx_client=httpx_client
                    if httpx_client is not None
                    else httpx.Client(timeout=_defaulted_timeout, follow_redirects=follow_redirects)
                    if follow_redirects is not None
                    else httpx.Client(timeout=_defaulted_timeout),
                    timeout=_defaulted_timeout,
                    max_retries=_defaulted_max_retries,
                    stream_reconnection_enabled=stream_reconnection_enabled,
                    max_stream_reconnection_attempts=max_stream_reconnection_attempts,
                    logging=logging,
                ),
            )
            self._client_wrapper = SyncClientWrapper(
                base_url=_get_base_url(base_url=base_url, environment=environment),
                headers=headers,
                token=_token_getter_override if _token_getter_override is not None else oauth_token_provider.get_token,
                httpx_client=httpx_client
                if httpx_client is not None
                else httpx.Client(timeout=_defaulted_timeout, follow_redirects=follow_redirects)
                if follow_redirects is not None
                else httpx.Client(timeout=_defaulted_timeout),
                timeout=_defaulted_timeout,
                max_retries=_defaulted_max_retries,
                stream_reconnection_enabled=stream_reconnection_enabled,
                max_stream_reconnection_attempts=max_stream_reconnection_attempts,
                logging=logging,
            )
        else:
            raise ApiError(
                body="The client must be instantiated with either 'token' or both 'client_id' and 'client_secret'"
            )
        self._auth: typing.Optional[AuthClient] = None
        self._system: typing.Optional[SystemClient] = None
        self._pets: typing.Optional[PetsClient] = None

    @property
    def auth(self):
        if self._auth is None:
            from .auth.client import AuthClient  # noqa: E402

            self._auth = AuthClient(client_wrapper=self._client_wrapper)
        return self._auth

    @property
    def system(self):
        if self._system is None:
            from .system.client import SystemClient  # noqa: E402

            self._system = SystemClient(client_wrapper=self._client_wrapper)
        return self._system

    @property
    def pets(self):
        if self._pets is None:
            from .pets.client import PetsClient  # noqa: E402

            self._pets = PetsClient(client_wrapper=self._client_wrapper)
        return self._pets


def _make_default_async_client(
    timeout: typing.Optional[float],
    follow_redirects: typing.Optional[bool],
) -> httpx.AsyncClient:
    try:
        import httpx_aiohttp  # type: ignore[import-not-found]
    except ImportError:
        pass
    else:
        if follow_redirects is not None:
            return httpx_aiohttp.HttpxAiohttpClient(timeout=timeout, follow_redirects=follow_redirects)
        return httpx_aiohttp.HttpxAiohttpClient(timeout=timeout)

    if follow_redirects is not None:
        return httpx.AsyncClient(timeout=timeout, follow_redirects=follow_redirects)
    return httpx.AsyncClient(timeout=timeout)


class AsyncSeedApi:
    """
    Use this class to access the different functions within the SDK. You can instantiate any number of clients with different configuration that will propagate to these functions.

    Parameters
    ----------

    base_url : typing.Optional[str]
        The base url to use for requests from the client.

    client_id : str
        The client identifier used for authentication.

    client_secret : str
        The client secret used for authentication.

    scopes : str
    tenant : str
    timeout : typing.Optional[float]
        The timeout to be used, in seconds, for requests. By default the timeout is 60 seconds, unless a custom httpx client is used, in which case this default is not enforced.

    max_retries : typing.Optional[int]
        The default maximum number of retries for failed requests. Defaults to 2. Per-request `max_retries` in `request_options` takes precedence over this value.

    follow_redirects : typing.Optional[bool]
        Whether the default httpx client follows redirects or not, this is irrelevant if a custom httpx client is passed in.

    httpx_client : typing.Optional[httpx.AsyncClient]
        The httpx client to use for making requests, a preconfigured client is used by default, however this is useful should you want to pass in any custom httpx configuration.

    # or ...

    base_url : typing.Optional[str]
        The base url to use for requests from the client.

    token : typing.Callable[[], str]
        Authenticate by providing a callable that returns a pre-generated bearer token. In this mode, OAuth client credentials are not required.

    timeout : typing.Optional[float]
        The timeout to be used, in seconds, for requests. By default the timeout is 60 seconds, unless a custom httpx client is used, in which case this default is not enforced.

    max_retries : typing.Optional[int]
        The default maximum number of retries for failed requests. Defaults to 2. Per-request `max_retries` in `request_options` takes precedence over this value.

    follow_redirects : typing.Optional[bool]
        Whether the default httpx client follows redirects or not, this is irrelevant if a custom httpx client is passed in.

    httpx_client : typing.Optional[httpx.AsyncClient]
        The httpx client to use for making requests, a preconfigured client is used by default, however this is useful should you want to pass in any custom httpx configuration.

    Examples
    --------
    from seed import AsyncSeedApi

    client = AsyncSeedApi(
        client_id="YOUR_CLIENT_ID",
        client_secret="YOUR_CLIENT_SECRET",
    )

    # or ...

    from seed import AsyncSeedApi

    client = AsyncSeedApi(
        base_url="https://yourhost.com/path/to/api",
        token="YOUR_BEARER_TOKEN",
    )
    """

    @typing.overload
    def __init__(
        self,
        *,
        base_url: typing.Optional[str] = None,
        environment: SeedApiEnvironment = SeedApiEnvironment.DEFAULT,
        headers: typing.Optional[typing.Dict[str, str]] = None,
        timeout: typing.Optional[float] = None,
        max_retries: typing.Optional[int] = None,
        stream_reconnection_enabled: typing.Optional[bool] = None,
        max_stream_reconnection_attempts: typing.Optional[int] = None,
        follow_redirects: typing.Optional[bool] = True,
        httpx_client: typing.Optional[httpx.AsyncClient] = None,
        logging: typing.Optional[typing.Union[LogConfig, Logger]] = None,
        client_id: typing.Optional[str] = os.getenv("ACME_CLIENT_ID"),
        client_secret: typing.Optional[str] = os.getenv("ACME_CLIENT_SECRET"),
        scopes: str,
        tenant: str,
    ): ...
    @typing.overload
    def __init__(
        self,
        *,
        base_url: typing.Optional[str] = None,
        environment: SeedApiEnvironment = SeedApiEnvironment.DEFAULT,
        headers: typing.Optional[typing.Dict[str, str]] = None,
        timeout: typing.Optional[float] = None,
        max_retries: typing.Optional[int] = None,
        stream_reconnection_enabled: typing.Optional[bool] = None,
        max_stream_reconnection_attempts: typing.Optional[int] = None,
        follow_redirects: typing.Optional[bool] = True,
        httpx_client: typing.Optional[httpx.AsyncClient] = None,
        logging: typing.Optional[typing.Union[LogConfig, Logger]] = None,
        token: typing.Callable[[], str],
    ): ...
    def __init__(
        self,
        *,
        base_url: typing.Optional[str] = None,
        environment: SeedApiEnvironment = SeedApiEnvironment.DEFAULT,
        headers: typing.Optional[typing.Dict[str, str]] = None,
        client_id: typing.Optional[str] = os.getenv("ACME_CLIENT_ID"),
        client_secret: typing.Optional[str] = os.getenv("ACME_CLIENT_SECRET"),
        scopes: typing.Optional[str] = None,
        tenant: typing.Optional[str] = None,
        token: typing.Optional[typing.Callable[[], str]] = None,
        _token_getter_override: typing.Optional[typing.Callable[[], str]] = None,
        timeout: typing.Optional[float] = None,
        max_retries: typing.Optional[int] = None,
        stream_reconnection_enabled: typing.Optional[bool] = None,
        max_stream_reconnection_attempts: typing.Optional[int] = None,
        follow_redirects: typing.Optional[bool] = True,
        httpx_client: typing.Optional[httpx.AsyncClient] = None,
        logging: typing.Optional[typing.Union[LogConfig, Logger]] = None,
    ):
        _defaulted_timeout = timeout if timeout is not None else 60 if httpx_client is None else None
        _defaulted_max_retries = max_retries if max_retries is not None else 2
        if token is not None:
            self._client_wrapper = AsyncClientWrapper(
                base_url=_get_base_url(base_url=base_url, environment=environment),
                headers=headers,
                httpx_client=httpx_client
                if httpx_client is not None
                else _make_default_async_client(timeout=_defaulted_timeout, follow_redirects=follow_redirects),
                timeout=_defaulted_timeout,
                max_retries=_defaulted_max_retries,
                stream_reconnection_enabled=stream_reconnection_enabled,
                max_stream_reconnection_attempts=max_stream_reconnection_attempts,
                logging=logging,
                token=_token_getter_override if _token_getter_override is not None else token,
            )
        elif client_id is not None and client_secret is not None:
            if scopes is None:
                raise ApiError(body="The 'scopes' parameter is required when using 'client_id' and 'client_secret'")
            if tenant is None:
                raise ApiError(body="The 'tenant' parameter is required when using 'client_id' and 'client_secret'")
            oauth_token_provider = AsyncOAuthTokenProvider(
                client_id=client_id,
                client_secret=client_secret,
                scopes=scopes,
                tenant=tenant,
                client_wrapper=AsyncClientWrapper(
                    base_url=_get_base_url(base_url=base_url, environment=environment),
                    headers=headers,
                    httpx_client=httpx_client
                    if httpx_client is not None
                    else httpx.AsyncClient(timeout=_defaulted_timeout, follow_redirects=follow_redirects)
                    if follow_redirects is not None
                    else httpx.AsyncClient(timeout=_defaulted_timeout),
                    timeout=_defaulted_timeout,
                    max_retries=_defaulted_max_retries,
                    stream_reconnection_enabled=stream_reconnection_enabled,
                    max_stream_reconnection_attempts=max_stream_reconnection_attempts,
                    logging=logging,
                ),
            )
            self._client_wrapper = AsyncClientWrapper(
                base_url=_get_base_url(base_url=base_url, environment=environment),
                headers=headers,
                token=_token_getter_override,
                async_token=oauth_token_provider.get_token,
                httpx_client=httpx_client
                if httpx_client is not None
                else _make_default_async_client(timeout=_defaulted_timeout, follow_redirects=follow_redirects),
                timeout=_defaulted_timeout,
                max_retries=_defaulted_max_retries,
                stream_reconnection_enabled=stream_reconnection_enabled,
                max_stream_reconnection_attempts=max_stream_reconnection_attempts,
                logging=logging,
            )
        else:
            raise ApiError(
                body="The client must be instantiated with either 'token' or both 'client_id' and 'client_secret'"
            )
        self._auth: typing.Optional[AsyncAuthClient] = None
        self._system: typing.Optional[AsyncSystemClient] = None
        self._pets: typing.Optional[AsyncPetsClient] = None

    @property
    def auth(self):
        if self._auth is None:
            from .auth.client import AsyncAuthClient  # noqa: E402

            self._auth = AsyncAuthClient(client_wrapper=self._client_wrapper)
        return self._auth

    @property
    def system(self):
        if self._system is None:
            from .system.client import AsyncSystemClient  # noqa: E402

            self._system = AsyncSystemClient(client_wrapper=self._client_wrapper)
        return self._system

    @property
    def pets(self):
        if self._pets is None:
            from .pets.client import AsyncPetsClient  # noqa: E402

            self._pets = AsyncPetsClient(client_wrapper=self._client_wrapper)
        return self._pets


def _get_base_url(*, base_url: typing.Optional[str] = None, environment: SeedApiEnvironment) -> str:
    if base_url is not None:
        return base_url
    elif environment is not None:
        return environment.value
    else:
        raise Exception("Please pass in either base_url or environment to construct the client")
