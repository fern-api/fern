# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import os
import typing

import httpx
from .core.client_wrapper import AsyncClientWrapper, SyncClientWrapper

if typing.TYPE_CHECKING:
    from .dummy.client import AsyncDummyClient, DummyClient


class SeedRegionalRouting:
    """
    Use this class to access the different functions within the SDK. You can instantiate any number of clients with different configuration that will propagate to these functions.

    Parameters
    ----------
    base_url : typing.Optional[str]
        The base url to use for requests from the client. Overrides the URL constructed from region/edge.

    region : typing.Optional[str]
        The region to use for requests from the client.

    edge : typing.Optional[str]
        The edge to use for requests from the client.

    token : typing.Union[str, typing.Callable[[], str]]
    headers : typing.Optional[typing.Dict[str, str]]
        Additional headers to send with every request.

    timeout : typing.Optional[float]
        The timeout to be used, in seconds, for requests. By default the timeout is 60 seconds, unless a custom httpx client is used, in which case this default is not enforced.

    follow_redirects : typing.Optional[bool]
        Whether the default httpx client follows redirects or not, this is irrelevant if a custom httpx client is passed in.

    httpx_client : typing.Optional[httpx.Client]
        The httpx client to use for making requests, a preconfigured client is used by default, however this is useful should you want to pass in any custom httpx configuration.

    Examples
    --------
    from seed import SeedRegionalRouting

    client = SeedRegionalRouting(
        token="YOUR_TOKEN",
        base_url="https://yourhost.com/path/to/api",
    )
    """

    def __init__(
        self,
        *,
        base_url: typing.Optional[str] = None,
        region: typing.Optional[str] = os.getenv("EXAMPLE_REGION"),
        edge: typing.Optional[str] = os.getenv("EXAMPLE_EDGE"),
        token: typing.Union[str, typing.Callable[[], str]],
        headers: typing.Optional[typing.Dict[str, str]] = None,
        timeout: typing.Optional[float] = None,
        follow_redirects: typing.Optional[bool] = True,
        httpx_client: typing.Optional[httpx.Client] = None,
    ):
        _defaulted_timeout = (
            timeout if timeout is not None else 60 if httpx_client is None else httpx_client.timeout.read
        )
        self._client_wrapper = SyncClientWrapper(
            base_url=_get_base_url_from_template(base_url=base_url, region=region, edge=edge),
            token=token,
            headers=headers,
            httpx_client=httpx_client
            if httpx_client is not None
            else httpx.Client(timeout=_defaulted_timeout, follow_redirects=follow_redirects)
            if follow_redirects is not None
            else httpx.Client(timeout=_defaulted_timeout),
            timeout=_defaulted_timeout,
        )
        self._dummy: typing.Optional[DummyClient] = None

    @property
    def dummy(self):
        if self._dummy is None:
            from .dummy.client import DummyClient  # noqa: E402

            self._dummy = DummyClient(client_wrapper=self._client_wrapper)
        return self._dummy


class AsyncSeedRegionalRouting:
    """
    Use this class to access the different functions within the SDK. You can instantiate any number of clients with different configuration that will propagate to these functions.

    Parameters
    ----------
    base_url : typing.Optional[str]
        The base url to use for requests from the client. Overrides the URL constructed from region/edge.

    region : typing.Optional[str]
        The region to use for requests from the client.

    edge : typing.Optional[str]
        The edge to use for requests from the client.

    token : typing.Union[str, typing.Callable[[], str]]
    headers : typing.Optional[typing.Dict[str, str]]
        Additional headers to send with every request.

    timeout : typing.Optional[float]
        The timeout to be used, in seconds, for requests. By default the timeout is 60 seconds, unless a custom httpx client is used, in which case this default is not enforced.

    follow_redirects : typing.Optional[bool]
        Whether the default httpx client follows redirects or not, this is irrelevant if a custom httpx client is passed in.

    httpx_client : typing.Optional[httpx.AsyncClient]
        The httpx client to use for making requests, a preconfigured client is used by default, however this is useful should you want to pass in any custom httpx configuration.

    Examples
    --------
    from seed import AsyncSeedRegionalRouting

    client = AsyncSeedRegionalRouting(
        token="YOUR_TOKEN",
        base_url="https://yourhost.com/path/to/api",
    )
    """

    def __init__(
        self,
        *,
        base_url: typing.Optional[str] = None,
        region: typing.Optional[str] = os.getenv("EXAMPLE_REGION"),
        edge: typing.Optional[str] = os.getenv("EXAMPLE_EDGE"),
        token: typing.Union[str, typing.Callable[[], str]],
        headers: typing.Optional[typing.Dict[str, str]] = None,
        timeout: typing.Optional[float] = None,
        follow_redirects: typing.Optional[bool] = True,
        httpx_client: typing.Optional[httpx.AsyncClient] = None,
    ):
        _defaulted_timeout = (
            timeout if timeout is not None else 60 if httpx_client is None else httpx_client.timeout.read
        )
        self._client_wrapper = AsyncClientWrapper(
            base_url=_get_base_url_from_template(base_url=base_url, region=region, edge=edge),
            token=token,
            headers=headers,
            httpx_client=httpx_client
            if httpx_client is not None
            else httpx.AsyncClient(timeout=_defaulted_timeout, follow_redirects=follow_redirects)
            if follow_redirects is not None
            else httpx.AsyncClient(timeout=_defaulted_timeout),
            timeout=_defaulted_timeout,
        )
        self._dummy: typing.Optional[AsyncDummyClient] = None

    @property
    def dummy(self):
        if self._dummy is None:
            from .dummy.client import AsyncDummyClient  # noqa: E402

            self._dummy = AsyncDummyClient(client_wrapper=self._client_wrapper)
        return self._dummy


def _get_base_url_from_template(
    *, base_url: typing.Optional[str] = None, region: typing.Optional[str] = None, edge: typing.Optional[str] = None
) -> str:
    if base_url is not None:
        return base_url
    template = "https://api.{region}.{edge}.example.com"
    if region is None:
        raise Exception("region is required when base_url is not provided")
    template = template.replace("{region}", region)
    if edge is None:
        raise Exception("edge is required when base_url is not provided")
    template = template.replace("{edge}", edge)
    return template
