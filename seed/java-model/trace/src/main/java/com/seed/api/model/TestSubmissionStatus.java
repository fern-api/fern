/**
 * This file was auto-generated by Fern from our API Definition.
 */

package com.seed.api.model;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.util.Objects;
import java.util.Optional;

public final class TestSubmissionStatus {
  private final Value value;

  @JsonCreator(
      mode = JsonCreator.Mode.DELEGATING
  )
  private TestSubmissionStatus(Value value) {
    this.value = value;
  }

  public <T> T visit(Visitor<T> visitor) {
    return value.visit(visitor);
  }

  public static TestSubmissionStatus stopped(TestSubmissionStatusStopped value) {
    return new TestSubmissionStatus(new StoppedValue(value));
  }

  public static TestSubmissionStatus errored(TestSubmissionStatusErrored value) {
    return new TestSubmissionStatus(new ErroredValue(value));
  }

  public static TestSubmissionStatus running(TestSubmissionStatusRunning value) {
    return new TestSubmissionStatus(new RunningValue(value));
  }

  public static TestSubmissionStatus testCaseIdToState(
      TestSubmissionStatusTestCaseIdToState value) {
    return new TestSubmissionStatus(new TestCaseIdToStateValue(value));
  }

  public boolean isStopped() {
    return value instanceof StoppedValue;
  }

  public boolean isErrored() {
    return value instanceof ErroredValue;
  }

  public boolean isRunning() {
    return value instanceof RunningValue;
  }

  public boolean isTestCaseIdToState() {
    return value instanceof TestCaseIdToStateValue;
  }

  public boolean _isUnknown() {
    return value instanceof _UnknownValue;
  }

  public Optional<TestSubmissionStatusStopped> getStopped() {
    if (isStopped()) {
      return Optional.of(((StoppedValue) value).value);
    }
    return Optional.empty();
  }

  public Optional<TestSubmissionStatusErrored> getErrored() {
    if (isErrored()) {
      return Optional.of(((ErroredValue) value).value);
    }
    return Optional.empty();
  }

  public Optional<TestSubmissionStatusRunning> getRunning() {
    if (isRunning()) {
      return Optional.of(((RunningValue) value).value);
    }
    return Optional.empty();
  }

  public Optional<TestSubmissionStatusTestCaseIdToState> getTestCaseIdToState() {
    if (isTestCaseIdToState()) {
      return Optional.of(((TestCaseIdToStateValue) value).value);
    }
    return Optional.empty();
  }

  public Optional<Object> _getUnknown() {
    if (_isUnknown()) {
      return Optional.of(((_UnknownValue) value).value);
    }
    return Optional.empty();
  }

  @Override
  public boolean equals(Object other) {
    if (this == other) return true;
    return other instanceof TestSubmissionStatus && value.equals(((TestSubmissionStatus) other).value);
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public String toString() {
    return value.toString();
  }

  @JsonValue
  private Value getValue() {
    return this.value;
  }

  public interface Visitor<T> {
    T visitStopped(TestSubmissionStatusStopped stopped);

    T visitErrored(TestSubmissionStatusErrored errored);

    T visitRunning(TestSubmissionStatusRunning running);

    T visitTestCaseIdToState(TestSubmissionStatusTestCaseIdToState testCaseIdToState);

    T _visitUnknown(Object unknownType);
  }

  @JsonTypeInfo(
      use = JsonTypeInfo.Id.NAME,
      property = "type",
      visible = true,
      defaultImpl = _UnknownValue.class
  )
  @JsonSubTypes({
      @JsonSubTypes.Type(StoppedValue.class),
      @JsonSubTypes.Type(ErroredValue.class),
      @JsonSubTypes.Type(RunningValue.class),
      @JsonSubTypes.Type(TestCaseIdToStateValue.class)
  })
  @JsonIgnoreProperties(
      ignoreUnknown = true
  )
  private interface Value {
    <T> T visit(Visitor<T> visitor);
  }

  @JsonTypeName("stopped")
  @JsonIgnoreProperties("type")
  private static final class StoppedValue implements Value {
    @JsonUnwrapped
    @JsonIgnoreProperties(
        value = "type",
        allowSetters = true
    )
    private TestSubmissionStatusStopped value;

    @JsonCreator(
        mode = JsonCreator.Mode.PROPERTIES
    )
    private StoppedValue() {
    }

    private StoppedValue(TestSubmissionStatusStopped value) {
      this.value = value;
    }

    @java.lang.Override
    public <T> T visit(Visitor<T> visitor) {
      return visitor.visitStopped(value);
    }

    @java.lang.Override
    public boolean equals(Object other) {
      if (this == other) return true;
      return other instanceof StoppedValue && equalTo((StoppedValue) other);
    }

    private boolean equalTo(StoppedValue other) {
      return value.equals(other.value);
    }

    @java.lang.Override
    public int hashCode() {
      return Objects.hash(this.value);
    }

    @java.lang.Override
    public String toString() {
      return "TestSubmissionStatus{" + "value: " + value + "}";
    }
  }

  @JsonTypeName("errored")
  @JsonIgnoreProperties("type")
  private static final class ErroredValue implements Value {
    @JsonUnwrapped
    @JsonIgnoreProperties(
        value = "type",
        allowSetters = true
    )
    private TestSubmissionStatusErrored value;

    @JsonCreator(
        mode = JsonCreator.Mode.PROPERTIES
    )
    private ErroredValue() {
    }

    private ErroredValue(TestSubmissionStatusErrored value) {
      this.value = value;
    }

    @java.lang.Override
    public <T> T visit(Visitor<T> visitor) {
      return visitor.visitErrored(value);
    }

    @java.lang.Override
    public boolean equals(Object other) {
      if (this == other) return true;
      return other instanceof ErroredValue && equalTo((ErroredValue) other);
    }

    private boolean equalTo(ErroredValue other) {
      return value.equals(other.value);
    }

    @java.lang.Override
    public int hashCode() {
      return Objects.hash(this.value);
    }

    @java.lang.Override
    public String toString() {
      return "TestSubmissionStatus{" + "value: " + value + "}";
    }
  }

  @JsonTypeName("running")
  @JsonIgnoreProperties("type")
  private static final class RunningValue implements Value {
    @JsonUnwrapped
    @JsonIgnoreProperties(
        value = "type",
        allowSetters = true
    )
    private TestSubmissionStatusRunning value;

    @JsonCreator(
        mode = JsonCreator.Mode.PROPERTIES
    )
    private RunningValue() {
    }

    private RunningValue(TestSubmissionStatusRunning value) {
      this.value = value;
    }

    @java.lang.Override
    public <T> T visit(Visitor<T> visitor) {
      return visitor.visitRunning(value);
    }

    @java.lang.Override
    public boolean equals(Object other) {
      if (this == other) return true;
      return other instanceof RunningValue && equalTo((RunningValue) other);
    }

    private boolean equalTo(RunningValue other) {
      return value.equals(other.value);
    }

    @java.lang.Override
    public int hashCode() {
      return Objects.hash(this.value);
    }

    @java.lang.Override
    public String toString() {
      return "TestSubmissionStatus{" + "value: " + value + "}";
    }
  }

  @JsonTypeName("testCaseIdToState")
  @JsonIgnoreProperties("type")
  private static final class TestCaseIdToStateValue implements Value {
    @JsonUnwrapped
    @JsonIgnoreProperties(
        value = "type",
        allowSetters = true
    )
    private TestSubmissionStatusTestCaseIdToState value;

    @JsonCreator(
        mode = JsonCreator.Mode.PROPERTIES
    )
    private TestCaseIdToStateValue() {
    }

    private TestCaseIdToStateValue(TestSubmissionStatusTestCaseIdToState value) {
      this.value = value;
    }

    @java.lang.Override
    public <T> T visit(Visitor<T> visitor) {
      return visitor.visitTestCaseIdToState(value);
    }

    @java.lang.Override
    public boolean equals(Object other) {
      if (this == other) return true;
      return other instanceof TestCaseIdToStateValue && equalTo((TestCaseIdToStateValue) other);
    }

    private boolean equalTo(TestCaseIdToStateValue other) {
      return value.equals(other.value);
    }

    @java.lang.Override
    public int hashCode() {
      return Objects.hash(this.value);
    }

    @java.lang.Override
    public String toString() {
      return "TestSubmissionStatus{" + "value: " + value + "}";
    }
  }

  @JsonIgnoreProperties("type")
  private static final class _UnknownValue implements Value {
    private String type;

    @JsonValue
    private Object value;

    @JsonCreator(
        mode = JsonCreator.Mode.PROPERTIES
    )
    private _UnknownValue(@JsonProperty("value") Object value) {
    }

    @java.lang.Override
    public <T> T visit(Visitor<T> visitor) {
      return visitor._visitUnknown(value);
    }

    @java.lang.Override
    public boolean equals(Object other) {
      if (this == other) return true;
      return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
    }

    private boolean equalTo(_UnknownValue other) {
      return type.equals(other.type) && value.equals(other.value);
    }

    @java.lang.Override
    public int hashCode() {
      return Objects.hash(this.type, this.value);
    }

    @java.lang.Override
    public String toString() {
      return "TestSubmissionStatus{" + "type: " + type + ", value: " + value + "}";
    }
  }
}
