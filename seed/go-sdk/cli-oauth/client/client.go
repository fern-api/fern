// Code generated by Fern. DO NOT EDIT.

package client

import (
	context "context"
	errors "errors"
	os "os"

	fern "github.com/cli-oauth/fern"
	auth "github.com/cli-oauth/fern/auth"
	core "github.com/cli-oauth/fern/core"
	internal "github.com/cli-oauth/fern/internal"
	option "github.com/cli-oauth/fern/option"
	pets "github.com/cli-oauth/fern/pets"
	system "github.com/cli-oauth/fern/system"
)

type Client struct {
	Auth   *auth.Client
	System *system.Client
	Pets   *pets.Client

	options *core.RequestOptions
	baseURL string
	caller  *internal.Caller
}

func NewClient(opts ...option.RequestOption) *Client {
	options := core.NewRequestOptions(opts...)
	if options.ClientID == "" {
		options.ClientID = os.Getenv("ACME_CLIENT_ID")
	}
	if options.ClientSecret == "" {
		options.ClientSecret = os.Getenv("ACME_CLIENT_SECRET")
	}
	oauthTokenProvider := core.NewTokenProvider(
		0,
	)
	authOptions := *options
	authClient := auth.NewClient(
		&authOptions,
	)
	options.SetTokenGetter(func() (string, error) {
		return oauthTokenProvider.GetOrFetch(func() (string, int64, error) {
			response, err := authClient.GetToken(context.Background(), &fern.GetTokenAuthRequest{
				ClientID:     options.ClientID,
				ClientSecret: options.ClientSecret,
				GrantType:    "client_credentials",
			})
			if err != nil {
				return "", 0, err
			}
			if response.AccessToken == "" {
				return "", 0, errors.New(
					"oauth response missing access token",
				)
			}
			expiresIn := int64(core.DefaultExpirySeconds)
			if response.ExpiresIn > 0 {
				expiresIn = int64(response.ExpiresIn)
			}
			return response.AccessToken, expiresIn, nil
		})
	})
	return &Client{
		Auth:    auth.NewClient(options),
		System:  system.NewClient(options),
		Pets:    pets.NewClient(options),
		options: options,
		baseURL: options.BaseURL,
		caller: internal.NewCaller(
			&internal.CallerParams{
				Client:         options.HTTPClient,
				MaxAttempts:    options.MaxAttempts,
				DisableRetries: options.DisableRetries,
			},
		),
	}
}
