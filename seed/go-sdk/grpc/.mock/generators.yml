{}

# TODO: Add support for Protobuf API definitions.
#
# A single Protobuf root must be configured, along
# with one or more .proto definitions that define APIs
# (i.e. via a `service` declaration).
#
# api:
#   - grpc:
#       root: proto
#       target: proto/user/v1/user.proto