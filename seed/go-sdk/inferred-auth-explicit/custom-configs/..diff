diff --git a/.fern/metadata.json b/.fern/metadata.json
index cef89e12341..7d6896d171b 100644
--- a/.fern/metadata.json
+++ b/.fern/metadata.json
@@ -3,7 +3,7 @@
   "generatorName": "fernapi/fern-go-sdk",
   "generatorVersion": "local",
   "generatorConfig": {
-    "enableWireTests": false
+    "enableWireTests": true
   },
   "originGitCommit": "DUMMY",
   "originGitCommitIsDirty": null,
diff --git a/auth/auth_test/auth_test.go b/auth/auth_test/auth_test.go
new file mode 100644
index 00000000000..505c2dcfc53
--- /dev/null
+++ b/auth/auth_test/auth_test.go
@@ -0,0 +1,139 @@
+// Code generated by Fern. DO NOT EDIT.
+
+package auth_test
+
+import (
+	bytes "bytes"
+	context "context"
+	json "encoding/json"
+	http "net/http"
+	os "os"
+	testing "testing"
+
+	fern "github.com/inferred-auth-explicit/fern"
+	client "github.com/inferred-auth-explicit/fern/client"
+	option "github.com/inferred-auth-explicit/fern/option"
+	require "github.com/stretchr/testify/require"
+)
+
+func VerifyRequestCount(
+	t *testing.T,
+	testId string,
+	method string,
+	urlPath string,
+	queryParams map[string]any,
+	expected int,
+) {
+	wiremockURL := os.Getenv("WIREMOCK_URL")
+	if wiremockURL == "" {
+		wiremockURL = "http://localhost:8080"
+	}
+	WiremockAdminURL := wiremockURL + "/__admin"
+	var reqBody bytes.Buffer
+	reqBody.WriteString(`{"method":"`)
+	reqBody.WriteString(method)
+	reqBody.WriteString(`","urlPath":"`)
+	reqBody.WriteString(urlPath)
+	reqBody.WriteString(`","headers":{"X-Test-Id":{"equalTo":"`)
+	reqBody.WriteString(testId)
+	reqBody.WriteString(`"}}`)
+	if len(queryParams) > 0 {
+		reqBody.WriteString(`,"queryParameters":{`)
+		first := true
+		for key, value := range queryParams {
+			if !first {
+				reqBody.WriteString(",")
+			}
+			reqBody.WriteString(`"`)
+			reqBody.WriteString(key)
+			switch v := value.(type) {
+			case string:
+				reqBody.WriteString(`":{"equalTo":"`)
+				reqBody.WriteString(v)
+				reqBody.WriteString(`"}`)
+			case []string:
+				reqBody.WriteString(`":{"hasExactly":[`)
+				for i, item := range v {
+					if i > 0 {
+						reqBody.WriteString(",")
+					}
+					reqBody.WriteString(`{"equalTo":"`)
+					reqBody.WriteString(item)
+					reqBody.WriteString(`"}`)
+				}
+				reqBody.WriteString(`]}`)
+			}
+			first = false
+		}
+		reqBody.WriteString("}")
+	}
+	reqBody.WriteString("}")
+	resp, err := http.Post(WiremockAdminURL+"/requests/find", "application/json", &reqBody)
+	require.NoError(t, err)
+	var result struct {
+		Requests []interface{} `json:"requests"`
+	}
+	json.NewDecoder(resp.Body).Decode(&result)
+	require.Equal(t, expected, len(result.Requests))
+}
+
+func TestAuthGetTokenWithClientCredentialsWithWireMock(
+	t *testing.T,
+) {
+	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
+	if WireMockBaseURL == "" {
+		WireMockBaseURL = "http://localhost:8080"
+	}
+	client := client.NewClient(
+		option.WithBaseURL(WireMockBaseURL),
+	)
+	request := &fern.GetTokenRequest{
+		XAPIKey:      "X-Api-Key",
+		ClientID:     "client_id",
+		ClientSecret: "client_secret",
+		Scope: fern.String(
+			"scope",
+		),
+	}
+	_, invocationErr := client.Auth.GetTokenWithClientCredentials(
+		context.TODO(),
+		request,
+		option.WithHTTPHeader(
+			http.Header{"X-Test-Id": []string{"TestAuthGetTokenWithClientCredentialsWithWireMock"}},
+		),
+	)
+
+	require.NoError(t, invocationErr, "Client method call should succeed")
+	VerifyRequestCount(t, "TestAuthGetTokenWithClientCredentialsWithWireMock", "POST", "/token", nil, 1)
+}
+
+func TestAuthRefreshTokenWithWireMock(
+	t *testing.T,
+) {
+	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
+	if WireMockBaseURL == "" {
+		WireMockBaseURL = "http://localhost:8080"
+	}
+	client := client.NewClient(
+		option.WithBaseURL(WireMockBaseURL),
+	)
+	request := &fern.RefreshTokenRequest{
+		XAPIKey:      "X-Api-Key",
+		ClientID:     "client_id",
+		ClientSecret: "client_secret",
+		RefreshToken: "refresh_token",
+		Scope: fern.String(
+			"scope",
+		),
+	}
+	_, invocationErr := client.Auth.RefreshToken(
+		context.TODO(),
+		request,
+		option.WithHTTPHeader(
+			http.Header{"X-Test-Id": []string{"TestAuthRefreshTokenWithWireMock"}},
+		),
+	)
+
+	require.NoError(t, invocationErr, "Client method call should succeed")
+	VerifyRequestCount(t, "TestAuthRefreshTokenWithWireMock", "POST", "/token/refresh", nil, 1)
+}
diff --git a/auth/inferred_auth_wire_test/inferred_auth_wire_test.go b/auth/inferred_auth_wire_test/inferred_auth_wire_test.go
new file mode 100644
index 00000000000..e702dc669b0
--- /dev/null
+++ b/auth/inferred_auth_wire_test/inferred_auth_wire_test.go
@@ -0,0 +1,184 @@
+// Code generated by Fern. DO NOT EDIT.
+
+package inferred_auth_wire_test
+
+import (
+	context "context"
+	io "io"
+	http "net/http"
+	strings "strings"
+	sync "sync"
+	testing "testing"
+
+	client "github.com/inferred-auth-explicit/fern/client"
+	option "github.com/inferred-auth-explicit/fern/option"
+	assert "github.com/stretchr/testify/assert"
+	require "github.com/stretchr/testify/require"
+)
+
+// capturedRequest stores information about an intercepted HTTP request.
+type capturedRequest struct {
+	url     string
+	method  string
+	headers http.Header
+	body    []byte
+}
+
+// requestTrackingTransport captures all HTTP requests for inspection in tests.
+// It returns mock responses: a token response for the token endpoint, and an
+// empty 200 OK for all other requests.
+type requestTrackingTransport struct {
+	mu       sync.Mutex
+	requests []capturedRequest
+}
+
+func (t *requestTrackingTransport) RoundTrip(req *http.Request) (*http.Response, error) {
+	captured := capturedRequest{
+		url:     req.URL.String(),
+		method:  req.Method,
+		headers: req.Header.Clone(),
+	}
+
+	if req.Body != nil {
+		body, err := io.ReadAll(req.Body)
+		if err != nil {
+			return nil, err
+		}
+		captured.body = body
+	}
+
+	t.mu.Lock()
+	t.requests = append(t.requests, captured)
+	t.mu.Unlock()
+
+	if strings.HasSuffix(req.URL.Path, "/token") {
+		responseBody := `{
+			"access_token": "test_access_token",
+			"expires_in": 3600
+		}`
+		return &http.Response{
+			StatusCode: 200,
+			Body:       io.NopCloser(strings.NewReader(responseBody)),
+			Header: http.Header{
+				"Content-Type": []string{"application/json"},
+			},
+		}, nil
+	}
+
+	// Default response for non-token endpoints
+	return &http.Response{
+		StatusCode: 200,
+		Body:       io.NopCloser(strings.NewReader(`{}`)),
+		Header: http.Header{
+			"Content-Type": []string{"application/json"},
+		},
+	}, nil
+}
+
+// TestInferredAuthTokenAutoFetch verifies that calling a non-auth endpoint automatically
+// triggers a token fetch and injects the Authorization: Bearer header on the API request.
+func TestInferredAuthTokenAutoFetch(t *testing.T) {
+	transport := &requestTrackingTransport{}
+	httpClient := &http.Client{Transport: transport}
+
+	c := client.NewClient(
+		option.WithBaseURL("http://localhost:8080"),
+		option.WithHTTPClient(httpClient),
+		option.WithXAPIKey("test_xapikey"),
+		option.WithClientID("test_clientid"),
+		option.WithClientSecret("test_clientsecret"),
+	)
+
+	err := c.NestedNoAuth.API.GetSomething(context.Background())
+	if err != nil {
+		t.Logf("GetSomething returned error (may be expected in test): %v", err)
+	}
+
+	// Should have captured exactly 2 requests: token fetch + API call
+	require.Equal(t, 2, len(transport.requests),
+		"Expected 2 requests (token fetch + API call), got %d", len(transport.requests))
+
+	// First request should be the token endpoint
+	assert.True(t, strings.HasSuffix(transport.requests[0].url, "/token"),
+		"First request should be to token endpoint /token, got %s", transport.requests[0].url)
+
+	// Second request should have the Authorization: Bearer header
+	authHeader := transport.requests[1].headers.Get("Authorization")
+	assert.Equal(t, "Bearer test_access_token", authHeader,
+		"API request should have Authorization: Bearer header")
+}
+
+// TestInferredAuthTokenCaching verifies that the token is cached and reused
+// across multiple API calls without triggering additional token fetches.
+func TestInferredAuthTokenCaching(t *testing.T) {
+	transport := &requestTrackingTransport{}
+	httpClient := &http.Client{Transport: transport}
+
+	c := client.NewClient(
+		option.WithBaseURL("http://localhost:8080"),
+		option.WithHTTPClient(httpClient),
+		option.WithXAPIKey("test_xapikey"),
+		option.WithClientID("test_clientid"),
+		option.WithClientSecret("test_clientsecret"),
+	)
+
+	// Make two API calls
+	err1 := c.NestedNoAuth.API.GetSomething(context.Background())
+	if err1 != nil {
+		t.Logf("GetSomething returned error (may be expected in test): %v", err1)
+	}
+	err2 := c.NestedNoAuth.API.GetSomething(context.Background())
+	if err2 != nil {
+		t.Logf("GetSomething returned error (may be expected in test): %v", err2)
+	}
+
+	// Should have captured exactly 3 requests: 1 token fetch + 2 API calls
+	require.Equal(t, 3, len(transport.requests),
+		"Expected 3 requests (1 token fetch + 2 API calls), got %d", len(transport.requests))
+
+	// Only the first request should be to the token endpoint
+	assert.True(t, strings.HasSuffix(transport.requests[0].url, "/token"),
+		"First request should be to token endpoint")
+
+	// Both API requests should have the Authorization header
+	assert.Equal(t, "Bearer test_access_token", transport.requests[1].headers.Get("Authorization"),
+		"First API request should have Authorization header")
+	assert.Equal(t, "Bearer test_access_token", transport.requests[2].headers.Get("Authorization"),
+		"Second API request should have Authorization header")
+}
+
+// TestInferredAuthCustomHeaders verifies that custom headers set on the client
+// are included in authenticated API requests.
+func TestInferredAuthCustomHeaders(t *testing.T) {
+	transport := &requestTrackingTransport{}
+	httpClient := &http.Client{Transport: transport}
+
+	customHeaders := http.Header{}
+	customHeaders.Set("X-Custom-Header", "custom-value")
+	customHeaders.Set("X-Sandbox-Auth", "sandbox-token-123")
+
+	c := client.NewClient(
+		option.WithBaseURL("http://localhost:8080"),
+		option.WithHTTPClient(httpClient),
+		option.WithXAPIKey("test_xapikey"),
+		option.WithClientID("test_clientid"),
+		option.WithClientSecret("test_clientsecret"),
+		option.WithHTTPHeader(customHeaders),
+	)
+
+	err := c.NestedNoAuth.API.GetSomething(context.Background())
+	if err != nil {
+		t.Logf("GetSomething returned error (may be expected in test): %v", err)
+	}
+
+	// Should have captured at least 2 requests (token fetch + API call)
+	require.GreaterOrEqual(t, len(transport.requests), 2,
+		"Expected at least 2 requests")
+
+	// The API request (last captured) should include custom headers
+	apiRequest := transport.requests[len(transport.requests)-1]
+	assert.Equal(t, "custom-value", apiRequest.headers.Get("X-Custom-Header"),
+		"X-Custom-Header should be included in API request")
+	assert.Equal(t, "sandbox-token-123", apiRequest.headers.Get("X-Sandbox-Auth"),
+		"X-Sandbox-Auth should be included in API request")
+}
diff --git a/nested/api/nested_api_test/nested_api_test.go b/nested/api/nested_api_test/nested_api_test.go
new file mode 100644
index 00000000000..0c17affef6c
--- /dev/null
+++ b/nested/api/nested_api_test/nested_api_test.go
@@ -0,0 +1,98 @@
+// Code generated by Fern. DO NOT EDIT.
+
+package nested_api_test
+
+import (
+	bytes "bytes"
+	context "context"
+	json "encoding/json"
+	http "net/http"
+	os "os"
+	testing "testing"
+
+	client "github.com/inferred-auth-explicit/fern/client"
+	option "github.com/inferred-auth-explicit/fern/option"
+	require "github.com/stretchr/testify/require"
+)
+
+func VerifyRequestCount(
+	t *testing.T,
+	testId string,
+	method string,
+	urlPath string,
+	queryParams map[string]any,
+	expected int,
+) {
+	wiremockURL := os.Getenv("WIREMOCK_URL")
+	if wiremockURL == "" {
+		wiremockURL = "http://localhost:8080"
+	}
+	WiremockAdminURL := wiremockURL + "/__admin"
+	var reqBody bytes.Buffer
+	reqBody.WriteString(`{"method":"`)
+	reqBody.WriteString(method)
+	reqBody.WriteString(`","urlPath":"`)
+	reqBody.WriteString(urlPath)
+	reqBody.WriteString(`","headers":{"X-Test-Id":{"equalTo":"`)
+	reqBody.WriteString(testId)
+	reqBody.WriteString(`"}}`)
+	if len(queryParams) > 0 {
+		reqBody.WriteString(`,"queryParameters":{`)
+		first := true
+		for key, value := range queryParams {
+			if !first {
+				reqBody.WriteString(",")
+			}
+			reqBody.WriteString(`"`)
+			reqBody.WriteString(key)
+			switch v := value.(type) {
+			case string:
+				reqBody.WriteString(`":{"equalTo":"`)
+				reqBody.WriteString(v)
+				reqBody.WriteString(`"}`)
+			case []string:
+				reqBody.WriteString(`":{"hasExactly":[`)
+				for i, item := range v {
+					if i > 0 {
+						reqBody.WriteString(",")
+					}
+					reqBody.WriteString(`{"equalTo":"`)
+					reqBody.WriteString(item)
+					reqBody.WriteString(`"}`)
+				}
+				reqBody.WriteString(`]}`)
+			}
+			first = false
+		}
+		reqBody.WriteString("}")
+	}
+	reqBody.WriteString("}")
+	resp, err := http.Post(WiremockAdminURL+"/requests/find", "application/json", &reqBody)
+	require.NoError(t, err)
+	var result struct {
+		Requests []interface{} `json:"requests"`
+	}
+	json.NewDecoder(resp.Body).Decode(&result)
+	require.Equal(t, expected, len(result.Requests))
+}
+
+func TestNestedAPIGetSomethingWithWireMock(
+	t *testing.T,
+) {
+	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
+	if WireMockBaseURL == "" {
+		WireMockBaseURL = "http://localhost:8080"
+	}
+	client := client.NewClient(
+		option.WithBaseURL(WireMockBaseURL),
+	)
+	invocationErr := client.Nested.API.GetSomething(
+		context.TODO(),
+		option.WithHTTPHeader(
+			http.Header{"X-Test-Id": []string{"TestNestedAPIGetSomethingWithWireMock"}},
+		),
+	)
+
+	require.NoError(t, invocationErr, "Client method call should succeed")
+	VerifyRequestCount(t, "TestNestedAPIGetSomethingWithWireMock", "GET", "/nested/get-something", nil, 1)
+}
diff --git a/nestednoauth/api/nested_no_auth_api_test/nested_no_auth_api_test.go b/nestednoauth/api/nested_no_auth_api_test/nested_no_auth_api_test.go
new file mode 100644
index 00000000000..6addc9b42ce
--- /dev/null
+++ b/nestednoauth/api/nested_no_auth_api_test/nested_no_auth_api_test.go
@@ -0,0 +1,98 @@
+// Code generated by Fern. DO NOT EDIT.
+
+package nested_no_auth_api_test
+
+import (
+	bytes "bytes"
+	context "context"
+	json "encoding/json"
+	http "net/http"
+	os "os"
+	testing "testing"
+
+	client "github.com/inferred-auth-explicit/fern/client"
+	option "github.com/inferred-auth-explicit/fern/option"
+	require "github.com/stretchr/testify/require"
+)
+
+func VerifyRequestCount(
+	t *testing.T,
+	testId string,
+	method string,
+	urlPath string,
+	queryParams map[string]any,
+	expected int,
+) {
+	wiremockURL := os.Getenv("WIREMOCK_URL")
+	if wiremockURL == "" {
+		wiremockURL = "http://localhost:8080"
+	}
+	WiremockAdminURL := wiremockURL + "/__admin"
+	var reqBody bytes.Buffer
+	reqBody.WriteString(`{"method":"`)
+	reqBody.WriteString(method)
+	reqBody.WriteString(`","urlPath":"`)
+	reqBody.WriteString(urlPath)
+	reqBody.WriteString(`","headers":{"X-Test-Id":{"equalTo":"`)
+	reqBody.WriteString(testId)
+	reqBody.WriteString(`"}}`)
+	if len(queryParams) > 0 {
+		reqBody.WriteString(`,"queryParameters":{`)
+		first := true
+		for key, value := range queryParams {
+			if !first {
+				reqBody.WriteString(",")
+			}
+			reqBody.WriteString(`"`)
+			reqBody.WriteString(key)
+			switch v := value.(type) {
+			case string:
+				reqBody.WriteString(`":{"equalTo":"`)
+				reqBody.WriteString(v)
+				reqBody.WriteString(`"}`)
+			case []string:
+				reqBody.WriteString(`":{"hasExactly":[`)
+				for i, item := range v {
+					if i > 0 {
+						reqBody.WriteString(",")
+					}
+					reqBody.WriteString(`{"equalTo":"`)
+					reqBody.WriteString(item)
+					reqBody.WriteString(`"}`)
+				}
+				reqBody.WriteString(`]}`)
+			}
+			first = false
+		}
+		reqBody.WriteString("}")
+	}
+	reqBody.WriteString("}")
+	resp, err := http.Post(WiremockAdminURL+"/requests/find", "application/json", &reqBody)
+	require.NoError(t, err)
+	var result struct {
+		Requests []interface{} `json:"requests"`
+	}
+	json.NewDecoder(resp.Body).Decode(&result)
+	require.Equal(t, expected, len(result.Requests))
+}
+
+func TestNestedNoAuthAPIGetSomethingWithWireMock(
+	t *testing.T,
+) {
+	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
+	if WireMockBaseURL == "" {
+		WireMockBaseURL = "http://localhost:8080"
+	}
+	client := client.NewClient(
+		option.WithBaseURL(WireMockBaseURL),
+	)
+	invocationErr := client.NestedNoAuth.API.GetSomething(
+		context.TODO(),
+		option.WithHTTPHeader(
+			http.Header{"X-Test-Id": []string{"TestNestedNoAuthAPIGetSomethingWithWireMock"}},
+		),
+	)
+
+	require.NoError(t, invocationErr, "Client method call should succeed")
+	VerifyRequestCount(t, "TestNestedNoAuthAPIGetSomethingWithWireMock", "GET", "/nested-no-auth/get-something", nil, 1)
+}
diff --git a/simple/simple_test/simple_test.go b/simple/simple_test/simple_test.go
new file mode 100644
index 00000000000..afe684feb3a
--- /dev/null
+++ b/simple/simple_test/simple_test.go
@@ -0,0 +1,98 @@
+// Code generated by Fern. DO NOT EDIT.
+
+package simple_test
+
+import (
+	bytes "bytes"
+	context "context"
+	json "encoding/json"
+	http "net/http"
+	os "os"
+	testing "testing"
+
+	client "github.com/inferred-auth-explicit/fern/client"
+	option "github.com/inferred-auth-explicit/fern/option"
+	require "github.com/stretchr/testify/require"
+)
+
+func VerifyRequestCount(
+	t *testing.T,
+	testId string,
+	method string,
+	urlPath string,
+	queryParams map[string]any,
+	expected int,
+) {
+	wiremockURL := os.Getenv("WIREMOCK_URL")
+	if wiremockURL == "" {
+		wiremockURL = "http://localhost:8080"
+	}
+	WiremockAdminURL := wiremockURL + "/__admin"
+	var reqBody bytes.Buffer
+	reqBody.WriteString(`{"method":"`)
+	reqBody.WriteString(method)
+	reqBody.WriteString(`","urlPath":"`)
+	reqBody.WriteString(urlPath)
+	reqBody.WriteString(`","headers":{"X-Test-Id":{"equalTo":"`)
+	reqBody.WriteString(testId)
+	reqBody.WriteString(`"}}`)
+	if len(queryParams) > 0 {
+		reqBody.WriteString(`,"queryParameters":{`)
+		first := true
+		for key, value := range queryParams {
+			if !first {
+				reqBody.WriteString(",")
+			}
+			reqBody.WriteString(`"`)
+			reqBody.WriteString(key)
+			switch v := value.(type) {
+			case string:
+				reqBody.WriteString(`":{"equalTo":"`)
+				reqBody.WriteString(v)
+				reqBody.WriteString(`"}`)
+			case []string:
+				reqBody.WriteString(`":{"hasExactly":[`)
+				for i, item := range v {
+					if i > 0 {
+						reqBody.WriteString(",")
+					}
+					reqBody.WriteString(`{"equalTo":"`)
+					reqBody.WriteString(item)
+					reqBody.WriteString(`"}`)
+				}
+				reqBody.WriteString(`]}`)
+			}
+			first = false
+		}
+		reqBody.WriteString("}")
+	}
+	reqBody.WriteString("}")
+	resp, err := http.Post(WiremockAdminURL+"/requests/find", "application/json", &reqBody)
+	require.NoError(t, err)
+	var result struct {
+		Requests []interface{} `json:"requests"`
+	}
+	json.NewDecoder(resp.Body).Decode(&result)
+	require.Equal(t, expected, len(result.Requests))
+}
+
+func TestSimpleGetSomethingWithWireMock(
+	t *testing.T,
+) {
+	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
+	if WireMockBaseURL == "" {
+		WireMockBaseURL = "http://localhost:8080"
+	}
+	client := client.NewClient(
+		option.WithBaseURL(WireMockBaseURL),
+	)
+	invocationErr := client.Simple.GetSomething(
+		context.TODO(),
+		option.WithHTTPHeader(
+			http.Header{"X-Test-Id": []string{"TestSimpleGetSomethingWithWireMock"}},
+		),
+	)
+
+	require.NoError(t, invocationErr, "Client method call should succeed")
+	VerifyRequestCount(t, "TestSimpleGetSomethingWithWireMock", "GET", "/get-something", nil, 1)
+}
diff --git a/wiremock/docker-compose.test.yml b/wiremock/docker-compose.test.yml
new file mode 100644
index 00000000000..fabb06a62cc
--- /dev/null
+++ b/wiremock/docker-compose.test.yml
@@ -0,0 +1,14 @@
+services:
+  wiremock:
+    image: wiremock/wiremock:3.9.1
+    ports:
+      - "0:8080"
+    volumes:
+      - ./wiremock-mappings.json:/home/wiremock/mappings/wiremock-mappings.json
+    command: ["--global-response-templating", "--verbose"]
+    healthcheck:
+      test: ["CMD", "curl", "-f", "http://localhost:8080/__admin/health"]
+      interval: 2s
+      timeout: 5s
+      retries: 15
+      start_period: 5s
diff --git a/wiremock/wiremock-mappings.json b/wiremock/wiremock-mappings.json
new file mode 100644
index 00000000000..0f4a31293f9
--- /dev/null
+++ b/wiremock/wiremock-mappings.json
@@ -0,0 +1,140 @@
+{
+  "mappings": [
+    {
+      "id": "f559340c-badf-4147-a5e3-753e957b6d3c",
+      "name": "getTokenWithClientCredentials - default",
+      "request": {
+        "urlPathTemplate": "/token",
+        "method": "POST"
+      },
+      "response": {
+        "status": 200,
+        "body": "{\n  \"access_token\": \"access_token\",\n  \"expires_in\": 1,\n  \"refresh_token\": \"refresh_token\"\n}",
+        "headers": {
+          "Content-Type": "application/json"
+        }
+      },
+      "uuid": "f559340c-badf-4147-a5e3-753e957b6d3c",
+      "persistent": true,
+      "priority": 3,
+      "metadata": {
+        "mocklab": {
+          "created": {
+            "at": "2020-01-01T00:00:00.000Z",
+            "via": "SYSTEM"
+          }
+        }
+      }
+    },
+    {
+      "id": "f9c99fe3-6745-4170-96e3-47d0f3df0476",
+      "name": "refreshToken - default",
+      "request": {
+        "urlPathTemplate": "/token/refresh",
+        "method": "POST"
+      },
+      "response": {
+        "status": 200,
+        "body": "{\n  \"access_token\": \"access_token\",\n  \"expires_in\": 1,\n  \"refresh_token\": \"refresh_token\"\n}",
+        "headers": {
+          "Content-Type": "application/json"
+        }
+      },
+      "uuid": "f9c99fe3-6745-4170-96e3-47d0f3df0476",
+      "persistent": true,
+      "priority": 3,
+      "metadata": {
+        "mocklab": {
+          "created": {
+            "at": "2020-01-01T00:00:00.000Z",
+            "via": "SYSTEM"
+          }
+        }
+      }
+    },
+    {
+      "id": "741b2725-af64-481d-93e7-f94c045bd88e",
+      "name": "getSomething - default",
+      "request": {
+        "urlPathTemplate": "/nested-no-auth/get-something",
+        "method": "GET"
+      },
+      "response": {
+        "status": 200,
+        "body": "\"\"",
+        "headers": {
+          "Content-Type": "application/json"
+        }
+      },
+      "uuid": "741b2725-af64-481d-93e7-f94c045bd88e",
+      "persistent": true,
+      "priority": 3,
+      "metadata": {
+        "mocklab": {
+          "created": {
+            "at": "2020-01-01T00:00:00.000Z",
+            "via": "SYSTEM"
+          }
+        }
+      },
+      "postServeActions": []
+    },
+    {
+      "id": "a5c25db6-caf3-403e-abf1-82391213dd64",
+      "name": "getSomething - default",
+      "request": {
+        "urlPathTemplate": "/nested/get-something",
+        "method": "GET"
+      },
+      "response": {
+        "status": 200,
+        "body": "\"\"",
+        "headers": {
+          "Content-Type": "application/json"
+        }
+      },
+      "uuid": "a5c25db6-caf3-403e-abf1-82391213dd64",
+      "persistent": true,
+      "priority": 3,
+      "metadata": {
+        "mocklab": {
+          "created": {
+            "at": "2020-01-01T00:00:00.000Z",
+            "via": "SYSTEM"
+          }
+        }
+      },
+      "postServeActions": []
+    },
+    {
+      "id": "48bcf2a9-f263-4bb1-abc6-528079e16f35",
+      "name": "getSomething - default",
+      "request": {
+        "urlPathTemplate": "/get-something",
+        "method": "GET"
+      },
+      "response": {
+        "status": 200,
+        "body": "\"\"",
+        "headers": {
+          "Content-Type": "application/json"
+        }
+      },
+      "uuid": "48bcf2a9-f263-4bb1-abc6-528079e16f35",
+      "persistent": true,
+      "priority": 3,
+      "metadata": {
+        "mocklab": {
+          "created": {
+            "at": "2020-01-01T00:00:00.000Z",
+            "via": "SYSTEM"
+          }
+        }
+      },
+      "postServeActions": []
+    }
+  ],
+  "meta": {
+    "total": 5
+  }
+}
\ No newline at end of file
