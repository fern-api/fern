// Code generated by Fern. DO NOT EDIT.

package core

import (
	"sync"
	"time"
)

const (
	// tokenExpirationBufferMinutes is subtracted from the token expiration time
	// to ensure we refresh the token before it actually expires.
	tokenExpirationBufferMinutes = 2

	// DefaultExpirySeconds is used as a fallback when the token response doesn't include
	// an expiry value.
	DefaultExpirySeconds = 3600 // 1 hour
)

// TokenProvider manages access tokens with caching and automatic refresh.
type TokenProvider struct {
	mu          sync.Mutex
	accessToken string
	expiresAt   time.Time

	// defaultExpirySeconds controls behavior when the token response doesn't include
	// an expiry value. If 0, the token is treated as never expiring. If > 0, the token
	// is assigned this default expiry (e.g. 3600 for inferred auth).
	defaultExpirySeconds int

	// fetchMu ensures only one goroutine fetches a new token at a time
	fetchMu sync.Mutex
}

// NewTokenProvider creates a new TokenProvider.
//
// defaultExpirySeconds controls the behavior when the token response doesn't include an
// expiry value. Use 0 to treat tokens as never expiring (e.g. for OAuth), or a positive
// value to apply a default expiry (e.g. 3600 for inferred auth).
func NewTokenProvider(defaultExpirySeconds int) *TokenProvider {
	return &TokenProvider{
		defaultExpirySeconds: defaultExpirySeconds,
	}
}

// SetToken sets the cached access token and its expiration time.
// The expiresIn parameter is the number of seconds until the token expires.
func (t *TokenProvider) SetToken(accessToken string, expiresIn int) {
	t.mu.Lock()
	defer t.mu.Unlock()
	t.accessToken = accessToken
	if expiresIn > 0 {
		bufferSeconds := tokenExpirationBufferMinutes * 60
		effectiveExpiresIn := expiresIn - bufferSeconds
		if effectiveExpiresIn < 0 {
			effectiveExpiresIn = 0
		}
		t.expiresAt = time.Now().Add(time.Duration(effectiveExpiresIn) * time.Second)
	} else if t.defaultExpirySeconds > 0 {
		// Apply default expiry when no expiry info is provided.
		effectiveExpiresIn := t.defaultExpirySeconds - (tokenExpirationBufferMinutes * 60)
		if effectiveExpiresIn < 0 {
			effectiveExpiresIn = 0
		}
		t.expiresAt = time.Now().Add(time.Duration(effectiveExpiresIn) * time.Second)
	} else {
		// No expiration info and no default — token won't auto-refresh based on time.
		t.expiresAt = time.Time{}
	}
}

// GetToken returns the cached access token if it's still valid.
// Returns an empty string if the token is expired or not set.
func (t *TokenProvider) GetToken() string {
	t.mu.Lock()
	defer t.mu.Unlock()
	if t.accessToken == "" {
		return ""
	}
	if !t.expiresAt.IsZero() && time.Now().After(t.expiresAt) {
		return ""
	}
	return t.accessToken
}

// GetOrFetch returns a valid token, fetching a new one if necessary.
// The fetchFunc is called at most once even if multiple goroutines call GetOrFetch
// concurrently when the token is expired. It should return (accessToken, expiresInSeconds, error).
func (t *TokenProvider) GetOrFetch(fetchFunc func() (string, int, error)) (string, error) {
	// Fast path: check if we have a valid token
	if token := t.GetToken(); token != "" {
		return token, nil
	}

	// Slow path: acquire fetch lock to ensure only one goroutine fetches
	t.fetchMu.Lock()
	defer t.fetchMu.Unlock()

	// Double-check after acquiring lock (another goroutine may have fetched)
	if token := t.GetToken(); token != "" {
		return token, nil
	}

	// Fetch new token
	accessToken, expiresIn, err := fetchFunc()
	if err != nil {
		return "", err
	}

	t.SetToken(accessToken, expiresIn)
	return accessToken, nil
}

// NeedsRefresh returns true if the token needs to be refreshed.
func (t *TokenProvider) NeedsRefresh() bool {
	t.mu.Lock()
	defer t.mu.Unlock()
	if t.accessToken == "" {
		return true
	}
	if !t.expiresAt.IsZero() && time.Now().After(t.expiresAt) {
		return true
	}
	return false
}

// Reset clears the cached token.
func (t *TokenProvider) Reset() {
	t.mu.Lock()
	defer t.mu.Unlock()
	t.accessToken = ""
	t.expiresAt = time.Time{}
}
