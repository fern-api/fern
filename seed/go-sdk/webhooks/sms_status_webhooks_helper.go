// Code generated by Fern. DO NOT EDIT.

package webhooks

import (
	errors "errors"
	fmt "fmt"
	sort "sort"
	strings "strings"

	core "github.com/webhooks/fern/core"
)

// SmsStatusWebhooksHelper verifies an HMAC webhook signature.
//
// Extract the signature from the "x-twilio-signature" header and pass it as the signatureHeader parameter.
// The requestBody parameter accepts either a raw string or a map[string][]string of POST body parameters.
// When a map is provided, keys are sorted and each key's values are deduped and sorted, then concatenated as key-value pairs before signing.
// This helper verifies both classic form-encoded and JSON requests: it branches at runtime on whether the body-hash query parameter is present on the notification URL.
// For a JSON request the raw body is verified against that separately-transmitted hash and the signature is checked over the notification URL only.
// Pass the exact raw body as requestBody and the verbatim notification URL as notificationUrl.
// The signature is verified against several normalized forms of the notification URL, succeeding if any candidate matches.
type SmsStatusWebhooksHelper struct{}

// VerifySignature verifies an HMAC webhook signature.
func (SmsStatusWebhooksHelper) VerifySignature(
	requestBody interface{},
	signatureHeader string,
	signatureKey string,
	notificationUrl string,
) (bool, error) {
	if requestBody == nil || signatureHeader == "" || signatureKey == "" {
		return false, errors.New("Missing required parameters for webhook signature verification")
	}

	transmittedBodyHash, hasBodyHash := core.GetWebhookQueryParameter(notificationUrl, "bodySHA256")
	if hasBodyHash {
		rawBody, ok := requestBody.(string)
		if !ok {
			return false, nil
		}
		expectedBodyHash, err := core.ComputeHash(rawBody, "sha256", "hex")
		if err != nil {
			return false, err
		}
		if !core.TimingSafeEqual(expectedBodyHash, transmittedBodyHash) {
			return false, nil
		}
	}
	var bodyString string
	switch body := requestBody.(type) {
	case string:
		bodyString = body
	case map[string][]string:
		keys := make([]string, 0, len(body))
		for key := range body {
			keys = append(keys, key)
		}
		sort.Strings(keys)
		var builder strings.Builder
		for _, key := range keys {
			seen := make(map[string]struct{}, len(body[key]))
			uniqueValues := make([]string, 0, len(body[key]))
			for _, value := range body[key] {
				if _, exists := seen[value]; exists {
					continue
				}
				seen[value] = struct{}{}
				uniqueValues = append(uniqueValues, value)
			}
			sort.Strings(uniqueValues)
			for _, value := range uniqueValues {
				builder.WriteString(key)
				builder.WriteString(value)
			}
		}
		bodyString = builder.String()
	case map[string]string:
		keys := make([]string, 0, len(body))
		for key := range body {
			keys = append(keys, key)
		}
		sort.Strings(keys)
		var builder strings.Builder
		for _, key := range keys {
			builder.WriteString(key)
			builder.WriteString(body[key])
		}
		bodyString = builder.String()
	default:
		return false, fmt.Errorf("unsupported request body type: %T", requestBody)
	}
	candidates := core.NotificationUrlCandidates(notificationUrl, true, true)
	for _, candidateUrl := range candidates {
		var payload string
		if hasBodyHash {
			payload = candidateUrl
		} else {
			payload = strings.Join([]string{candidateUrl, bodyString}, "")
		}
		expected, err := core.ComputeHmacSignature(payload, signatureKey, "sha1", "base64")
		if err != nil {
			return false, err
		}
		if core.TimingSafeEqual(signatureHeader, expected) {
			return true, nil
		}
	}
	return false, nil
}
