// Code generated by Fern. DO NOT EDIT.

package client

import (
	fmt "fmt"

	fern "github.com/go-multi-env-url-templating/fern"
	client "github.com/go-multi-env-url-templating/fern/auth/client"
	core "github.com/go-multi-env-url-templating/fern/core"
	internal "github.com/go-multi-env-url-templating/fern/internal"
	option "github.com/go-multi-env-url-templating/fern/option"
	thingsclient "github.com/go-multi-env-url-templating/fern/things/client"
)

type Client struct {
	Auth   *client.Client
	Things *thingsclient.Client

	options *core.RequestOptions
	baseURL string
	caller  *internal.Caller
}

func NewClient(opts ...option.RequestOption) *Client {
	options := core.NewRequestOptions(opts...)
	if options.BaseURL == "" && (options.Region != "") {
		region := options.Region
		if region == "" {
			region = "us1"
		}
		switch options.Environment {
		case nil, fern.Environments.Production:
			options.Environment = fern.Environment{
				Acme: fmt.Sprintf(
					"https://api.%s.acme.com",
					region,
				),
				Oauth: fmt.Sprintf(
					"https://oauth.%s.acme.com",
					region,
				),
			}
		case fern.Environments.Staging:
			options.Environment = fern.Environment{
				Acme: fmt.Sprintf(
					"https://api.stage.%s.acme.com",
					region,
				),
				Oauth: fmt.Sprintf(
					"https://oauth.stage.%s.acme.com",
					region,
				),
			}
		case fern.Environments.Development:
			options.Environment = fern.Environment{
				Acme: fmt.Sprintf(
					"https://api.dev.%s.acme.com",
					region,
				),
				Oauth: fmt.Sprintf(
					"https://oauth.dev.%s.acme.com",
					region,
				),
			}
		}
	}
	return &Client{
		Auth:    client.NewClient(options),
		Things:  thingsclient.NewClient(options),
		options: options,
		baseURL: options.BaseURL,
		caller: internal.NewCaller(
			&internal.CallerParams{
				Client:         options.HTTPClient,
				MaxAttempts:    options.MaxAttempts,
				DisableRetries: options.DisableRetries,
			},
		),
	}
}
