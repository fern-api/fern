// Code generated by Fern. DO NOT EDIT.

package core

import (
	io "io"
	net "net"
	http "net/http"
	url "net/url"
	sync "sync"
	time "time"
)

// RequestOption adapts the behavior of the client or an individual request.
type RequestOption interface {
	applyRequestOptions(*RequestOptions)
}

// RequestOptions defines all of the possible request options.
//
// This type is primarily used by the generated code and is not meant
// to be used directly; use the option package instead.
type RequestOptions struct {
	BaseURL                    string
	HTTPClient                 HTTPClient
	HTTPHeader                 http.Header
	BodyProperties             map[string]interface{}
	QueryParameters            url.Values
	MaxAttempts                uint
	MaxBufSize                 int
	MaxStreamReconnectAttempts uint
	DisableStreamReconnection  bool
	DisableRetries             bool
	// ConnectTimeout bounds the time spent establishing a connection
	// (including TLS handshake). Zero means no per-call override.
	ConnectTimeout time.Duration
	// ReadTimeout bounds the time spent waiting for response headers and
	// the idle time between reads of the response body. Zero means no
	// per-call override.
	ReadTimeout time.Duration
	// WriteTimeout bounds the time spent writing the request body. Zero
	// means no per-call override.
	WriteTimeout time.Duration
	Token        string
	TokenFunc    func() (string, error)
}

// NewRequestOptions returns a new *RequestOptions value.
//
// This function is primarily used by the generated code and is not meant
// to be used directly; use RequestOption instead.
func NewRequestOptions(opts ...RequestOption) *RequestOptions {
	options := &RequestOptions{
		HTTPHeader:      make(http.Header),
		BodyProperties:  make(map[string]interface{}),
		QueryParameters: make(url.Values),
	}
	for _, opt := range opts {
		opt.applyRequestOptions(options)
	}
	if options.HTTPClient == nil {
		options.HTTPClient = newTimeoutHTTPClient(
			resolveTimeout(options.ConnectTimeout, time.Duration(5000000000)),
			resolveTimeout(options.ReadTimeout, time.Duration(30000000000)),
			resolveTimeout(options.WriteTimeout, time.Duration(30000000000)),
		)
	}
	return options
}

// ToHeader maps the configured request options into a http.Header used
// for the request(s).
func (r *RequestOptions) ToHeader() http.Header {
	header := r.cloneHeader()
	if r.Token != "" {
		header.Set("Authorization", "Bearer "+r.Token)
	} else if r.TokenFunc != nil {
		if token, err := r.TokenFunc(); err == nil && token != "" {
			header.Set("Authorization", "Bearer "+token)
		}
	}
	return header
}

func (r *RequestOptions) cloneHeader() http.Header {
	headers := r.HTTPHeader.Clone()
	headers.Set("X-Fern-Language", "Go")
	headers.Set("X-Fern-SDK-Name", "github.com/examples/fern")
	headers.Set("X-Fern-SDK-Version", "v0.0.1")
	headers.Set("User-Agent", "github.com/examples/fern/0.0.1")
	return headers
}

// BaseURLOption implements the RequestOption interface.
type BaseURLOption struct {
	BaseURL string
}

func (b *BaseURLOption) applyRequestOptions(opts *RequestOptions) {
	opts.BaseURL = b.BaseURL
}

// HTTPClientOption implements the RequestOption interface.
type HTTPClientOption struct {
	HTTPClient HTTPClient
}

func (h *HTTPClientOption) applyRequestOptions(opts *RequestOptions) {
	opts.HTTPClient = h.HTTPClient
}

// HTTPHeaderOption implements the RequestOption interface.
type HTTPHeaderOption struct {
	HTTPHeader http.Header
}

func (h *HTTPHeaderOption) applyRequestOptions(opts *RequestOptions) {
	opts.HTTPHeader = h.HTTPHeader
}

// BodyPropertiesOption implements the RequestOption interface.
type BodyPropertiesOption struct {
	BodyProperties map[string]interface{}
}

func (b *BodyPropertiesOption) applyRequestOptions(opts *RequestOptions) {
	opts.BodyProperties = b.BodyProperties
}

// QueryParametersOption implements the RequestOption interface.
type QueryParametersOption struct {
	QueryParameters url.Values
}

func (q *QueryParametersOption) applyRequestOptions(opts *RequestOptions) {
	opts.QueryParameters = q.QueryParameters
}

// MaxAttemptsOption implements the RequestOption interface.
type MaxAttemptsOption struct {
	MaxAttempts uint
}

func (m *MaxAttemptsOption) applyRequestOptions(opts *RequestOptions) {
	opts.MaxAttempts = m.MaxAttempts
}

// MaxBufSizeOption implements the RequestOption interface.
type MaxBufSizeOption struct {
	MaxBufSize int
}

func (m *MaxBufSizeOption) applyRequestOptions(opts *RequestOptions) {
	opts.MaxBufSize = m.MaxBufSize
}

// MaxStreamReconnectAttemptsOption implements the RequestOption interface.
type MaxStreamReconnectAttemptsOption struct {
	MaxStreamReconnectAttempts uint
}

func (m *MaxStreamReconnectAttemptsOption) applyRequestOptions(opts *RequestOptions) {
	opts.MaxStreamReconnectAttempts = m.MaxStreamReconnectAttempts
}

// WithoutStreamReconnectionOption implements the RequestOption interface.
type WithoutStreamReconnectionOption struct{}

func (w *WithoutStreamReconnectionOption) applyRequestOptions(opts *RequestOptions) {
	opts.DisableStreamReconnection = true
}

// WithoutRetriesOption implements the RequestOption interface.
type WithoutRetriesOption struct{}

func (w *WithoutRetriesOption) applyRequestOptions(opts *RequestOptions) {
	opts.DisableRetries = true
}

// TokenOption implements the RequestOption interface.
type TokenOption struct {
	Token string
}

func (t *TokenOption) applyRequestOptions(opts *RequestOptions) {
	opts.Token = t.Token
}

// TokenFuncOption implements the RequestOption interface.
type TokenFuncOption struct {
	TokenFunc func() (string, error)
}

func (t *TokenFuncOption) applyRequestOptions(opts *RequestOptions) {
	opts.TokenFunc = t.TokenFunc
}

// ConnectTimeoutOption implements the RequestOption interface.
type ConnectTimeoutOption struct {
	ConnectTimeout time.Duration
}

func (c *ConnectTimeoutOption) applyRequestOptions(opts *RequestOptions) {
	opts.ConnectTimeout = c.ConnectTimeout
}

// ReadTimeoutOption implements the RequestOption interface.
type ReadTimeoutOption struct {
	ReadTimeout time.Duration
}

func (r *ReadTimeoutOption) applyRequestOptions(opts *RequestOptions) {
	opts.ReadTimeout = r.ReadTimeout
}

// WriteTimeoutOption implements the RequestOption interface.
type WriteTimeoutOption struct {
	WriteTimeout time.Duration
}

func (w *WriteTimeoutOption) applyRequestOptions(opts *RequestOptions) {
	opts.WriteTimeout = w.WriteTimeout
}

// resolveTimeout returns the per-call override when non-zero, otherwise the
// configured default. A zero result means the phase is unbounded.
func resolveTimeout(override, fallback time.Duration) time.Duration {
	if override > 0 {
		return override
	}
	return fallback
}

// newTimeoutHTTPClient builds an *http.Client backed by a custom transport that
// applies independent connect/read/write timeouts. Any phase with a zero
// duration is left unbounded.
//
// The write timeout is an approximation: net/http exposes no first-class client
// write deadline, so we bound the idle time between reads of the request body
// (which the transport performs while writing the body to the wire). Likewise,
// the read timeout combines Transport.ResponseHeaderTimeout with an idle
// deadline between reads of the response body to approximate a full body-read
// timeout.
func newTimeoutHTTPClient(connect, read, write time.Duration) HTTPClient {
	dialer := &net.Dialer{
		Timeout: connect,
	}
	base := &http.Transport{
		Proxy:                 http.ProxyFromEnvironment,
		DialContext:           dialer.DialContext,
		ForceAttemptHTTP2:     true,
		MaxIdleConns:          100,
		IdleConnTimeout:       90 * time.Second,
		TLSHandshakeTimeout:   connect,
		ResponseHeaderTimeout: read,
		ExpectContinueTimeout: time.Second,
	}
	return &http.Client{
		Transport: &timeoutTransport{
			base:  base,
			read:  read,
			write: write,
		},
	}
}

// timeoutTransport wraps an *http.Transport to approximate per-phase read/write
// timeouts that net/http does not expose natively.
type timeoutTransport struct {
	base  *http.Transport
	read  time.Duration
	write time.Duration
}

func (t *timeoutTransport) RoundTrip(req *http.Request) (*http.Response, error) {
	if t.write > 0 && req.Body != nil {
		req.Body = &idleTimeoutReadCloser{
			rc:      req.Body,
			timeout: t.write,
		}
	}
	resp, err := t.base.RoundTrip(req)
	if err != nil {
		return nil, err
	}
	if t.read > 0 && resp.Body != nil {
		resp.Body = &idleTimeoutReadCloser{
			rc:      resp.Body,
			timeout: t.read,
		}
	}
	return resp, nil
}

// idleTimeoutReadCloser enforces a maximum idle duration for a single Read call.
// If a Read does not complete within the timeout, it returns a timeout error and
// closes the underlying reader to abort the in-flight I/O. This approximates a
// socket read/write deadline without direct access to the underlying connection.
//
// The background reader reads into an internal buffer rather than directly into
// the caller's slice, so a Read that has already timed out can never write into
// a buffer the caller may have reused. Closing the underlying reader on timeout
// unblocks and releases that goroutine instead of leaking it.
type idleTimeoutReadCloser struct {
	rc      io.ReadCloser
	timeout time.Duration

	buf []byte // reused across sequential Reads; abandoned after a timeout

	mu     sync.Mutex
	closed bool
}

type readResult struct {
	n   int
	err error
}

func (r *idleTimeoutReadCloser) Read(p []byte) (int, error) {
	if cap(r.buf) < len(p) {
		r.buf = make([]byte, len(p))
	}
	buf := r.buf[:len(p)]
	done := make(chan readResult, 1)
	go func() {
		n, err := r.rc.Read(buf)
		done <- readResult{n: n, err: err}
	}()
	timer := time.NewTimer(r.timeout)
	defer timer.Stop()
	select {
	case res := <-done:
		copy(p, buf[:res.n])
		return res.n, res.err
	case <-timer.C:
		// Abandon the buffer so a late write from the background goroutine cannot
		// corrupt a subsequent Read, and close the reader to unblock it.
		r.buf = nil
		_ = r.closeUnderlying()
		return 0, &timeoutError{}
	}
}

func (r *idleTimeoutReadCloser) Close() error {
	return r.closeUnderlying()
}

func (r *idleTimeoutReadCloser) closeUnderlying() error {
	r.mu.Lock()
	defer r.mu.Unlock()
	if r.closed {
		return nil
	}
	r.closed = true
	return r.rc.Close()
}

// timeoutError implements net.Error so callers can detect a timeout via the
// standard Timeout() method.
type timeoutError struct{}

func (e *timeoutError) Error() string   { return "fern: i/o timeout" }
func (e *timeoutError) Timeout() bool   { return true }
func (e *timeoutError) Temporary() bool { return true }
