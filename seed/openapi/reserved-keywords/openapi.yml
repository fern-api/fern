openapi: 3.0.1
info:
  title: reserved-keywords
  version: ''
paths:
  /:
    post:
      operationId: package_test
      tags:
        - Package
      parameters:
        - name: for
          in: query
          required: true
          schema:
            type: string
          example: for
          examples:
            Example1:
              value: for
      responses:
        '204':
          description: ''
components:
  schemas:
    Package:
      title: Package
      type: object
      properties:
        name:
          type: string
      required:
        - name
    Record:
      title: Record
      type: object
      properties:
        foo:
          type: object
          additionalProperties:
            type: string
        3d:
          type: integer
      required:
        - foo
        - 3d
  securitySchemes: {}
