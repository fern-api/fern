openapi: 3.1.0
info:
  title: simple-api
  version: 0.0.0
paths:
  /users/{id}:
    get:
      operationId: user_get
      tags:
        - User
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
          examples:
            Example1:
              value: id
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
              examples:
                Example1:
                  value:
                    id: id
                    name: name
                    email: email
      security:
        - Bearer: []
components:
  schemas:
    User:
      title: User
      type: object
      properties:
        id:
          type: string
          examples:
            - id
        name:
          type: string
          examples:
            - name
        email:
          type: string
          examples:
            - email
      required:
        - id
        - name
        - email
  securitySchemes:
    Bearer:
      type: http
      scheme: bearer
servers:
  - url: https://api.example.com
    description: Production
