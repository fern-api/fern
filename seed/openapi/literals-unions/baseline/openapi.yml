openapi: 3.1.0
info:
  title: literals-unions
  version: 0.0.0
paths: {}
components:
  schemas:
    LiteralString:
      title: LiteralString
      type: string
      const: literally
      description: A string literal.
    UnionOverLiteral:
      title: UnionOverLiteral
      oneOf:
        - type: string
        - $ref: '#/components/schemas/LiteralString'
      description: An undiscriminated union over a literal.
  securitySchemes: {}
