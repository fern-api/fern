openapi: 3.0.1
info:
  title: x-fern-default test
  version: ''
paths:
  /test/{region}/resource:
    get:
      operationId: test_get
      tags:
        - ''
      parameters:
        - name: region
          in: path
          required: true
          schema:
            type: string
          example: region
          examples:
            Example1:
              value: region
        - name: limit
          in: query
          required: false
          schema:
            type: string
            nullable: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TestGetResponse'
              examples:
                Example1:
                  value:
                    message: message
components:
  schemas:
    TestGetResponse:
      title: TestGetResponse
      type: object
      properties:
        message:
          type: string
          nullable: true
  securitySchemes: {}
