openapi: 3.0.1
info:
  title: alias-extends
  version: ''
paths:
  /extends/extended-inline-request-body:
    post:
      operationId: _extendedInlineRequestBody
      tags:
        - ''
      parameters: []
      responses:
        '204':
          description: ''
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                child:
                  type: string
                  example: child
              required:
                - child
              allOf:
                - $ref: '#/components/schemas/AliasType'
            examples:
              Example1:
                value:
                  parent: parent
                  child: child
components:
  schemas:
    AliasType:
      title: AliasType
      $ref: '#/components/schemas/Parent'
    Parent:
      title: Parent
      type: object
      properties:
        parent:
          type: string
      required:
        - parent
    Child:
      title: Child
      type: object
      properties:
        child:
          type: string
      required:
        - child
      allOf:
        - $ref: '#/components/schemas/Parent'
  securitySchemes: {}
