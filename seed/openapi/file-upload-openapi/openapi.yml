openapi: 3.1.0
info:
  title: api
  version: 0.0.0
paths:
  /upload-file:
    post:
      description: Upload a file to the database
      operationId: fileUploadExample_uploadFile
      tags:
        - FileUploadExample
      parameters: []
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FileId'
              examples:
                Example1:
                  value: string
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                name:
                  type: string
                file:
                  type: string
                  format: binary
components:
  schemas:
    FileId:
      title: FileId
      type: string
      description: The unique identifier for a File in the database
  securitySchemes: {}
