// This file was auto-generated by Fern from our API Definition.

import { SeedApiClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("CoreClient", () => {
    test("listThings", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            environment: { acme: server.baseUrl, oauth: server.baseUrl },
        });

        const rawResponseBody = { id: "id" };

        server.mockEndpoint().get("/things").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.core.listThings();
        expect(response).toEqual(rawResponseBody);
    });
});
