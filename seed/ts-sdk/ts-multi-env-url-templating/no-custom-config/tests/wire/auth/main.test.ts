// This file was auto-generated by Fern from our API Definition.

import { SeedApiClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("AuthClient", () => {
    test("getToken", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            environment: { acme: server.baseUrl, oauth: server.baseUrl },
        });

        const rawResponseBody = { access_token: "access_token" };

        server.mockEndpoint().post("/token").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.auth.getToken();
        expect(response).toEqual(rawResponseBody);
    });
});
