// This file was auto-generated by Fern from our API Definition.

import { SeedTsPaginationUriRequestParamsClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("UsersClient", () => {
    test("listWithUriPagination", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTsPaginationUriRequestParamsClient({ maxRetries: 0, environment: server.baseUrl });

        const rawResponseBody = {
            data: [
                { name: "Alice", id: 1 },
                { name: "Bob", id: 2 },
            ],
        };
        const mockResponseBody = { ...rawResponseBody, next: `${server.baseUrl}/accounts/acct_1/uri` };
        server
            .mockEndpoint()
            .get("/accounts/acct_1/uri")
            .respondWith()
            .statusCode(200)
            .jsonBody(mockResponseBody)
            .build();

        const expected = rawResponseBody;
        const page = await client.users.listWithUriPagination("acct_1", {
            filter: "name",
            page_size: 2,
        });

        expect(expected.data).toEqual(page.data);
    });

    test("listWithPathPagination", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTsPaginationUriRequestParamsClient({ maxRetries: 0, environment: server.baseUrl });

        const rawResponseBody = {
            data: [
                { name: "Alice", id: 1 },
                { name: "Bob", id: 2 },
            ],
        };
        const mockResponseBody = { ...rawResponseBody, next: "/accounts/acct_1/path" };
        server
            .mockEndpoint()
            .get("/accounts/acct_1/path")
            .respondWith()
            .statusCode(200)
            .jsonBody(mockResponseBody)
            .build();

        const expected = rawResponseBody;
        const page = await client.users.listWithPathPagination("acct_1", {
            filter: "name",
            page_size: 2,
        });

        expect(expected.data).toEqual(page.data);
    });

    test("searchWithUriPagination", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTsPaginationUriRequestParamsClient({ maxRetries: 0, environment: server.baseUrl });
        const rawRequestBody = { query: "alice" };
        const rawResponseBody = { data: [{ name: "Alice", id: 1 }] };
        const mockResponseBody = { ...rawResponseBody, next: `${server.baseUrl}/accounts/acct_1/search` };
        server
            .mockEndpoint()
            .post("/accounts/acct_1/search")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(mockResponseBody)
            .build();

        const expected = rawResponseBody;
        const page = await client.users.searchWithUriPagination("acct_1", {
            query: "alice",
        });

        expect(expected.data).toEqual(page.data);
    });
});
