// This file was auto-generated by Fern from our API Definition.

import { MovieDoesNotExistError } from "../../src/api/resources/imdb/errors/MovieDoesNotExistError";
import { MovieId } from "../../src/api/resources/imdb/types/MovieId";
import { SeedApiClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("ImdbClient", () => {
    test("createMovie", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { title: "title", rating: 1.1 };
        const rawResponseBody = "string";
        server
            .mockEndpoint()
            .post("/movies/create-movie")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.imdb.createMovie({
            title: "title",
            rating: 1.1,
        });
        expect(response).toEqual(MovieId("string"));
    });

    test("getMovie (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = { id: "id", title: "title", rating: 1.1 };
        server.mockEndpoint().get("/movies/movieId").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.imdb.getMovie(MovieId("movieId"));
        expect(response).toEqual({
            id: MovieId("id"),
            title: "title",
            rating: 1.1,
        });
    });

    test("getMovie (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = "string";
        server.mockEndpoint().get("/movies/movieId").respondWith().statusCode(404).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.imdb.getMovie(MovieId("movieId"));
        }).rejects.toThrow(MovieDoesNotExistError);
    });
});
