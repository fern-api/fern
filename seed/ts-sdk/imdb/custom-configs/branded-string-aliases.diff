diff --git a/.fern/metadata.json b/.fern/metadata.json
index d8b7bf00e12..3d7cbe000d6 100644
--- a/.fern/metadata.json
+++ b/.fern/metadata.json
@@ -2,6 +2,10 @@
     "cliVersion": "DUMMY",
     "generatorName": "fernapi/fern-typescript-sdk",
     "generatorVersion": "local",
+    "generatorConfig": {
+        "useBrandedStringAliases": true,
+        "maxRetries": 5
+    },
     "originGitCommit": "DUMMY",
     "originGitCommitIsDirty": null,
     "invokedBy": "ci",
diff --git a/reference.md b/reference.md
index d66e8e4144c..d2344bcf031 100644
--- a/reference.md
+++ b/reference.md
@@ -79,7 +79,7 @@ await client.imdb.createMovie({
 <dd>
 
 ```typescript
-await client.imdb.getMovie("movieId");
+await client.imdb.getMovie(SeedApi.MovieId("movieId"));
 
 ```
 </dd>
diff --git a/snippet.json b/snippet.json
index abb1f3cc77e..8069c2d223e 100644
--- a/snippet.json
+++ b/snippet.json
@@ -19,7 +19,7 @@
             },
             "snippet": {
                 "type": "typescript",
-                "client": "import { SeedApiClient } from \"@fern/imdb\";\n\nconst client = new SeedApiClient({ environment: \"YOUR_BASE_URL\", token: \"YOUR_TOKEN\" });\nawait client.imdb.getMovie(\"movieId\");\n"
+                "client": "import { SeedApi, SeedApiClient } from \"@fern/imdb\";\n\nconst client = new SeedApiClient({ environment: \"YOUR_BASE_URL\", token: \"YOUR_TOKEN\" });\nawait client.imdb.getMovie(SeedApi.MovieId(\"movieId\"));\n"
             }
         }
     ],
diff --git a/src/api/resources/imdb/client/Client.ts b/src/api/resources/imdb/client/Client.ts
index 4ff120202ae..236c2e30604 100644
--- a/src/api/resources/imdb/client/Client.ts
+++ b/src/api/resources/imdb/client/Client.ts
@@ -89,7 +89,7 @@ export class ImdbClient {
      * @throws {@link SeedApi.MovieDoesNotExistError}
      *
      * @example
-     *     await client.imdb.getMovie("movieId")
+     *     await client.imdb.getMovie(SeedApi.MovieId("movieId"))
      */
     public getMovie(
         movieId: SeedApi.MovieId,
diff --git a/src/api/resources/imdb/types/MovieId.ts b/src/api/resources/imdb/types/MovieId.ts
index 3c9d8c3f6d0..1762b2ad751 100644
--- a/src/api/resources/imdb/types/MovieId.ts
+++ b/src/api/resources/imdb/types/MovieId.ts
@@ -1,3 +1,11 @@
 // This file was auto-generated by Fern from our API Definition.
 
-export type MovieId = string;
+import type * as SeedApi from "../../../index.js";
+
+export type MovieId = string & {
+    MovieId: void;
+};
+
+export function MovieId(value: string): SeedApi.MovieId {
+    return value as unknown as SeedApi.MovieId;
+}
diff --git a/src/core/fetcher/requestWithRetries.ts b/src/core/fetcher/requestWithRetries.ts
index 1f689688c4b..a45878364e5 100644
--- a/src/core/fetcher/requestWithRetries.ts
+++ b/src/core/fetcher/requestWithRetries.ts
@@ -1,6 +1,6 @@
 const INITIAL_RETRY_DELAY = 1000; // in milliseconds
 const MAX_RETRY_DELAY = 60000; // in milliseconds
-const DEFAULT_MAX_RETRIES = 2;
+const DEFAULT_MAX_RETRIES = 5;
 const JITTER_FACTOR = 0.2; // 20% random jitter
 
 function addPositiveJitter(delay: number): number {
diff --git a/tests/wire/imdb.test.ts b/tests/wire/imdb.test.ts
index 92b03baf030..c87e08a29e3 100644
--- a/tests/wire/imdb.test.ts
+++ b/tests/wire/imdb.test.ts
@@ -35,7 +35,7 @@ describe("ImdbClient", () => {
 
         server.mockEndpoint().get("/movies/movieId").respondWith().statusCode(200).jsonBody(rawResponseBody).build();
 
-        const response = await client.imdb.getMovie("movieId");
+        const response = await client.imdb.getMovie(SeedApi.MovieId("movieId"));
         expect(response).toEqual(rawResponseBody);
     });
 
@@ -48,7 +48,7 @@ describe("ImdbClient", () => {
         server.mockEndpoint().get("/movies/movieId").respondWith().statusCode(404).jsonBody(rawResponseBody).build();
 
         await expect(async () => {
-            return await client.imdb.getMovie("movieId");
+            return await client.imdb.getMovie(SeedApi.MovieId("movieId"));
         }).rejects.toThrow(SeedApi.MovieDoesNotExistError);
     });
 });
