// This file was auto-generated by Fern from our API Definition.

import * as SeedErrors from "./api/index.js";
import type * as core from "./core/index.js";
import * as errors from "./errors/index.js";

export interface BaseClientOptions {
    environment: core.Supplier<string>;
    /** Specify a custom URL to connect the client to. */
    baseUrl?: core.Supplier<string>;
    /** Additional headers to include in requests. */
    headers?: Record<string, string | core.Supplier<string | null | undefined> | null | undefined>;
    /** The default maximum time to wait for a response in seconds. */
    timeoutInSeconds?: number;
    /** The default number of times to retry the request. Defaults to 2. */
    maxRetries?: number;
    /** Provide a custom fetch implementation. Useful for platforms that don't have a built-in fetch or need a custom implementation. */
    fetch?: typeof fetch;
    /** Configure logging for the client. */
    logging?: core.logging.LogConfig | core.logging.Logger;
}

export interface BaseRequestOptions {
    /** The maximum time to wait for a response in seconds. */
    timeoutInSeconds?: number;
    /** The number of times to retry the request. Defaults to 2. */
    maxRetries?: number;
    /** A hook to abort the request. */
    abortSignal?: AbortSignal;
    /** Additional query string parameters to include in the request. */
    queryParams?: Record<string, unknown>;
    /** Additional headers to include in the request. */
    headers?: Record<string, string | core.Supplier<string | null | undefined> | null | undefined>;
}
export function handleGlobalStatusCodeError(
    error: core.Fetcher.FailedStatusCodeError,
    rawResponse: core.RawResponse,
): never {
    switch (error.statusCode) {
        case 404:
            throw new SeedErrors.NotFoundError(error.body as SeedErrors.ErrorBody, rawResponse);
        case 400:
            throw new SeedErrors.BadRequestError(error.body as SeedErrors.ErrorBody, rawResponse);
        case 500:
            throw new SeedErrors.InternalServerError(error.body as SeedErrors.ErrorBody, rawResponse);
        default:
            throw new errors.SeedErrorsError({
                statusCode: error.statusCode,
                body: error.body,
                rawResponse: rawResponse,
            });
    }
}
export function handleNonStatusCodeError(
    error: core.Fetcher.Error,
    rawResponse: core.RawResponse,
    method: string,
    path: string,
): never {
    switch (error.reason) {
        case "non-json":
            throw new errors.SeedErrorsError({
                statusCode: error.statusCode,
                body: error.rawBody,
                rawResponse: rawResponse,
            });
        case "timeout":
            throw new errors.SeedErrorsTimeoutError(`Timeout exceeded when calling ${method} ${path}.`);
        case "unknown":
            throw new errors.SeedErrorsError({
                message: error.errorMessage,
                rawResponse: rawResponse,
            });
        default:
            throw new errors.SeedErrorsError({
                message: "Unknown error",
                rawResponse: rawResponse,
            });
    }
}
