// This file was auto-generated by Fern from our API Definition.

import type { BaseClientOptions, BaseRequestOptions } from "../../../../BaseClient.js";
import { type NormalizedClientOptions, normalizeClientOptions } from "../../../../BaseClient.js";
import { mergeHeaders } from "../../../../core/headers.js";
import * as core from "../../../../core/index.js";
import { handleNonStatusCodeError } from "../../../../errors/handleNonStatusCodeError.js";
import * as errors from "../../../../errors/index.js";
import type * as SeedTsBytesUploadPathParam from "../../../index.js";

export declare namespace ServiceClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {}
}

export class ServiceClient {
    protected readonly _options: NormalizedClientOptions<ServiceClient.Options>;

    constructor(options: ServiceClient.Options) {
        this._options = normalizeClientOptions(options);
    }

    /**
     * Bytes endpoint with required path parameters whose only example supplies no
     * path-parameter values (autogenerated examples are skipped for bytes bodies). The
     * generated example must still pass a value of the declared type for every path
     * parameter instead of omitting it or passing `undefined`. A parameter with a client
     * default is left out of the example, since the client supplies it.
     *
     * @param {core.file.Uploadable} uploadable
     * @param {string} tenantId
     * @param {string} objectPath
     * @param {number} revision
     * @param {string} uploadedAt
     * @param {SeedTsBytesUploadPathParam.BucketRegion} region
     * @param {ServiceClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link errors.SeedTsBytesUploadPathParamError}
     * @throws {@link errors.SeedTsBytesUploadPathParamTimeoutError}
     *
     * @example
     *     import { createReadStream } from "fs";
     *     await client.service.uploadWithPathParam(createReadStream("path/to/file"), "acme", "objectPath", 1000000, "2024-01-15T09:30:00Z", "us-east")
     */
    public uploadWithPathParam(
        uploadable: core.file.Uploadable,
        tenantId: string,
        objectPath: string,
        revision: number,
        uploadedAt: string,
        region: SeedTsBytesUploadPathParam.BucketRegion,
        requestOptions?: ServiceClient.RequestOptions,
    ): core.HttpResponsePromise<void> {
        return core.HttpResponsePromise.fromPromise(
            this.__uploadWithPathParam(uploadable, tenantId, objectPath, revision, uploadedAt, region, requestOptions),
        );
    }

    private async __uploadWithPathParam(
        uploadable: core.file.Uploadable,
        tenantId: string,
        objectPath: string,
        revision: number,
        uploadedAt: string,
        region: SeedTsBytesUploadPathParam.BucketRegion,
        requestOptions?: ServiceClient.RequestOptions,
    ): Promise<core.WithRawResponse<void>> {
        const _binaryUploadRequest = await core.file.toBinaryUploadRequest(uploadable);
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            this._options?.headers,
            _binaryUploadRequest.headers,
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)),
                `upload-content/${core.url.encodePathParam(tenantId ?? "acme")}/${core.url.encodePathParam(objectPath)}/${core.url.encodePathParam(revision)}/${core.url.encodePathParam(uploadedAt)}/${core.url.encodePathParam(region)}`,
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/octet-stream",
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            requestType: "bytes",
            duplex: "half",
            body: _binaryUploadRequest.body,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: undefined, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            throw new errors.SeedTsBytesUploadPathParamError({
                statusCode: _response.error.statusCode,
                body: _response.error.body,
                rawResponse: _response.rawResponse,
            });
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "POST",
            "/upload-content/{tenantId}/{objectPath}/{revision}/{uploadedAt}/{region}",
        );
    }

    /**
     * Endpoint with a request wrapper carrying the path parameters. Its second example
     * supplies no path-parameter values, so the generated example must still populate every
     * path parameter the wrapper carries (`inlinePathParameters`) or passes positionally.
     *
     * @param {SeedTsBytesUploadPathParam.UpdateMetadataRequest} request
     * @param {ServiceClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link errors.SeedTsBytesUploadPathParamError}
     * @throws {@link errors.SeedTsBytesUploadPathParamTimeoutError}
     *
     * @example
     *     await client.service.updateMetadataWithPathParam({
     *         tenantId: "acme",
     *         objectPath: "path/to/object.txt",
     *         label: "primary"
     *     })
     *
     * @example
     *     await client.service.updateMetadataWithPathParam({
     *         objectPath: "objectPath"
     *     })
     */
    public updateMetadataWithPathParam(
        request: SeedTsBytesUploadPathParam.UpdateMetadataRequest,
        requestOptions?: ServiceClient.RequestOptions,
    ): core.HttpResponsePromise<void> {
        return core.HttpResponsePromise.fromPromise(this.__updateMetadataWithPathParam(request, requestOptions));
    }

    private async __updateMetadataWithPathParam(
        request: SeedTsBytesUploadPathParam.UpdateMetadataRequest,
        requestOptions?: ServiceClient.RequestOptions,
    ): Promise<core.WithRawResponse<void>> {
        const { tenantId, objectPath, label } = request;
        const _queryParams: Record<string, unknown> = {
            label,
        };
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(this._options?.headers, requestOptions?.headers);
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)),
                `upload-content/${core.url.encodePathParam(tenantId ?? "acme")}/${core.url.encodePathParam(objectPath)}/metadata`,
            ),
            method: "POST",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: undefined, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            throw new errors.SeedTsBytesUploadPathParamError({
                statusCode: _response.error.statusCode,
                body: _response.error.body,
                rawResponse: _response.rawResponse,
            });
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "POST",
            "/upload-content/{tenantId}/{objectPath}/metadata",
        );
    }
}
