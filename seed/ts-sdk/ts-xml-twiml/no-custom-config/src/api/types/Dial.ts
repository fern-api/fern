// This file was auto-generated by Fern from our API Definition.

import * as core from "../../core/index.js";
import * as SeedApi from "../index.js";

export class Dial implements core.xml.XmlSerializable {
    number?: string;
    status_callback_event?: string[];
    record?: Dial.Record_.Item[];
    numbers?: SeedApi.Number[];
    /** Attributes not declared in the API definition. */
    additionalAttributes: Record<string, string>;
    /** Child elements not declared in the API definition. */
    additionalChildren: core.xml.XmlElement[];

    constructor(fields: Dial.Fields = {}) {
        this.number = fields.number;
        this.status_callback_event = fields.status_callback_event;
        this.record = fields.record;
        this.numbers = fields.numbers;
        this.additionalAttributes = fields.additionalAttributes ?? {};
        this.additionalChildren = fields.additionalChildren ?? [];
    }

    static builder(fields: Dial.Fields = {}): Dial.Builder {
        return new Dial.Builder(fields);
    }

    /** Parses a `<Dial>` element. */
    static fromXml(xml: string | core.xml.XmlNode): Dial {
        const node = core.xml.parseXml(xml, "Dial");
        return new Dial({
            number: core.xml.xmlScalar(core.xml.xmlText(node), core.xml.xmlString, "Dial.number"),
            status_callback_event: core.xml.xmlScalarList(
                core.xml.xmlAttribute(node, "statusCallbackEvent"),
                " ",
                core.xml.xmlString,
                "Dial.statusCallbackEvent",
            ),
            record: core.xml.xmlScalarList(
                core.xml.xmlAttribute(node, "record"),
                " ",
                core.xml.xmlEnum(["record-from-answer", "record-from-ringing"]),
                "Dial.record",
            ),
            numbers: core.xml.xmlChildren<SeedApi.Number>(
                node,
                { Number: (child) => SeedApi.Number.fromXml(child) },
                { wrapper: "Numbers" },
            ),
            additionalAttributes: core.xml.xmlExtraAttributes(node, ["statusCallbackEvent", "record"]),
            additionalChildren: core.xml.xmlUnknownChildren(node, ["Numbers"], { Numbers: ["Number"] }),
        });
    }

    toXml(): string {
        return core.xml.serializeXmlElement({
            name: "Dial",
            namespace: "https://www.twilio.com/twiml",
            prefix: "tw",
            attributes: [
                { name: "statusCallbackEvent", value: this.status_callback_event, separator: " " },
                { name: "record", value: this.record, separator: " " },
                ...core.xml.extraXmlAttributes(this.additionalAttributes),
            ],
            text: this.number,
            children: [{ name: "Numbers", value: this.numbers, wrapped: true }],
            additionalChildren: this.additionalChildren,
        });
    }

    toString(): string {
        return this.toXml();
    }
}

export namespace Dial {
    export type Record_ = Record_.Item[];

    export namespace Record_ {
        export const Item = {
            RecordFromAnswer: "record-from-answer",
            RecordFromRinging: "record-from-ringing",
        } as const;
        export type Item = (typeof Item)[keyof typeof Item];
    }

    export interface Fields {
        number?: string;
        status_callback_event?: string[];
        record?: Dial.Record_.Item[];
        numbers?: SeedApi.Number[];
        additionalAttributes?: Record<string, string>;
        additionalChildren?: core.xml.XmlElement[];
    }

    export class Builder implements core.xml.XmlBuilder<Dial> {
        private readonly fields: Partial<Dial.Fields>;
        private readonly elements: { numbers?: (SeedApi.Number | core.xml.XmlBuilder<SeedApi.Number>)[] };

        constructor(fields: Partial<Dial.Fields> = {}) {
            const { numbers, ...rest } = fields;
            this.fields = rest;
            this.elements = { numbers };
        }

        /** Parses a `<Dial>` element into a builder. */
        static fromXml(xml: string | core.xml.XmlNode): Dial.Builder {
            return new Dial.Builder(Dial.fromXml(xml));
        }

        number(number: string | undefined): this {
            this.fields.number = number;
            return this;
        }

        status_callback_event(status_callback_event: string[] | undefined): this {
            this.fields.status_callback_event = status_callback_event;
            return this;
        }

        record(record: Dial.Record_.Item[] | undefined): this {
            this.fields.record = record;
            return this;
        }

        numbers(numbers: (SeedApi.Number | core.xml.XmlBuilder<SeedApi.Number>)[] | undefined): this {
            this.elements.numbers = numbers;
            return this;
        }

        /** Adds a `<Number>` child and returns its builder. */
        addNumber(fields?: Partial<SeedApi.Number.Fields>): SeedApi.Number.Builder {
            const builder = new SeedApi.Number.Builder(fields);
            this.elements.numbers = [...(this.elements.numbers ?? []), builder];
            return builder;
        }

        /** Sets an attribute that is not declared in the API definition. */
        attribute(name: string, value: string): this {
            this.fields.additionalAttributes = { ...this.fields.additionalAttributes, [name]: value };
            return this;
        }

        /** Appends a child element that is not declared in the API definition. */
        addChild(child: core.xml.XmlElement): this {
            this.fields.additionalChildren = [...(this.fields.additionalChildren ?? []), child];
            return this;
        }

        build(): Dial {
            return new Dial({ ...this.fields, numbers: core.xml.xmlBuildAll(this.elements.numbers) });
        }

        toXml(): string {
            return this.build().toXml();
        }

        toString(): string {
            return this.toXml();
        }
    }
}
