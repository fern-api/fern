/**
 * This file was auto-generated by Fern from our API Definition.
 */

import { SeedMultiLineDocsClient } from "../src/Client";

const client = new SeedMultiLineDocsClient({ environment: process.env.TESTS_BASE_URL || "test" });

describe("User", () => {
    test("getUser", async () => {
        const response = await client.user.getUser("string");
        expect(response).toEqual(undefined);
    });

    test("createUser", async () => {
        const response = await client.user.createUser({
            name: "string",
            age: 1,
        });
        expect(response).toEqual({ id: "string", name: "string", age: 1 });
    });
});
