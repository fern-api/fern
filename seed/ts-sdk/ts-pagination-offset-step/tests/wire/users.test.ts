// This file was auto-generated by Fern from our API Definition.

import { SeedTsPaginationOffsetStepClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("UsersClient", () => {
    test("list (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTsPaginationOffsetStepClient({ maxRetries: 0, environment: server.baseUrl });

        const rawResponseBody = {
            total_count: 10,
            data: [
                { name: "Alice", id: 1 },
                { name: "Bob", id: 2 },
            ],
        };

        server
            .mockEndpoint({ once: false })
            .get("/users")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const expected = rawResponseBody;
        const page = await client.users.list({
            offset: 0,
            limit: 2,
        });

        expect(expected.data).toEqual(page.data);
        expect(page.hasNextPage()).toBe(true);
        const nextPage = await page.getNextPage();
        expect(expected.data).toEqual(nextPage.data);
    });

    test("list (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTsPaginationOffsetStepClient({ maxRetries: 0, environment: server.baseUrl });

        const rawResponseBody = {
            total_count: 2,
            data: [
                { name: "Alice", id: 1 },
                { name: "Bob", id: 2 },
            ],
        };

        server.mockEndpoint().get("/users").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const expected = rawResponseBody;
        const page = await client.users.list({
            offset: 0,
            limit: 10,
        });

        expect(expected.data).toEqual(page.data);
    });

    test("listWithHasNextPage", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTsPaginationOffsetStepClient({ maxRetries: 0, environment: server.baseUrl });

        const rawResponseBody = {
            has_next_page: true,
            total_count: 20,
            data: [
                { name: "Alice", id: 1 },
                { name: "Bob", id: 2 },
            ],
        };

        server
            .mockEndpoint({ once: false })
            .get("/users/with-has-next-page")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const expected = rawResponseBody;
        const page = await client.users.listWithHasNextPage({
            offset: 0,
            limit: 10,
        });

        expect(expected.data).toEqual(page.data);
        expect(page.hasNextPage()).toBe(true);
        const nextPage = await page.getNextPage();
        expect(expected.data).toEqual(nextPage.data);
    });
});
