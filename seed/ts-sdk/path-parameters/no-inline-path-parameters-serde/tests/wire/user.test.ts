// This file was auto-generated by Fern from our API Definition.

import { SeedApiClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("UserClient", () => {
    test("getuser", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });

        const rawResponseBody = { name: "name", tags: ["tags"] };

        server
            .mockEndpoint()
            .get("/tenant_id/user/user_id")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.user.getuser("tenant_id", "user_id");
        expect(response).toEqual({
            name: "name",
            tags: ["tags"],
        });
    });

    test("updateuser", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });
        const rawRequestBody = { name: "name", tags: ["tags"] };
        const rawResponseBody = { name: "name", tags: ["tags"] };

        server
            .mockEndpoint()
            .patch("/tenant_id/user/user_id")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.user.updateuser("tenant_id", "user_id", {
            body: {
                name: "name",
                tags: ["tags"],
            },
        });
        expect(response).toEqual({
            name: "name",
            tags: ["tags"],
        });
    });

    test("createuser", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });
        const rawRequestBody = { name: "name", tags: ["tags"] };
        const rawResponseBody = { name: "name", tags: ["tags"] };

        server
            .mockEndpoint()
            .post("/tenant_id/user/")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.user.createuser("tenant_id", {
            body: {
                name: "name",
                tags: ["tags"],
            },
        });
        expect(response).toEqual({
            name: "name",
            tags: ["tags"],
        });
    });

    test("searchusers", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });

        const rawResponseBody = [{ name: "name", tags: ["tags"] }];

        server
            .mockEndpoint()
            .get("/tenant_id/user/user_id/search")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.user.searchusers("tenant_id", "user_id");
        expect(response).toEqual([
            {
                name: "name",
                tags: ["tags"],
            },
        ]);
    });

    test("getusermetadata", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });

        const rawResponseBody = { name: "name", tags: ["tags"] };

        server
            .mockEndpoint()
            .get("/tenant_id/user/user_id/metadata/v1")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.user.getusermetadata("tenant_id", "user_id", 1);
        expect(response).toEqual({
            name: "name",
            tags: ["tags"],
        });
    });

    test("getuserspecifics", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });

        const rawResponseBody = { name: "name", tags: ["tags"] };

        server
            .mockEndpoint()
            .get("/tenant_id/user/user_id/specifics/1/thought")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.user.getuserspecifics("tenant_id", "user_id", 1, "thought");
        expect(response).toEqual({
            name: "name",
            tags: ["tags"],
        });
    });
});
