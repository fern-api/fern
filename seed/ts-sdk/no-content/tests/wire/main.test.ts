// This file was auto-generated by Fern from our API Definition.

import * as SeedApi from "../../src/api/index";
import { SeedApiClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("SeedApiClient", () => {
    test("plants_get (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });

        const rawResponseBody = { id: "id", name: "name", species: "species" };

        server.mockEndpoint().get("/plants/plantId").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.plantsGet({
            plantId: "plantId",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("plants_get (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });

        const rawResponseBody = { message: "message" };

        server.mockEndpoint().get("/plants/plantId").respondWith().statusCode(404).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.plantsGet({
                plantId: "plantId",
            });
        }).rejects.toThrow(SeedApi.NotFoundError);
    });

    test("plants_delete (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });

        server.mockEndpoint().delete("/plants/plantId").respondWith().statusCode(200).build();

        const response = await client.plantsDelete({
            plantId: "plantId",
        });
        expect(response).toEqual(undefined);
    });

    test("plants_delete (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });

        const rawResponseBody = { message: "message" };

        server.mockEndpoint().delete("/plants/plantId").respondWith().statusCode(404).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.plantsDelete({
                plantId: "plantId",
            });
        }).rejects.toThrow(SeedApi.NotFoundError);
    });
});
