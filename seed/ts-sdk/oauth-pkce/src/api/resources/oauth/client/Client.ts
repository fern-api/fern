// This file was auto-generated by Fern from our API Definition.

import type { BaseClientOptions, BaseRequestOptions } from "../../../../BaseClient.js";
import { type NormalizedClientOptions, normalizeClientOptions } from "../../../../BaseClient.js";
import { mergeHeaders } from "../../../../core/headers.js";
import * as core from "../../../../core/index.js";
import { handleNonStatusCodeError } from "../../../../errors/handleNonStatusCodeError.js";
import * as errors from "../../../../errors/index.js";
import type * as SeedOauthPkce from "../../../index.js";

export declare namespace OauthClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {}
}

export class OauthClient {
    protected readonly _options: NormalizedClientOptions<OauthClient.Options>;

    constructor(options: OauthClient.Options) {
        this._options = normalizeClientOptions(options);
    }

    /**
     * Authorization-code grant with PKCE. `response_type` is a required literal that is
     * hardcoded by the generated method; `code_challenge_method` is an optional literal
     * that must still be sent on the wire when provided.
     *
     * @param {SeedOauthPkce.AuthorizeRequest} request
     * @param {OauthClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link errors.SeedOauthPkceError}
     * @throws {@link errors.SeedOauthPkceTimeoutError}
     *
     * @example
     *     await client.oauth.authorize({
     *         response_type: "code",
     *         client_id: "client_abc123",
     *         redirect_uri: "https://example.com/callback",
     *         code_challenge: "E9Melhoa2OwvFrEMTJguCHaoeK1t8URWbuGJSstw-cM",
     *         code_challenge_method: "S256",
     *         scope: "read write",
     *         state: "xyz"
     *     })
     */
    public authorize(
        request: SeedOauthPkce.AuthorizeRequest,
        requestOptions?: OauthClient.RequestOptions,
    ): core.HttpResponsePromise<SeedOauthPkce.AuthorizeResponse> {
        return core.HttpResponsePromise.fromPromise(this.__authorize(request, requestOptions));
    }

    private async __authorize(
        request: SeedOauthPkce.AuthorizeRequest,
        requestOptions?: OauthClient.RequestOptions,
    ): Promise<core.WithRawResponse<SeedOauthPkce.AuthorizeResponse>> {
        const {
            response_type: responseType,
            client_id: clientId,
            redirect_uri: redirectUri,
            code_challenge: codeChallenge,
            code_challenge_method: codeChallengeMethod,
            scope,
            state,
        } = request;
        const _queryParams: Record<string, unknown> = {
            response_type: responseType,
            client_id: clientId,
            redirect_uri: redirectUri,
            code_challenge: codeChallenge,
            code_challenge_method: codeChallengeMethod != null ? codeChallengeMethod : undefined,
            scope,
            state,
        };
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(this._options?.headers, requestOptions?.headers);
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)),
                "oauth/authorize",
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as SeedOauthPkce.AuthorizeResponse, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            throw new errors.SeedOauthPkceError({
                statusCode: _response.error.statusCode,
                body: _response.error.body,
                rawResponse: _response.rawResponse,
            });
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "GET", "/oauth/authorize");
    }
}
