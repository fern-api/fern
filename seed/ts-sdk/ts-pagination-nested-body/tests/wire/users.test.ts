// This file was auto-generated by Fern from our API Definition.

import { SeedTsPaginationNestedBodyClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("UsersClient", () => {
    test("listWithNestedOffset", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTsPaginationNestedBodyClient({ maxRetries: 0, environment: server.baseUrl });
        const rawRequestBody = { filter: "active" };
        const rawResponseBody = {
            total_count: 10,
            data: [
                { name: "Alice", id: 1 },
                { name: "Bob", id: 2 },
            ],
        };

        server
            .mockEndpoint({ once: false })
            .post("/users/nested-offset")
            .jsonBody(rawRequestBody, { ignoredFields: ["options.offset"] })
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const expected = rawResponseBody;
        const page = await client.users.listWithNestedOffset({
            filter: "active",
        });

        expect(expected.data).toEqual(page.data);
        expect(page.hasNextPage()).toBe(true);
        const nextPage = await page.getNextPage();
        expect(expected.data).toEqual(nextPage.data);
    });

    test("listWithNestedOffsetAndStep", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTsPaginationNestedBodyClient({ maxRetries: 0, environment: server.baseUrl });
        const rawRequestBody = { filter: "active", options: { offset: 0, count: 2 } };
        const rawResponseBody = {
            total_count: 10,
            data: [
                { name: "Alice", id: 1 },
                { name: "Bob", id: 2 },
            ],
        };

        server
            .mockEndpoint({ once: false })
            .post("/users/nested-offset-step")
            .jsonBody(rawRequestBody, { ignoredFields: ["options.offset"] })
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const expected = rawResponseBody;
        const page = await client.users.listWithNestedOffsetAndStep({
            filter: "active",
            options: {
                offset: 0,
                count: 2,
            },
        });

        expect(expected.data).toEqual(page.data);
        expect(page.hasNextPage()).toBe(true);
        const nextPage = await page.getNextPage();
        expect(expected.data).toEqual(nextPage.data);
    });

    test("listWithNestedCursor", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTsPaginationNestedBodyClient({ maxRetries: 0, environment: server.baseUrl });
        const rawRequestBody = { filter: "active" };
        const rawResponseBody = { next_cursor: "next", data: [{ name: "Alice", id: 1 }] };

        server
            .mockEndpoint({ once: false })
            .post("/users/nested-cursor")
            .jsonBody(rawRequestBody, { ignoredFields: ["options.cursor"] })
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const expected = rawResponseBody;
        const page = await client.users.listWithNestedCursor({
            filter: "active",
        });

        expect(expected.data).toEqual(page.data);
        expect(page.hasNextPage()).toBe(true);
        const nextPage = await page.getNextPage();
        expect(expected.data).toEqual(nextPage.data);
    });

    test("listWithDeeplyNestedCursor", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTsPaginationNestedBodyClient({ maxRetries: 0, environment: server.baseUrl });
        const rawRequestBody = { filter: "active" };
        const rawResponseBody = { next_cursor: "next", data: [{ name: "Alice", id: 1 }] };

        server
            .mockEndpoint({ once: false })
            .post("/users/deeply-nested-cursor")
            .jsonBody(rawRequestBody, { ignoredFields: ["options.pagination.cursor"] })
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const expected = rawResponseBody;
        const page = await client.users.listWithDeeplyNestedCursor({
            filter: "active",
        });

        expect(expected.data).toEqual(page.data);
        expect(page.hasNextPage()).toBe(true);
        const nextPage = await page.getNextPage();
        expect(expected.data).toEqual(nextPage.data);
    });
});
