// @vitest-environment happy-dom
// This file was auto-generated by Fern from our API Definition.
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { renderHook, waitFor } from "@testing-library/react";
import type { ReactNode } from "react";
import { createElement } from "react";
import { describe, expect, it, vi } from "vitest";

import { ClientProvider } from "../../../src/react-query/context.js";
import { useGetAndReturnListOfPrimitivesMutation } from "../../../src/react-query/endpoints/container/index.js";
import { useTestGet, useTestPutMutation } from "../../../src/react-query/endpoints/httpMethods/index.js";
import { useListItems } from "../../../src/react-query/endpoints/pagination/index.js";
import { useWithMixedCase } from "../../../src/react-query/endpoints/urls/index.js";
import { usePostWithNoRequestBodyMutation } from "../../../src/react-query/noReqBody/index.js";

function createMockClient(): any {
    return {
        endpoints: {
            container: {
                getAndReturnListOfPrimitives: vi.fn().mockResolvedValue("mock-result"),
                getAndReturnListOfObjects: vi.fn().mockResolvedValue("mock-result"),
                getAndReturnSetOfPrimitives: vi.fn().mockResolvedValue("mock-result"),
                getAndReturnSetOfObjects: vi.fn().mockResolvedValue("mock-result"),
                getAndReturnMapPrimToPrim: vi.fn().mockResolvedValue("mock-result"),
                getAndReturnMapOfPrimToObject: vi.fn().mockResolvedValue("mock-result"),
                getAndReturnMapOfPrimToUndiscriminatedUnion: vi.fn().mockResolvedValue("mock-result"),
                getAndReturnOptional: vi.fn().mockResolvedValue("mock-result"),
            },
            contentType: {
                postJsonPatchContentType: vi.fn().mockResolvedValue("mock-result"),
                postJsonPatchContentWithCharsetType: vi.fn().mockResolvedValue("mock-result"),
            },
            enum: {
                getAndReturnEnum: vi.fn().mockResolvedValue("mock-result"),
            },
            httpMethods: {
                testGet: vi.fn().mockResolvedValue("mock-result"),
                testPost: vi.fn().mockResolvedValue("mock-result"),
                testPut: vi.fn().mockResolvedValue("mock-result"),
                testPatch: vi.fn().mockResolvedValue("mock-result"),
                testDelete: vi.fn().mockResolvedValue("mock-result"),
            },
            object: {
                getAndReturnWithOptionalField: vi.fn().mockResolvedValue("mock-result"),
                getAndReturnWithRequiredField: vi.fn().mockResolvedValue("mock-result"),
                getAndReturnWithMapOfMap: vi.fn().mockResolvedValue("mock-result"),
                getAndReturnNestedWithOptionalField: vi.fn().mockResolvedValue("mock-result"),
                getAndReturnNestedWithRequiredField: vi.fn().mockResolvedValue("mock-result"),
                getAndReturnNestedWithRequiredFieldAsList: vi.fn().mockResolvedValue("mock-result"),
                getAndReturnWithUnknownField: vi.fn().mockResolvedValue("mock-result"),
                getAndReturnWithDatetimeLikeString: vi.fn().mockResolvedValue("mock-result"),
            },
            pagination: {
                listItems: vi.fn().mockResolvedValue("mock-result"),
            },
            params: {
                getWithPath: vi.fn().mockResolvedValue("mock-result"),
                getWithInlinePath: vi.fn().mockResolvedValue("mock-result"),
                getWithQuery: vi.fn().mockResolvedValue("mock-result"),
                getWithAllowMultipleQuery: vi.fn().mockResolvedValue("mock-result"),
                getWithPathAndQuery: vi.fn().mockResolvedValue("mock-result"),
                getWithInlinePathAndQuery: vi.fn().mockResolvedValue("mock-result"),
                modifyWithPath: vi.fn().mockResolvedValue("mock-result"),
                modifyWithInlinePath: vi.fn().mockResolvedValue("mock-result"),
                uploadWithPath: vi.fn().mockResolvedValue("mock-result"),
            },
            primitive: {
                getAndReturnString: vi.fn().mockResolvedValue("mock-result"),
                getAndReturnInt: vi.fn().mockResolvedValue("mock-result"),
                getAndReturnLong: vi.fn().mockResolvedValue("mock-result"),
                getAndReturnDouble: vi.fn().mockResolvedValue("mock-result"),
                getAndReturnBool: vi.fn().mockResolvedValue("mock-result"),
                getAndReturnDatetime: vi.fn().mockResolvedValue("mock-result"),
                getAndReturnDate: vi.fn().mockResolvedValue("mock-result"),
                getAndReturnUuid: vi.fn().mockResolvedValue("mock-result"),
                getAndReturnBase64: vi.fn().mockResolvedValue("mock-result"),
            },
            put: {
                add: vi.fn().mockResolvedValue("mock-result"),
            },
            union: {
                getAndReturnUnion: vi.fn().mockResolvedValue("mock-result"),
            },
            urls: {
                withMixedCase: vi.fn().mockResolvedValue("mock-result"),
                noEndingSlash: vi.fn().mockResolvedValue("mock-result"),
                withEndingSlash: vi.fn().mockResolvedValue("mock-result"),
                withUnderscores: vi.fn().mockResolvedValue("mock-result"),
            },
        },
        inlinedRequests: {
            postWithObjectBodyandResponse: vi.fn().mockResolvedValue("mock-result"),
        },
        noAuth: {
            postWithNoAuth: vi.fn().mockResolvedValue("mock-result"),
        },
        noReqBody: {
            getWithNoRequestBody: vi.fn().mockResolvedValue("mock-result"),
            postWithNoRequestBody: vi.fn().mockResolvedValue("mock-result"),
        },
        reqWithHeaders: {
            getWithCustomHeader: vi.fn().mockResolvedValue("mock-result"),
        },
    };
}

function createWrapper(client: any) {
    const queryClient = new QueryClient({
        defaultOptions: {
            queries: { retry: false },
            mutations: { retry: false },
        },
    });
    return function Wrapper({ children }: { children: ReactNode }) {
        return createElement(
            QueryClientProvider,
            { client: queryClient },
            createElement(ClientProvider, { client }, children),
        );
    };
}

describe("Query Hooks", () => {
    describe("useWithMixedCase (no-param query)", () => {
        it("should fetch data using the client from context", async () => {
            const client = createMockClient();
            const wrapper = createWrapper(client);
            const { result } = renderHook(() => useWithMixedCase(), { wrapper });
            await waitFor(() => expect(result.current.isSuccess).toBe(true));
            expect(client.endpoints.urls.withMixedCase).toHaveBeenCalled();
        });

        it("should accept TanStack Query option overrides", async () => {
            const client = createMockClient();
            const wrapper = createWrapper(client);
            const { result } = renderHook(() => useWithMixedCase(undefined, { enabled: false }), { wrapper });
            expect(result.current.isFetching).toBe(false);
            expect(client.endpoints.urls.withMixedCase).not.toHaveBeenCalled();
        });
    });

    describe("useListItems (single-param query)", () => {
        it("should pass request to the client method", async () => {
            const client = createMockClient();
            const wrapper = createWrapper(client);
            const request = {} as any;
            const { result } = renderHook(() => useListItems(request), { wrapper });
            await waitFor(() => expect(result.current.isSuccess).toBe(true));
            expect(client.endpoints.pagination.listItems).toHaveBeenCalledWith(request, undefined);
        });
    });

    describe("useTestGet (multi-param query)", () => {
        it("should pass path param and request to the client method", async () => {
            const client = createMockClient();
            const wrapper = createWrapper(client);
            const { result } = renderHook(() => useTestGet("test-path"), { wrapper });
            await waitFor(() => expect(result.current.isSuccess).toBe(true));
            expect(client.endpoints.httpMethods.testGet).toHaveBeenCalledWith("test-path", undefined);
        });
    });
});

describe("Mutation Hooks", () => {
    describe("usePostWithNoRequestBodyMutation (no-param mutation)", () => {
        it("should call the client method on mutate()", async () => {
            const client = createMockClient();
            const wrapper = createWrapper(client);
            const { result } = renderHook(() => usePostWithNoRequestBodyMutation(), { wrapper });
            result.current.mutate();
            await waitFor(() => expect(result.current.isSuccess).toBe(true));
            expect(client.noReqBody.postWithNoRequestBody).toHaveBeenCalled();
        });
    });

    describe("useGetAndReturnListOfPrimitivesMutation (single-param mutation)", () => {
        it("should pass the variable directly (not as tuple)", async () => {
            const client = createMockClient();
            const wrapper = createWrapper(client);
            const { result } = renderHook(() => useGetAndReturnListOfPrimitivesMutation(), { wrapper });
            result.current.mutate("test-value" as any);
            await waitFor(() => expect(result.current.isSuccess).toBe(true));
            expect(client.endpoints.container.getAndReturnListOfPrimitives).toHaveBeenCalledWith(
                "test-value",
                undefined,
            );
        });
    });

    describe("useTestPutMutation (multi-param mutation)", () => {
        it("should pass tuple args to the client method", async () => {
            const client = createMockClient();
            const wrapper = createWrapper(client);
            const { result } = renderHook(() => useTestPutMutation(), { wrapper });
            result.current.mutate(["path", "body"] as any);
            await waitFor(() => expect(result.current.isSuccess).toBe(true));
            expect(client.endpoints.httpMethods.testPut).toHaveBeenCalledWith("path", "body");
        });
    });

    describe("mutation option overrides", () => {
        it("should accept onSuccess callback", async () => {
            const client = createMockClient();
            const wrapper = createWrapper(client);
            const onSuccess = vi.fn();
            const { result } = renderHook(() => usePostWithNoRequestBodyMutation(undefined, { onSuccess }), {
                wrapper,
            });
            result.current.mutate();
            await waitFor(() => expect(result.current.isSuccess).toBe(true));
            expect(onSuccess).toHaveBeenCalled();
        });
    });
});
