// This file was auto-generated by Fern from our API Definition.
import { describe, expect, it } from "vitest";

import { testGetQueryKey } from "../../../src/react-query/endpoints/httpMethods/index.js";
import { listItemsQueryKey } from "../../../src/react-query/endpoints/pagination/index.js";
import { getWithInlinePathQueryKey, getWithPathQueryKey } from "../../../src/react-query/endpoints/params/index.js";
import { withMixedCaseQueryKey } from "../../../src/react-query/endpoints/urls/index.js";

describe("Query Key Functions", () => {
    describe("no-param queries", () => {
        it("should return a key with service path segments only", () => {
            const key = withMixedCaseQueryKey();
            expect(key).toEqual(["SeedExhaustiveClient", "endpoints", "urls", "withMixedCase"]);
        });

        it("should return a readonly tuple", () => {
            const key = withMixedCaseQueryKey();
            expect(Array.isArray(key)).toBe(true);
        });

        it("should return the same key on repeated calls (stable)", () => {
            const key1 = withMixedCaseQueryKey();
            const key2 = withMixedCaseQueryKey();
            expect(key1).toEqual(key2);
        });
    });

    describe("single-param queries", () => {
        it("should include the request parameter in the key", () => {
            const request = { test: "value" };
            const key = getWithInlinePathQueryKey(request as any);
            expect(key).toEqual([...["SeedExhaustiveClient", "endpoints", "params", "getWithInlinePath"], request]);
        });

        it("should produce different keys for different request params", () => {
            const key1 = getWithInlinePathQueryKey({ a: 1 } as any);
            const key2 = getWithInlinePathQueryKey({ a: 2 } as any);
            expect(key1).not.toEqual(key2);
        });
    });

    describe("multi-param queries", () => {
        it("should include path param and request in the key", () => {
            const key = testGetQueryKey("test-path");
            expect(key).toEqual([...["SeedExhaustiveClient", "endpoints", "httpMethods", "testGet"], "test-path"]);
        });

        it("should produce different keys for different path params", () => {
            const key1 = testGetQueryKey("path-a");
            const key2 = testGetQueryKey("path-b");
            expect(key1).not.toEqual(key2);
        });
    });

    describe("pagination queries", () => {
        it("should include request in the key", () => {
            const key = listItemsQueryKey({} as any);
            expect(key).toEqual([...["SeedExhaustiveClient", "endpoints", "pagination", "listItems"], {}]);
        });
    });

    describe("key uniqueness across services", () => {
        it("should always start with the root client name", () => {
            expect(testGetQueryKey("x")[0]).toBe("SeedExhaustiveClient");
            expect(listItemsQueryKey({} as any)[0]).toBe("SeedExhaustiveClient");
            expect(getWithPathQueryKey("x")[0]).toBe("SeedExhaustiveClient");
        });
    });

    describe("requestOptions exclusion", () => {
        it("should not include requestOptions in query keys (only request params)", () => {
            const key = getWithInlinePathQueryKey({ test: "value" } as any);
            // Query key functions don't accept requestOptions at all,
            // which prevents cache pollution by design.
            expect(key).toHaveLength(5);
        });
    });
});
