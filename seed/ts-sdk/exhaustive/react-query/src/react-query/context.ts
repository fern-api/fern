// This file was auto-generated by Fern from our API Definition.

import type { ReactNode } from "react";
import { createContext, createElement, useContext } from "react";
import type { SeedExhaustiveClient } from "../Client.js";

type ClientInstance = InstanceType<typeof SeedExhaustiveClient>;

const ClientContext = createContext<ClientInstance | null>(null);

export function useClient(): ClientInstance {
    const client = useContext(ClientContext);
    if (client == null) {
        throw new Error(
            "useClient must be used within a <ClientProvider>. Wrap your component tree with <ClientProvider client={...}>.",
        );
    }
    return client;
}

export function ClientProvider({ client, children }: { client: ClientInstance; children: ReactNode }): ReactNode {
    return createElement(ClientContext.Provider, { value: client }, children);
}
