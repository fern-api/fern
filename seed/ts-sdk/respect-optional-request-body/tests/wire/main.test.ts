// This file was auto-generated by Fern from our API Definition.

import { SeedApiClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("SeedApiClient", () => {
    test("refund (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });

        server.mockEndpoint().post("/refunds/refund-id").respondWith().statusCode(200).build();

        const response = await client.refund({
            id: "refund-id",
        });
        expect(response).toEqual(undefined);
    });

    test("refund (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });
        const rawRequestBody = { amount: 60 };

        server.mockEndpoint().post("/refunds/refund-id").jsonBody(rawRequestBody).respondWith().statusCode(200).build();

        const response = await client.refund({
            id: "refund-id",
            body: {
                amount: 60,
            },
        });
        expect(response).toEqual(undefined);
    });

    test("requiredRefund", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });
        const rawRequestBody = { amount: 60 };

        server
            .mockEndpoint()
            .post("/refunds/refund-id/required")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.requiredRefund({
            id: "refund-id",
            body: {
                amount: 60,
            },
        });
        expect(response).toEqual(undefined);
    });

    test("bulkRefund", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });

        server.mockEndpoint().post("/refunds").respondWith().statusCode(200).build();

        const response = await client.bulkRefund();
        expect(response).toEqual(undefined);
    });
});
