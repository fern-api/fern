// This file was auto-generated by Fern from our API Definition.

import { SeedApiClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("SeedApiClient", () => {
    test("listUsers", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });

        const rawResponseBody = [{ id: "id", email: "email", name: "name" }];

        server.mockEndpoint().get("/users").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.listUsers();
        expect(response).toEqual(rawResponseBody);
    });

    test("getUser", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });

        const rawResponseBody = { id: "id", email: "email", name: "name" };

        server.mockEndpoint().get("/users/userId").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.getUser({
            userId: "userId",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("listInvoices", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });

        const rawResponseBody = [{ id: "id", amountCents: 1000000, currency: "USD" }];

        server.mockEndpoint().get("/invoices").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.listInvoices();
        expect(response).toEqual(rawResponseBody);
    });

    test("getInvoice", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });

        const rawResponseBody = { id: "id", amountCents: 1000000, currency: "USD" };

        server
            .mockEndpoint()
            .get("/invoices/invoiceId")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.getInvoice({
            invoiceId: "invoiceId",
        });
        expect(response).toEqual(rawResponseBody);
    });
});
