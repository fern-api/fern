// This file was auto-generated by Fern from our API Definition.

import type { BaseClientOptions, BaseRequestOptions } from "./BaseClient.js";
import { normalizeClientOptions } from "./BaseClient.js";
import * as core from "./core/index.js";
import * as SeedAlias from "./api/index.js";
import { mergeHeaders } from "./core/headers.js";
import * as errors from "./errors/index.js";

export declare namespace SeedAliasClient {
    export interface Options extends BaseClientOptions {
    }

    export interface RequestOptions extends BaseRequestOptions {
    }
}

export class SeedAliasClient {
    protected readonly _options: SeedAliasClient.Options;

    constructor(options: SeedAliasClient.Options) {

        this._options = normalizeClientOptions(options);
    }

    /**
     * @param {SeedAlias.TypeId} typeId
     * @param {SeedAliasClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @example
     *     await client.get("typeId")
     */
    public get(typeId: SeedAlias.TypeId, requestOptions?: SeedAliasClient.RequestOptions): core.HttpResponsePromise<void> {
        return core.HttpResponsePromise.fromPromise(this.__get(typeId, requestOptions));
    }

    private async __get(typeId: SeedAlias.TypeId, requestOptions?: SeedAliasClient.RequestOptions): Promise<core.WithRawResponse<void>> {
        let _headers: core.Fetcher.Args["headers"] = mergeHeaders(this._options?.headers, requestOptions?.headers);
        const _response = await core.fetcher({
            url: core.url.join(await core.Supplier.get(this._options.baseUrl) ?? await core.Supplier.get(this._options.environment), `/${core.url.encodePathParam(typeId)}`),
            method: "GET",
            headers: _headers,
            queryParameters: requestOptions?.queryParams,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging
        });
        if (_response.ok) {
            return { data: undefined, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            throw new errors.SeedAliasError({
                statusCode: _response.error.statusCode,
                body: _response.error.body,
                rawResponse: _response.rawResponse
            });
        }

        switch (_response.error.reason) {
            case "non-json": throw new errors.SeedAliasError({
                statusCode: _response.error.statusCode,
                body: _response.error.rawBody,
                rawResponse: _response.rawResponse
            });
            case "timeout": throw new errors.SeedAliasTimeoutError("Timeout exceeded when calling GET /{typeId}.");
            case "unknown": throw new errors.SeedAliasError({
                message: _response.error.errorMessage,
                rawResponse: _response.rawResponse
            });
        }
    }
}
