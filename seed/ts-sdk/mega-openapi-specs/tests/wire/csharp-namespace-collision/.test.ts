// This file was auto-generated by Fern from our API Definition.

import { SeedApiClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("Client", () => {
    test("createUser", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { id: "id", name: "name", email: "email", password: "password" };
        const rawResponseBody = { id: "id", name: "name", email: "email", password: "password" };

        server
            .mockEndpoint()
            .post("/users/create-user")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.csharpNamespaceCollision..createUser({
    id: "id",
    name: "name",
    email: "email",
    password: "password"
});
        expect(response).toEqual(rawResponseBody);
    });

    test("createTask", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { id: "id", name: "name", email: "email", password: "password" };
        const rawResponseBody = { id: "id", name: "name", email: "email", password: "password" };

        server
            .mockEndpoint()
            .post("/users/create-task")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.csharpNamespaceCollision..createTask({
    id: "id",
    name: "name",
    email: "email",
    password: "password"
});
        expect(response).toEqual(rawResponseBody);
    });
});
