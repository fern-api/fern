// This file was auto-generated by Fern from our API Definition.

import { SeedApiClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("Client", () => {
    test("createUser", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { line1: "line1", city: "city", state: "state", zip: "zip", country: "USA" };
        const rawResponseBody = {
            line1: "line1",
            line2: "line2",
            city: "city",
            state: "state",
            zip: "zip",
            country: "USA",
        };

        server
            .mockEndpoint()
            .post("/users/create-user")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.csharpSystemCollision..createUser({
    line1: "line1",
    city: "city",
    state: "state",
    zip: "zip",
    country: "USA"
});
        expect(response).toEqual(rawResponseBody);
    });

    test("createTask", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = {
            name: "name",
            user: { line1: "line1", city: "city", state: "state", zip: "zip", country: "USA" },
        };
        const rawResponseBody = {
            name: "name",
            user: { line1: "line1", line2: "line2", city: "city", state: "state", zip: "zip", country: "USA" },
        };

        server
            .mockEndpoint()
            .post("/users/create-task")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.csharpSystemCollision..createTask({
    name: "name",
    user: {
        line1: "line1",
        city: "city",
        state: "state",
        zip: "zip",
        country: "USA"
    }
});
        expect(response).toEqual(rawResponseBody);
    });

    test("emptyResponse", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = {
            name: "name",
            user: { line1: "line1", city: "city", state: "state", zip: "zip", country: "USA" },
        };

        server.mockEndpoint().post("/users/empty").jsonBody(rawRequestBody).respondWith().statusCode(200).build();

        const response = await client.csharpSystemCollision..emptyResponse({
    name: "name",
    user: {
        line1: "line1",
        city: "city",
        state: "state",
        zip: "zip",
        country: "USA"
    }
});
        expect(response).toEqual(undefined);
    });
});
