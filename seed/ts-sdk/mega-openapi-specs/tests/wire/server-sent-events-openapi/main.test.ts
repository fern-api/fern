// This file was auto-generated by Fern from our API Definition.

import { SeedApiClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("ServerSentEventsOpenapiClient", () => {
    test("streamProtocolNoCollision", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = {};
        const rawResponseBody = "event: heartbeat\ndata: {}\n\n";

        server
            .mockEndpoint()
            .post("/stream/protocol-no-collision")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .sseBody(rawResponseBody)
            .build();

        const response = await client.serverSentEventsOpenapi.streamProtocolNoCollision({});
        const events: unknown[] = [];
        for await (const event of response) {
            events.push(event);
        }
        expect(events).toEqual([
            {
                event: "heartbeat",
            },
        ]);
    });

    test("streamProtocolCollision", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = {};
        const rawResponseBody = "event: heartbeat\ndata: {}\n\n";

        server
            .mockEndpoint()
            .post("/stream/protocol-collision")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .sseBody(rawResponseBody)
            .build();

        const response = await client.serverSentEventsOpenapi.streamProtocolCollision({});
        const events: unknown[] = [];
        for await (const event of response) {
            events.push(event);
        }
        expect(events).toEqual([
            {
                event: "heartbeat",
            },
        ]);
    });

    test("streamDataContext", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = {};
        const rawResponseBody = 'event: \ndata: {"timestamp":"2024-01-15T09:30:00Z","event":"heartbeat"}\n\n';

        server
            .mockEndpoint()
            .post("/stream/data-context")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .sseBody(rawResponseBody)
            .build();

        const response = await client.serverSentEventsOpenapi.streamDataContext({});
        const events: unknown[] = [];
        for await (const event of response) {
            events.push(event);
        }
        expect(events).toEqual([
            {
                event: "heartbeat",
                timestamp: "2024-01-15T09:30:00Z",
            },
        ]);
    });

    test("streamNoContext", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = {};
        const rawResponseBody = 'event: \ndata: {"timestamp":"2024-01-15T09:30:00Z","event":"heartbeat"}\n\n';

        server
            .mockEndpoint()
            .post("/stream/no-context")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .sseBody(rawResponseBody)
            .build();

        const response = await client.serverSentEventsOpenapi.streamNoContext({});
        const events: unknown[] = [];
        for await (const event of response) {
            events.push(event);
        }
        expect(events).toEqual([
            {
                event: "heartbeat",
                timestamp: "2024-01-15T09:30:00Z",
            },
        ]);
    });

    test("streamProtocolWithFlatSchema", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = {};
        const rawResponseBody = 'event: heartbeat\ndata: {"timestamp":"2024-01-15T09:30:00Z"}\n\n';

        server
            .mockEndpoint()
            .post("/stream/protocol-with-flat-schema")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .sseBody(rawResponseBody)
            .build();

        const response = await client.serverSentEventsOpenapi.streamProtocolWithFlatSchema({});
        const events: unknown[] = [];
        for await (const event of response) {
            events.push(event);
        }
        expect(events).toEqual([
            {
                event: "heartbeat",
                timestamp: "2024-01-15T09:30:00Z",
            },
        ]);
    });

    test("streamDataContextWithEnvelopeSchema", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = {};
        const rawResponseBody = 'event: \ndata: {"event":"heartbeat"}\n\n';

        server
            .mockEndpoint()
            .post("/stream/data-context-with-envelope-schema")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .sseBody(rawResponseBody)
            .build();

        const response = await client.serverSentEventsOpenapi.streamDataContextWithEnvelopeSchema({});
        const events: unknown[] = [];
        for await (const event of response) {
            events.push(event);
        }
        expect(events).toEqual([
            {
                event: "heartbeat",
            },
        ]);
    });

    test("streamOasSpecNative", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = {};
        const rawResponseBody = 'event: \ndata: {"data":"data","event":"event","id":"id","retry":1}\n\n';

        server
            .mockEndpoint()
            .post("/stream/oas-spec-native")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .sseBody(rawResponseBody)
            .build();

        const response = await client.serverSentEventsOpenapi.streamOasSpecNative({});
        const events: unknown[] = [];
        for await (const event of response) {
            events.push(event);
        }
        expect(events).toEqual([
            {
                data: "data",
                event: "event",
                id: "id",
                retry: 1,
            },
        ]);
    });

    test("streamXFernStreamingCondition", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { query: "query", stream: false };
        const rawResponseBody = { answer: "answer", finishReason: "complete" };

        server
            .mockEndpoint()
            .post("/stream/x-fern-streaming-condition")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.serverSentEventsOpenapi.streamXFernStreamingCondition({
            query: "query",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("streamXFernStreamingSharedSchema", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { prompt: "prompt", model: "model", stream: false };
        const rawResponseBody = { answer: "answer", finishReason: "complete" };

        server
            .mockEndpoint()
            .post("/stream/x-fern-streaming-shared-schema")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.serverSentEventsOpenapi.streamXFernStreamingSharedSchema({
            prompt: "prompt",
            model: "model",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("validateCompletion", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { prompt: "prompt", model: "model" };
        const rawResponseBody = { answer: "answer", finishReason: "complete" };

        server
            .mockEndpoint()
            .post("/validate-completion")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.serverSentEventsOpenapi.validateCompletion({
            prompt: "prompt",
            model: "model",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("streamXFernStreamingUnion", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { type: "message", stream_response: false, prompt: "prompt", message: "message" };
        const rawResponseBody = { answer: "answer", finishReason: "complete" };

        server
            .mockEndpoint()
            .post("/stream/x-fern-streaming-union")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.serverSentEventsOpenapi.streamXFernStreamingUnion({
            type: "message",
            prompt: "prompt",
            message: "message",
            stream_response: false,
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("validateUnionRequest", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { prompt: "prompt" };
        const rawResponseBody = { valid: true };

        server
            .mockEndpoint()
            .post("/validate-union-request")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.serverSentEventsOpenapi.validateUnionRequest({
            prompt: "prompt",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("streamXFernStreamingNullableCondition", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { query: "query", stream: false };
        const rawResponseBody = { answer: "answer", finishReason: "complete" };

        server
            .mockEndpoint()
            .post("/stream/x-fern-streaming-nullable-condition")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.serverSentEventsOpenapi.streamXFernStreamingNullableCondition({
            query: "query",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("streamXFernStreamingSseOnly", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = {};
        const rawResponseBody = 'event: \ndata: "string"\n\n';

        server
            .mockEndpoint()
            .post("/stream/x-fern-streaming-sse-only")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .sseBody(rawResponseBody)
            .build();

        const response = await client.serverSentEventsOpenapi.streamXFernStreamingSseOnly({});
        const events: unknown[] = [];
        for await (const event of response) {
            events.push(event);
        }
        expect(events).toEqual(["string"]);
    });
});
