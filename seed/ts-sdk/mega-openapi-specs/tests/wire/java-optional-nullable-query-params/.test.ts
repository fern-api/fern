// This file was auto-generated by Fern from our API Definition.

import { SeedApiClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("Client", () => {
    test("search", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });

        const rawResponseBody = {
            results: ["results"],
            totalCount: 1,
            query: "query",
            limit: 1,
            includeArchived: true,
            sortOrder: "ASC",
        };

        server.mockEndpoint().get("/api/search").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.javaOptionalNullableQueryParams..search();
        expect(response).toEqual(rawResponseBody);
    });
});
