// This file was auto-generated by Fern from our API Definition.

import * as SeedApi from "../../../src/api/index";
import { SeedApiClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("PlaylistClient", () => {
    test("createPlaylist", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { name: "name", problems: ["problems"] };
        const rawResponseBody = {
            name: "name",
            problems: ["problems"],
            playlist_id: "playlist_id",
            "owner-id": "owner-id",
        };

        server
            .mockEndpoint()
            .post("/v2/playlist/1/create")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.trace.playlist.createPlaylist({
            serviceParam: 1,
            datetime: "2024-01-15T09:30:00Z",
            body: {
                name: "name",
                problems: ["problems"],
            },
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("getPlaylists", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });

        const rawResponseBody = [
            { name: "name", problems: ["problems"], playlist_id: "playlist_id", "owner-id": "owner-id" },
        ];

        server.mockEndpoint().get("/v2/playlist/1/all").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.trace.playlist.getPlaylists({
            serviceParam: 1,
            otherField: "otherField",
            multiLineDocs: "multiLineDocs",
            multipleField: ["multipleField"],
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("getPlaylist (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });

        const rawResponseBody = {
            name: "name",
            problems: ["problems"],
            playlist_id: "playlist_id",
            "owner-id": "owner-id",
        };

        server
            .mockEndpoint()
            .get("/v2/playlist/1/playlistId")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.trace.playlist.getPlaylist({
            serviceParam: 1,
            playlistId: "playlistId",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("getPlaylist (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/v2/playlist/1/playlistId")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.trace.playlist.getPlaylist({
                serviceParam: 1,
                playlistId: "playlistId",
            });
        }).rejects.toThrow(SeedApi.trace.UnauthorizedError);
    });

    test("getPlaylist (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/v2/playlist/1/playlistId")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.trace.playlist.getPlaylist({
                serviceParam: 1,
                playlistId: "playlistId",
            });
        }).rejects.toThrow(SeedApi.trace.NotFoundError);
    });

    test("updatePlaylist (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { name: "name", problems: ["problems"] };
        const rawResponseBody = {
            name: "name",
            problems: ["problems"],
            playlist_id: "playlist_id",
            "owner-id": "owner-id",
        };

        server
            .mockEndpoint()
            .put("/v2/playlist/1/playlistId")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.trace.playlist.updatePlaylist({
            serviceParam: 1,
            playlistId: "playlistId",
            body: {
                name: "name",
                problems: ["problems"],
            },
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("updatePlaylist (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { name: "name", problems: ["problems", "problems"] };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .put("/v2/playlist/1/playlistId")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.trace.playlist.updatePlaylist({
                serviceParam: 1,
                playlistId: "playlistId",
                body: {
                    name: "name",
                    problems: ["problems", "problems"],
                },
            });
        }).rejects.toThrow(SeedApi.trace.NotFoundError);
    });

    test("deletePlaylist", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });

        server.mockEndpoint().delete("/v2/playlist/1/playlist_id").respondWith().statusCode(200).build();

        const response = await client.trace.playlist.deletePlaylist({
            serviceParam: 1,
            playlistId: "playlist_id",
        });
        expect(response).toEqual(undefined);
    });
});
