// This file was auto-generated by Fern from our API Definition.

import { AcmeClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("UnionClient", () => {
    test("get", async () => {
        const server = mockServerPool.createServer();
        const client = new AcmeClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });

        const rawResponseBody = { radius: 1.1, type: "circle" };

        server.mockEndpoint().get("/union/id").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.unionsWithLocalDate.union.get({
            id: "id",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("update", async () => {
        const server = mockServerPool.createServer();
        const client = new AcmeClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { radius: 1.1, type: "circle" };
        const rawResponseBody = true;

        server
            .mockEndpoint()
            .patch("/union")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.unionsWithLocalDate.union.update({
            radius: 1.1,
            type: "circle",
        });
        expect(response).toEqual(rawResponseBody);
    });
});
