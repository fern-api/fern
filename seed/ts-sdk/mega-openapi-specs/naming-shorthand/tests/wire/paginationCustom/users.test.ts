// This file was auto-generated by Fern from our API Definition.

import { AcmeClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("UsersClient", () => {
    test("listWithCustomPager", async () => {
        const server = mockServerPool.createServer();
        const client = new AcmeClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });

        const rawResponseBody = {
            limit: 1,
            count: 1,
            has_more: true,
            links: [{ rel: "rel", method: "method", href: "href" }],
            data: ["data"],
        };

        server.mockEndpoint().get("/users").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.paginationCustom.users.listWithCustomPager();
        expect(response).toEqual(rawResponseBody);
    });
});
