// This file was auto-generated by Fern from our API Definition.

import { AcmeClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("ValidationClient", () => {
    test("create", async () => {
        const server = mockServerPool.createServer();
        const client = new AcmeClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { decimal: 1.1, even: 1, name: "name", shape: "SQUARE" };
        const rawResponseBody = { decimal: 1.1, even: 2, name: "rules", shape: "SQUARE" };

        server
            .mockEndpoint()
            .post("/create")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.validation.validation.create({
            decimal: 1.1,
            even: 1,
            name: "name",
            shape: "SQUARE",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("get", async () => {
        const server = mockServerPool.createServer();
        const client = new AcmeClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });

        const rawResponseBody = { decimal: 1.1, even: 2, name: "rules", shape: "SQUARE" };

        server.mockEndpoint().get("/").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.validation.validation.get({
            decimal: 1.1,
            even: 1,
            name: "name",
        });
        expect(response).toEqual(rawResponseBody);
    });
});
