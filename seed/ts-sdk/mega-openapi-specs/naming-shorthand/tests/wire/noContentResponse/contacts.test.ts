// This file was auto-generated by Fern from our API Definition.

import { AcmeClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("ContactsClient", () => {
    test("create", async () => {
        const server = mockServerPool.createServer();
        const client = new AcmeClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { name: "name" };
        const rawResponseBody = { id: "id", name: "name", email: "email" };

        server
            .mockEndpoint()
            .post("/contacts")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.noContentResponse.contacts.create({
            name: "name",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("get", async () => {
        const server = mockServerPool.createServer();
        const client = new AcmeClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });

        const rawResponseBody = { id: "id", name: "name", email: "email" };

        server.mockEndpoint().get("/contacts/id").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.noContentResponse.contacts.get({
            id: "id",
        });
        expect(response).toEqual(rawResponseBody);
    });
});
