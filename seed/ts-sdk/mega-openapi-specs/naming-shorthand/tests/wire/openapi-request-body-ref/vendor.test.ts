// This file was auto-generated by Fern from our API Definition.

import { AcmeClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("VendorClient", () => {
    test("updateVendor", async () => {
        const server = mockServerPool.createServer();
        const client = new AcmeClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { name: "name" };
        const rawResponseBody = {
            id: "id",
            name: "name",
            status: "ACTIVE",
            update_request: { name: "name", status: "ACTIVE" },
        };

        server
            .mockEndpoint()
            .put("/vendors/vendor_id")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.openapiRequestBodyRef.vendor.updateVendor({
            vendor_id: "vendor_id",
            body: {
                name: "name",
            },
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("createVendor", async () => {
        const server = mockServerPool.createServer();
        const client = new AcmeClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { name: "name" };
        const rawResponseBody = {
            id: "id",
            name: "name",
            status: "ACTIVE",
            update_request: { name: "name", status: "ACTIVE" },
        };

        server
            .mockEndpoint()
            .post("/vendors")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.openapiRequestBodyRef.vendor.createVendor({
            name: "name",
        });
        expect(response).toEqual(rawResponseBody);
    });
});
