// This file was auto-generated by Fern from our API Definition.

import { AcmeClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("DummyClient", () => {
    test("generate-stream", async () => {
        const server = mockServerPool.createServer();
        const client = new AcmeClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { stream: true, num_events: 1 };

        server.mockEndpoint().post("/generate-stream").jsonBody(rawRequestBody).respondWith().statusCode(200).build();

        const response = await client.streaming.dummy.generateStream({
            stream: true,
            num_events: 1,
        });
        expect(response).toEqual(undefined);
    });

    test("generate", async () => {
        const server = mockServerPool.createServer();
        const client = new AcmeClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { stream: false, num_events: 5 };
        const rawResponseBody = { id: "id", name: "name" };

        server
            .mockEndpoint()
            .post("/generate")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.streaming.dummy.generate({
            stream: false,
            num_events: 5,
        });
        expect(response).toEqual(rawResponseBody);
    });
});
