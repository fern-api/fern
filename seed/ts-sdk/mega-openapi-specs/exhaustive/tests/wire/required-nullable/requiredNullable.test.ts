// This file was auto-generated by Fern from our API Definition.

import { SeedApiClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("RequiredNullableClient", () => {
    test("getFoo", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });

        const rawResponseBody = {
            bar: "bar",
            nullable_bar: "nullable_bar",
            nullable_required_bar: "nullable_required_bar",
            required_bar: "required_bar",
        };

        server.mockEndpoint().get("/foo").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.requiredNullable.requiredNullable.getFoo({
            required_baz: "required_baz",
            required_nullable_baz: "required_nullable_baz",
        });
        expect(response).toEqual({
            body: rawResponseBody,
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });

    test("updateFoo", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = {};
        const rawResponseBody = {
            bar: "bar",
            nullable_bar: "nullable_bar",
            nullable_required_bar: "nullable_required_bar",
            required_bar: "required_bar",
        };

        server
            .mockEndpoint()
            .patch("/foo/id")
            .header("X-Idempotency-Key", "X-Idempotency-Key")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.requiredNullable.requiredNullable.updateFoo({
            "X-Idempotency-Key": "X-Idempotency-Key",
            id: "id",
        });
        expect(response).toEqual({
            body: rawResponseBody,
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });
});
