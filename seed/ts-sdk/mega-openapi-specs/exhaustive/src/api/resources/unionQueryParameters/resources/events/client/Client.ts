// This file was auto-generated by Fern from our API Definition.

import type { BaseClientOptions, BaseRequestOptions } from "../../../../../../BaseClient.js";
import { type NormalizedClientOptions, normalizeClientOptions } from "../../../../../../BaseClient.js";
import { mergeHeaders, mergeOnlyDefinedHeaders } from "../../../../../../core/headers.js";
import * as core from "../../../../../../core/index.js";
import { toJson } from "../../../../../../core/json.js";
import * as environments from "../../../../../../environments.js";
import * as SeedApi from "../../../../../index.js";

export declare namespace EventsClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {}
}

export class EventsClient {
    protected readonly _options: NormalizedClientOptions<EventsClient.Options>;

    constructor(options: EventsClient.Options) {
        this._options = normalizeClientOptions(options);
    }

    /**
     * Subscribe to events with a oneOf-style query parameter that may be a
     * scalar enum value or a list of enum values.
     *
     * @param {SeedApi.unionQueryParameters.SubscribeEventsRequest} request
     * @param {EventsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @example
     *     await client.unionQueryParameters.events.subscribe()
     */
    public subscribe(
        request: SeedApi.unionQueryParameters.SubscribeEventsRequest = {},
        requestOptions?: EventsClient.RequestOptions,
    ): core.HttpResponsePromise<core.APIResponse<string, SeedApi.unionQueryParameters.events.subscribe.Error>> {
        return core.HttpResponsePromise.fromPromise(this.__subscribe(request, requestOptions));
    }

    private async __subscribe(
        request: SeedApi.unionQueryParameters.SubscribeEventsRequest = {},
        requestOptions?: EventsClient.RequestOptions,
    ): Promise<core.WithRawResponse<core.APIResponse<string, SeedApi.unionQueryParameters.events.subscribe.Error>>> {
        const { event_type: eventType, tags } = request;
        const _queryParams: Record<string, unknown> = {
            event_type: Array.isArray(eventType)
                ? eventType.map((item) => (typeof item === "string" ? item : toJson(item)))
                : eventType !== undefined
                  ? typeof eventType === "string"
                      ? eventType
                      : toJson(eventType)
                  : undefined,
            tags: Array.isArray(tags)
                ? tags.map((item) => (typeof item === "string" ? item : toJson(item)))
                : tags !== undefined
                  ? typeof tags === "string"
                      ? tags
                      : toJson(tags)
                  : undefined,
        };
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-API-Version": requestOptions?.apiVersion ?? this._options?.apiVersion ?? "2024-02-08",
                "X-Api-Key": requestOptions?.apiKey ?? this._options?.apiKey,
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    ((await core.Supplier.get(this._options.environment)) ?? environments.SeedApiEnvironment.Production)
                        .base,
                "events",
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs:
                requestOptions?.timeoutInSeconds != null
                    ? requestOptions.timeoutInSeconds * 1000
                    : this._options?.timeoutInSeconds != null
                      ? this._options?.timeoutInSeconds * 1000
                      : undefined,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            withCredentials: true,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: {
                    ok: true,
                    body: _response.body as string,
                    headers: _response.headers,
                    rawResponse: _response.rawResponse,
                },
                rawResponse: _response.rawResponse,
            };
        }

        return {
            data: {
                ok: false,
                error: SeedApi.unionQueryParameters.events.subscribe.Error._unknown(_response.error),
                rawResponse: _response.rawResponse,
            },
            rawResponse: _response.rawResponse,
        };
    }
}
