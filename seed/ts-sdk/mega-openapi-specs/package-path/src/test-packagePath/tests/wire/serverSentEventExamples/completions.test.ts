// This file was auto-generated by Fern from our API Definition.

import * as SeedApi from "../../../api/index";
import { SeedApiClient } from "../../../Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("CompletionsClient", () => {
    test("stream (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { query: "foo" };

        server.mockEndpoint().post("/stream").jsonBody(rawRequestBody).respondWith().statusCode(200).build();

        const response = await client.serverSentEventExamples.completions.stream({
            query: "foo",
        });
        expect(response).toEqual(undefined);
    });

    test("stream (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { query: "" };

        server.mockEndpoint().post("/stream").jsonBody(rawRequestBody).respondWith().statusCode(200).build();

        const response = await client.serverSentEventExamples.completions.stream({
            query: "",
        });
        expect(response).toEqual(undefined);
    });

    test("stream (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { query: "query" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/stream")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.serverSentEventExamples.completions.stream({
                query: "query",
            });
        }).rejects.toThrow(SeedApi.serverSentEventExamples.BadRequestError);
    });

    test("streamEvents (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { query: "query" };

        server.mockEndpoint().post("/stream-events").jsonBody(rawRequestBody).respondWith().statusCode(200).build();

        const response = await client.serverSentEventExamples.completions.streamEvents({
            query: "query",
        });
        expect(response).toEqual(undefined);
    });

    test("streamEvents (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { query: "" };

        server.mockEndpoint().post("/stream-events").jsonBody(rawRequestBody).respondWith().statusCode(200).build();

        const response = await client.serverSentEventExamples.completions.streamEvents({
            query: "",
        });
        expect(response).toEqual(undefined);
    });

    test("streamEvents (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { query: "query" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/stream-events")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.serverSentEventExamples.completions.streamEvents({
                query: "query",
            });
        }).rejects.toThrow(SeedApi.serverSentEventExamples.BadRequestError);
    });

    test("streamEventsDiscriminantInData (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { query: "query" };

        server
            .mockEndpoint()
            .post("/stream-events-discriminant-in-data")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.serverSentEventExamples.completions.streamEventsDiscriminantInData({
            query: "query",
        });
        expect(response).toEqual(undefined);
    });

    test("streamEventsDiscriminantInData (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { query: "query" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/stream-events-discriminant-in-data")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.serverSentEventExamples.completions.streamEventsDiscriminantInData({
                query: "query",
            });
        }).rejects.toThrow(SeedApi.serverSentEventExamples.BadRequestError);
    });

    test("streamEventsContextProtocol (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { query: "query" };

        server
            .mockEndpoint()
            .post("/stream-events-context-protocol")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.serverSentEventExamples.completions.streamEventsContextProtocol({
            query: "query",
        });
        expect(response).toEqual(undefined);
    });

    test("streamEventsContextProtocol (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { query: "" };

        server
            .mockEndpoint()
            .post("/stream-events-context-protocol")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.serverSentEventExamples.completions.streamEventsContextProtocol({
            query: "",
        });
        expect(response).toEqual(undefined);
    });

    test("streamEventsContextProtocol (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { query: "query" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/stream-events-context-protocol")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.serverSentEventExamples.completions.streamEventsContextProtocol({
                query: "query",
            });
        }).rejects.toThrow(SeedApi.serverSentEventExamples.BadRequestError);
    });
});
