// This file was auto-generated by Fern from our API Definition.

import * as SeedApi from "../../../api/index";
import { SeedApiClient } from "../../../Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("DummyClient", () => {
    test("generate-stream (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { num_events: 1 };

        server.mockEndpoint().post("/generate-stream").jsonBody(rawRequestBody).respondWith().statusCode(200).build();

        const response = await client.javaStreamingAcceptHeader.dummy.generateStream({
            num_events: 1,
        });
        expect(response).toEqual(undefined);
    });

    test("generate-stream (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { num_events: 1 };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/generate-stream")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.javaStreamingAcceptHeader.dummy.generateStream({
                num_events: 1,
            });
        }).rejects.toThrow(SeedApi.javaStreamingAcceptHeader.BadRequestError);
    });

    test("generate (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { num_events: 5 };
        const rawResponseBody = { id: "id", name: "name" };

        server
            .mockEndpoint()
            .post("/generate")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.javaStreamingAcceptHeader.dummy.generate({
            num_events: 5,
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("generate (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { num_events: 1 };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/generate")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.javaStreamingAcceptHeader.dummy.generate({
                num_events: 1,
            });
        }).rejects.toThrow(SeedApi.javaStreamingAcceptHeader.BadRequestError);
    });
});
