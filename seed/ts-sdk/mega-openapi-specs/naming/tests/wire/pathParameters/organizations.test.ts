// This file was auto-generated by Fern from our API Definition.

import { AcmeSdkClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("OrganizationsClient", () => {
    test("getOrganization", async () => {
        const server = mockServerPool.createServer();
        const client = new AcmeSdkClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });

        const rawResponseBody = { name: "name", tags: ["tags"] };

        server
            .mockEndpoint()
            .get("/tenant_id/organizations/organization_id/")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.pathParameters.organizations.getOrganization({
            tenant_id: "tenant_id",
            organization_id: "organization_id",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("getOrganizationUser", async () => {
        const server = mockServerPool.createServer();
        const client = new AcmeSdkClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });

        const rawResponseBody = { name: "name", tags: ["tags"] };

        server
            .mockEndpoint()
            .get("/tenant_id/organizations/organization_id/users/user_id")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.pathParameters.organizations.getOrganizationUser({
            tenant_id: "tenant_id",
            organization_id: "organization_id",
            user_id: "user_id",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("searchOrganizations", async () => {
        const server = mockServerPool.createServer();
        const client = new AcmeSdkClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });

        const rawResponseBody = [{ name: "name", tags: ["tags"] }];

        server
            .mockEndpoint()
            .get("/tenant_id/organizations/organization_id/search")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.pathParameters.organizations.searchOrganizations({
            tenant_id: "tenant_id",
            organization_id: "organization_id",
        });
        expect(response).toEqual(rawResponseBody);
    });
});
