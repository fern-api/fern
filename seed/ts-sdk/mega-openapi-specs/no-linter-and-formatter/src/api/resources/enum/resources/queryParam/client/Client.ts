// This file was auto-generated by Fern from our API Definition.

import type { BaseClientOptions, BaseRequestOptions } from "../../../../../../BaseClient.js";
import { normalizeClientOptions, type NormalizedClientOptions } from "../../../../../../BaseClient.js";
import * as core from "../../../../../../core/index.js";
import { mergeHeaders, mergeOnlyDefinedHeaders } from "../../../../../../core/headers.js";
import { toJson } from "../../../../../../core/json.js";
import * as environments from "../../../../../../environments.js";
import { handleNonStatusCodeError } from "../../../../../../errors/handleNonStatusCodeError.js";
import * as errors from "../../../../../../errors/index.js";
import * as SeedApi from "../../../../../index.js";

export declare namespace QueryParamClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {
    }
}

export class QueryParamClient {
    protected readonly _options: NormalizedClientOptions<QueryParamClient.Options>;

    constructor(options: QueryParamClient.Options) {

        this._options = normalizeClientOptions(options);
    }

    /**
     * @param {SeedApi.enum_.SendQueryParamRequest} request
     * @param {QueryParamClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @example
     *     await client.enum.queryParam.send({
     *         operand: ">",
     *         operandOrColor: "red"
     *     })
     */
    public send(request: SeedApi.enum_.SendQueryParamRequest, requestOptions?: QueryParamClient.RequestOptions): core.HttpResponsePromise<void> {
        return core.HttpResponsePromise.fromPromise(this.__send(request, requestOptions));
    }

    private async __send(request: SeedApi.enum_.SendQueryParamRequest, requestOptions?: QueryParamClient.RequestOptions): Promise<core.WithRawResponse<void>> {
        const { operand, maybeOperand, operandOrColor, maybeOperandOrColor } = request;
        const _queryParams: Record<string, unknown> = {
            operand,
            maybeOperand: maybeOperand !== undefined ? maybeOperand : undefined,
            operandOrColor,
            maybeOperandOrColor: Array.isArray(maybeOperandOrColor) ? maybeOperandOrColor.map(item => typeof item === "string" ? item : toJson(item)) : maybeOperandOrColor !== undefined ? typeof maybeOperandOrColor === "string" ? maybeOperandOrColor : toJson(maybeOperandOrColor) : undefined
        };
        let _headers: core.Fetcher.Args["headers"] = mergeHeaders(this._options?.headers, mergeOnlyDefinedHeaders({ "X-API-Version": requestOptions?.apiVersion ?? (this._options?.apiVersion ?? "2024-02-08"), "X-Api-Key": requestOptions?.apiKey ?? this._options?.apiKey }), requestOptions?.headers);
        const _response = await core.fetcher({
            url: core.url.join(await core.Supplier.get(this._options.baseUrl) ?? (await core.Supplier.get(this._options.environment) ?? environments.SeedApiEnvironment.Production).base, "query"),
            method: "POST",
            headers: _headers,
            queryString: core.url.queryBuilder().addMany(_queryParams).mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging
        });
        if (_response.ok) {
            return { data: undefined, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            throw new errors.SeedApiError({
                statusCode: _response.error.statusCode,
                body: _response.error.body,
                rawResponse: _response.rawResponse
            });
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "POST", "/query");
    }

    /**
     * @param {SeedApi.enum_.SendListQueryParamRequest} request
     * @param {QueryParamClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @example
     *     await client.enum.queryParam.sendList({
     *         operand: [">"],
     *         operandOrColor: ["red"]
     *     })
     */
    public sendList(request: SeedApi.enum_.SendListQueryParamRequest = {}, requestOptions?: QueryParamClient.RequestOptions): core.HttpResponsePromise<void> {
        return core.HttpResponsePromise.fromPromise(this.__sendList(request, requestOptions));
    }

    private async __sendList(request: SeedApi.enum_.SendListQueryParamRequest = {}, requestOptions?: QueryParamClient.RequestOptions): Promise<core.WithRawResponse<void>> {
        const { operand, maybeOperand, operandOrColor, maybeOperandOrColor } = request;
        const _queryParams: Record<string, unknown> = {
            operand: Array.isArray(operand) ? operand.map(item => item) : operand != null ? operand : undefined,
            maybeOperand: Array.isArray(maybeOperand) ? maybeOperand.map(item => item) : maybeOperand !== undefined ? maybeOperand : undefined,
            operandOrColor: Array.isArray(operandOrColor) ? operandOrColor.map(item => typeof item === "string" ? item : toJson(item)) : operandOrColor != null ? typeof operandOrColor === "string" ? operandOrColor : toJson(operandOrColor) : undefined,
            maybeOperandOrColor: Array.isArray(maybeOperandOrColor) ? maybeOperandOrColor.map(item => typeof item === "string" ? item : toJson(item)) : maybeOperandOrColor !== undefined ? typeof maybeOperandOrColor === "string" ? maybeOperandOrColor : toJson(maybeOperandOrColor) : undefined
        };
        let _headers: core.Fetcher.Args["headers"] = mergeHeaders(this._options?.headers, mergeOnlyDefinedHeaders({ "X-API-Version": requestOptions?.apiVersion ?? (this._options?.apiVersion ?? "2024-02-08"), "X-Api-Key": requestOptions?.apiKey ?? this._options?.apiKey }), requestOptions?.headers);
        const _response = await core.fetcher({
            url: core.url.join(await core.Supplier.get(this._options.baseUrl) ?? (await core.Supplier.get(this._options.environment) ?? environments.SeedApiEnvironment.Production).base, "query-list"),
            method: "POST",
            headers: _headers,
            queryString: core.url.queryBuilder().addMany(_queryParams).mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging
        });
        if (_response.ok) {
            return { data: undefined, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            throw new errors.SeedApiError({
                statusCode: _response.error.statusCode,
                body: _response.error.body,
                rawResponse: _response.rawResponse
            });
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "POST", "/query-list");
    }
}
