// This file was auto-generated by Fern from our API Definition.

import { SeedApiClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("UserClient", () => {
    test("getUser", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });

        const rawResponseBody = { name: "name", tags: ["tags"] };

        server
            .mockEndpoint()
            .get("/tenant_id/user/user_id")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.pathParameters.user.getUser({
            tenant_id: "tenant_id",
            user_id: "user_id",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("updateUser", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { name: "name", tags: ["tags"] };
        const rawResponseBody = { name: "name", tags: ["tags"] };

        server
            .mockEndpoint()
            .patch("/tenant_id/user/user_id")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.pathParameters.user.updateUser({
            tenant_id: "tenant_id",
            user_id: "user_id",
            name: "name",
            tags: ["tags"],
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("createUser", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });
        const rawRequestBody = { name: "name", tags: ["tags"] };
        const rawResponseBody = { name: "name", tags: ["tags"] };

        server
            .mockEndpoint()
            .post("/tenant_id/user/")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.pathParameters.user.createUser({
            tenant_id: "tenant_id",
            name: "name",
            tags: ["tags"],
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("searchUsers", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });

        const rawResponseBody = [{ name: "name", tags: ["tags"] }];

        server
            .mockEndpoint()
            .get("/tenant_id/user/user_id/search")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.pathParameters.user.searchUsers({
            tenant_id: "tenant_id",
            user_id: "user_id",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("getUserMetadata", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });

        const rawResponseBody = { name: "name", tags: ["tags"] };

        server
            .mockEndpoint()
            .get("/tenant_id/user/user_id/metadata/v1")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.pathParameters.user.getUserMetadata({
            tenant_id: "tenant_id",
            user_id: "user_id",
            version: 1,
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("getUserSpecifics", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });

        const rawResponseBody = { name: "name", tags: ["tags"] };

        server
            .mockEndpoint()
            .get("/tenant_id/user/user_id/specifics/1/thought")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.pathParameters.user.getUserSpecifics({
            tenant_id: "tenant_id",
            user_id: "user_id",
            version: 1,
            thought: "thought",
        });
        expect(response).toEqual(rawResponseBody);
    });
});
