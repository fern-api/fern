// This file was auto-generated by Fern from our API Definition.

import * as SeedApi from "../../../../../src/api/index";
import { SeedApiClient } from "../../../../../src/Client";
import { mockServerPool } from "../../../../mock-server/MockServerPool";

describe("MetadataClient", () => {
    test("getMetadata", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });

        const rawResponseBody = { id: "id", value: { key: "value" } };

        server
            .mockEndpoint()
            .get("/users/events/metadata/")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.mixedFileDirectory.user.events.metadata.getMetadata({
            id: SeedApi.mixedFileDirectory.Id("id"),
        });
        expect(response).toEqual(rawResponseBody);
    });
});
