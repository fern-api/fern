// This file was auto-generated by Fern from our API Definition.

import type * as SeedApi from "../../../index.js";

export interface TypeWithOptionalUnion {
    myUnion?: (SeedApi.undiscriminatedUnions.MyUnion | null) | undefined;
}

/**
 * Several different types are accepted.
 */
export type MyUnion = string | number | number[] | number[][] | string[];

/**
 * Nested layer 2.
 */
export type NestedUnionL2 = boolean | string[];

/**
 * Nested layer 1.
 */
export type NestedUnionL1 = number | string[] | SeedApi.undiscriminatedUnions.NestedUnionL2;

/**
 * Nested union root.
 */
export type NestedUnionRoot = string | string[] | SeedApi.undiscriminatedUnions.NestedUnionL1;

/**
 * Duplicate types.
 */
export type UnionWithDuplicateTypes = string | number | string[];

export type MetadataUnion =
    | SeedApi.undiscriminatedUnions.OptionalMetadata
    | null
    | SeedApi.undiscriminatedUnions.NamedMetadata;

export interface NamedMetadata {
    name: string;
    value: Record<string, unknown>;
}

export type OptionalMetadata = Record<string, unknown> | null;

/**
 * Undiscriminated unions can act as a map key
 * as long as all of their values are valid keys
 * (i.e. do they have a valid string representation).
 */
export type Metadata = Record<string, string>;

export type Key = SeedApi.undiscriminatedUnions.KeyType | "default";

export const KeyType = {
    Name: "name",
    Value: "value",
} as const;
export type KeyType = (typeof KeyType)[keyof typeof KeyType];

/**
 * Tests that base-properties on an undiscriminated union are correctly
 * represented in the IR and generated code.
 */
export type UnionWithBaseProperties =
    | SeedApi.undiscriminatedUnions.NamedMetadata
    | SeedApi.undiscriminatedUnions.OptionalMetadata
    | null;

/**
 * Multiple string types that all resolve to String in Java.
 * This tests the fix for duplicate method signatures.
 */
export type UnionWithIdenticalStrings = string;

/**
 * Mix of primitives where some resolve to the same Java type.
 */
export type UnionWithIdenticalPrimitives = number | number | string;

/**
 * Tests that nested properties with camelCase wire names are properly
 * converted from snake_case Ruby keys when passed as Hash values.
 */
export type PaymentMethodUnion =
    | SeedApi.undiscriminatedUnions.TokenizeCard
    | SeedApi.undiscriminatedUnions.ConvertToken;

export interface TokenizeCard {
    method: string;
    cardNumber: string;
}

export interface ConvertToken {
    method: string;
    tokenId: string;
}

/**
 * Tests that union members named 'Type' or 'Value' don't collide with internal properties
 */
export type UnionWithReservedNames = "type" | "value" | string;

/**
 * A user identifier (alias for string)
 */
export type UserId = string;

/**
 * A name (alias for string)
 */
export type Name = string;

/**
 * Union with multiple named type aliases that all resolve to the same C# type (string).
 * Without the fix, this would generate duplicate implicit operators:
 *   public static implicit operator UnionWithTypeAliases(string value) => ...
 *   public static implicit operator UnionWithTypeAliases(string value) => ...
 *   public static implicit operator UnionWithTypeAliases(string value) => ...
 * causing CS0557 compiler error.
 */
export type UnionWithTypeAliases = string | SeedApi.undiscriminatedUnions.UserId | SeedApi.undiscriminatedUnions.Name;

export interface LeafTypeA {
    alpha: string;
    beta: number;
}

export interface LeafTypeB {
    gamma: string;
}

/**
 * Inner union with two object variants that have disjoint required keys.
 * Tests that required-key guards work correctly inside nested union contexts.
 */
export type NestedObjectUnion = SeedApi.undiscriminatedUnions.LeafTypeA | SeedApi.undiscriminatedUnions.LeafTypeB;

export interface WrapperObject {
    inner: SeedApi.undiscriminatedUnions.NestedObjectUnion;
    label: string;
}

/**
 * Outer union where one variant is an object containing a nested union field.
 * Tests that the deserializer correctly handles transitive union deserialization.
 */
export type OuterNestedUnion = string | SeedApi.undiscriminatedUnions.WrapperObject;

/**
 * An alias around LeafObjectA.
 */
export type AliasedLeafA = SeedApi.undiscriminatedUnions.LeafObjectA;

export type AliasToLeafB = SeedApi.undiscriminatedUnions.LeafObjectB;

export interface LeafObjectA {
    onlyInA: string;
    sharedNumber: number;
}

export interface LeafObjectB {
    onlyInB: string;
}

/**
 * Undiscriminated union whose members are named aliases of object types
 * (including an alias-of-alias). Required keys are disjoint, so a correct
 * deserializer must emit containsKey() guards for each alias variant.
 */
export type AliasedObjectUnion = unknown;
