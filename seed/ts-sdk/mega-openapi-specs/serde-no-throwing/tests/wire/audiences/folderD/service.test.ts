// This file was auto-generated by Fern from our API Definition.

import { SeedApiClient } from "../../../../src/Client";
import { mockServerPool } from "../../../mock-server/MockServerPool";

describe("ServiceClient", () => {
    test("getDirectThread", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });

        const rawResponseBody = { foo: "foo" };

        server.mockEndpoint().get("/partner-path").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.audiences.folderD.service.getDirectThread();
        expect(response).toEqual({
            body: {
                foo: "foo",
            },
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });
});
