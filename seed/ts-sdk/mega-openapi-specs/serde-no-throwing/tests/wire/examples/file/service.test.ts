// This file was auto-generated by Fern from our API Definition.

import { SeedApiClient } from "../../../../src/Client";
import { mockServerPool } from "../../../mock-server/MockServerPool";

describe("ServiceClient", () => {
    test("getFile", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            token: "test",
            apiVersion: "test",
            apiKey: "test",
            environment: { base: server.baseUrl, auth: server.baseUrl, upload: server.baseUrl },
        });

        const rawResponseBody = { name: "file.txt", contents: "..." };

        server.mockEndpoint().get("/file/file.txt").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.examples.file.service.getFile({
            filename: "file.txt",
        });
        expect(response).toEqual({
            body: {
                name: "file.txt",
                contents: "...",
            },
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });
});
