// This file was auto-generated by Fern from our API Definition.

import type { BaseClientOptions, BaseRequestOptions } from "../../../../BaseClient.js";
import { type NormalizedClientOptions, normalizeClientOptions } from "../../../../BaseClient.js";
import { mergeHeaders, mergeOnlyDefinedHeaders } from "../../../../core/headers.js";
import * as core from "../../../../core/index.js";
import { toJson } from "../../../../core/json.js";
import * as environments from "../../../../environments.js";
import * as serializers from "../../../../serialization/index.js";
import * as SeedApi from "../../../index.js";

export declare namespace QueryParametersOpenapiClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {}
}

export class QueryParametersOpenapiClient {
    protected readonly _options: NormalizedClientOptions<QueryParametersOpenapiClient.Options>;

    constructor(options: QueryParametersOpenapiClient.Options) {
        this._options = normalizeClientOptions(options);
    }

    /**
     * @param {SeedApi.queryParametersOpenapi.SearchRequest} request
     * @param {QueryParametersOpenapiClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @example
     *     await client.queryParametersOpenapi.search({
     *         limit: 1,
     *         id: "id",
     *         date: "2023-01-15",
     *         deadline: new Date("2024-01-15T09:30:00.000Z"),
     *         bytes: "bytes",
     *         user: {
     *             name: "name",
     *             tags: ["tags", "tags"]
     *         },
     *         userList: {
     *             name: "name",
     *             tags: ["tags", "tags"]
     *         },
     *         optionalDeadline: new Date("2024-01-15T09:30:00.000Z"),
     *         keyValue: {
     *             "keyValue": "keyValue"
     *         },
     *         optionalString: "optionalString",
     *         nestedUser: {
     *             name: "name",
     *             user: {
     *                 name: "name",
     *                 tags: ["tags", "tags"]
     *             }
     *         },
     *         optionalUser: {
     *             name: "name",
     *             tags: ["tags", "tags"]
     *         },
     *         excludeUser: {
     *             name: "name",
     *             tags: ["tags", "tags"]
     *         },
     *         filter: "filter",
     *         tags: "tags",
     *         optionalTags: "optionalTags",
     *         neighbor: {
     *             name: "name",
     *             tags: ["tags", "tags"]
     *         },
     *         neighborRequired: {
     *             name: "name",
     *             tags: ["tags", "tags"]
     *         }
     *     })
     */
    public search(
        request: SeedApi.queryParametersOpenapi.SearchRequest,
        requestOptions?: QueryParametersOpenapiClient.RequestOptions,
    ): core.HttpResponsePromise<
        core.APIResponse<SeedApi.queryParametersOpenapi.SearchResponse, SeedApi.queryParametersOpenapi.search.Error>
    > {
        return core.HttpResponsePromise.fromPromise(this.__search(request, requestOptions));
    }

    private async __search(
        request: SeedApi.queryParametersOpenapi.SearchRequest,
        requestOptions?: QueryParametersOpenapiClient.RequestOptions,
    ): Promise<
        core.WithRawResponse<
            core.APIResponse<SeedApi.queryParametersOpenapi.SearchResponse, SeedApi.queryParametersOpenapi.search.Error>
        >
    > {
        const {
            limit,
            id,
            date,
            deadline,
            bytes,
            user,
            userList,
            optionalDeadline,
            keyValue,
            optionalString,
            nestedUser,
            optionalUser,
            excludeUser,
            filter,
            tags,
            optionalTags,
            neighbor,
            neighborRequired,
        } = request;
        const _queryParams: Record<string, unknown> = {
            limit,
            id,
            date,
            deadline: deadline.toISOString(),
            bytes,
            user: serializers.queryParametersOpenapi.User.jsonOrThrow(user, {
                unrecognizedObjectKeys: "passthrough",
                allowUnrecognizedUnionMembers: true,
                allowUnrecognizedEnumValues: true,
                omitUndefined: true,
                breadcrumbsPrefix: ["request", "user"],
            }),
            userList: Array.isArray(userList)
                ? await Promise.all(
                      userList.map(async (item) =>
                          serializers.queryParametersOpenapi.User.jsonOrThrow(item, {
                              unrecognizedObjectKeys: "passthrough",
                              allowUnrecognizedUnionMembers: true,
                              allowUnrecognizedEnumValues: true,
                              omitUndefined: true,
                              breadcrumbsPrefix: ["request", "userList"],
                          }),
                      ),
                  )
                : userList != null
                  ? serializers.queryParametersOpenapi.User.jsonOrThrow(userList, {
                        unrecognizedObjectKeys: "passthrough",
                        allowUnrecognizedUnionMembers: true,
                        allowUnrecognizedEnumValues: true,
                        omitUndefined: true,
                        breadcrumbsPrefix: ["request", "userList"],
                    })
                  : userList,
            optionalDeadline: optionalDeadline != null ? optionalDeadline?.toISOString() : undefined,
            keyValue: keyValue != null ? toJson(keyValue) : undefined,
            optionalString,
            nestedUser:
                nestedUser != null
                    ? serializers.queryParametersOpenapi.NestedUser.jsonOrThrow(nestedUser, {
                          unrecognizedObjectKeys: "passthrough",
                          allowUnrecognizedUnionMembers: true,
                          allowUnrecognizedEnumValues: true,
                          omitUndefined: true,
                          breadcrumbsPrefix: ["request", "nestedUser"],
                      })
                    : nestedUser,
            optionalUser:
                optionalUser != null
                    ? serializers.queryParametersOpenapi.User.jsonOrThrow(optionalUser, {
                          unrecognizedObjectKeys: "passthrough",
                          allowUnrecognizedUnionMembers: true,
                          allowUnrecognizedEnumValues: true,
                          omitUndefined: true,
                          breadcrumbsPrefix: ["request", "optionalUser"],
                      })
                    : optionalUser,
            excludeUser: Array.isArray(excludeUser)
                ? await Promise.all(
                      excludeUser.map(async (item) =>
                          serializers.queryParametersOpenapi.User.jsonOrThrow(item, {
                              unrecognizedObjectKeys: "passthrough",
                              allowUnrecognizedUnionMembers: true,
                              allowUnrecognizedEnumValues: true,
                              omitUndefined: true,
                              breadcrumbsPrefix: ["request", "excludeUser"],
                          }),
                      ),
                  )
                : excludeUser != null
                  ? serializers.queryParametersOpenapi.User.jsonOrThrow(excludeUser, {
                        unrecognizedObjectKeys: "passthrough",
                        allowUnrecognizedUnionMembers: true,
                        allowUnrecognizedEnumValues: true,
                        omitUndefined: true,
                        breadcrumbsPrefix: ["request", "excludeUser"],
                    })
                  : excludeUser,
            filter,
            tags,
            optionalTags,
            neighbor: Array.isArray(neighbor)
                ? neighbor.map((item) =>
                      (() => {
                          const mapped = serializers.SearchRequestNeighbor.jsonOrThrow(item, {
                              unrecognizedObjectKeys: "strip",
                              omitUndefined: true,
                          });
                          return typeof mapped === "string" ? mapped : toJson(mapped);
                      })(),
                  )
                : neighbor != null
                  ? (() => {
                        const mapped = serializers.SearchRequestNeighbor.jsonOrThrow(neighbor, {
                            unrecognizedObjectKeys: "strip",
                            omitUndefined: true,
                        });
                        return typeof mapped === "string" ? mapped : toJson(mapped);
                    })()
                  : undefined,
            neighborRequired: Array.isArray(neighborRequired)
                ? neighborRequired.map((item) =>
                      (() => {
                          const mapped = serializers.queryParametersOpenapi.SearchRequestNeighborRequired.jsonOrThrow(
                              item,
                              { unrecognizedObjectKeys: "strip", omitUndefined: true },
                          );
                          return typeof mapped === "string" ? mapped : toJson(mapped);
                      })(),
                  )
                : (() => {
                      const mapped = serializers.queryParametersOpenapi.SearchRequestNeighborRequired.jsonOrThrow(
                          neighborRequired,
                          { unrecognizedObjectKeys: "strip", omitUndefined: true },
                      );
                      return typeof mapped === "string" ? mapped : toJson(mapped);
                  })(),
        };
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-API-Version": requestOptions?.apiVersion ?? this._options?.apiVersion ?? "2024-02-08",
                "X-Api-Key": requestOptions?.apiKey ?? this._options?.apiKey,
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    ((await core.Supplier.get(this._options.environment)) ?? environments.SeedApiEnvironment.Production)
                        .base,
                "user/getUsername",
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .add("tags", _queryParams.tags, { style: "comma" })
                .add("optionalTags", _queryParams.optionalTags, { style: "comma" })
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: {
                    ok: true,
                    body: serializers.queryParametersOpenapi.SearchResponse.parseOrThrow(_response.body, {
                        unrecognizedObjectKeys: "passthrough",
                        allowUnrecognizedUnionMembers: true,
                        allowUnrecognizedEnumValues: true,
                        skipValidation: true,
                        breadcrumbsPrefix: ["response"],
                    }),
                    headers: _response.headers,
                    rawResponse: _response.rawResponse,
                },
                rawResponse: _response.rawResponse,
            };
        }

        return {
            data: {
                ok: false,
                error: SeedApi.queryParametersOpenapi.search.Error._unknown(_response.error),
                rawResponse: _response.rawResponse,
            },
            rawResponse: _response.rawResponse,
        };
    }
}
