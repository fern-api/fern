// This file was auto-generated by Fern from our API Definition.

import type { BaseClientOptions, BaseRequestOptions } from "../../../../../../BaseClient.js";
import { type NormalizedClientOptions, normalizeClientOptions } from "../../../../../../BaseClient.js";
import { mergeHeaders, mergeOnlyDefinedHeaders } from "../../../../../../core/headers.js";
import * as core from "../../../../../../core/index.js";
import * as environments from "../../../../../../environments.js";
import * as serializers from "../../../../../../serialization/index.js";
import * as SeedApi from "../../../../../index.js";

export declare namespace MultipartFormClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {}
}

export class MultipartFormClient {
    protected readonly _options: NormalizedClientOptions<MultipartFormClient.Options>;

    constructor(options: MultipartFormClient.Options) {
        this._options = normalizeClientOptions(options);
    }

    /**
     * @param {SeedApi.enum_.MultipartFormMultipartFormRequest} request
     * @param {MultipartFormClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @example
     *     import { createReadStream } from "fs";
     *     await client.enum.multipartForm.multipartForm({})
     */
    public multipartForm(
        request: SeedApi.enum_.MultipartFormMultipartFormRequest,
        requestOptions?: MultipartFormClient.RequestOptions,
    ): core.HttpResponsePromise<core.APIResponse<void, SeedApi.enum_.multipartForm.multipartForm.Error>> {
        return core.HttpResponsePromise.fromPromise(this.__multipartForm(request, requestOptions));
    }

    private async __multipartForm(
        request: SeedApi.enum_.MultipartFormMultipartFormRequest,
        requestOptions?: MultipartFormClient.RequestOptions,
    ): Promise<core.WithRawResponse<core.APIResponse<void, SeedApi.enum_.multipartForm.multipartForm.Error>>> {
        const _body = await core.newFormData();
        if (request.color != null) {
            _body.append(
                "color",
                serializers.enum_.Color.jsonOrThrow(request.color, {
                    unrecognizedObjectKeys: "strip",
                    omitUndefined: true,
                }),
            );
        }

        if (request.maybeColor != null) {
            _body.append(
                "maybeColor",
                serializers.enum_.Color.jsonOrThrow(request.maybeColor, {
                    unrecognizedObjectKeys: "strip",
                    omitUndefined: true,
                }),
            );
        }

        if (request.colorList != null) {
            for (const _item of request.colorList) {
                _body.append(
                    "colorList",
                    serializers.enum_.Color.jsonOrThrow(_item, {
                        unrecognizedObjectKeys: "strip",
                        omitUndefined: true,
                    }),
                );
            }
        }

        if (request.maybeColorList != null) {
            for (const _item of request.maybeColorList) {
                _body.append(
                    "maybeColorList",
                    serializers.enum_.Color.jsonOrThrow(_item, {
                        unrecognizedObjectKeys: "strip",
                        omitUndefined: true,
                    }),
                );
            }
        }

        const _maybeEncodedRequest = await _body.getRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-API-Version": requestOptions?.apiVersion ?? this._options?.apiVersion ?? "2024-02-08",
                "X-Api-Key": requestOptions?.apiKey ?? this._options?.apiKey,
                ..._maybeEncodedRequest.headers,
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    ((await core.Supplier.get(this._options.environment)) ?? environments.SeedApiEnvironment.Production)
                        .base,
                "multipart",
            ),
            method: "POST",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            requestType: "file",
            duplex: _maybeEncodedRequest.duplex,
            body: _maybeEncodedRequest.body,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: {
                    ok: true,
                    body: undefined,
                    headers: _response.headers,
                    rawResponse: _response.rawResponse,
                },
                rawResponse: _response.rawResponse,
            };
        }

        return {
            data: {
                ok: false,
                error: SeedApi.enum_.multipartForm.multipartForm.Error._unknown(_response.error),
                rawResponse: _response.rawResponse,
            },
            rawResponse: _response.rawResponse,
        };
    }
}
