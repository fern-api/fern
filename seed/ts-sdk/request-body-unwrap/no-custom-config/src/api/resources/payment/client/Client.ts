// This file was auto-generated by Fern from our API Definition.

import type { BaseClientOptions, BaseRequestOptions } from "../../../../BaseClient.js";
import { type NormalizedClientOptions, normalizeClientOptions } from "../../../../BaseClient.js";
import { mergeHeaders } from "../../../../core/headers.js";
import * as core from "../../../../core/index.js";
import { handleNonStatusCodeError } from "../../../../errors/handleNonStatusCodeError.js";
import * as errors from "../../../../errors/index.js";
import type * as SeedApi from "../../../index.js";

export declare namespace PaymentClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {}
}

export class PaymentClient {
    protected readonly _options: NormalizedClientOptions<PaymentClient.Options>;

    constructor(options: PaymentClient.Options) {
        this._options = normalizeClientOptions(options);
    }

    /**
     * @param {SeedApi.CreatePaymentRequest} request
     * @param {PaymentClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @example
     *     await client.payment.createPayment({
     *         data: {
     *             type: "payment",
     *             attributes: {
     *                 amount: 1,
     *                 currency: "currency"
     *             }
     *         }
     *     })
     */
    public createPayment(
        request: SeedApi.CreatePaymentRequest,
        requestOptions?: PaymentClient.RequestOptions,
    ): core.HttpResponsePromise<SeedApi.Payment> {
        return core.HttpResponsePromise.fromPromise(this.__createPayment(request, requestOptions));
    }

    private async __createPayment(
        request: SeedApi.CreatePaymentRequest,
        requestOptions?: PaymentClient.RequestOptions,
    ): Promise<core.WithRawResponse<SeedApi.Payment>> {
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(this._options?.headers, requestOptions?.headers);
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)),
                "payments",
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            requestType: "json",
            body: {
                data: {
                    attributes: {
                        amount: request.amount,
                        currency: request.currency,
                        description: request.description,
                    },
                },
            },
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as SeedApi.Payment, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            throw new errors.SeedApiError({
                statusCode: _response.error.statusCode,
                body: _response.error.body,
                rawResponse: _response.rawResponse,
            });
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "POST", "/payments");
    }

    /**
     * @param {SeedApi.GetPaymentRequest} request
     * @param {PaymentClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @example
     *     await client.payment.getPayment({
     *         payment_id: "payment_id"
     *     })
     */
    public getPayment(
        request: SeedApi.GetPaymentRequest,
        requestOptions?: PaymentClient.RequestOptions,
    ): core.HttpResponsePromise<SeedApi.Payment> {
        return core.HttpResponsePromise.fromPromise(this.__getPayment(request, requestOptions));
    }

    private async __getPayment(
        request: SeedApi.GetPaymentRequest,
        requestOptions?: PaymentClient.RequestOptions,
    ): Promise<core.WithRawResponse<SeedApi.Payment>> {
        const { payment_id: paymentId } = request;
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(this._options?.headers, requestOptions?.headers);
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)),
                `payments/${core.url.encodePathParam(paymentId)}`,
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as SeedApi.Payment, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            throw new errors.SeedApiError({
                statusCode: _response.error.statusCode,
                body: _response.error.body,
                rawResponse: _response.rawResponse,
            });
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "GET", "/payments/{payment_id}");
    }
}
