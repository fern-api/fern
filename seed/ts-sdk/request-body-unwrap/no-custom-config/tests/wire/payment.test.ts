// This file was auto-generated by Fern from our API Definition.

import { SeedApiClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("PaymentClient", () => {
    test("createPayment", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });
        const rawRequestBody = { data: { type: "payment", attributes: { amount: 1, currency: "currency" } } };
        const rawResponseBody = {
            id: "id",
            amount: 1,
            currency: "currency",
            description: "description",
            status: "pending",
        };

        server
            .mockEndpoint()
            .post("/payments")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.payment.createPayment({
            data: {
                type: "payment",
                attributes: {
                    amount: 1,
                    currency: "currency",
                },
            },
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("getPayment", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });

        const rawResponseBody = {
            id: "id",
            amount: 1,
            currency: "currency",
            description: "description",
            status: "pending",
        };

        server
            .mockEndpoint()
            .get("/payments/payment_id")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.payment.getPayment({
            payment_id: "payment_id",
        });
        expect(response).toEqual(rawResponseBody);
    });
});
