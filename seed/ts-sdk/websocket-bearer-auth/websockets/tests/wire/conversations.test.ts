// This file was auto-generated by Fern from our API Definition.

import { SeedWebsocketBearerAuthClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("ConversationsClient", () => {
    test("list", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedWebsocketBearerAuthClient({
            maxRetries: 0,
            apiKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = [
            { id: "id", status: "status" },
            { id: "id", status: "status" },
        ];

        server.mockEndpoint().get("/conversations").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.conversations.list();
        expect(response).toEqual([
            {
                id: "id",
                status: "status",
            },
            {
                id: "id",
                status: "status",
            },
        ]);
    });
});
