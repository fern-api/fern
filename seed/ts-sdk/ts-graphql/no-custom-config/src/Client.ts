// This file was auto-generated by Fern from our API Definition.

import { MutationClient } from "./api/resources/mutation/client/Client.js";
import { QueryClient } from "./api/resources/query/client/Client.js";
import { SubscriptionClient } from "./api/resources/subscription/client/Client.js";
import type { BaseClientOptions, BaseRequestOptions } from "./BaseClient.js";
import { type NormalizedClientOptionsWithAuth, normalizeClientOptionsWithAuth } from "./BaseClient.js";
import * as core from "./core/index.js";

export declare namespace SeedApiClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {}
}

export class SeedApiClient {
    protected readonly _options: NormalizedClientOptionsWithAuth<SeedApiClient.Options>;
    protected _query: QueryClient | undefined;
    protected _mutation: MutationClient | undefined;
    protected _subscription: SubscriptionClient | undefined;

    constructor(options: SeedApiClient.Options) {
        this._options = normalizeClientOptionsWithAuth(options);
    }

    public get query(): QueryClient {
        return (this._query ??= new QueryClient(this._options));
    }

    public get mutation(): MutationClient {
        return (this._mutation ??= new MutationClient(this._options));
    }

    public get subscription(): SubscriptionClient {
        return (this._subscription ??= new SubscriptionClient(this._options));
    }

    /**
     * Make a passthrough request using the SDK's configured auth, retry, logging, etc.
     * This is useful for making requests to endpoints not yet supported in the SDK.
     * The input can be a URL string, URL object, or Request object. Relative paths are resolved against the configured base URL.
     *
     * @param {Request | string | URL} input - The URL, path, or Request object.
     * @param {RequestInit} init - Standard fetch RequestInit options.
     * @param {core.PassthroughRequest.RequestOptions} requestOptions - Per-request overrides (timeout, retries, headers, abort signal).
     * @returns {Promise<Response>} A standard Response object.
     */
    public async fetch(
        input: Request | string | URL,
        init?: RequestInit,
        requestOptions?: core.PassthroughRequest.RequestOptions,
    ): Promise<Response> {
        return core.makePassthroughRequest(
            input,
            init,
            {
                baseUrl: this._options.baseUrl ?? this._options.environment,
                headers: this._options.headers,
                timeoutInSeconds: this._options.timeoutInSeconds,
                maxRetries: this._options.maxRetries,
                fetch: this._options.fetch,
                logging: this._options.logging,
                getAuthHeaders: async () => (await this._options.authProvider.getAuthRequest()).headers,
            },
            requestOptions,
        );
    }

    /**
     * Execute a raw GraphQL document, bypassing the typed operation surface.
     * Useful for power users who maintain their own GraphQL queries/mutations.
     * Returns the `{ data, errors }` envelope; pass `throwOnError: true` to throw `GraphqlError` on operation errors.
     *
     * @param {string} query - The GraphQL document to execute.
     * @param {TVariables} variables - The GraphQL variables for the document.
     * @param {RequestOptions} requestOptions - Per-request overrides (timeout, retries, headers, abort signal, throwOnError).
     */
    public async raw<TResult = unknown, TVariables = Record<string, unknown>>(
        query: string,
        variables?: TVariables,
        requestOptions?: core.PassthroughRequest.RequestOptions & { throwOnError?: boolean },
    ): Promise<core.GraphqlResponse<TResult | undefined>> {
        const _response = await this.fetch(
            "/graphql",
            {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({ query, variables: variables ?? {} }),
            },
            requestOptions,
        );
        const _gqlBody = (await _response.json()) as { data?: TResult; errors?: core.GraphqlResponseError[] };
        if (requestOptions?.throwOnError === true && _gqlBody?.errors != null && _gqlBody.errors.length > 0) {
            throw new core.GraphqlError({ errors: _gqlBody.errors, data: _gqlBody.data });
        }
        return { data: _gqlBody?.data, errors: _gqlBody?.errors };
    }
}
