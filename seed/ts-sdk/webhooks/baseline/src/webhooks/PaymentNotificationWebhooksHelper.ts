// This file was auto-generated by Fern from our API Definition.

import * as core from "../core/index.js";

const TIMESTAMP_TOLERANCE_SECONDS = 300;
const SIGNATURE_PREFIX = "rsa=";

/**
 * Verify an asymmetric webhook signature.
 *
 * Extract the signature from the "x-payment-signature" header and pass it as the signatureHeader parameter.
 * Public keys are fetched from the JWKS endpoint at https://api.example.com/.well-known/jwks.json.
 * Extract the key ID from the "x-key-id" header and pass it as the keyIdHeader parameter.
 * Extract the timestamp from the "x-payment-timestamp" header and pass it as the timestampHeader parameter.
 */
export class PaymentNotificationWebhooksHelper {
    public static async verifySignature(
        requestBody: string,
        signatureHeader: string,
        keyIdHeader: string | undefined,
        timestampHeader: string,
    ): Promise<boolean> {
        if (requestBody == null || signatureHeader == null) {
            throw new Error("Missing required parameters for webhook signature verification");
        }

        if (timestampHeader == null || timestampHeader === "") {
            throw new Error("Missing timestamp header 'x-payment-timestamp' for webhook signature verification");
        }

        const timestampMs = new Date(timestampHeader).getTime();
        if (Number.isNaN(timestampMs)) {
            throw new Error("Invalid timestamp format: expected ISO 8601 date string");
        }

        if (Math.abs(Date.now() - timestampMs) > TIMESTAMP_TOLERANCE_SECONDS * 1000) {
            return false;
        }

        const sig = signatureHeader.startsWith(SIGNATURE_PREFIX)
            ? signatureHeader.slice(SIGNATURE_PREFIX.length)
            : signatureHeader;

        const payload = requestBody;

        const resolvedKey = await await core.fetchJwks({
            url: "https://api.example.com/.well-known/jwks.json",
            keyId: keyIdHeader,
        });

        return await core.verifyAsymmetricSignature({
            payload: payload,
            signature: sig,
            publicKey: resolvedKey,
            algorithm: "RSA_SHA256",
            encoding: "base64",
        });
    }
}
