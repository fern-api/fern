// This file was auto-generated by Fern from our API Definition.

import { SeedApiClient } from "../../src/Client";
import { setGlobalBodyParameterIfAbsent } from "../../src/core/globalParameters";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("ProductsClient", () => {
    test("search", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            xCustomHeader: "test",
            apiVersion: "test",
            environment: server.baseUrl,
        });
        const rawRequestBody = setGlobalBodyParameterIfAbsent({}, ["config", "currency"], "USD");
        const rawResponseBody = { results: [{ id: "id", name: "name", price: 1.1 }] };

        server
            .mockEndpoint()
            .post("/v1/products/regionId/search")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.products.search({
            regionId: "regionId",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("get", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({
            maxRetries: 0,
            xCustomHeader: "test",
            apiVersion: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { id: "id", name: "name", price: 1.1 };

        server
            .mockEndpoint()
            .get("/v1/products/regionId/productId")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.products.get({
            regionId: "regionId",
            productId: "productId",
        });
        expect(response).toEqual(rawResponseBody);
    });
});
