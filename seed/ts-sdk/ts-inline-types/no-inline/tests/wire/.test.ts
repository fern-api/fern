// This file was auto-generated by Fern from our API Definition.

import { SeedApiClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("Client", () => {
    test("getRoot", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });
        const rawRequestBody = { bar: { foo: "foo" }, foo: "foo" };
        const rawResponseBody = {
            foo: "foo",
            bar: {
                foo: "foo",
                bar: { foo: "foo", bar: "bar", myEnum: "SUNNY", ref: { foo: "foo" } },
                ref: { foo: "foo" },
            },
            fooMap: { key: { foo: "foo", ref: { foo: "foo" } } },
            fooList: [{ foo: "foo", ref: { foo: "foo" } }],
            fooSet: [{ foo: "foo", ref: { foo: "foo" } }],
            ref: { foo: "foo" },
        };

        server
            .mockEndpoint()
            .post("/root/root")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client..getRoot({
    bar: {
        foo: "foo"
    },
    foo: "foo"
});
        expect(response).toEqual(rawResponseBody);
    });

    test("getDiscriminatedUnion", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });
        const rawRequestBody = {
            bar: { foo: "foo", bar: { foo: "foo", ref: { foo: "foo" } }, ref: { foo: "foo" }, type: "type1" },
            foo: "foo",
        };

        server
            .mockEndpoint()
            .post("/root/discriminated-union")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client..getDiscriminatedUnion({
    bar: {
        foo: "foo",
        bar: {
            foo: "foo",
            ref: {
                foo: "foo"
            }
        },
        ref: {
            foo: "foo"
        },
        type: "type1"
    },
    foo: "foo"
});
        expect(response).toEqual(undefined);
    });

    test("getUndiscriminatedUnion", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });
        const rawRequestBody = { bar: "SUNNY", foo: "foo" };

        server
            .mockEndpoint()
            .post("/root/undiscriminated-union")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client..getUndiscriminatedUnion({
    bar: "SUNNY",
    foo: "foo"
});
        expect(response).toEqual(undefined);
    });
});
