// This file was auto-generated by Fern from our API Definition.

import { SeedTsPaginationUriPathParamsClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("UsersClient", () => {
    test("listWithUriPagination", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTsPaginationUriPathParamsClient({
            maxRetries: 0,
            token: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = {
            data: [
                { name: "name", id: 1 },
                { name: "name", id: 1 },
            ],
            next: "next",
        };
        const mockResponseBody = { ...rawResponseBody, next: `${server.baseUrl}/accounts/accountId/users/uri` };
        server
            .mockEndpoint({ once: false })
            .get("/accounts/accountId/users/uri")
            .header("X-Api-Version", "X-Api-Version")
            .respondWith()
            .statusCode(200)
            .jsonBody(mockResponseBody)
            .build();

        const expected = rawResponseBody;
        const page = await client.users.listWithUriPagination({
            "X-Api-Version": "X-Api-Version",
            accountId: "accountId",
            pageSize: 1,
        });

        expect(expected.data).toEqual(page.data);
        expect(page.hasNextPage()).toBe(true);
        const nextPage = await page.getNextPage();
        expect(expected.data).toEqual(nextPage.data);
    });

    test("listWithPathPagination", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTsPaginationUriPathParamsClient({
            maxRetries: 0,
            token: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = {
            data: [
                { name: "name", id: 1 },
                { name: "name", id: 1 },
            ],
            next: "next",
        };
        const mockResponseBody = { ...rawResponseBody, next: "/accounts/accountId/users/path" };
        server
            .mockEndpoint({ once: false })
            .get("/accounts/accountId/users/path")
            .respondWith()
            .statusCode(200)
            .jsonBody(mockResponseBody)
            .build();

        const expected = rawResponseBody;
        const page = await client.users.listWithPathPagination({
            accountId: "accountId",
            pageSize: 1,
        });

        expect(expected.data).toEqual(page.data);
        expect(page.hasNextPage()).toBe(true);
        const nextPage = await page.getNextPage();
        expect(expected.data).toEqual(nextPage.data);
    });
});
