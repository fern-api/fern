/**
 * This file was auto-generated by Fern from our API Definition.
 */

import { mockServerPool } from "../mock-server/MockServerPool.js";
import { SeedAnyAuthClient } from "../../src/Client";

describe("User", () => {
    test("get", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedAnyAuthClient({
            token: process.env.MY_TOKEN || "test",
            apiKey: process.env.MY_API_KEY || "test",
            clientId: process.env.MY_CLIENT_ID || "test",
            clientSecret: process.env.MY_CLIENT_SECRET || "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = [
            { id: "id", name: "name" },
            { id: "id", name: "name" },
        ];
        server.mockEndpoint().post("/users").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.user.get();
        expect(response).toEqual([
            {
                id: "id",
                name: "name",
            },
            {
                id: "id",
                name: "name",
            },
        ]);
    });
});
