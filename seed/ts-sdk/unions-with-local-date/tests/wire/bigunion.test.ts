// This file was auto-generated by Fern from our API Definition.

import { SeedApiClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("BigunionClient", () => {
    test("get", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });

        const rawResponseBody = { value: "value", type: "normalSweet" };

        server.mockEndpoint().get("/bigunion/id").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.bigunion.get({
            id: "id",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("update", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });
        const rawRequestBody = { value: "value", type: "normalSweet" };
        const rawResponseBody = true;

        server
            .mockEndpoint()
            .patch("/bigunion")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.bigunion.update({
            value: "value",
            type: "normalSweet",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("updateMany", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });
        const rawRequestBody = [{ value: "value", type: "normalSweet" }];
        const rawResponseBody = { key: true };

        server
            .mockEndpoint()
            .patch("/bigunion/many")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.bigunion.updateMany([
            {
                value: "value",
                type: "normalSweet",
            },
        ]);
        expect(response).toEqual(rawResponseBody);
    });
});
