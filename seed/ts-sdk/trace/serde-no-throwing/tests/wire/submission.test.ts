// This file was auto-generated by Fern from our API Definition.

import { SeedApiClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("SubmissionClient", () => {
    test("createexecutionsession", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            sessionId: "sessionId",
            executionSessionUrl: "executionSessionUrl",
            language: "JAVA",
            status: "CREATING_CONTAINER",
        };

        server
            .mockEndpoint()
            .post("/sessions/create-session/JAVA")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.submission.createexecutionsession({
            language: "JAVA",
        });
        expect(response).toEqual({
            body: {
                sessionId: "sessionId",
                executionSessionUrl: "executionSessionUrl",
                language: "JAVA",
                status: "CREATING_CONTAINER",
            },
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });

    test("getexecutionsession", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            sessionId: "sessionId",
            executionSessionUrl: "executionSessionUrl",
            language: "JAVA",
            status: "CREATING_CONTAINER",
        };

        server
            .mockEndpoint()
            .get("/sessions/sessionId")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.submission.getexecutionsession({
            sessionId: "sessionId",
        });
        expect(response).toEqual({
            body: {
                sessionId: "sessionId",
                executionSessionUrl: "executionSessionUrl",
                language: "JAVA",
                status: "CREATING_CONTAINER",
            },
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });

    test("stopexecutionsession", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        server.mockEndpoint().delete("/sessions/stop/sessionId").respondWith().statusCode(200).build();

        const response = await client.submission.stopexecutionsession({
            sessionId: "sessionId",
        });
        expect(response).toEqual({
            body: undefined,
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });

    test("getexecutionsessionsstate", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            states: {
                key: {
                    lastTimeContacted: "lastTimeContacted",
                    sessionId: "sessionId",
                    isWarmInstance: true,
                    awsTaskId: "awsTaskId",
                    language: "JAVA",
                    status: "CREATING_CONTAINER",
                },
            },
            numWarmingInstances: 1,
            warmingSessionIds: ["warmingSessionIds"],
        };

        server
            .mockEndpoint()
            .get("/sessions/execution-sessions-state")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.submission.getexecutionsessionsstate();
        expect(response).toEqual({
            body: {
                states: {
                    key: {
                        lastTimeContacted: "lastTimeContacted",
                        sessionId: "sessionId",
                        isWarmInstance: true,
                        awsTaskId: "awsTaskId",
                        language: "JAVA",
                        status: "CREATING_CONTAINER",
                    },
                },
                numWarmingInstances: 1,
                warmingSessionIds: ["warmingSessionIds"],
            },
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });
});
