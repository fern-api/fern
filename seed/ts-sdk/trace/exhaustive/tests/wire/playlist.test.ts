// This file was auto-generated by Fern from our API Definition.

import { SeedTraceClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("PlaylistClient", () => {
    test("createPlaylist", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTraceClient({
            maxRetries: 0,
            token: "test",
            xRandomHeader: "test",
            environment: server.baseUrl,
        });
        const rawRequestBody = { name: "name", problems: ["problems", "problems"] };
        const rawResponseBody = {
            playlist_id: "playlist_id",
            "owner-id": "owner-id",
            name: "name",
            problems: ["problems", "problems"],
        };
        server
            .mockEndpoint()
            .post("/v2/playlist/1/create")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.playlist.createPlaylist(1, {
            datetime: "2024-01-15T09:30:00Z",
            optionalDatetime: "2024-01-15T09:30:00Z",
            body: {
                name: "name",
                problems: [ProblemId("problems"), ProblemId("problems")],
            },
        });
        expect(response).toEqual({
            body: {
                playlist_id: PlaylistId("playlist_id"),
                "owner-id": UserId("owner-id"),
                name: "name",
                problems: [ProblemId("problems"), ProblemId("problems")],
            },
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });

    test("getPlaylists", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTraceClient({
            maxRetries: 0,
            token: "test",
            xRandomHeader: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = [
            { playlist_id: "playlist_id", "owner-id": "owner-id", name: "name", problems: ["problems", "problems"] },
            { playlist_id: "playlist_id", "owner-id": "owner-id", name: "name", problems: ["problems", "problems"] },
        ];
        server.mockEndpoint().get("/v2/playlist/1/all").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.playlist.getPlaylists(1, {
            limit: 1,
            otherField: "otherField",
            multiLineDocs: "multiLineDocs",
            optionalMultipleField: "optionalMultipleField",
            multipleField: "multipleField",
        });
        expect(response).toEqual({
            body: [
                {
                    playlist_id: PlaylistId("playlist_id"),
                    "owner-id": UserId("owner-id"),
                    name: "name",
                    problems: [ProblemId("problems"), ProblemId("problems")],
                },
                {
                    playlist_id: PlaylistId("playlist_id"),
                    "owner-id": UserId("owner-id"),
                    name: "name",
                    problems: [ProblemId("problems"), ProblemId("problems")],
                },
            ],
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });

    test("getPlaylist", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTraceClient({
            maxRetries: 0,
            token: "test",
            xRandomHeader: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = {
            playlist_id: "playlist_id",
            "owner-id": "owner-id",
            name: "name",
            problems: ["problems", "problems"],
        };
        server
            .mockEndpoint()
            .get("/v2/playlist/1/playlistId")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.playlist.getPlaylist(1, PlaylistId("playlistId"));
        expect(response).toEqual({
            body: {
                playlist_id: PlaylistId("playlist_id"),
                "owner-id": UserId("owner-id"),
                name: "name",
                problems: [ProblemId("problems"), ProblemId("problems")],
            },
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });

    test("updatePlaylist", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTraceClient({
            maxRetries: 0,
            token: "test",
            xRandomHeader: "test",
            environment: server.baseUrl,
        });
        const rawRequestBody = { name: "name", problems: ["problems", "problems"] };
        const rawResponseBody = {
            playlist_id: "playlist_id",
            "owner-id": "owner-id",
            name: "name",
            problems: ["problems", "problems"],
        };
        server
            .mockEndpoint()
            .put("/v2/playlist/1/playlistId")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.playlist.updatePlaylist(1, PlaylistId("playlistId"), {
            name: "name",
            problems: [ProblemId("problems"), ProblemId("problems")],
        });
        expect(response).toEqual({
            body: {
                playlist_id: PlaylistId("playlist_id"),
                "owner-id": UserId("owner-id"),
                name: "name",
                problems: [ProblemId("problems"), ProblemId("problems")],
            },
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });

    test("deletePlaylist", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTraceClient({
            maxRetries: 0,
            token: "test",
            xRandomHeader: "test",
            environment: server.baseUrl,
        });

        server.mockEndpoint().delete("/v2/playlist/1/playlist_id").respondWith().statusCode(200).build();

        const response = await client.playlist.deletePlaylist(1, PlaylistId("playlist_id"));
        expect(response).toEqual({
            body: undefined,
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });
});
