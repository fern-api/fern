// This file was auto-generated by Fern from our API Definition.

import { PlaylistIdNotFoundError } from "../../src/api/resources/playlist/errors/PlaylistIdNotFoundError";
import { UnauthorizedError } from "../../src/api/resources/playlist/errors/UnauthorizedError";
import { SeedTraceClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("PlaylistClient", () => {
    test("createPlaylist", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTraceClient({
            maxRetries: 0,
            token: "test",
            xRandomHeader: "test",
            environment: server.baseUrl,
        });
        const rawRequestBody = { name: "name", problems: ["problems", "problems"] };
        const rawResponseBody = {
            playlist_id: "playlist_id",
            "owner-id": "owner-id",
            name: "name",
            problems: ["problems", "problems"],
        };
        server
            .mockEndpoint()
            .post("/v2/playlist/1/create")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.playlist.createPlaylist(1, {
            datetime: new Date("2024-01-15T09:30:00.000Z"),
            optionalDatetime: new Date("2024-01-15T09:30:00.000Z"),
            body: {
                name: "name",
                problems: ["problems", "problems"],
            },
        });
        expect(response).toEqual({
            playlistId: "playlist_id",
            ownerId: "owner-id",
            name: "name",
            problems: ["problems", "problems"],
        });
    });

    test("getPlaylists", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTraceClient({
            maxRetries: 0,
            token: "test",
            xRandomHeader: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = [
            { playlist_id: "playlist_id", "owner-id": "owner-id", name: "name", problems: ["problems", "problems"] },
            { playlist_id: "playlist_id", "owner-id": "owner-id", name: "name", problems: ["problems", "problems"] },
        ];
        server.mockEndpoint().get("/v2/playlist/1/all").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.playlist.getPlaylists(1, {
            limit: 1,
            otherField: "otherField",
            multiLineDocs: "multiLineDocs",
            optionalMultipleField: "optionalMultipleField",
            multipleField: "multipleField",
        });
        expect(response).toEqual([
            {
                playlistId: "playlist_id",
                ownerId: "owner-id",
                name: "name",
                problems: ["problems", "problems"],
            },
            {
                playlistId: "playlist_id",
                ownerId: "owner-id",
                name: "name",
                problems: ["problems", "problems"],
            },
        ]);
    });

    test("getPlaylist (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTraceClient({
            maxRetries: 0,
            token: "test",
            xRandomHeader: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = {
            playlist_id: "playlist_id",
            "owner-id": "owner-id",
            name: "name",
            problems: ["problems", "problems"],
        };
        server
            .mockEndpoint()
            .get("/v2/playlist/1/playlistId")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.playlist.getPlaylist(1, "playlistId");
        expect(response).toEqual({
            playlistId: "playlist_id",
            ownerId: "owner-id",
            name: "name",
            problems: ["problems", "problems"],
        });
    });

    test("getPlaylist (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTraceClient({
            maxRetries: 0,
            token: "test",
            xRandomHeader: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { type: "playlistId", value: "string" };
        server
            .mockEndpoint()
            .get("/v2/playlist/1/playlistId")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.playlist.getPlaylist(1, "playlistId");
        }).rejects.toThrow(PlaylistIdNotFoundError);
    });

    test("getPlaylist (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTraceClient({
            maxRetries: 0,
            token: "test",
            xRandomHeader: "test",
            environment: server.baseUrl,
        });

        server.mockEndpoint().get("/v2/playlist/1/playlistId").respondWith().statusCode(401).build();

        await expect(async () => {
            return await client.playlist.getPlaylist(1, "playlistId");
        }).rejects.toThrow(UnauthorizedError);
    });

    test("updatePlaylist (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTraceClient({
            maxRetries: 0,
            token: "test",
            xRandomHeader: "test",
            environment: server.baseUrl,
        });
        const rawRequestBody = { name: "name", problems: ["problems", "problems"] };
        const rawResponseBody = {
            playlist_id: "playlist_id",
            "owner-id": "owner-id",
            name: "name",
            problems: ["problems", "problems"],
        };
        server
            .mockEndpoint()
            .put("/v2/playlist/1/playlistId")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.playlist.updatePlaylist(1, "playlistId", {
            name: "name",
            problems: ["problems", "problems"],
        });
        expect(response).toEqual({
            playlistId: "playlist_id",
            ownerId: "owner-id",
            name: "name",
            problems: ["problems", "problems"],
        });
    });

    test("updatePlaylist (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTraceClient({
            maxRetries: 0,
            token: "test",
            xRandomHeader: "test",
            environment: server.baseUrl,
        });
        const rawRequestBody = { name: "name", problems: ["problems", "problems"] };
        const rawResponseBody = { type: "playlistId", value: "string" };
        server
            .mockEndpoint()
            .put("/v2/playlist/1/playlistId")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.playlist.updatePlaylist(1, "playlistId", {
                name: "name",
                problems: ["problems", "problems"],
            });
        }).rejects.toThrow(PlaylistIdNotFoundError);
    });

    test("deletePlaylist", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedTraceClient({
            maxRetries: 0,
            token: "test",
            xRandomHeader: "test",
            environment: server.baseUrl,
        });

        server.mockEndpoint().delete("/v2/playlist/1/playlist_id").respondWith().statusCode(200).build();

        const response = await client.playlist.deletePlaylist(1, "playlist_id");
        expect(response).toEqual(undefined);
    });
});
