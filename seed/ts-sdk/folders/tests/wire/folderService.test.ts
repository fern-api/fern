// This file was auto-generated by Fern from our API Definition.

import * as SeedApi from "../../src/api/index";
import { SeedApiClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("FolderServiceClient", () => {
    test("folder_service_endpoint", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });

        server.mockEndpoint().get("/service").respondWith().statusCode(200).build();

        const response = await client.folderService.folderServiceEndpoint();
        expect(response).toEqual(undefined);
    });

    test("folder_service_unknownRequest (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });
        const rawRequestBody = { key: "value" };

        server.mockEndpoint().post("/service").jsonBody(rawRequestBody).respondWith().statusCode(200).build();

        const response = await client.folderService.folderServiceUnknownRequest({
            key: "value",
        });
        expect(response).toEqual(undefined);
    });

    test("folder_service_unknownRequest (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new SeedApiClient({ maxRetries: 0, environment: server.baseUrl });
        const rawRequestBody = { key: "value" };
        const rawResponseBody = "string";

        server
            .mockEndpoint()
            .post("/service")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.folderService.folderServiceUnknownRequest({
                key: "value",
            });
        }).rejects.toThrow(SeedApi.NotFoundError);
    });
});
