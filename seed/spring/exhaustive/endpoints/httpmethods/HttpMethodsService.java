/**
 * This file was auto-generated by Fern from our API Definition.
 */

package endpoints.httpmethods;

import core.BearerAuth;
import java.lang.String;
import java.security.Principal;
import org.springframework.web.bind.annotation.DeleteMapping;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PatchMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.PutMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestHeader;
import org.springframework.web.bind.annotation.RequestMapping;
import types.object.ObjectWithOptionalField;
import types.object.ObjectWithRequiredField;

@RequestMapping(
    path = "/http-methods"
)
public interface HttpMethodsService {
  @GetMapping(
      value = "/{id}",
      produces = "application/json"
  )
  String testGet(@RequestHeader("Authorization") BearerAuth auth, Principal principal,
      @PathVariable("id") String id);

  @PostMapping(
      value = "",
      produces = "application/json",
      consumes = "application/json"
  )
  ObjectWithOptionalField testPost(@RequestHeader("Authorization") BearerAuth auth,
      Principal principal, @RequestBody ObjectWithRequiredField body);

  @PutMapping(
      value = "/{id}",
      produces = "application/json",
      consumes = "application/json"
  )
  ObjectWithOptionalField testPut(@RequestHeader("Authorization") BearerAuth auth,
      Principal principal, @PathVariable("id") String id,
      @RequestBody ObjectWithRequiredField body);

  @PatchMapping(
      value = "/{id}",
      produces = "application/json",
      consumes = "application/json"
  )
  ObjectWithOptionalField testPatch(@RequestHeader("Authorization") BearerAuth auth,
      Principal principal, @PathVariable("id") String id,
      @RequestBody ObjectWithOptionalField body);

  @DeleteMapping(
      value = "/{id}",
      produces = "application/json"
  )
  boolean testDelete(@RequestHeader("Authorization") BearerAuth auth, Principal principal,
      @PathVariable("id") String id);
}
