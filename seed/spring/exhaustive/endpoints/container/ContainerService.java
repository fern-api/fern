/**
 * This file was auto-generated by Fern from our API Definition.
 */

package endpoints.container;

import core.BearerAuth;
import java.lang.String;
import java.security.Principal;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.Set;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestHeader;
import org.springframework.web.bind.annotation.RequestMapping;
import types.object.ObjectWithRequiredField;

@RequestMapping(
    path = "/container"
)
public interface ContainerService {
  @PostMapping(
      value = "/list-of-primitives",
      produces = "application/json",
      consumes = "application/json"
  )
  List<String> getAndReturnListOfPrimitives(@RequestHeader("Authorization") BearerAuth auth,
      Principal principal, @RequestBody List<String> body);

  @PostMapping(
      value = "/list-of-objects",
      produces = "application/json",
      consumes = "application/json"
  )
  List<ObjectWithRequiredField> getAndReturnListOfObjects(
      @RequestHeader("Authorization") BearerAuth auth, Principal principal,
      @RequestBody List<ObjectWithRequiredField> body);

  @PostMapping(
      value = "/set-of-primitives",
      produces = "application/json",
      consumes = "application/json"
  )
  Set<String> getAndReturnSetOfPrimitives(@RequestHeader("Authorization") BearerAuth auth,
      Principal principal, @RequestBody Set<String> body);

  @PostMapping(
      value = "/set-of-objects",
      produces = "application/json",
      consumes = "application/json"
  )
  Set<ObjectWithRequiredField> getAndReturnSetOfObjects(
      @RequestHeader("Authorization") BearerAuth auth, Principal principal,
      @RequestBody Set<ObjectWithRequiredField> body);

  @PostMapping(
      value = "/map-prim-to-prim",
      produces = "application/json",
      consumes = "application/json"
  )
  Map<String, String> getAndReturnMapPrimToPrim(@RequestHeader("Authorization") BearerAuth auth,
      Principal principal, @RequestBody Map<String, String> body);

  @PostMapping(
      value = "/map-prim-to-object",
      produces = "application/json",
      consumes = "application/json"
  )
  Map<String, ObjectWithRequiredField> getAndReturnMapOfPrimToObject(
      @RequestHeader("Authorization") BearerAuth auth, Principal principal,
      @RequestBody Map<String, ObjectWithRequiredField> body);

  @PostMapping(
      value = "/opt-objects",
      produces = "application/json",
      consumes = "application/json"
  )
  Optional<ObjectWithRequiredField> getAndReturnOptional(
      @RequestHeader("Authorization") BearerAuth auth, Principal principal,
      @RequestBody Optional<ObjectWithRequiredField> body);
}
