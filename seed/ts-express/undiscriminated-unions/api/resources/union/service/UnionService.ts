// This file was auto-generated by Fern from our API Definition.

import express from "express";
import * as errors from "../../../../errors/index";
import * as serializers from "../../../../serialization/index";
import type * as SeedUndiscriminatedUnions from "../../../index";

export interface UnionServiceMethods {
    get(
        req: express.Request<never, SeedUndiscriminatedUnions.MyUnion, SeedUndiscriminatedUnions.MyUnion, never>,
        res: {
            send: (responseBody: SeedUndiscriminatedUnions.MyUnion) => Promise<void>;
            cookie: (cookie: string, value: string, options?: express.CookieOptions) => void;
            locals: any;
        },
        next: express.NextFunction,
    ): void | Promise<void>;
    getMetadata(
        req: express.Request<never, SeedUndiscriminatedUnions.Metadata, never, never>,
        res: {
            send: (responseBody: SeedUndiscriminatedUnions.Metadata) => Promise<void>;
            cookie: (cookie: string, value: string, options?: express.CookieOptions) => void;
            locals: any;
        },
        next: express.NextFunction,
    ): void | Promise<void>;
    updateMetadata(
        req: express.Request<never, boolean, SeedUndiscriminatedUnions.MetadataUnion, never>,
        res: {
            send: (responseBody: boolean) => Promise<void>;
            cookie: (cookie: string, value: string, options?: express.CookieOptions) => void;
            locals: any;
        },
        next: express.NextFunction,
    ): void | Promise<void>;
    call(
        req: express.Request<never, boolean, SeedUndiscriminatedUnions.Request, never>,
        res: {
            send: (responseBody: boolean) => Promise<void>;
            cookie: (cookie: string, value: string, options?: express.CookieOptions) => void;
            locals: any;
        },
        next: express.NextFunction,
    ): void | Promise<void>;
    duplicateTypesUnion(
        req: express.Request<
            never,
            SeedUndiscriminatedUnions.UnionWithDuplicateTypes,
            SeedUndiscriminatedUnions.UnionWithDuplicateTypes,
            never
        >,
        res: {
            send: (responseBody: SeedUndiscriminatedUnions.UnionWithDuplicateTypes) => Promise<void>;
            cookie: (cookie: string, value: string, options?: express.CookieOptions) => void;
            locals: any;
        },
        next: express.NextFunction,
    ): void | Promise<void>;
    nestedUnions(
        req: express.Request<never, string, SeedUndiscriminatedUnions.NestedUnionRoot, never>,
        res: {
            send: (responseBody: string) => Promise<void>;
            cookie: (cookie: string, value: string, options?: express.CookieOptions) => void;
            locals: any;
        },
        next: express.NextFunction,
    ): void | Promise<void>;
    testCamelCaseProperties(
        req: express.Request<never, string, SeedUndiscriminatedUnions.PaymentRequest, never>,
        res: {
            send: (responseBody: string) => Promise<void>;
            cookie: (cookie: string, value: string, options?: express.CookieOptions) => void;
            locals: any;
        },
        next: express.NextFunction,
    ): void | Promise<void>;
}

export class UnionService {
    private router;

    constructor(
        private readonly methods: UnionServiceMethods,
        middleware: express.RequestHandler[] = [],
    ) {
        this.router = express.Router({ mergeParams: true }).use(
            express.json({
                strict: false,
            }),
            ...middleware,
        );
    }

    public addMiddleware(handler: express.RequestHandler): this {
        this.router.use(handler);
        return this;
    }

    public toRouter(): express.Router {
        this.router.post("", async (req, res, next) => {
            const request = serializers.MyUnion.parse(req.body);
            if (request.ok) {
                req.body = request.value;
                try {
                    await this.methods.get(
                        req as any,
                        {
                            send: async (responseBody) => {
                                res.json(
                                    serializers.MyUnion.jsonOrThrow(responseBody, { unrecognizedObjectKeys: "strip" }),
                                );
                            },
                            cookie: res.cookie.bind(res),
                            locals: res.locals,
                        },
                        next,
                    );
                    if (!res.writableEnded) {
                        next();
                    }
                } catch (error) {
                    if (error instanceof errors.SeedUndiscriminatedUnionsError) {
                        console.warn(
                            `Endpoint 'get' unexpectedly threw ${error.constructor.name}. If this was intentional, please add ${error.constructor.name} to the endpoint's errors list in your Fern Definition.`,
                        );
                        await error.send(res);
                    } else {
                        res.status(500).json("Internal Server Error");
                    }
                    next(error);
                }
            } else {
                res.status(422).json({
                    errors: request.errors.map(
                        (error) => `${["request", ...error.path].join(" -> ")}: ${error.message}`,
                    ),
                });
                next(request.errors);
            }
        });
        this.router.get("/metadata", async (req, res, next) => {
            try {
                await this.methods.getMetadata(
                    req as any,
                    {
                        send: async (responseBody) => {
                            res.json(
                                serializers.Metadata.jsonOrThrow(responseBody, { unrecognizedObjectKeys: "strip" }),
                            );
                        },
                        cookie: res.cookie.bind(res),
                        locals: res.locals,
                    },
                    next,
                );
                if (!res.writableEnded) {
                    next();
                }
            } catch (error) {
                if (error instanceof errors.SeedUndiscriminatedUnionsError) {
                    console.warn(
                        `Endpoint 'getMetadata' unexpectedly threw ${error.constructor.name}. If this was intentional, please add ${error.constructor.name} to the endpoint's errors list in your Fern Definition.`,
                    );
                    await error.send(res);
                } else {
                    res.status(500).json("Internal Server Error");
                }
                next(error);
            }
        });
        this.router.put("/metadata", async (req, res, next) => {
            const request = serializers.MetadataUnion.parse(req.body);
            if (request.ok) {
                req.body = request.value;
                try {
                    await this.methods.updateMetadata(
                        req as any,
                        {
                            send: async (responseBody) => {
                                res.json(
                                    serializers.union.updateMetadata.Response.jsonOrThrow(responseBody, {
                                        unrecognizedObjectKeys: "strip",
                                    }),
                                );
                            },
                            cookie: res.cookie.bind(res),
                            locals: res.locals,
                        },
                        next,
                    );
                    if (!res.writableEnded) {
                        next();
                    }
                } catch (error) {
                    if (error instanceof errors.SeedUndiscriminatedUnionsError) {
                        console.warn(
                            `Endpoint 'updateMetadata' unexpectedly threw ${error.constructor.name}. If this was intentional, please add ${error.constructor.name} to the endpoint's errors list in your Fern Definition.`,
                        );
                        await error.send(res);
                    } else {
                        res.status(500).json("Internal Server Error");
                    }
                    next(error);
                }
            } else {
                res.status(422).json({
                    errors: request.errors.map(
                        (error) => `${["request", ...error.path].join(" -> ")}: ${error.message}`,
                    ),
                });
                next(request.errors);
            }
        });
        this.router.post("/call", async (req, res, next) => {
            const request = serializers.Request.parse(req.body);
            if (request.ok) {
                req.body = request.value;
                try {
                    await this.methods.call(
                        req as any,
                        {
                            send: async (responseBody) => {
                                res.json(
                                    serializers.union.call.Response.jsonOrThrow(responseBody, {
                                        unrecognizedObjectKeys: "strip",
                                    }),
                                );
                            },
                            cookie: res.cookie.bind(res),
                            locals: res.locals,
                        },
                        next,
                    );
                    if (!res.writableEnded) {
                        next();
                    }
                } catch (error) {
                    if (error instanceof errors.SeedUndiscriminatedUnionsError) {
                        console.warn(
                            `Endpoint 'call' unexpectedly threw ${error.constructor.name}. If this was intentional, please add ${error.constructor.name} to the endpoint's errors list in your Fern Definition.`,
                        );
                        await error.send(res);
                    } else {
                        res.status(500).json("Internal Server Error");
                    }
                    next(error);
                }
            } else {
                res.status(422).json({
                    errors: request.errors.map(
                        (error) => `${["request", ...error.path].join(" -> ")}: ${error.message}`,
                    ),
                });
                next(request.errors);
            }
        });
        this.router.post("/duplicate", async (req, res, next) => {
            const request = serializers.UnionWithDuplicateTypes.parse(req.body);
            if (request.ok) {
                req.body = request.value;
                try {
                    await this.methods.duplicateTypesUnion(
                        req as any,
                        {
                            send: async (responseBody) => {
                                res.json(
                                    serializers.UnionWithDuplicateTypes.jsonOrThrow(responseBody, {
                                        unrecognizedObjectKeys: "strip",
                                    }),
                                );
                            },
                            cookie: res.cookie.bind(res),
                            locals: res.locals,
                        },
                        next,
                    );
                    if (!res.writableEnded) {
                        next();
                    }
                } catch (error) {
                    if (error instanceof errors.SeedUndiscriminatedUnionsError) {
                        console.warn(
                            `Endpoint 'duplicateTypesUnion' unexpectedly threw ${error.constructor.name}. If this was intentional, please add ${error.constructor.name} to the endpoint's errors list in your Fern Definition.`,
                        );
                        await error.send(res);
                    } else {
                        res.status(500).json("Internal Server Error");
                    }
                    next(error);
                }
            } else {
                res.status(422).json({
                    errors: request.errors.map(
                        (error) => `${["request", ...error.path].join(" -> ")}: ${error.message}`,
                    ),
                });
                next(request.errors);
            }
        });
        this.router.post("/nested", async (req, res, next) => {
            const request = serializers.NestedUnionRoot.parse(req.body);
            if (request.ok) {
                req.body = request.value;
                try {
                    await this.methods.nestedUnions(
                        req as any,
                        {
                            send: async (responseBody) => {
                                res.json(
                                    serializers.union.nestedUnions.Response.jsonOrThrow(responseBody, {
                                        unrecognizedObjectKeys: "strip",
                                    }),
                                );
                            },
                            cookie: res.cookie.bind(res),
                            locals: res.locals,
                        },
                        next,
                    );
                    if (!res.writableEnded) {
                        next();
                    }
                } catch (error) {
                    if (error instanceof errors.SeedUndiscriminatedUnionsError) {
                        console.warn(
                            `Endpoint 'nestedUnions' unexpectedly threw ${error.constructor.name}. If this was intentional, please add ${error.constructor.name} to the endpoint's errors list in your Fern Definition.`,
                        );
                        await error.send(res);
                    } else {
                        res.status(500).json("Internal Server Error");
                    }
                    next(error);
                }
            } else {
                res.status(422).json({
                    errors: request.errors.map(
                        (error) => `${["request", ...error.path].join(" -> ")}: ${error.message}`,
                    ),
                });
                next(request.errors);
            }
        });
        this.router.post("/camel-case", async (req, res, next) => {
            const request = serializers.PaymentRequest.parse(req.body);
            if (request.ok) {
                req.body = request.value;
                try {
                    await this.methods.testCamelCaseProperties(
                        req as any,
                        {
                            send: async (responseBody) => {
                                res.json(
                                    serializers.union.testCamelCaseProperties.Response.jsonOrThrow(responseBody, {
                                        unrecognizedObjectKeys: "strip",
                                    }),
                                );
                            },
                            cookie: res.cookie.bind(res),
                            locals: res.locals,
                        },
                        next,
                    );
                    if (!res.writableEnded) {
                        next();
                    }
                } catch (error) {
                    if (error instanceof errors.SeedUndiscriminatedUnionsError) {
                        console.warn(
                            `Endpoint 'testCamelCaseProperties' unexpectedly threw ${error.constructor.name}. If this was intentional, please add ${error.constructor.name} to the endpoint's errors list in your Fern Definition.`,
                        );
                        await error.send(res);
                    } else {
                        res.status(500).json("Internal Server Error");
                    }
                    next(error);
                }
            } else {
                res.status(422).json({
                    errors: request.errors.map(
                        (error) => `${["request", ...error.path].join(" -> ")}: ${error.message}`,
                    ),
                });
                next(request.errors);
            }
        });
        return this.router;
    }
}
