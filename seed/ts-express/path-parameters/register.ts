// This file was auto-generated by Fern from our API Definition.

import type express from "express";
import type { OrganizationsService } from "./api/resources/organizations/service/OrganizationsService";
import type { UserService } from "./api/resources/user/service/UserService";

export function register(
    expressApp: express.Express | express.Router,
    services: {
        organizations: OrganizationsService;
        user: UserService;
    },
): void {
    (expressApp as any).use("/organizations/:organization_id", services.organizations.toRouter());
    (expressApp as any).use("/user", services.user.toRouter());
}
