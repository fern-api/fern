// This file was auto-generated by Fern from our API Definition.

import express from "express";
import * as errors from "../../../../errors/index";
import * as serializers from "../../../../serialization/index";
import type * as SeedPathParameters from "../../../index";

export interface OrganizationsServiceMethods {
    getOrganization(
        req: express.Request<
            {
                organization_id: string;
            },
            SeedPathParameters.Organization,
            never,
            never
        >,
        res: {
            send: (responseBody: SeedPathParameters.Organization) => Promise<void>;
            cookie: (cookie: string, value: string, options?: express.CookieOptions) => void;
            locals: any;
        },
        next: express.NextFunction,
    ): void | Promise<void>;
    getOrganizationUser(
        req: express.Request<
            {
                organization_id: string;
                user_id: string;
            },
            SeedPathParameters.User,
            never,
            never
        >,
        res: {
            send: (responseBody: SeedPathParameters.User) => Promise<void>;
            cookie: (cookie: string, value: string, options?: express.CookieOptions) => void;
            locals: any;
        },
        next: express.NextFunction,
    ): void | Promise<void>;
    searchOrganizations(
        req: express.Request<
            {
                organization_id: string;
            },
            SeedPathParameters.Organization[],
            never,
            {
                limit?: number;
            }
        >,
        res: {
            send: (responseBody: SeedPathParameters.Organization[]) => Promise<void>;
            cookie: (cookie: string, value: string, options?: express.CookieOptions) => void;
            locals: any;
        },
        next: express.NextFunction,
    ): void | Promise<void>;
}

export class OrganizationsService {
    private router;

    constructor(
        private readonly methods: OrganizationsServiceMethods,
        middleware: express.RequestHandler[] = [],
    ) {
        this.router = express.Router({ mergeParams: true }).use(
            express.json({
                strict: false,
            }),
            ...middleware,
        );
    }

    public addMiddleware(handler: express.RequestHandler): this {
        this.router.use(handler);
        return this;
    }

    public toRouter(): express.Router {
        this.router.get("/", async (req, res, next) => {
            try {
                await this.methods.getOrganization(
                    req as any,
                    {
                        send: async (responseBody) => {
                            res.json(
                                serializers.Organization.jsonOrThrow(responseBody, { unrecognizedObjectKeys: "strip" }),
                            );
                        },
                        cookie: res.cookie.bind(res),
                        locals: res.locals,
                    },
                    next,
                );
                if (!res.writableEnded) {
                    next();
                }
            } catch (error) {
                if (error instanceof errors.SeedPathParametersError) {
                    console.warn(
                        `Endpoint 'getOrganization' unexpectedly threw ${error.constructor.name}. If this was intentional, please add ${error.constructor.name} to the endpoint's errors list in your Fern Definition.`,
                    );
                    await error.send(res);
                } else {
                    res.status(500).json("Internal Server Error");
                }
                next(error);
            }
        });
        this.router.get("/users/:user_id", async (req, res, next) => {
            try {
                await this.methods.getOrganizationUser(
                    req as any,
                    {
                        send: async (responseBody) => {
                            res.json(serializers.User.jsonOrThrow(responseBody, { unrecognizedObjectKeys: "strip" }));
                        },
                        cookie: res.cookie.bind(res),
                        locals: res.locals,
                    },
                    next,
                );
                if (!res.writableEnded) {
                    next();
                }
            } catch (error) {
                if (error instanceof errors.SeedPathParametersError) {
                    console.warn(
                        `Endpoint 'getOrganizationUser' unexpectedly threw ${error.constructor.name}. If this was intentional, please add ${error.constructor.name} to the endpoint's errors list in your Fern Definition.`,
                    );
                    await error.send(res);
                } else {
                    res.status(500).json("Internal Server Error");
                }
                next(error);
            }
        });
        this.router.get("/search", async (req, res, next) => {
            try {
                await this.methods.searchOrganizations(
                    req as any,
                    {
                        send: async (responseBody) => {
                            res.json(
                                serializers.organizations.searchOrganizations.Response.jsonOrThrow(responseBody, {
                                    unrecognizedObjectKeys: "strip",
                                }),
                            );
                        },
                        cookie: res.cookie.bind(res),
                        locals: res.locals,
                    },
                    next,
                );
                if (!res.writableEnded) {
                    next();
                }
            } catch (error) {
                if (error instanceof errors.SeedPathParametersError) {
                    console.warn(
                        `Endpoint 'searchOrganizations' unexpectedly threw ${error.constructor.name}. If this was intentional, please add ${error.constructor.name} to the endpoint's errors list in your Fern Definition.`,
                    );
                    await error.send(res);
                } else {
                    res.status(500).json("Internal Server Error");
                }
                next(error);
            }
        });
        return this.router;
    }
}
