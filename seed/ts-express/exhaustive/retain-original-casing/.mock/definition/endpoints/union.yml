imports:
  unions: ../types/union.yml

service:
  auth: true
  base-path: /union
  endpoints:
    getAndReturnUnion:
      method: POST
      path: ""
      request: unions.Animal
      response: unions.Animal
