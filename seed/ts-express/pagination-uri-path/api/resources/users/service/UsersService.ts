// This file was auto-generated by Fern from our API Definition.

import express from "express";
import * as errors from "../../../../errors/index";
import * as serializers from "../../../../serialization/index";
import type * as SeedPaginationUriPath from "../../../index";

export interface UsersServiceMethods {
    listWithUriPagination(
        req: express.Request<never, SeedPaginationUriPath.ListUsersUriPaginationResponse, never, never>,
        res: {
            send: (responseBody: SeedPaginationUriPath.ListUsersUriPaginationResponse) => Promise<void>;
            cookie: (cookie: string, value: string, options?: express.CookieOptions) => void;
            locals: any;
        },
        next: express.NextFunction,
    ): void | Promise<void>;
    listWithPathPagination(
        req: express.Request<never, SeedPaginationUriPath.ListUsersPathPaginationResponse, never, never>,
        res: {
            send: (responseBody: SeedPaginationUriPath.ListUsersPathPaginationResponse) => Promise<void>;
            cookie: (cookie: string, value: string, options?: express.CookieOptions) => void;
            locals: any;
        },
        next: express.NextFunction,
    ): void | Promise<void>;
}

export class UsersService {
    private router;

    constructor(
        private readonly methods: UsersServiceMethods,
        middleware: express.RequestHandler[] = [],
    ) {
        this.router = express.Router({ mergeParams: true }).use(
            express.json({
                strict: false,
            }),
            ...middleware,
        );
    }

    public addMiddleware(handler: express.RequestHandler): this {
        this.router.use(handler);
        return this;
    }

    public toRouter(): express.Router {
        this.router.get("/uri", async (req, res, next) => {
            try {
                await this.methods.listWithUriPagination(
                    req as any,
                    {
                        send: async (responseBody) => {
                            res.json(
                                serializers.ListUsersUriPaginationResponse.jsonOrThrow(responseBody, {
                                    unrecognizedObjectKeys: "strip",
                                }),
                            );
                        },
                        cookie: res.cookie.bind(res),
                        locals: res.locals,
                    },
                    next,
                );
                if (!res.writableEnded) {
                    next();
                }
            } catch (error) {
                if (error instanceof errors.SeedPaginationUriPathError) {
                    console.warn(
                        `Endpoint 'listWithUriPagination' unexpectedly threw ${error.constructor.name}. If this was intentional, please add ${error.constructor.name} to the endpoint's errors list in your Fern Definition.`,
                    );
                    await error.send(res);
                } else {
                    res.status(500).json("Internal Server Error");
                }
                next(error);
            }
        });
        this.router.get("/path", async (req, res, next) => {
            try {
                await this.methods.listWithPathPagination(
                    req as any,
                    {
                        send: async (responseBody) => {
                            res.json(
                                serializers.ListUsersPathPaginationResponse.jsonOrThrow(responseBody, {
                                    unrecognizedObjectKeys: "strip",
                                }),
                            );
                        },
                        cookie: res.cookie.bind(res),
                        locals: res.locals,
                    },
                    next,
                );
                if (!res.writableEnded) {
                    next();
                }
            } catch (error) {
                if (error instanceof errors.SeedPaginationUriPathError) {
                    console.warn(
                        `Endpoint 'listWithPathPagination' unexpectedly threw ${error.constructor.name}. If this was intentional, please add ${error.constructor.name} to the endpoint's errors list in your Fern Definition.`,
                    );
                    await error.send(res);
                } else {
                    res.status(500).json("Internal Server Error");
                }
                next(error);
            }
        });
        return this.router;
    }
}
