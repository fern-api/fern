// This file was auto-generated by Fern from our API Definition.

import express from "express";
import * as errors from "../../../../errors/index";
import * as serializers from "../../../../serialization/index";
import type * as SeedResponseProperty from "../../../index";

export interface ServiceServiceMethods {
    getMovie(
        req: express.Request<never, SeedResponseProperty.Response, string, never>,
        res: {
            send: (responseBody: SeedResponseProperty.Response) => Promise<void>;
            cookie: (cookie: string, value: string, options?: express.CookieOptions) => void;
            locals: any;
        },
        next: express.NextFunction,
    ): void | Promise<void>;
    getMovieDocs(
        req: express.Request<never, SeedResponseProperty.Response, string, never>,
        res: {
            send: (responseBody: SeedResponseProperty.Response) => Promise<void>;
            cookie: (cookie: string, value: string, options?: express.CookieOptions) => void;
            locals: any;
        },
        next: express.NextFunction,
    ): void | Promise<void>;
    getMovieName(
        req: express.Request<never, SeedResponseProperty.StringResponse, string, never>,
        res: {
            send: (responseBody: SeedResponseProperty.StringResponse) => Promise<void>;
            cookie: (cookie: string, value: string, options?: express.CookieOptions) => void;
            locals: any;
        },
        next: express.NextFunction,
    ): void | Promise<void>;
    getMovieMetadata(
        req: express.Request<never, SeedResponseProperty.Response, string, never>,
        res: {
            send: (responseBody: SeedResponseProperty.Response) => Promise<void>;
            cookie: (cookie: string, value: string, options?: express.CookieOptions) => void;
            locals: any;
        },
        next: express.NextFunction,
    ): void | Promise<void>;
    getOptionalMovie(
        req: express.Request<never, SeedResponseProperty.Response | undefined, string, never>,
        res: {
            send: (responseBody: SeedResponseProperty.Response | undefined) => Promise<void>;
            cookie: (cookie: string, value: string, options?: express.CookieOptions) => void;
            locals: any;
        },
        next: express.NextFunction,
    ): void | Promise<void>;
    getOptionalMovieDocs(
        req: express.Request<never, SeedResponseProperty.OptionalWithDocs | undefined, string, never>,
        res: {
            send: (responseBody: SeedResponseProperty.OptionalWithDocs | undefined) => Promise<void>;
            cookie: (cookie: string, value: string, options?: express.CookieOptions) => void;
            locals: any;
        },
        next: express.NextFunction,
    ): void | Promise<void>;
    getOptionalMovieName(
        req: express.Request<never, SeedResponseProperty.OptionalStringResponse | undefined, string, never>,
        res: {
            send: (responseBody: SeedResponseProperty.OptionalStringResponse | undefined) => Promise<void>;
            cookie: (cookie: string, value: string, options?: express.CookieOptions) => void;
            locals: any;
        },
        next: express.NextFunction,
    ): void | Promise<void>;
}

export class ServiceService {
    private router;

    constructor(
        private readonly methods: ServiceServiceMethods,
        middleware: express.RequestHandler[] = [],
    ) {
        this.router = express.Router({ mergeParams: true }).use(
            express.json({
                strict: false,
            }),
            ...middleware,
        );
    }

    public addMiddleware(handler: express.RequestHandler): this {
        this.router.use(handler);
        return this;
    }

    public toRouter(): express.Router {
        this.router.post("/movie", async (req, res, next) => {
            const request = serializers.service.getMovie.Request.parse(req.body);
            if (request.ok) {
                req.body = request.value;
                try {
                    await this.methods.getMovie(
                        req as any,
                        {
                            send: async (responseBody) => {
                                res.json(
                                    serializers.Response.jsonOrThrow(responseBody, { unrecognizedObjectKeys: "strip" }),
                                );
                            },
                            cookie: res.cookie.bind(res),
                            locals: res.locals,
                        },
                        next,
                    );
                    if (!res.writableEnded) {
                        next();
                    }
                } catch (error) {
                    if (error instanceof errors.SeedResponsePropertyError) {
                        console.warn(
                            `Endpoint 'getMovie' unexpectedly threw ${error.constructor.name}. If this was intentional, please add ${error.constructor.name} to the endpoint's errors list in your Fern Definition.`,
                        );
                        await error.send(res);
                    } else {
                        res.status(500).json("Internal Server Error");
                    }
                    next(error);
                }
            } else {
                res.status(422).json({
                    errors: request.errors.map(
                        (error) => `${["request", ...error.path].join(" -> ")}: ${error.message}`,
                    ),
                });
                next(request.errors);
            }
        });
        this.router.post("/movie", async (req, res, next) => {
            const request = serializers.service.getMovieDocs.Request.parse(req.body);
            if (request.ok) {
                req.body = request.value;
                try {
                    await this.methods.getMovieDocs(
                        req as any,
                        {
                            send: async (responseBody) => {
                                res.json(
                                    serializers.Response.jsonOrThrow(responseBody, { unrecognizedObjectKeys: "strip" }),
                                );
                            },
                            cookie: res.cookie.bind(res),
                            locals: res.locals,
                        },
                        next,
                    );
                    if (!res.writableEnded) {
                        next();
                    }
                } catch (error) {
                    if (error instanceof errors.SeedResponsePropertyError) {
                        console.warn(
                            `Endpoint 'getMovieDocs' unexpectedly threw ${error.constructor.name}. If this was intentional, please add ${error.constructor.name} to the endpoint's errors list in your Fern Definition.`,
                        );
                        await error.send(res);
                    } else {
                        res.status(500).json("Internal Server Error");
                    }
                    next(error);
                }
            } else {
                res.status(422).json({
                    errors: request.errors.map(
                        (error) => `${["request", ...error.path].join(" -> ")}: ${error.message}`,
                    ),
                });
                next(request.errors);
            }
        });
        this.router.post("/movie", async (req, res, next) => {
            const request = serializers.service.getMovieName.Request.parse(req.body);
            if (request.ok) {
                req.body = request.value;
                try {
                    await this.methods.getMovieName(
                        req as any,
                        {
                            send: async (responseBody) => {
                                res.json(
                                    serializers.StringResponse.jsonOrThrow(responseBody, {
                                        unrecognizedObjectKeys: "strip",
                                    }),
                                );
                            },
                            cookie: res.cookie.bind(res),
                            locals: res.locals,
                        },
                        next,
                    );
                    if (!res.writableEnded) {
                        next();
                    }
                } catch (error) {
                    if (error instanceof errors.SeedResponsePropertyError) {
                        console.warn(
                            `Endpoint 'getMovieName' unexpectedly threw ${error.constructor.name}. If this was intentional, please add ${error.constructor.name} to the endpoint's errors list in your Fern Definition.`,
                        );
                        await error.send(res);
                    } else {
                        res.status(500).json("Internal Server Error");
                    }
                    next(error);
                }
            } else {
                res.status(422).json({
                    errors: request.errors.map(
                        (error) => `${["request", ...error.path].join(" -> ")}: ${error.message}`,
                    ),
                });
                next(request.errors);
            }
        });
        this.router.post("/movie", async (req, res, next) => {
            const request = serializers.service.getMovieMetadata.Request.parse(req.body);
            if (request.ok) {
                req.body = request.value;
                try {
                    await this.methods.getMovieMetadata(
                        req as any,
                        {
                            send: async (responseBody) => {
                                res.json(
                                    serializers.Response.jsonOrThrow(responseBody, { unrecognizedObjectKeys: "strip" }),
                                );
                            },
                            cookie: res.cookie.bind(res),
                            locals: res.locals,
                        },
                        next,
                    );
                    if (!res.writableEnded) {
                        next();
                    }
                } catch (error) {
                    if (error instanceof errors.SeedResponsePropertyError) {
                        console.warn(
                            `Endpoint 'getMovieMetadata' unexpectedly threw ${error.constructor.name}. If this was intentional, please add ${error.constructor.name} to the endpoint's errors list in your Fern Definition.`,
                        );
                        await error.send(res);
                    } else {
                        res.status(500).json("Internal Server Error");
                    }
                    next(error);
                }
            } else {
                res.status(422).json({
                    errors: request.errors.map(
                        (error) => `${["request", ...error.path].join(" -> ")}: ${error.message}`,
                    ),
                });
                next(request.errors);
            }
        });
        this.router.post("/movie", async (req, res, next) => {
            const request = serializers.service.getOptionalMovie.Request.parse(req.body);
            if (request.ok) {
                req.body = request.value;
                try {
                    await this.methods.getOptionalMovie(
                        req as any,
                        {
                            send: async (responseBody) => {
                                res.json(
                                    serializers.service.getOptionalMovie.Response.jsonOrThrow(responseBody, {
                                        unrecognizedObjectKeys: "strip",
                                    }),
                                );
                            },
                            cookie: res.cookie.bind(res),
                            locals: res.locals,
                        },
                        next,
                    );
                    if (!res.writableEnded) {
                        next();
                    }
                } catch (error) {
                    if (error instanceof errors.SeedResponsePropertyError) {
                        console.warn(
                            `Endpoint 'getOptionalMovie' unexpectedly threw ${error.constructor.name}. If this was intentional, please add ${error.constructor.name} to the endpoint's errors list in your Fern Definition.`,
                        );
                        await error.send(res);
                    } else {
                        res.status(500).json("Internal Server Error");
                    }
                    next(error);
                }
            } else {
                res.status(422).json({
                    errors: request.errors.map(
                        (error) => `${["request", ...error.path].join(" -> ")}: ${error.message}`,
                    ),
                });
                next(request.errors);
            }
        });
        this.router.post("/movie", async (req, res, next) => {
            const request = serializers.service.getOptionalMovieDocs.Request.parse(req.body);
            if (request.ok) {
                req.body = request.value;
                try {
                    await this.methods.getOptionalMovieDocs(
                        req as any,
                        {
                            send: async (responseBody) => {
                                res.json(
                                    serializers.OptionalWithDocs.jsonOrThrow(responseBody, {
                                        unrecognizedObjectKeys: "strip",
                                    }),
                                );
                            },
                            cookie: res.cookie.bind(res),
                            locals: res.locals,
                        },
                        next,
                    );
                    if (!res.writableEnded) {
                        next();
                    }
                } catch (error) {
                    if (error instanceof errors.SeedResponsePropertyError) {
                        console.warn(
                            `Endpoint 'getOptionalMovieDocs' unexpectedly threw ${error.constructor.name}. If this was intentional, please add ${error.constructor.name} to the endpoint's errors list in your Fern Definition.`,
                        );
                        await error.send(res);
                    } else {
                        res.status(500).json("Internal Server Error");
                    }
                    next(error);
                }
            } else {
                res.status(422).json({
                    errors: request.errors.map(
                        (error) => `${["request", ...error.path].join(" -> ")}: ${error.message}`,
                    ),
                });
                next(request.errors);
            }
        });
        this.router.post("/movie", async (req, res, next) => {
            const request = serializers.service.getOptionalMovieName.Request.parse(req.body);
            if (request.ok) {
                req.body = request.value;
                try {
                    await this.methods.getOptionalMovieName(
                        req as any,
                        {
                            send: async (responseBody) => {
                                res.json(
                                    serializers.OptionalStringResponse.jsonOrThrow(responseBody, {
                                        unrecognizedObjectKeys: "strip",
                                    }),
                                );
                            },
                            cookie: res.cookie.bind(res),
                            locals: res.locals,
                        },
                        next,
                    );
                    if (!res.writableEnded) {
                        next();
                    }
                } catch (error) {
                    if (error instanceof errors.SeedResponsePropertyError) {
                        console.warn(
                            `Endpoint 'getOptionalMovieName' unexpectedly threw ${error.constructor.name}. If this was intentional, please add ${error.constructor.name} to the endpoint's errors list in your Fern Definition.`,
                        );
                        await error.send(res);
                    } else {
                        res.status(500).json("Internal Server Error");
                    }
                    next(error);
                }
            } else {
                res.status(422).json({
                    errors: request.errors.map(
                        (error) => `${["request", ...error.path].join(" -> ")}: ${error.message}`,
                    ),
                });
                next(request.errors);
            }
        });
        return this.router;
    }
}
