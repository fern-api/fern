// This file was auto-generated by Fern from our API Definition.

import express from "express";
import * as errors from "../../errors/index";
import * as serializers from "../../serialization/index";
import type * as SeedApi from "../index";

export interface RootServiceMethods {
    testGet(
        req: express.Request<
            {
                region: string;
            },
            SeedApi.TestGetResponse,
            never,
            {
                limit?: string;
            }
        >,
        res: {
            send: (responseBody: SeedApi.TestGetResponse) => Promise<void>;
            cookie: (cookie: string, value: string, options?: express.CookieOptions) => void;
            locals: any;
        },
        next: express.NextFunction,
    ): void | Promise<void>;
}

export class RootService {
    private router;

    constructor(
        private readonly methods: RootServiceMethods,
        middleware: express.RequestHandler[] = [],
    ) {
        this.router = express.Router({ mergeParams: true }).use(
            express.json({
                strict: false,
            }),
            ...middleware,
        );
    }

    public addMiddleware(handler: express.RequestHandler): this {
        this.router.use(handler);
        return this;
    }

    public toRouter(): express.Router {
        this.router.get("/test/:region/resource", async (req, res, next) => {
            try {
                await this.methods.testGet(
                    req as any,
                    {
                        send: async (responseBody) => {
                            res.status(200).json(
                                serializers.TestGetResponse.jsonOrThrow(responseBody, {
                                    unrecognizedObjectKeys: "strip",
                                }),
                            );
                        },
                        cookie: res.cookie.bind(res),
                        locals: res.locals,
                    },
                    next,
                );
                if (!res.writableEnded) {
                    next();
                }
            } catch (error) {
                if (error instanceof errors.SeedApiError) {
                    console.warn(
                        `Endpoint 'test_get' unexpectedly threw ${error.constructor.name}. If this was intentional, please add ${error.constructor.name} to the endpoint's errors list in your Fern Definition.`,
                    );
                    await error.send(res);
                } else {
                    res.status(500).json("Internal Server Error");
                }
                next(error);
            }
        });
        return this.router;
    }
}
