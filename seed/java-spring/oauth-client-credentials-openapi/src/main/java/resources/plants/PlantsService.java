/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.plants;

import core.BearerAuth;
import java.lang.String;
import java.security.Principal;
import java.util.List;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestHeader;
import org.springframework.web.bind.annotation.RequestMapping;
import types.Plant;

@RequestMapping(
    path = "/"
)
public interface PlantsService {
  @GetMapping(
      value = "/plants",
      produces = "application/json"
  )
  List<Plant> list(@RequestHeader("Authorization") BearerAuth auth, Principal principal);

  @GetMapping(
      value = "/plants/{plantId}",
      produces = "application/json"
  )
  Plant get(@RequestHeader("Authorization") BearerAuth auth, Principal principal,
      @PathVariable("plantId") String plantId);
}
