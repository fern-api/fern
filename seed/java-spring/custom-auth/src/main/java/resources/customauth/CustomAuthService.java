/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.customauth;

import core.CustomAuthScheme;
import java.lang.Object;
import java.security.Principal;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestHeader;
import org.springframework.web.bind.annotation.RequestMapping;
import resources.errors.exceptions.BadRequest;
import resources.errors.exceptions.UnauthorizedRequest;

@RequestMapping(
    path = "/"
)
public interface CustomAuthService {
  @GetMapping(
      value = "/custom-auth",
      produces = "application/json"
  )
  boolean getWithCustomAuth(@RequestHeader("X-API-KEY") CustomAuthScheme auth, Principal principal)
      throws UnauthorizedRequest;

  @PostMapping(
      value = "/custom-auth",
      produces = "application/json",
      consumes = "application/json"
  )
  boolean postWithCustomAuth(@RequestHeader("X-API-KEY") CustomAuthScheme auth, Principal principal,
      @RequestBody Object body) throws UnauthorizedRequest, BadRequest;
}
