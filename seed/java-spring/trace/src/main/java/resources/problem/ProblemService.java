/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.problem;

import java.lang.String;
import java.util.Optional;
import org.springframework.web.bind.annotation.DeleteMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestHeader;
import org.springframework.web.bind.annotation.RequestMapping;
import resources.commons.types.ProblemId;
import resources.problem.requests.GetDefaultStarterFilesRequest;
import resources.problem.types.CreateProblemRequest;
import resources.problem.types.CreateProblemResponse;
import resources.problem.types.GetDefaultStarterFilesResponse;
import resources.problem.types.UpdateProblemResponse;

@RequestMapping(
    path = "/problem-crud"
)
public interface ProblemService {
  @PostMapping(
      value = "/create",
      produces = "application/json",
      consumes = "application/json"
  )
  CreateProblemResponse createProblem(
      @RequestHeader("X-Random-Header") Optional<String> xRandomHeader,
      @RequestBody CreateProblemRequest body);

  @PostMapping(
      value = "/update/{problemId}",
      produces = "application/json",
      consumes = "application/json"
  )
  UpdateProblemResponse updateProblem(
      @RequestHeader("X-Random-Header") Optional<String> xRandomHeader,
      @PathVariable("problemId") ProblemId problemId, @RequestBody CreateProblemRequest body);

  @DeleteMapping(
      value = "/delete/{problemId}",
      produces = "application/json"
  )
  void deleteProblem(@RequestHeader("X-Random-Header") Optional<String> xRandomHeader,
      @PathVariable("problemId") ProblemId problemId);

  @PostMapping(
      value = "/default-starter-files",
      produces = "application/json",
      consumes = "application/json"
  )
  GetDefaultStarterFilesResponse getDefaultStarterFiles(
      @RequestHeader("X-Random-Header") Optional<String> xRandomHeader,
      @RequestBody GetDefaultStarterFilesRequest body);
}
