/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.endpoints.urls;

import core.BearerAuth;
import java.lang.String;
import java.security.Principal;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestHeader;
import org.springframework.web.bind.annotation.RequestMapping;

@RequestMapping(
    path = "/urls"
)
public interface UrlsService {
  @GetMapping(
      value = "/MixedCase",
      produces = "application/json"
  )
  String withMixedCase(@RequestHeader("Authorization") BearerAuth auth, Principal principal);

  @GetMapping(
      value = "/no-ending-slash",
      produces = "application/json"
  )
  String noEndingSlash(@RequestHeader("Authorization") BearerAuth auth, Principal principal);

  @GetMapping(
      value = "/with-ending-slash/",
      produces = "application/json"
  )
  String withEndingSlash(@RequestHeader("Authorization") BearerAuth auth, Principal principal);

  @GetMapping(
      value = "/with_underscores",
      produces = "application/json"
  )
  String withUnderscores(@RequestHeader("Authorization") BearerAuth auth, Principal principal);
}
