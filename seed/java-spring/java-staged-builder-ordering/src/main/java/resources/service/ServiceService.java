/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.service;

import java.lang.String;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import resources.types.types.ComplexStaged;
import resources.types.types.MediumStaged;
import resources.types.types.MixedStaged;
import resources.types.types.Parent;
import resources.types.types.SimpleStaged;

@RequestMapping(
    path = "/staged-builder"
)
public interface ServiceService {
  @PostMapping(
      value = "/simple",
      produces = "application/json",
      consumes = "application/json"
  )
  String createSimple(@RequestBody SimpleStaged body);

  @PostMapping(
      value = "/medium",
      produces = "application/json",
      consumes = "application/json"
  )
  String createMedium(@RequestBody MediumStaged body);

  @PostMapping(
      value = "/complex",
      produces = "application/json",
      consumes = "application/json"
  )
  String createComplex(@RequestBody ComplexStaged body);

  @PostMapping(
      value = "/mixed",
      produces = "application/json",
      consumes = "application/json"
  )
  String createMixed(@RequestBody MixedStaged body);

  @PostMapping(
      value = "/parent",
      produces = "application/json",
      consumes = "application/json"
  )
  String createParent(@RequestBody Parent body);
}
