/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.health.service;

import core.BearerAuth;
import java.lang.String;
import java.security.Principal;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestHeader;
import org.springframework.web.bind.annotation.RequestMapping;

@RequestMapping(
    path = "/"
)
public interface ServiceService {
  @GetMapping(
      value = "/check/{id}",
      produces = "application/json"
  )
  void check(@RequestHeader("Authorization") BearerAuth auth, Principal principal,
      @PathVariable("id") String id);

  @GetMapping(
      value = "/ping",
      produces = "application/json"
  )
  boolean ping(@RequestHeader("Authorization") BearerAuth auth, Principal principal);
}
