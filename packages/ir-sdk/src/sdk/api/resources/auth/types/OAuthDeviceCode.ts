// This file was auto-generated by Fern from our API Definition.

import type * as FernIr from "../../../index.js";

/**
 * The OAuth 2.0 Device Authorization Grant (RFC 8628) for public clients such as CLIs and
 * native apps on input-constrained or browserless devices. The client requests a device code
 * and user code from the device authorization endpoint, instructs the user to approve at the
 * verification URI in a browser, and polls the token endpoint until the user approves and
 * tokens are issued, without a client secret.
 */
export interface OAuthDeviceCode {
    /** The public client ID for the CLI/native app. Public clients do not use a client secret. */
    clientId: FernIr.OAuthPublicClientId;
    /**
     * The absolute URL of the device authorization endpoint that issues the device code,
     * user code, and verification URI.
     */
    deviceAuthorizationUrl: string;
    /**
     * The absolute URL of the token endpoint polled to exchange the device code for tokens
     * once the user has approved.
     */
    tokenUrl: string;
    /** The absolute URL used to refresh tokens. Defaults to `tokenUrl` when omitted. */
    refreshUrl: string | undefined;
    /** The scopes requested during device authorization. */
    scopes: string[] | undefined;
    /**
     * Additional public, literal parameters included in the device authorization request
     * (e.g. `audience`, `resource`). These must not override protocol-reserved parameters.
     */
    deviceAuthorizationParameters: Record<string, string> | undefined;
    /** Additional public, literal parameters included in the device-code token exchange (polling) request. */
    tokenParameters: Record<string, string> | undefined;
    /** Additional public, literal parameters included in the refresh token request. */
    refreshParameters: Record<string, string> | undefined;
    /** The header the token is applied to. Defaults to `Authorization`. */
    tokenHeader: string | undefined;
    /**
     * The prefix for the token header value. Defaults to `Bearer`.
     * An empty prefix means the raw token is sent.
     */
    tokenPrefix: string | undefined;
}
