// This file was auto-generated by Fern from our API Definition.

import type * as FernIr from "../../../index.js";

/**
 * Binds the request body to the signature via a separately transmitted hash of the
 * raw body, rather than including the body directly in the signed payload. Required by
 * providers (such as Twilio for JSON bodies) that place a hash of the raw body in the
 * notification URL as a query parameter and sign the resulting URL. Verifying the
 * signature then requires (1) recomputing the encoded hash of the raw body and
 * comparing it to the transmitted value, and (2) verifying the HMAC over the payload
 * as usual. Both checks must pass. The hash algorithm and encoding are independent of
 * the outer HMAC's algorithm and encoding.
 */
export interface WebhookBodyHashBinding {
    algorithm: FernIr.WebhookBodyHashAlgorithm;
    encoding: FernIr.WebhookSignatureEncoding;
    location: FernIr.WebhookBodyHashLocation;
}
