// This file was auto-generated by Fern from our API Definition.

import type * as FernIr from "../../../index.js";

/**
 * Configuration for the name/casing generator. Stored in the IR for backwards compatibility
 * so that IR migrations can reconstruct the correct CasingsGenerator when inflating
 * compressed NameOrString values back to full Name objects.
 */
export interface CasingsConfig {
    /** The target language, used to apply language-specific casing rules and reserved keywords. */
    generationLanguage: FernIr.SupportedSdkLanguage | undefined;
    /** Reserved keywords for the target language. Names that match these will be escaped. */
    keywords: string[] | undefined;
    /** Whether to apply smart casing (e.g., capitalizing acronyms like HTTP → Http in camelCase). */
    smartCasing: boolean;
}
