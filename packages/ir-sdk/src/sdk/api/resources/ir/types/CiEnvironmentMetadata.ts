// This file was auto-generated by Fern from our API Definition.

/**
 * Structured metadata about the CI environment that ran the generation.
 */
export interface CiEnvironmentMetadata {
    /** The CI provider (e.g. "github", "gitlab", "bitbucket"). */
    provider: string;
    /** The repository slug (e.g. "owner/name"). */
    repo: string | undefined;
    /** The CI run/pipeline identifier. */
    runId: string | undefined;
    /** A URL pointing to the CI run. */
    runUrl: string | undefined;
    /** The commit SHA the CI run is operating on. */
    commitSha: string | undefined;
    /** The branch or ref the CI run is operating on. */
    branch: string | undefined;
    /** The user or bot that triggered the CI run. */
    actor: string | undefined;
}
