// This file was auto-generated by Fern from our API Definition.

import type * as FernIr from "../../../index.js";

export interface TwimlTag extends FernIr.WithDocs {
    id: FernIr.TwimlTagId;
    /** Name of the generated builder class (e.g. `say`, `sip_uri`, `voice_response`). */
    name: FernIr.NameOrString;
    /** Exact element name written to XML, casing already applied (e.g. `Say`, `break`, `Uri`). */
    xmlName: string;
    /** Text content accepted by this tag, if any. */
    body: FernIr.TwimlBody | undefined;
    attributes: FernIr.TwimlAttribute[];
    /** Enums scoped to this tag, keyed by the name used in `TwimlType.enum`. */
    enums: Record<string, FernIr.TwimlEnum>;
    /** Tags that may be nested inside this one. May include this tag's own id (recursive SSML). */
    children: FernIr.TwimlTagId[];
    visibility: FernIr.TwimlVisibility;
}
