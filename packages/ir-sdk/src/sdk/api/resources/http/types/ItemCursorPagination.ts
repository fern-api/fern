// This file was auto-generated by Fern from our API Definition.

import type * as FernIr from "../../../index.js";

/**
 * Cursor pagination where the cursor for the next page is read from one of the
 * elements of the current page's results, rather than from a property defined at
 * the top-level of the response.
 *
 * For example, an API that returns `{ "data": [{ "token": "..." }] }` and expects
 * the token of the last element to be sent back as the cursor for the next page.
 */
export interface ItemCursorPagination {
    page: FernIr.RequestProperty;
    results: FernIr.ResponseProperty;
    /**
     * The property that contains the cursor, resolved relative to an element of
     * the results.
     */
    itemCursor: FernIr.ResponseProperty;
    /** The element of the results that the cursor is read from. */
    element: FernIr.ItemCursorElement;
}
