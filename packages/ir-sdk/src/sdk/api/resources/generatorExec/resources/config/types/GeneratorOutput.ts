// This file was auto-generated by Fern from our API Definition.

/**
 * Output metadata written by the generator to config.json in the output directory.
 * This allows generators to communicate build/test capabilities and other metadata
 * back to the caller (seed, Fern CLI, autorelease). The output directory is already
 * mounted as a Docker volume, so no additional volume mount is needed.
 */
export interface GeneratorOutput {
    /**
     * The Docker image to use for building and testing the generated code.
     * When specified, the caller should use this image to run .fern/build.sh
     * and .fern/test.sh. This decouples the build/test image from seed.yml
     * and makes it available to any caller (seed, Fern CLI, autorelease).
     */
    buildTestDockerImage: string | undefined;
}
