// This file was auto-generated by Fern from our API Definition.

import type * as FernIr from "../../../index.js";

/**
 * How an object property serializes when its parent object is an XML element.
 * Only populated on properties of objects with an `XmlEncoding`.
 */
export interface XmlPropertyEncoding {
    kind: FernIr.XmlPropertyKind;
    /**
     * The attribute or element name on the wire. Falls back to the property's
     * wire value when absent.
     */
    name: string | undefined;
    /**
     * For list properties: whether the items are wrapped in a container element
     * named `name`. Absent or false means items are emitted directly as children.
     */
    wrapped: boolean | undefined;
    /**
     * For list-valued attributes or text: the string used to join items into a
     * single value (e.g. `" "` for space-delimited lists).
     */
    listSeparator: string | undefined;
}
