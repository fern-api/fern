// This file was auto-generated by Fern from our API Definition.

/**
 * Present when an object type is an XML element (e.g. a markup document such as
 * Twilio's TwiML, which the generated SDK builds and serializes rather than sends
 * as JSON). Populated from the OpenAPI `xml` object on the schema.
 */
export interface XmlEncoding {
    /** The XML element name, exactly as it appears on the wire (casing preserved). */
    name: string;
    /** The XML namespace URI, if any. */
    namespace: string | undefined;
    /** The namespace prefix to use for the element name. */
    prefix: string | undefined;
}
