// This file was auto-generated by Fern from our API Definition.

import type * as FernIr from "../../../index.js";

/**
 * A single GraphQL argument on an object/interface field (e.g. `first` on `products(first: Int)`).
 * Used by SDK generators to type the field's `$args` selection and to declare the matching GraphQL
 * variable when building the query at runtime.
 */
export interface GraphqlFieldArgument {
    name: FernIr.NameAndWireValueOrString;
    /** The argument's value type, for generating the typed `$args` shape. */
    valueType: FernIr.TypeReference;
    /**
     * The argument's GraphQL SDL type, e.g. `Int`, `SortBy!`, or `[ProductFilter!]`. Used verbatim
     * to declare the GraphQL variable (`$var: <graphqlType>`) at query-build time.
     */
    graphqlType: string;
    docs: string | undefined;
}
