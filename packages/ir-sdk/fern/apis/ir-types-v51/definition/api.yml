name: ir-v51
docs: |
  - Add `TypeReference`s to `ExampleContainer` types, especially helpful in the case of empty container
  - The `TypeDeclaration` type now has `userDefinedExamples` and `autogeneratedExamples` in place of the
    previous `example` field.