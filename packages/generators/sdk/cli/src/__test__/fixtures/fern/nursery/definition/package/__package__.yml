types:
  PackageString: string
services:
  http:
    PackageService:
      base-path: /
      auth: false
      endpoints:
        getString:
          method: GET
          path: /
          response: PackageString
