types:
  RootString: string
services:
  http:
    RootService:
      url: ServerA
      base-path: /
      auth: false
      endpoints:
        getString:
          method: GET
          path: /
          response: RootString
