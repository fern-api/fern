types:
  PackageString: string
services:
  http:
    PackageService:
      url: ServerA
      base-path: /
      auth: false
      endpoints:
        getString:
          method: GET
          path: /
          response: PackageString
