ids:
  - ChatMessageId

types:
  ChatMessage:
    properties:
      id: ChatMessageId
      title: string
      content: string

  SendChatRequest:
    properties:
      title: string
      content: string

errors:
  GenericMessage:
    properties:
      type: string

services:
  websocket:
    ChatService:
      path: /chat
      auth: none
      client:
        operations:
          sendChat:
            request: SendChatRequest
            response: ChatMessage
            errors:
              - GenericMessage
