{
  "name": "@fernapi/api-generation",
  "version": "0.1.4",
  "private": true,
  "scripts": {
    "clean": "tsc --build --clean && /bin/rm -r generated",
    "codegen": "fern generate src ir/ir.json && run codegen:typescript",
    "codegen:typescript": "fern-typescript model ir/ir.json generated/typescript"
  },
  "devDependencies": {
    "@fernapi/fern": "0.1.3",
    "@fernapi/fern-typescript": "^0.1.9"
  }
}
