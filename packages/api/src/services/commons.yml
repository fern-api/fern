imports:
  commons: ../commons.yml
  types: ../types.yml
types:
  BaseService:
    extends: commons.WithDocs
    properties:
      name: types.NamedType
  Encoding:
    union:
      json: {}
      custom: CustomWireMessageEncoding
  CustomWireMessageEncoding:
    properties:
      encoding: string
  ResponseErrors:
    extends: commons.WithDocs
    properties:
      discriminant: string
      possibleErrors: list<ResponseError>
  ResponseError:
    extends: commons.WithDocs
    properties:
      discriminantValue: string
      error: types.NamedType
