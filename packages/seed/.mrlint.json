{
  "type": "cli",
  "cliName": "seed",
  "additionalFiles": [
    "fern/"
  ],
  "environment": {
    "environments": {
      "prod": {
        "cliName": "seed",
        "cliPackageName": "@fern-api/seed-cli"
      }
    },
    "variables": []
  },
  "rules": {
    "package-json": {
      "scripts": {
        "dist:cli": "pnpm compile && node build.cjs && cp -R ../../test-definitions/fern dist",
        "publish:cli": "pnpm dist:cli && cd dist && pnpm npm publish --access public",
        "generate": "fern generate --local"
      }
    }
  },
  "private": true
}