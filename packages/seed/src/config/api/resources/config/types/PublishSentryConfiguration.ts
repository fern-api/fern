// This file was auto-generated by Fern from our API Definition.

import type * as FernSeedConfig from "../../../index.js";

/**
 * Per-generator Sentry upload configuration. Only fields that are set
 * are processed, so each generator can opt in incrementally.
 */
export interface PublishSentryConfiguration {
    /**
     * Generator name used to construct the Sentry release identifier
     * (`${name}:${version}`). This should be the Docker image name
     * without the `fernapi/` registry prefix (e.g. `fern-typescript-sdk`).
     * Must not contain slashes — sentry-cli rejects release names with them.
     */
    name: string;
    /**
     * Sentry project slug for this generator (e.g. `csharp-sdk`,
     * `go-model`). Each generator has its own project so that issues,
     * releases, and alerts are scoped per SDK.
     */
    project: string;
    /**
     * JS/TS source map directories. Supports multiple entries so hybrid
     * generators can upload maps for both their v1 and v2 CLIs.
     */
    sourceMaps?: FernSeedConfig.SentrySourceMapArtifact[] | undefined;
}
