// This file was auto-generated by Fern from our API Definition.

import type * as FernSeedConfig from "../../../index.js";

/**
 * Options that apply to a fixture's implicit baseline (customConfig=null) run.
 * Mirrors a subset of FixtureConfigurations — only the fields that make sense
 * for a baseline (e.g. no outputFolder, no storeFullSnapshot).
 */
export interface BaselineConfiguration {
    customConfig?: unknown | undefined;
    audiences?: string[] | undefined;
    outputVersion?: string | undefined;
    /**
     * If true, dynamic snippet tests are not generated for the baseline run.
     * Use when the baseline cannot exercise dynamic snippets (e.g. grpc/proto
     * fixtures whose snippets reference types only emitted by variants).
     */
    disableDynamicSnippetTests?: boolean | undefined;
    /** Overrides the default output mode for the baseline. */
    outputMode?: FernSeedConfig.OutputMode | undefined;
    license?: unknown | undefined;
    /**
     * Either a boolean to skip all scripts, or a list of script names to skip.
     * When true, build and test scripts are skipped for the baseline.
     */
    skipScripts?: FernSeedConfig.SkipScripts | undefined;
    readmeConfig?: FernSeedConfig.ReadmeConfig | undefined;
    publishConfig?: unknown | undefined;
    publishMetadata?: unknown | undefined;
}
