import { Colors, Icon } from "@blueprintjs/core";
import { IconNames } from "@blueprintjs/icons";
import React from "react";

export const TypeSidebarItemIcon: React.FC = () => {
    return <Icon icon={IconNames.CUBE} color={Colors.TURQUOISE3} />;
};
