import { Colors, Icon } from "@blueprintjs/core";
import { IconNames } from "@blueprintjs/icons";
import React from "react";

export const ErrorSidebarItemIcon: React.FC = () => {
    return <Icon icon={IconNames.HIGH_PRIORITY} color={Colors.ORANGE3} />;
};
