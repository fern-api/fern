/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as FernDocsConfig from "../../../index";

export interface FolderConfiguration extends FernDocsConfig.WithPermissions, FernDocsConfig.WithFeatureFlags {
    /** The relative path to a folder containing markdown files. Fern will automatically discover and add all markdown files from this folder to the navigation. */
    folder: string;
    /** The title to display for this folder section. If not provided, the folder name will be used. */
    title?: string;
    slug?: string;
    icon?: string;
    hidden?: boolean;
    skipSlug?: boolean;
    collapsed?: boolean;
    availability?: FernDocsConfig.Availability;
}
