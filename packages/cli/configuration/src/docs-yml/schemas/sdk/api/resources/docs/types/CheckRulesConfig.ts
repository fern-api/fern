// This file was auto-generated by Fern from our API Definition.

import type * as FernDocsConfig from "../../../index.js";

export interface CheckRulesConfig {
    /** Severity for OpenAPI example validation. Default is warn. */
    exampleValidation?: FernDocsConfig.CheckRuleSeverity;
    /** Severity for broken link detection. Defaults to error for broken internal links and warn for malformed URLs. */
    brokenLinks?: FernDocsConfig.CheckRuleSeverity;
    /** Severity for non-component OpenAPI reference validation. Default is error. */
    noNonComponentRefs?: FernDocsConfig.CheckRuleSeverity;
    /** Severity for local OpenAPI reference validation. Default is warn. */
    validLocalReferences?: FernDocsConfig.CheckRuleSeverity;
    /** Severity for circular redirect validation. Default is error. */
    noCircularRedirects?: FernDocsConfig.CheckRuleSeverity;
    /** Severity for docs endpoint URL validation. Default is warn. */
    validDocsEndpoints?: FernDocsConfig.CheckRuleSeverity;
    /** Severity for detecting previously published URLs that disappeared without an explicit redirect. Default is warn. */
    missingRedirects?: FernDocsConfig.CheckRuleSeverity;
}
