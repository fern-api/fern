// This file was auto-generated by Fern from our API Definition.

import type * as FernDocsConfig from "../../../index.js";

export interface WithVariant {
    /**
     * Renders the referenced markdown file as the given content variant. Only the matching
     * `<Variant name="...">` blocks are kept and `{{variant.<key>}}` placeholders are substituted.
     * When set on a section, it applies to all pages within it that do not set their own `variant`.
     */
    variant?: FernDocsConfig.VariantId;
}
