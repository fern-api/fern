// This file was auto-generated by Fern from our API Definition.

/**
 * A single agent skill surfaced in the "Install skills" modal. `name` is the skill identifier
 * (e.g. `fern-docs`); `url` optionally links to its source (a path in the repo, a docs page, etc.).
 */
export interface SkillEntry {
    /** The skill identifier. */
    name: string;
    /** Short description of what the skill does. */
    description?: string;
    /** Optional link to the skill's source. */
    url?: string;
}
