// This file was auto-generated by Fern from our API Definition.

export interface ContentSecurityPolicyConfig {
    /**
     * CSP hash sources (`sha256-…`, `sha384-…` or `sha512-…`) for inline `<style>` elements that your
     * custom JavaScript or third-party widgets insert, e.g. `sha256-47DEQpj8HBSa+/TImW+5JCeuQeRkm5NMpJWZG3hSuFU=`.
     * Setting this key (even to an empty list) enforces a strict `style-src` without `'unsafe-inline'` on
     * every page, including pages with custom JavaScript or integrations. Inline styles whose hash is not
     * listed are blocked. Up to 16 hashes.
     */
    styleHashes?: string[];
    /**
     * Whether `script-src` includes `'unsafe-eval'`. By default it is included only on sites with custom
     * JavaScript or Google Tag Manager. Set to `false` to drop it everywhere; scripts that call `eval` or
     * `new Function` (e.g. GTM Custom HTML tags) will then fail.
     */
    allowEval?: boolean;
}
