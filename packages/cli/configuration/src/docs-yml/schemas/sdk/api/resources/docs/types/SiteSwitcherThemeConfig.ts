// This file was auto-generated by Fern from our API Definition.

export interface SiteSwitcherThemeConfig {
    /** Render the site switcher. Defaults to false. */
    enabled?: boolean;
    /** Basepaths to pin first, in this order. Remaining sites follow alphabetically. */
    order?: string[];
    /** Basepaths to omit, including any sites nested beneath them. */
    hide?: string[];
    /** Display label overrides keyed by basepath. */
    labels?: Record<string, string>;
    /** List each site's products indented beneath it. Defaults to false. */
    showProducts?: boolean;
}
