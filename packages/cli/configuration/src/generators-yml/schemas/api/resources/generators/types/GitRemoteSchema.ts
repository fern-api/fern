// This file was auto-generated by Fern from our API Definition.

export interface GitRemoteSchema {
    /** The git repository URL (e.g., https://github.com/org/repo.git). */
    repo: string;
    /** Branch, tag, or commit SHA. Defaults to the repository's default branch. */
    ref?: string;
    /** Path to the spec file or directory within the repository. */
    path: string;
}
