// This file was auto-generated by Fern from our API Definition.

import type * as GeneratorsYml from "../../../index.js";

export interface OpenApiErrorResponsesSchema {
    /**
     * The OpenAPI schema used for error response bodies: either a path to a YAML/JSON file
     * (relative to this generators.yml) or an inline schema object. It is registered under
     * `components.schemas` and every affected response references it.
     */
    schema: GeneratorsYml.OpenApiErrorResponsesSchemaSource;
    /** Name of the generated error body type. Defaults to the schema's `title`, then `ProblemDetails`. */
    name?: string;
    /**
     * `all` replaces the body of every 4xx/5xx response with the configured schema.
     * `untyped` only fills in 4xx/5xx responses that declare no body schema, leaving existing
     * typed error responses untouched.
     * Defaults to `all`.
     */
    "apply-to"?: GeneratorsYml.OpenApiErrorResponsesApplyTo;
    /**
     * Error responses to add to operations that do not already declare them
     * (e.g. a `422` on every `post`, `put` and `patch`).
     */
    ensure?: GeneratorsYml.OpenApiErrorResponsesEnsureSchema[];
}
