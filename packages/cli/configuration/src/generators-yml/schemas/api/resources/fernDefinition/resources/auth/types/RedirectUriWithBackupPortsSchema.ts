// This file was auto-generated by Fern from our API Definition.

/**
 * A loopback callback URI with additional backup ports for the authorization-code flow.
 */
export interface RedirectUriWithBackupPortsSchema {
    /** The primary loopback callback URI, e.g. `http://127.0.0.1:8484/callback`. */
    url: string;
    /** Backup loopback ports, tried in order when the port in `url` is busy. Each reuses the host and path of `url` (only the port differs) and must be pre-registered with the authorization server. */
    ports?: number[];
}
