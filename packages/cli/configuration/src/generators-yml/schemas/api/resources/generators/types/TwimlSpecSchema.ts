// This file was auto-generated by Fern from our API Definition.

/**
 * A directory of TwiML tag definitions (one JSON file per tag, grouped by
 * namespace such as `voice/`, `messaging/`, `fax/`). Generators emit an XML
 * builder library from it instead of an HTTP client.
 */
export interface TwimlSpecSchema {
    /** Path to the directory containing the TwiML definitions. */
    twiml: string;
    /** Path to a directory of `.xml` example documents rendered as code snippets. */
    examples?: string;
}
