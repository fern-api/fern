// This file was auto-generated by Fern from our API Definition.

/**
 * On a type declaration: marks the object as an XML element (e.g. a TwiML verb).
 * On an object property: controls how the property serializes within its parent element.
 */
export interface XmlEncodingSchema {
    /** The element or attribute name on the wire, casing preserved. Defaults to the type or property name. */
    name?: string;
    /** The XML namespace URI. */
    namespace?: string;
    /** The namespace prefix for the element name. */
    prefix?: string;
    /** Serialize this property as an attribute of the parent element instead of a child element. */
    attribute?: boolean;
    /** Serialize this property as the text content of the parent element. */
    text?: boolean;
    /** For list properties, wrap the items in a container element named `name`. */
    wrapped?: boolean;
    /** For list-valued attributes or text, the string used to join items (e.g. a space). */
    separator?: string;
}
