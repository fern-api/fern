// This file was auto-generated by Fern from our API Definition.

import type * as FernDefinition from "../../../index.js";

/**
 * Defines how bearer auth should be transported for WebSocket channels
 * on runtimes that do not support custom WebSocket headers.
 */
export interface WebSocketAuthFallbackSchema {
    /** Where to send the bearer token as a fallback. */
    in: FernDefinition.WebSocketAuthFallbackTransport;
    /** Required when in is 'query'. The query parameter name. */
    name?: string;
    /** Required when in is 'websocket-subprotocol'. The subprotocol string with {token} placeholder. */
    format?: string;
}
