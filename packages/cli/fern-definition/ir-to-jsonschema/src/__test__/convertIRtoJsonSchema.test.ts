// Per-workspace test files are generated in __generated__/test-definitions/ by generateTests.mjs
// Each generated file independently loads its workspace, generates IR, and tests JSON schema conversion.
// This enables full parallel execution across vitest worker threads.
//
// To regenerate test files: pnpm generate-tests
// In watch mode, globalSetup.ts handles regeneration automatically.
