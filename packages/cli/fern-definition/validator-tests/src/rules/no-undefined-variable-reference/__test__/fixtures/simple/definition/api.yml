name: simple-api

variables:
  var1: string
  var2: string
  var3: string

base-path: /{foo}/{bar}/{baz}/{biz}
path-parameters:
  foo: $var1
  bar:
    variable: $var2
  baz: $var-missing
  biz:
    variable: missing-variable
