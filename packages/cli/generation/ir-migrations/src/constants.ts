/**
 * Minimum supported IR version for Fern CLI v4.0.0+
 * This constant drives:
 * - Generator version map generation
 * - Migration validation logic
 * - Error message generation
 *
 * Update this value when removing older IR versions in the future.
 */
export const MINIMUM_SUPPORTED_IR_VERSION = 53;
