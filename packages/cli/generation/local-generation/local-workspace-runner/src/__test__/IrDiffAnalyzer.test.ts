import { describe, expect, it } from "vitest";
import { analyzeIrDiff, IrDiffResult } from "../IrDiffAnalyzer.js";
import { VersionBump } from "../VersionUtils.js";

// ---------------------------------------------------------------------------
// Helpers to build minimal IR stubs
// ---------------------------------------------------------------------------

function makeName(original: string) {
    const casing = { safeName: original, unsafeName: original };
    return {
        originalName: original,
        camelCase: casing,
        pascalCase: casing,
        snakeCase: casing,
        screamingSnakeCase: casing
    };
}

function makeNameAndWireValue(wireValue: string) {
    return { wireValue, name: makeName(wireValue) };
}

function makePath(head: string, parts: Array<{ pathParameter: string; tail: string }> = []) {
    return { head, parts };
}

function primitiveType(v1: string) {
    return {
        type: "primitive" as const,
        primitive: { v1, v2: undefined },
        _visit: (() => undefined) as never
    };
}

function namedType(typeId: string) {
    return {
        type: "named" as const,
        typeId,
        fernFilepath: { allParts: [], packagePath: [], file: undefined },
        name: makeName(typeId),
        displayName: undefined,
        default: undefined,
        inline: undefined,
        _visit: (() => undefined) as never
    };
}

function optionalType(inner: ReturnType<typeof primitiveType>) {
    return {
        type: "container" as const,
        container: {
            type: "optional" as const,
            optional: inner,
            _visit: (() => undefined) as never
        },
        _visit: (() => undefined) as never
    };
}

function makeEndpoint(
    overrides: Partial<{
        id: string;
        name: string;
        method: string;
        fullPath: ReturnType<typeof makePath>;
        requestBody: unknown;
        queryParameters: unknown[];
        response: unknown;
    }> = {}
) {
    return {
        id: overrides.id ?? "endpoint_1",
        name: makeName(overrides.name ?? "getPlant"),
        displayName: undefined,
        method: overrides.method ?? "GET",
        headers: [],
        responseHeaders: undefined,
        baseUrl: undefined,
        v2BaseUrls: undefined,
        basePath: undefined,
        path: overrides.fullPath ?? makePath("/plants"),
        fullPath: overrides.fullPath ?? makePath("/plants"),
        pathParameters: [],
        allPathParameters: [],
        queryParameters: (overrides.queryParameters ?? []) as never[],
        requestBody: (overrides.requestBody ?? undefined) as never,
        v2RequestBodies: undefined,
        sdkRequest: undefined,
        response: (overrides.response ?? undefined) as never,
        v2Responses: undefined,
        errors: [],
        auth: false,
        security: undefined,
        idempotent: false,
        pagination: undefined,
        userSpecifiedExamples: [],
        autogeneratedExamples: [],
        v2Examples: undefined,
        transport: undefined,
        source: undefined,
        audiences: undefined,
        retries: undefined,
        apiPlayground: undefined,
        availability: undefined,
        docs: undefined
    };
}

function makeService(serviceId: string, endpoints: ReturnType<typeof makeEndpoint>[]) {
    return {
        availability: undefined,
        name: {
            fernFilepath: { allParts: [], packagePath: [], file: undefined }
        },
        displayName: undefined,
        basePath: makePath(""),
        endpoints,
        headers: [],
        pathParameters: [],
        encoding: undefined,
        transport: undefined,
        audiences: undefined
    };
}

function makeBaseIr(
    overrides: Partial<{
        services: Record<string, ReturnType<typeof makeService>>;
        types: Record<string, unknown>;
        errors: Record<string, unknown>;
        auth: { schemes: Array<{ type: string }> };
    }> = {}
) {
    return {
        fdrApiDefinitionId: undefined,
        apiVersion: undefined,
        apiName: makeName("test-api"),
        apiDisplayName: undefined,
        apiDocs: undefined,
        auth: overrides.auth ?? {
            requirement: "ALL" as const,
            schemes: [
                {
                    type: "bearer" as const,
                    token: makeNameAndWireValue("token"),
                    tokenEnvVar: undefined,
                    docs: undefined,
                    _visit: (() => undefined) as never
                }
            ],
            docs: undefined
        },
        headers: [],
        idempotencyHeaders: [],
        types: (overrides.types ?? {}) as never,
        services: (overrides.services ?? {}) as never,
        webhookGroups: {},
        websocketChannels: undefined,
        errors: (overrides.errors ?? {}) as never,
        subpackages: {},
        rootPackage: {
            fernFilepath: { allParts: [], packagePath: [], file: undefined },
            service: undefined,
            types: [],
            errors: [],
            webhooks: undefined,
            websocket: undefined,
            subpackages: [],
            hasEndpointsInTree: false,
            navigationConfig: undefined,
            docs: undefined
        },
        constants: {
            errorInstanceIdKey: makeNameAndWireValue("errorInstanceId")
        },
        environments: undefined,
        basePath: undefined,
        pathParameters: [],
        errorDiscriminationStrategy: {
            type: "statusCode" as const,
            _visit: (() => undefined) as never
        },
        sdkConfig: {
            isAuthMandatory: false,
            hasStreamingEndpoints: false,
            hasFileDownloadEndpoints: false,
            platformHeaders: {
                language: "X-Fern-Language",
                sdkName: "X-Fern-SDK-Name",
                sdkVersion: "X-Fern-SDK-Version",
                userAgent: undefined
            }
        },
        variables: [],
        serviceTypeReferenceInfo: {
            typesReferencedOnlyByService: {},
            sharedTypes: []
        },
        readmeConfig: undefined,
        sourceConfig: undefined,
        publishConfig: undefined,
        dynamic: undefined,
        selfHosted: undefined,
        audiences: undefined,
        generationMetadata: undefined,
        apiPlayground: undefined
    };
}

// ---------------------------------------------------------------------------
// Tests
// ---------------------------------------------------------------------------

describe("IrDiffAnalyzer", () => {
    // --- Endpoint removal → MAJOR ---
    it("classifies removed endpoint as MAJOR", () => {
        const previousIr = makeBaseIr({
            services: {
                svc1: makeService("svc1", [
                    makeEndpoint({ id: "ep1", name: "getPlant", method: "GET", fullPath: makePath("/plants") }),
                    makeEndpoint({
                        id: "ep2",
                        name: "deletePlant",
                        method: "DELETE",
                        fullPath: makePath("/plants/", [{ pathParameter: "plantId", tail: "" }])
                    })
                ])
            }
        });

        const currentIr = makeBaseIr({
            services: {
                svc1: makeService("svc1", [
                    makeEndpoint({ id: "ep1", name: "getPlant", method: "GET", fullPath: makePath("/plants") })
                ])
            }
        });

        const result = analyzeIrDiff(previousIr as never, currentIr as never, "typescript");
        expect(result.bump).toBe(VersionBump.MAJOR);
        expect(result.reasons.some((r) => r.rule === "endpoint_removed")).toBe(true);
    });

    // --- Endpoint addition → MINOR ---
    it("classifies added endpoint as MINOR", () => {
        const previousIr = makeBaseIr({
            services: {
                svc1: makeService("svc1", [
                    makeEndpoint({ id: "ep1", name: "getPlant", method: "GET", fullPath: makePath("/plants") })
                ])
            }
        });

        const currentIr = makeBaseIr({
            services: {
                svc1: makeService("svc1", [
                    makeEndpoint({ id: "ep1", name: "getPlant", method: "GET", fullPath: makePath("/plants") }),
                    makeEndpoint({ id: "ep2", name: "createPlant", method: "POST", fullPath: makePath("/plants") })
                ])
            }
        });

        const result = analyzeIrDiff(previousIr as never, currentIr as never, "typescript");
        expect(result.bump).toBe(VersionBump.MINOR);
        expect(result.reasons.some((r) => r.rule === "endpoint_added")).toBe(true);
    });

    // --- Required request field added → MAJOR ---
    it("classifies new required request field as MAJOR", () => {
        const previousIr = makeBaseIr({
            services: {
                svc1: makeService("svc1", [
                    makeEndpoint({
                        id: "ep1",
                        name: "createPlant",
                        method: "POST",
                        fullPath: makePath("/plants"),
                        requestBody: {
                            type: "inlinedRequestBody" as const,
                            name: makeName("CreatePlantRequest"),
                            extends: [],
                            properties: [
                                {
                                    name: makeNameAndWireValue("name"),
                                    valueType: primitiveType("STRING"),
                                    docs: undefined,
                                    availability: undefined,
                                    v2Examples: undefined,
                                    propertyAccess: undefined
                                }
                            ],
                            extendedProperties: undefined,
                            extraProperties: false,
                            docs: undefined,
                            v2Examples: undefined,
                            contentType: undefined,
                            _visit: (() => undefined) as never
                        }
                    })
                ])
            }
        });

        const currentIr = makeBaseIr({
            services: {
                svc1: makeService("svc1", [
                    makeEndpoint({
                        id: "ep1",
                        name: "createPlant",
                        method: "POST",
                        fullPath: makePath("/plants"),
                        requestBody: {
                            type: "inlinedRequestBody" as const,
                            name: makeName("CreatePlantRequest"),
                            extends: [],
                            properties: [
                                {
                                    name: makeNameAndWireValue("name"),
                                    valueType: primitiveType("STRING"),
                                    docs: undefined,
                                    availability: undefined,
                                    v2Examples: undefined,
                                    propertyAccess: undefined
                                },
                                {
                                    name: makeNameAndWireValue("species"),
                                    valueType: primitiveType("STRING"),
                                    docs: undefined,
                                    availability: undefined,
                                    v2Examples: undefined,
                                    propertyAccess: undefined
                                }
                            ],
                            extendedProperties: undefined,
                            extraProperties: false,
                            docs: undefined,
                            v2Examples: undefined,
                            contentType: undefined,
                            _visit: (() => undefined) as never
                        }
                    })
                ])
            }
        });

        const result = analyzeIrDiff(previousIr as never, currentIr as never, "typescript");
        expect(result.bump).toBe(VersionBump.MAJOR);
        expect(result.reasons.some((r) => r.rule === "required_request_field_added")).toBe(true);
    });

    // --- Optional request field added → MINOR ---
    it("classifies new optional request field as MINOR", () => {
        const previousIr = makeBaseIr({
            services: {
                svc1: makeService("svc1", [
                    makeEndpoint({
                        id: "ep1",
                        name: "createPlant",
                        method: "POST",
                        fullPath: makePath("/plants"),
                        requestBody: {
                            type: "inlinedRequestBody" as const,
                            name: makeName("CreatePlantRequest"),
                            extends: [],
                            properties: [
                                {
                                    name: makeNameAndWireValue("name"),
                                    valueType: primitiveType("STRING"),
                                    docs: undefined,
                                    availability: undefined,
                                    v2Examples: undefined,
                                    propertyAccess: undefined
                                }
                            ],
                            extendedProperties: undefined,
                            extraProperties: false,
                            docs: undefined,
                            v2Examples: undefined,
                            contentType: undefined,
                            _visit: (() => undefined) as never
                        }
                    })
                ])
            }
        });

        const currentIr = makeBaseIr({
            services: {
                svc1: makeService("svc1", [
                    makeEndpoint({
                        id: "ep1",
                        name: "createPlant",
                        method: "POST",
                        fullPath: makePath("/plants"),
                        requestBody: {
                            type: "inlinedRequestBody" as const,
                            name: makeName("CreatePlantRequest"),
                            extends: [],
                            properties: [
                                {
                                    name: makeNameAndWireValue("name"),
                                    valueType: primitiveType("STRING"),
                                    docs: undefined,
                                    availability: undefined,
                                    v2Examples: undefined,
                                    propertyAccess: undefined
                                },
                                {
                                    name: makeNameAndWireValue("description"),
                                    valueType: optionalType(primitiveType("STRING")),
                                    docs: undefined,
                                    availability: undefined,
                                    v2Examples: undefined,
                                    propertyAccess: undefined
                                }
                            ],
                            extendedProperties: undefined,
                            extraProperties: false,
                            docs: undefined,
                            v2Examples: undefined,
                            contentType: undefined,
                            _visit: (() => undefined) as never
                        }
                    })
                ])
            }
        });

        const result = analyzeIrDiff(previousIr as never, currentIr as never, "typescript");
        expect(result.bump).toBe(VersionBump.MINOR);
        expect(result.reasons.some((r) => r.rule === "optional_request_field_added")).toBe(true);
    });

    // --- Response field required→optional: MAJOR for typed languages ---
    it("classifies response field becoming optional as MAJOR for TypeScript", () => {
        const result = testResponseFieldRequiredToOptional("typescript");
        expect(result.bump).toBe(VersionBump.MAJOR);
        expect(result.reasons.some((r) => r.rule === "property_required_to_optional")).toBe(true);
    });

    it("classifies response field becoming optional as MAJOR for Java", () => {
        const result = testResponseFieldRequiredToOptional("java");
        expect(result.bump).toBe(VersionBump.MAJOR);
        expect(result.reasons.some((r) => r.rule === "property_required_to_optional")).toBe(true);
    });

    it("classifies response field becoming optional as MAJOR for Go", () => {
        const result = testResponseFieldRequiredToOptional("go");
        expect(result.bump).toBe(VersionBump.MAJOR);
        expect(result.reasons.some((r) => r.rule === "property_required_to_optional")).toBe(true);
    });

    it("classifies response field becoming optional as MINOR for Ruby", () => {
        const result = testResponseFieldRequiredToOptional("ruby");
        expect(result.bump).toBe(VersionBump.MINOR);
        expect(result.reasons.some((r) => r.rule === "property_required_to_optional")).toBe(true);
    });

    // --- New enum value: MAJOR for exhaustive languages ---
    it("classifies new enum value as MAJOR for TypeScript", () => {
        const result = testNewEnumValue("typescript");
        expect(result.bump).toBe(VersionBump.MAJOR);
        expect(result.reasons.some((r) => r.rule === "enum_value_added")).toBe(true);
    });

    it("classifies new enum value as MINOR for Python", () => {
        const result = testNewEnumValue("python");
        expect(result.bump).toBe(VersionBump.MINOR);
        expect(result.reasons.some((r) => r.rule === "enum_value_added")).toBe(true);
    });

    // --- No changes → PATCH ---
    it("returns PATCH when IR is identical", () => {
        const ir = makeBaseIr({
            services: {
                svc1: makeService("svc1", [
                    makeEndpoint({ id: "ep1", name: "getPlant", method: "GET", fullPath: makePath("/plants") })
                ])
            }
        });

        const result = analyzeIrDiff(ir as never, ir as never, "typescript");
        expect(result.bump).toBe(VersionBump.PATCH);
        expect(result.reasons).toHaveLength(0);
    });

    // --- New repo (no previous IR) → returns undefined/skips ---
    it("returns undefined when no previous IR snapshot exists", () => {
        // This is tested at the LocalTaskHandler level where previousIr is undefined.
        // The analyzeIrDiff function is only called when previousIr is defined.
        // We verify here that with identical IRs there are no reasons.
        const ir = makeBaseIr();
        const result = analyzeIrDiff(ir as never, ir as never, "typescript");
        expect(result.reasons).toHaveLength(0);
        expect(result.bump).toBe(VersionBump.PATCH);
    });

    // --- Aggregation: multiple changes → takes highest bump ---
    it("returns MAJOR when mix of MINOR and MAJOR changes present", () => {
        const previousIr = makeBaseIr({
            services: {
                svc1: makeService("svc1", [
                    makeEndpoint({ id: "ep1", name: "getPlant", method: "GET", fullPath: makePath("/plants") }),
                    makeEndpoint({
                        id: "ep2",
                        name: "deletePlant",
                        method: "DELETE",
                        fullPath: makePath("/plants/", [{ pathParameter: "plantId", tail: "" }])
                    })
                ])
            }
        });

        const currentIr = makeBaseIr({
            services: {
                svc1: makeService("svc1", [
                    makeEndpoint({ id: "ep1", name: "getPlant", method: "GET", fullPath: makePath("/plants") }),
                    // ep2 removed (MAJOR)
                    // ep3 added (MINOR)
                    makeEndpoint({
                        id: "ep3",
                        name: "waterPlant",
                        method: "POST",
                        fullPath: makePath("/plants/", [{ pathParameter: "plantId", tail: "/water" }])
                    })
                ])
            }
        });

        const result = analyzeIrDiff(previousIr as never, currentIr as never, "typescript");
        expect(result.bump).toBe(VersionBump.MAJOR);
        // Should have both MAJOR and MINOR reasons
        expect(result.reasons.some((r) => r.bump === VersionBump.MAJOR)).toBe(true);
        expect(result.reasons.some((r) => r.bump === VersionBump.MINOR)).toBe(true);
    });

    // --- Reasons array populated correctly ---
    it("populates reasons with rule name and human-readable description", () => {
        const previousIr = makeBaseIr({
            services: {
                svc1: makeService("svc1", [
                    makeEndpoint({ id: "ep1", name: "getPlant", method: "GET", fullPath: makePath("/plants") }),
                    makeEndpoint({
                        id: "ep2",
                        name: "deletePlant",
                        method: "DELETE",
                        fullPath: makePath("/plants/", [{ pathParameter: "plantId", tail: "" }])
                    })
                ])
            }
        });

        const currentIr = makeBaseIr({
            services: {
                svc1: makeService("svc1", [
                    makeEndpoint({ id: "ep1", name: "getPlant", method: "GET", fullPath: makePath("/plants") })
                ])
            }
        });

        const result = analyzeIrDiff(previousIr as never, currentIr as never, "typescript");

        expect(result.reasons.length).toBeGreaterThan(0);
        for (const reason of result.reasons) {
            expect(reason.rule).toBeTruthy();
            expect(reason.description).toBeTruthy();
            expect(typeof reason.rule).toBe("string");
            expect(typeof reason.description).toBe("string");
            expect(Object.values(VersionBump)).toContain(reason.bump);
        }

        // Check specific reason for removed endpoint
        const removedReason = result.reasons.find((r) => r.rule === "endpoint_removed");
        expect(removedReason).toBeDefined();
        expect(removedReason?.description).toContain("deletePlant");
    });

    // --- Endpoint path changed → MAJOR ---
    it("classifies endpoint path change as MAJOR", () => {
        const previousIr = makeBaseIr({
            services: {
                svc1: makeService("svc1", [
                    makeEndpoint({ id: "ep1", name: "getPlant", method: "GET", fullPath: makePath("/plants") })
                ])
            }
        });

        const currentIr = makeBaseIr({
            services: {
                svc1: makeService("svc1", [
                    makeEndpoint({ id: "ep1", name: "getPlant", method: "GET", fullPath: makePath("/vegetation") })
                ])
            }
        });

        const result = analyzeIrDiff(previousIr as never, currentIr as never, "typescript");
        expect(result.bump).toBe(VersionBump.MAJOR);
        expect(result.reasons.some((r) => r.rule === "endpoint_path_changed")).toBe(true);
    });

    // --- Endpoint method changed → MAJOR ---
    it("classifies endpoint method change as MAJOR", () => {
        const previousIr = makeBaseIr({
            services: {
                svc1: makeService("svc1", [
                    makeEndpoint({ id: "ep1", name: "updatePlant", method: "PUT", fullPath: makePath("/plants") })
                ])
            }
        });

        const currentIr = makeBaseIr({
            services: {
                svc1: makeService("svc1", [
                    makeEndpoint({ id: "ep1", name: "updatePlant", method: "PATCH", fullPath: makePath("/plants") })
                ])
            }
        });

        const result = analyzeIrDiff(previousIr as never, currentIr as never, "typescript");
        expect(result.bump).toBe(VersionBump.MAJOR);
        expect(result.reasons.some((r) => r.rule === "endpoint_method_changed")).toBe(true);
    });

    // --- Auth scheme changed → MAJOR ---
    it("classifies auth scheme change as MAJOR", () => {
        const previousIr = makeBaseIr({
            auth: {
                requirement: "ALL" as never,
                schemes: [
                    {
                        type: "bearer" as const,
                        token: makeNameAndWireValue("token"),
                        tokenEnvVar: undefined,
                        docs: undefined,
                        _visit: (() => undefined) as never
                    }
                ],
                docs: undefined
            } as never
        });

        const currentIr = makeBaseIr({
            auth: {
                requirement: "ALL" as never,
                schemes: [
                    {
                        type: "basic" as const,
                        username: makeNameAndWireValue("username"),
                        usernameEnvVar: undefined,
                        password: makeNameAndWireValue("password"),
                        passwordEnvVar: undefined,
                        docs: undefined,
                        _visit: (() => undefined) as never
                    }
                ],
                docs: undefined
            } as never
        });

        const result = analyzeIrDiff(previousIr as never, currentIr as never, "typescript");
        expect(result.bump).toBe(VersionBump.MAJOR);
        expect(result.reasons.some((r) => r.rule === "auth_scheme_changed")).toBe(true);
    });

    // --- Error removed → MAJOR ---
    it("classifies error removal as MAJOR", () => {
        const previousIr = makeBaseIr({
            errors: {
                error_1: {
                    name: {
                        errorId: "error_1",
                        fernFilepath: { allParts: [], packagePath: [], file: undefined },
                        name: makeName("PlantNotFoundError")
                    },
                    displayName: undefined,
                    discriminantValue: makeNameAndWireValue("PlantNotFoundError"),
                    type: undefined,
                    statusCode: 404,
                    isWildcardStatusCode: undefined,
                    examples: [],
                    v2Examples: undefined,
                    headers: undefined,
                    docs: undefined
                }
            }
        });

        const currentIr = makeBaseIr({
            errors: {}
        });

        const result = analyzeIrDiff(previousIr as never, currentIr as never, "typescript");
        expect(result.bump).toBe(VersionBump.MAJOR);
        expect(result.reasons.some((r) => r.rule === "error_removed")).toBe(true);
    });

    // --- Error renamed → MAJOR ---
    it("classifies error rename as MAJOR", () => {
        const previousIr = makeBaseIr({
            errors: {
                error_1: {
                    name: {
                        errorId: "error_1",
                        fernFilepath: { allParts: [], packagePath: [], file: undefined },
                        name: makeName("PlantNotFoundError")
                    },
                    displayName: undefined,
                    discriminantValue: makeNameAndWireValue("PlantNotFoundError"),
                    type: undefined,
                    statusCode: 404,
                    isWildcardStatusCode: undefined,
                    examples: [],
                    v2Examples: undefined,
                    headers: undefined,
                    docs: undefined
                }
            }
        });

        const currentIr = makeBaseIr({
            errors: {
                error_1: {
                    name: {
                        errorId: "error_1",
                        fernFilepath: { allParts: [], packagePath: [], file: undefined },
                        name: makeName("VegetationNotFoundError")
                    },
                    displayName: undefined,
                    discriminantValue: makeNameAndWireValue("VegetationNotFoundError"),
                    type: undefined,
                    statusCode: 404,
                    isWildcardStatusCode: undefined,
                    examples: [],
                    v2Examples: undefined,
                    headers: undefined,
                    docs: undefined
                }
            }
        });

        const result = analyzeIrDiff(previousIr as never, currentIr as never, "typescript");
        expect(result.bump).toBe(VersionBump.MAJOR);
        expect(result.reasons.some((r) => r.rule === "error_renamed")).toBe(true);
    });

    // --- Required request field made optional → MINOR ---
    it("classifies required request field made optional as MINOR", () => {
        const previousIr = makeBaseIr({
            services: {
                svc1: makeService("svc1", [
                    makeEndpoint({
                        id: "ep1",
                        name: "createPlant",
                        method: "POST",
                        fullPath: makePath("/plants"),
                        requestBody: {
                            type: "inlinedRequestBody" as const,
                            name: makeName("CreatePlantRequest"),
                            extends: [],
                            properties: [
                                {
                                    name: makeNameAndWireValue("name"),
                                    valueType: primitiveType("STRING"),
                                    docs: undefined,
                                    availability: undefined,
                                    v2Examples: undefined,
                                    propertyAccess: undefined
                                }
                            ],
                            extendedProperties: undefined,
                            extraProperties: false,
                            docs: undefined,
                            v2Examples: undefined,
                            contentType: undefined,
                            _visit: (() => undefined) as never
                        }
                    })
                ])
            }
        });

        const currentIr = makeBaseIr({
            services: {
                svc1: makeService("svc1", [
                    makeEndpoint({
                        id: "ep1",
                        name: "createPlant",
                        method: "POST",
                        fullPath: makePath("/plants"),
                        requestBody: {
                            type: "inlinedRequestBody" as const,
                            name: makeName("CreatePlantRequest"),
                            extends: [],
                            properties: [
                                {
                                    name: makeNameAndWireValue("name"),
                                    valueType: optionalType(primitiveType("STRING")),
                                    docs: undefined,
                                    availability: undefined,
                                    v2Examples: undefined,
                                    propertyAccess: undefined
                                }
                            ],
                            extendedProperties: undefined,
                            extraProperties: false,
                            docs: undefined,
                            v2Examples: undefined,
                            contentType: undefined,
                            _visit: (() => undefined) as never
                        }
                    })
                ])
            }
        });

        const result = analyzeIrDiff(previousIr as never, currentIr as never, "typescript");
        expect(result.bump).toBe(VersionBump.MINOR);
        expect(result.reasons.some((r) => r.rule === "required_request_field_made_optional")).toBe(true);
    });

    // --- maxPossibleBump matches bump ---
    it("maxPossibleBump equals the computed bump", () => {
        const previousIr = makeBaseIr({
            services: {
                svc1: makeService("svc1", [
                    makeEndpoint({ id: "ep1", name: "getPlant", method: "GET", fullPath: makePath("/plants") })
                ])
            }
        });

        const currentIr = makeBaseIr({
            services: {
                svc1: makeService("svc1", [
                    makeEndpoint({ id: "ep1", name: "getPlant", method: "GET", fullPath: makePath("/plants") }),
                    makeEndpoint({ id: "ep2", name: "createPlant", method: "POST", fullPath: makePath("/plants") })
                ])
            }
        });

        const result = analyzeIrDiff(previousIr as never, currentIr as never, "typescript");
        expect(result.maxPossibleBump).toBe(result.bump);
        expect(result.maxPossibleBump).toBe(VersionBump.MINOR);
    });

    // --- Response type changed → MAJOR ---
    it("classifies response type change as MAJOR", () => {
        const previousIr = makeBaseIr({
            services: {
                svc1: makeService("svc1", [
                    makeEndpoint({
                        id: "ep1",
                        name: "getPlant",
                        method: "GET",
                        fullPath: makePath("/plants"),
                        response: {
                            statusCode: 200,
                            isWildcardStatusCode: undefined,
                            body: {
                                type: "json" as const,
                                value: {
                                    type: "response" as const,
                                    responseBodyType: primitiveType("STRING"),
                                    docs: undefined,
                                    v2Examples: undefined
                                },
                                _visit: (() => undefined) as never
                            },
                            docs: undefined
                        }
                    })
                ])
            }
        });

        const currentIr = makeBaseIr({
            services: {
                svc1: makeService("svc1", [
                    makeEndpoint({
                        id: "ep1",
                        name: "getPlant",
                        method: "GET",
                        fullPath: makePath("/plants"),
                        response: {
                            statusCode: 200,
                            isWildcardStatusCode: undefined,
                            body: {
                                type: "json" as const,
                                value: {
                                    type: "response" as const,
                                    responseBodyType: primitiveType("INTEGER"),
                                    docs: undefined,
                                    v2Examples: undefined
                                },
                                _visit: (() => undefined) as never
                            },
                            docs: undefined
                        }
                    })
                ])
            }
        });

        const result = analyzeIrDiff(previousIr as never, currentIr as never, "typescript");
        expect(result.bump).toBe(VersionBump.MAJOR);
        expect(result.reasons.some((r) => r.rule === "response_type_changed")).toBe(true);
    });

    // --- Enum value removed → MAJOR ---
    it("classifies enum value removal as MAJOR", () => {
        const previousIr = makeBaseIr({
            types: {
                type_1: {
                    name: {
                        typeId: "type_1",
                        fernFilepath: { allParts: [], packagePath: [], file: undefined },
                        name: makeName("PlantType"),
                        displayName: undefined
                    },
                    shape: {
                        type: "enum" as const,
                        default: undefined,
                        values: [
                            { name: makeNameAndWireValue("SUCCULENT"), docs: undefined, availability: undefined },
                            { name: makeNameAndWireValue("FERN"), docs: undefined, availability: undefined },
                            { name: makeNameAndWireValue("CACTUS"), docs: undefined, availability: undefined }
                        ],
                        _visit: (() => undefined) as never
                    },
                    autogeneratedExamples: [],
                    userProvidedExamples: [],
                    v2Examples: undefined,
                    referencedTypes: new Set(),
                    encoding: undefined,
                    source: undefined,
                    inline: undefined,
                    availability: undefined,
                    docs: undefined
                }
            }
        });

        const currentIr = makeBaseIr({
            types: {
                type_1: {
                    name: {
                        typeId: "type_1",
                        fernFilepath: { allParts: [], packagePath: [], file: undefined },
                        name: makeName("PlantType"),
                        displayName: undefined
                    },
                    shape: {
                        type: "enum" as const,
                        default: undefined,
                        values: [
                            { name: makeNameAndWireValue("SUCCULENT"), docs: undefined, availability: undefined },
                            { name: makeNameAndWireValue("FERN"), docs: undefined, availability: undefined }
                        ],
                        _visit: (() => undefined) as never
                    },
                    autogeneratedExamples: [],
                    userProvidedExamples: [],
                    v2Examples: undefined,
                    referencedTypes: new Set(),
                    encoding: undefined,
                    source: undefined,
                    inline: undefined,
                    availability: undefined,
                    docs: undefined
                }
            }
        });

        const result = analyzeIrDiff(previousIr as never, currentIr as never, "typescript");
        expect(result.bump).toBe(VersionBump.MAJOR);
        expect(result.reasons.some((r) => r.rule === "enum_value_removed")).toBe(true);
    });
});

// ---------------------------------------------------------------------------
// Shared test helpers
// ---------------------------------------------------------------------------

function testResponseFieldRequiredToOptional(language: string): IrDiffResult {
    const previousIr = makeBaseIr({
        types: {
            type_1: {
                name: {
                    typeId: "type_1",
                    fernFilepath: { allParts: [], packagePath: [], file: undefined },
                    name: makeName("Plant"),
                    displayName: undefined
                },
                shape: {
                    type: "object" as const,
                    extends: [],
                    properties: [
                        {
                            name: makeNameAndWireValue("name"),
                            valueType: primitiveType("STRING"),
                            docs: undefined,
                            availability: undefined,
                            propertyAccess: undefined,
                            v2Examples: undefined
                        }
                    ],
                    extendedProperties: undefined,
                    extraProperties: false,
                    _visit: (() => undefined) as never
                },
                autogeneratedExamples: [],
                userProvidedExamples: [],
                v2Examples: undefined,
                referencedTypes: new Set(),
                encoding: undefined,
                source: undefined,
                inline: undefined,
                availability: undefined,
                docs: undefined
            }
        }
    });

    const currentIr = makeBaseIr({
        types: {
            type_1: {
                name: {
                    typeId: "type_1",
                    fernFilepath: { allParts: [], packagePath: [], file: undefined },
                    name: makeName("Plant"),
                    displayName: undefined
                },
                shape: {
                    type: "object" as const,
                    extends: [],
                    properties: [
                        {
                            name: makeNameAndWireValue("name"),
                            valueType: optionalType(primitiveType("STRING")),
                            docs: undefined,
                            availability: undefined,
                            propertyAccess: undefined,
                            v2Examples: undefined
                        }
                    ],
                    extendedProperties: undefined,
                    extraProperties: false,
                    _visit: (() => undefined) as never
                },
                autogeneratedExamples: [],
                userProvidedExamples: [],
                v2Examples: undefined,
                referencedTypes: new Set(),
                encoding: undefined,
                source: undefined,
                inline: undefined,
                availability: undefined,
                docs: undefined
            }
        }
    });

    return analyzeIrDiff(previousIr as never, currentIr as never, language);
}

function testNewEnumValue(language: string): IrDiffResult {
    const previousIr = makeBaseIr({
        types: {
            type_1: {
                name: {
                    typeId: "type_1",
                    fernFilepath: { allParts: [], packagePath: [], file: undefined },
                    name: makeName("PlantType"),
                    displayName: undefined
                },
                shape: {
                    type: "enum" as const,
                    default: undefined,
                    values: [
                        { name: makeNameAndWireValue("SUCCULENT"), docs: undefined, availability: undefined },
                        { name: makeNameAndWireValue("FERN"), docs: undefined, availability: undefined }
                    ],
                    _visit: (() => undefined) as never
                },
                autogeneratedExamples: [],
                userProvidedExamples: [],
                v2Examples: undefined,
                referencedTypes: new Set(),
                encoding: undefined,
                source: undefined,
                inline: undefined,
                availability: undefined,
                docs: undefined
            }
        }
    });

    const currentIr = makeBaseIr({
        types: {
            type_1: {
                name: {
                    typeId: "type_1",
                    fernFilepath: { allParts: [], packagePath: [], file: undefined },
                    name: makeName("PlantType"),
                    displayName: undefined
                },
                shape: {
                    type: "enum" as const,
                    default: undefined,
                    values: [
                        { name: makeNameAndWireValue("SUCCULENT"), docs: undefined, availability: undefined },
                        { name: makeNameAndWireValue("FERN"), docs: undefined, availability: undefined },
                        { name: makeNameAndWireValue("CACTUS"), docs: undefined, availability: undefined }
                    ],
                    _visit: (() => undefined) as never
                },
                autogeneratedExamples: [],
                userProvidedExamples: [],
                v2Examples: undefined,
                referencedTypes: new Set(),
                encoding: undefined,
                source: undefined,
                inline: undefined,
                availability: undefined,
                docs: undefined
            }
        }
    });

    return analyzeIrDiff(previousIr as never, currentIr as never, language);
}
