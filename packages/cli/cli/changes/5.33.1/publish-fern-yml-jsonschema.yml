- summary: |
    Publish a JSON Schema for `fern.yml` so YAML language servers can provide
    autocomplete and validation in IDEs. Regenerate via `fern schema --output`
    (or `pnpm fern-yml:jsonschema` in the monorepo).
  type: chore
