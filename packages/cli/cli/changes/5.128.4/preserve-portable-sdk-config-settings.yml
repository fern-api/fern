# yaml-language-server: $schema=../../../../../fern-changes-yml.schema.json

- summary: |
    Preserve portable API import settings, package metadata, README sections, response validation, and language-specific settings when migrating generators.yml to SDK Config.
  type: fix
