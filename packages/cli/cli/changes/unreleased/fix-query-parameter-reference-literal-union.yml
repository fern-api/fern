# yaml-language-server: $schema=../../../../../fern-changes-yml.schema.json

- summary: |
    Preserve constrained string, referenced, and non-string members in query parameter unions that also contain string literals.
    Previously, these members could be discarded when the union collapsed to an enum.
  type: fix
