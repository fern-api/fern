- summary: |
    Validate that a docs instance's `url` and `custom-domain` share the same basepath for all
    sites, not just basepath-aware ones. The CLI now fails with a clear error when they differ.
  type: fix
