# yaml-language-server: $schema=../../../../../fern-changes-yml.schema.json

- summary: |
    dynamic IR now sets `wrapperProperty` on auth schemes when the API has multiple auth schemes so snippet generators can nest constructor options the way generated SDKs expect.
  type: fix
