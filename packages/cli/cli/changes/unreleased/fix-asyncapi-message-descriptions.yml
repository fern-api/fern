- summary: |
    Propagate AsyncAPI message `description`/`summary` to WebSocket channel message docs so they appear in the API reference and generated SDK docstrings. Previously only the payload schema's description survived import.
  type: fix
