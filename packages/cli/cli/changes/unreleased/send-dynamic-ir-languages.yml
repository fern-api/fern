- summary: |
    Send `dynamicIRLanguages` on `registerApiDefinition` instead of a map of dynamic IR objects. The
    registry only needs the SDK language names to issue upload URLs; the IRs themselves are still
    uploaded to S3 separately.
  type: fix
