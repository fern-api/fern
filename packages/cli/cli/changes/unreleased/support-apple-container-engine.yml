# yaml-language-server: $schema=../../../../../fern-changes-yml.schema.json

- summary: |
    Add Apple's `container` as a supported container engine for local generation.
    Pass `--container-engine container` (or `--runner container` on the legacy CLI)
    to run generators with Apple's native container runtime instead of Docker or Podman.
  type: feat
