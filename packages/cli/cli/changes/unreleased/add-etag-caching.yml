- summary: |
    Cache serialized JSON response for the /load-with-url endpoint in
    fern docs dev. Eliminates redundant 32MB JSON.stringify on every
    request when the docs definition hasn't changed.
  type: chore
