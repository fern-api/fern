- summary: |
    `fern check` now reports unresolved OpenAPI `$ref`s (local pointers that do not exist in the
    specification, and refs to files that are missing on disk) instead of silently passing.
    Overlay `update` actions whose target matches no node now log a warning rather than being
    silently dropped.
  type: fix
