# yaml-language-server: $schema=../../../../../fern-changes-yml.schema.json

- summary: |
    Parse OpenAPI 3.1 raw binary response media types as file downloads and preserve raw binary component schemas without requiring legacy `format: binary` hints.
  type: fix
