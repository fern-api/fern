- summary: |
    Migrate CLI v2 cache directory from ~/.cache/fern/ to ~/.fern/ so all
    Fern-related files (bin, ir, logs, migrations) live under a single
    directory shared with CLI v1.
  type: chore
