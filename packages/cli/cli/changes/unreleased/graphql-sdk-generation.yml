# yaml-language-server: $schema=../../../../../fern-changes-yml.schema.json

- summary: |
    Add GraphQL SDK generation. GraphQL schemas now convert directly to IR (each root field
    becomes an HTTP operation with the new `Transport.graphql`) and merge into the standard
    `fern generate` pipeline, so GraphQL APIs generate typed SDKs alongside OpenAPI/gRPC.
    Autogenerated examples are produced for GraphQL operations, populating the SDK README,
    reference.md, and code snippets.
  type: feat
