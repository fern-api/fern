- summary: |
    Fix `fern docs dev` silently dying when the preview server process crashes (e.g. due to memory pressure on large doc sites). The server now auto-restarts with exponential backoff (up to 5 attempts) and surfaces the crash at error-level so users see what happened.
  type: fix
