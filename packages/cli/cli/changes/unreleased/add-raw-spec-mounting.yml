- summary: |
    Mount pre-processed raw API specs into generator Docker containers.
    Specs are bundled, overrides merged, overlays applied, and output as
    compact JSON before passing to generators.
  type: feat
