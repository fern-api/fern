- summary: |
    Improve error messages when ledger docs publishing fails: surface the
    server-side validation issues (field paths and messages) and suggest
    `FERN_DOCS_DEPLOY_MODE=legacy` as a workaround.
  type: fix
