- summary: |
    Hosted MCP deploys now fail fast, before uploading anything, when a generated tool name is longer than 64 characters or contains disallowed characters, and point at the operation (method + path) that needs a shorter `operationId`. Deploy errors from the registry now include each validation issue instead of only the headline message.
  type: fix
