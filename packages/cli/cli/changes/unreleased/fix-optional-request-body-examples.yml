- summary: |
    Fix `fern check` failing with "Expected example to be an object. Example is: undefined" when an
    endpoint example omits `request` for an endpoint that declares a request body. Omitting `request`
    now produces an example with no request body instead of a validation error, and such examples are
    no longer dropped from the generated IR.
  type: fix
