// export async function generateFernDocsForWorkspaces(opts: {
//     project: Project;
//     outputFilepath: AbsoluteFilePath;
// }): Promise<void> {
//     const docsWorkspace = await DocsWorkspace.fromPath(outputFilepath);
// }
