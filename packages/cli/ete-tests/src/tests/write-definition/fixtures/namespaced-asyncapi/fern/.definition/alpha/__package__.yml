types:
  Error:
    properties:
      code: integer
      message: string
    source:
      openapi: alpha_one.yml
  Pet:
    properties:
      id: long
      name: string
      tag: optional<string>
    source:
      openapi: alpha_one.yml
  Pets: list<Pet>
