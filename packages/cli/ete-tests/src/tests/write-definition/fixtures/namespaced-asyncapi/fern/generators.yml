# yaml-language-server: $schema=https://schema.buildwithfern.dev/generators-yml.json
api:
  namespaces:
    alpha:
      - path: alpha_one.yml
      - path: alpha_two.yml
    beta:
      - path: beta_one.yml
