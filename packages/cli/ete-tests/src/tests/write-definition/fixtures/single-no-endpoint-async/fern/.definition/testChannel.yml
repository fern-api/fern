channel:
  auth: false
  examples:
    - messages:
        - body: string
          type: publish
  messages:
    testChannel_sendMessage:
      body: string
      origin: client
  path: /test
  url: websocket
