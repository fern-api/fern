channel:
  auth: false
  examples:
    - messages:
        - body: string
          type: testChannel_sendMessage
  messages:
    testChannel_sendMessage:
      body: string
      docs: Message with marked data information.
      origin: client
  path: /test
  url: websocket
