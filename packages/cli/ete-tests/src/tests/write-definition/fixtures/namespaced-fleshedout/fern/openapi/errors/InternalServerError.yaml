description: |
  This error is returned when an uncategorised internal server error occurs.
content:
  application/json:
    schema:
      type: object
      properties:
        data:
          type: string
