# yaml-language-server: $schema=https://raw.githubusercontent.com/fern-api/fern/main/fern.schema.json

types:
  A: string
  C: string
  TwoService:
    properties: {}

errors:
  C:
    status-code: 400

services:
  http:
    OneService:
      auth: false
      base-path: /
      endpoints: {}
