openapi: 3.0.0
info:
  title: allOf Array Items Narrowing
  version: 1.0.0

paths: {}

components:
  schemas:
    PaginatedList:
      type: object
      properties:
        results:
          type: array
          items: {}

    Item:
      type: object
      properties:
        id:
          type: string
        name:
          type: string

    ItemList:
      allOf:
        - $ref: "#/components/schemas/PaginatedList"
        - properties:
            results:
              items:
                $ref: "#/components/schemas/Item"
