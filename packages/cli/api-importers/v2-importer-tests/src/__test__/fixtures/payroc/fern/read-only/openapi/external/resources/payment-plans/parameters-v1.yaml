openapi: 3.0.3
info:
  title: Payroc API
  version: '1'
paths: {}

components:             
  parameters:
    paymentPlanId:
      name: paymentPlanId
      in: path
      description: Unique identifier that the merchant assigned to the payment plan.
      required: true
      schema:
        maxLength: 48
        minLength: 1
        type: string
      example: PlanRef8765
