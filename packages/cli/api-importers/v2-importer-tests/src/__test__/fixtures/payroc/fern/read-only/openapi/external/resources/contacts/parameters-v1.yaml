openapi: 3.0.3
info:
  title: Payroc API
  version: '1'
paths: {}

components:
  parameters:
    contactId:
      name: contactId
      in: path
      description: Unique identifier for the contact.
      required: true
      style: simple
      explode: false
      schema:
        type: integer
      example: 1543
