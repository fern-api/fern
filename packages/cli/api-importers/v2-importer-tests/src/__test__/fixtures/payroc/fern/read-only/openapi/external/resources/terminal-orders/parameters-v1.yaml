openapi: 3.0.3
info:
  title: Payroc API
  version: '1'
paths: {}

components:
  parameters:
    terminalOrderId:
      name: terminalOrderId
      in: path
      description: Unique identifier of the terminal order.
      required: true
      style: simple
      explode: false
      schema:
        type: string
      example: '12345'
