openapi: 3.0.3
info:
  title: Payroc API
  version: '1'
paths: {}

components:    
  parameters:
    fundingAccountId:
      name: fundingAccountId
      in: path
      required: true
      description: Unique identifier of the funding account. 
      style: simple
      explode: false
      schema:
        type: integer
      example: 123
