openapi: 3.0.3
info:
  title: Test extension `x-fern-type`
  version: 1.0.0
paths: {}
components:
  schemas:
    BaseUser:
      x-fern-type: map<string, string>
      type: object
      properties:
        id:
          type: string
          format: uuid
