# yaml-language-server: $schema=https://schema.buildwithfern.dev/generators-yml.json
api:
  specs:
    - openapi: ../openapi.yml
  settings:
    # This is the convenience flag that should enable both respect-readonly-schemas and respect-writeonly-schemas
    respect-access-modifiers: true
