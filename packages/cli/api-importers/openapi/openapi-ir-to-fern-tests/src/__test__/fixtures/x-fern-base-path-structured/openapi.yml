openapi: 3.0.0
info:
  title: Acme API
  version: "1.0"
  description: The Acme API.
servers:
  - url: https://api.acme.com
x-fern-base-path:
  path: /{tenant}/{apiVersion}
  paths-include-base-path: true
  parameters:
    tenant:
      type: string
      docs: The tenant slug.
    apiVersion:
      type: string
      default: v1beta
paths:
  /{tenant}/{apiVersion}/widgets:
    post:
      operationId: createWidget
      parameters:
        - name: tenant
          in: path
          required: true
          schema:
            type: string
        - name: apiVersion
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Widget"
      responses:
        "200":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Widget"
components:
  schemas:
    Widget:
      type: object
      properties:
        name:
          type: string
      required:
        - name
