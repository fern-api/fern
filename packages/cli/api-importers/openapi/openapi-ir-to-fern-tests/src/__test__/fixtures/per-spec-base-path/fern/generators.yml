# yaml-language-server: $schema=https://schema.buildwithfern.dev/generators-yml.json
api:
  specs:
    - openapi: ../auth-api.yml
      namespace: auth
      settings:
        respect-per-spec-base-path: true
    - openapi: ../main-api.yml
      settings:
        respect-per-spec-base-path: true
