openapi: "3.0.0"
info:
  version: 1.0.0
  title: Test API
servers:
  - url: https://{tenant}.auth0.com
    x-fern-server-name: Production
    variables:
      tenant:
        default: "{TENANT}"
paths:
  /pets:
    get:
      operationId: list
      parameters:
        - name: limit
          schema:
            type: integer
      responses:
        "200":
          description: A response
          content:
            application/json:
              schema:
                type: string
