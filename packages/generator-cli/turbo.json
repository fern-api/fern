{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "compile": {
      "outputs": ["dist/**", "lib/**"]
    },
    "test": {
      "dependsOn": ["compile", "^compile"]
    }
  }
}
