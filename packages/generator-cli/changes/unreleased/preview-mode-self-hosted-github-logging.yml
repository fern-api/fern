- summary: |
    Self-hosted GitHub pull-request mode in preview mode now stops after the local commit with an INFO
    message explaining that changes were committed locally and push/PR creation were skipped. It no
    longer queries the GitHub API (existing-PR lookup, PR creation), which was unauthenticated in
    preview mode and surfaced as rate-limit errors.
  type: fix
