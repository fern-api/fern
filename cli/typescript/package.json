{
    "name": "birch-api",
    "version": "1.0.0",
    "description": "Birch API",
    "repository": "https://github.com/birch-solutions/birch-api",
    "author": "Birch Solutions, Inc.",
    "private": true,
    "workspaces": [
        "packages/**"
    ],
    "scripts": {
        "create-package": "ts-node ./packages/fe-scripts/src/monorepoCli.ts create-package",
        "clean": "yarn workspaces foreach --verbose --parallel run clean",
        "compile": "yarn workspaces foreach --verbose --parallel --topological run compile",
        "test": "yarn workspaces foreach --verbose --parallel --topological run test",
        "lint:eslint": "eslint .",
        "lint:eslint:fix": "eslint . --fix",
        "lint:style": "yarn workspaces foreach --verbose --parallel run lint:style",
        "lint:style:fix": "yarn workspaces foreach --verbose --parallel run lint:style:fix",
        "lint:monorepo": "ts-node ./packages/fe-scripts/src/monorepoCli.ts lint",
        "lint:monorepo:fix": "yarn && ts-node ./packages/fe-scripts/src/monorepoCli.ts lint --fix",
        "format": "yarn workspaces foreach --verbose --parallel run format",
        "format:check": "yarn workspaces foreach --verbose --parallel run format:check",
        "depcheck": "yarn workspaces foreach --verbose --parallel run depcheck",
        "prepare": "husky install"
    },
    "devDependencies": {
        "@types/node": "^17.0.23",
        "@typescript-eslint/eslint-plugin": "^5.17.0",
        "@typescript-eslint/parser": "^5.17.0",
        "depcheck": "^1.4.3",
        "eslint": "^8.12.0",
        "eslint-plugin-react": "^7.29.4",
        "eslint-plugin-react-hooks": "^4.4.0",
        "husky": "^7.0.4",
        "jest": "^27.5.1",
        "prettier": "^2.6.1",
        "stylelint": "^14.6.1",
        "ts-node": "^10.7.0",
        "typescript": "^4.6.3"
    },
    "packageManager": "yarn@3.2.0"
}
