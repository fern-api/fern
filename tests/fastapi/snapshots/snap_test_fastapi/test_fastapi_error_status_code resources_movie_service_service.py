# This file was auto-generated by Fern from our API Definition.

import abc
import functools
import inspect
import logging
import typing

import fastapi
import starlette

from ....core.abstract_fern_service import AbstractFernService
from ....core.exceptions.fern_http_exception import FernHTTPException
from ....core.route_args import get_route_args
from ..errors.invalid_movie_error import InvalidMovieError
from ..errors.movie_already_exists_error import MovieAlreadyExistsError
from ..errors.movie_not_found_error import MovieNotFoundError
from ..types.movie import Movie


class AbstractMovieService(AbstractFernService):
    """
    AbstractMovieService is an abstract class containing the methods that you should implement.

    Each method is associated with an API route, which will be registered
    with FastAPI when you register your implementation using Fern's register()
    function.
    """

    @abc.abstractmethod
    def get_movie(self, *, movie_id: str) -> Movie:
        ...

    @abc.abstractmethod
    def get_all_movies(self) -> typing.List[Movie]:
        ...

    @abc.abstractmethod
    def create_movie(self, *, body: Movie) -> None:
        ...

    @abc.abstractmethod
    def delete_movie(self, *, movie_id: str) -> None:
        ...

    """
    Below are internal methods used by Fern to register your implementation.
    You can ignore them.
    """

    @classmethod
    def _init_fern(cls, router: fastapi.APIRouter) -> None:
        cls.__init_get_movie(router=router)
        cls.__init_get_all_movies(router=router)
        cls.__init_create_movie(router=router)
        cls.__init_delete_movie(router=router)

    @classmethod
    def __init_get_movie(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.get_movie)
        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            elif parameter_name == "movie_id":
                new_parameters.append(parameter.replace(default=fastapi.Path(...)))
            else:
                new_parameters.append(parameter)
        setattr(cls.get_movie, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.get_movie)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> Movie:
            try:
                return cls.get_movie(*args, **kwargs)
            except MovieNotFoundError as e:
                raise e
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'get_movie' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        # this is necessary for FastAPI to find forward-ref'ed type hints.
        # https://github.com/tiangolo/fastapi/pull/5077
        wrapper.__globals__.update(cls.get_movie.__globals__)

        router.get(
            path="/movie/movie/{movie_id}",
            response_model=Movie,
            description=AbstractMovieService.get_movie.__doc__,
            **get_route_args(cls.get_movie, default_tag="movie"),
        )(wrapper)

    @classmethod
    def __init_get_all_movies(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.get_all_movies)
        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            else:
                new_parameters.append(parameter)
        setattr(cls.get_all_movies, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.get_all_movies)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> typing.List[Movie]:
            try:
                return cls.get_all_movies(*args, **kwargs)
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'get_all_movies' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        # this is necessary for FastAPI to find forward-ref'ed type hints.
        # https://github.com/tiangolo/fastapi/pull/5077
        wrapper.__globals__.update(cls.get_all_movies.__globals__)

        router.get(
            path="/movie/all-movies",
            response_model=typing.List[Movie],
            description=AbstractMovieService.get_all_movies.__doc__,
            **get_route_args(cls.get_all_movies, default_tag="movie"),
        )(wrapper)

    @classmethod
    def __init_create_movie(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.create_movie)
        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            elif parameter_name == "body":
                new_parameters.append(parameter.replace(default=fastapi.Body(...)))
            else:
                new_parameters.append(parameter)
        setattr(cls.create_movie, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.create_movie)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> None:
            try:
                return cls.create_movie(*args, **kwargs)
            except (MovieAlreadyExistsError, InvalidMovieError) as e:
                raise e
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'create_movie' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        # this is necessary for FastAPI to find forward-ref'ed type hints.
        # https://github.com/tiangolo/fastapi/pull/5077
        wrapper.__globals__.update(cls.create_movie.__globals__)

        router.post(
            path="/movie/movie",
            response_model=None,
            status_code=starlette.status.HTTP_204_NO_CONTENT,
            description=AbstractMovieService.create_movie.__doc__,
            **get_route_args(cls.create_movie, default_tag="movie"),
        )(wrapper)

    @classmethod
    def __init_delete_movie(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.delete_movie)
        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            elif parameter_name == "movie_id":
                new_parameters.append(parameter.replace(default=fastapi.Path(...)))
            else:
                new_parameters.append(parameter)
        setattr(cls.delete_movie, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.delete_movie)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> None:
            try:
                return cls.delete_movie(*args, **kwargs)
            except MovieNotFoundError as e:
                raise e
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'delete_movie' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        # this is necessary for FastAPI to find forward-ref'ed type hints.
        # https://github.com/tiangolo/fastapi/pull/5077
        wrapper.__globals__.update(cls.delete_movie.__globals__)

        router.delete(
            path="/movie/{movie_id}",
            response_model=None,
            status_code=starlette.status.HTTP_204_NO_CONTENT,
            description=AbstractMovieService.delete_movie.__doc__,
            **get_route_args(cls.delete_movie, default_tag="movie"),
        )(wrapper)
