default-group: local
groups:
  local:
    generators:
      - name: fernapi/fern-go-model
        version: latest
        config:
          module:
            path: github.com/example/organization
            version: "1.19"
        output:
          location: local-file-system
          path: ../../fixtures
