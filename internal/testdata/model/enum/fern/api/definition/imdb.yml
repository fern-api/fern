# Simple test for generating an enum.
types:
  Enum:
    enum:
      - value: ONE
        docs: "The first enum value."
      - TWO
      - THREE

  Something:
    enum:
      - one
      - One
      - ONe
      - ONE
