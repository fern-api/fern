# Simple test for generating a client with path parameters.
service:
  base-path: /users
  auth: false
  endpoints:
    getUser:
      method: GET
      path: /{userId}
      path-parameters:
        userId: string
      response: string
