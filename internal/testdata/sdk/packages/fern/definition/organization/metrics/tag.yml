types:
  Tag:
    union:
      number: integer
      string: string
      boolean: boolean

service:
  auth: false
  base-path: ""
  endpoints:
    postTag:
      path: /metrics/tag
      method: POST
      auth: true
      request: Tag
