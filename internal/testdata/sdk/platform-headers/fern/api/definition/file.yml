service:
  url: Core
  base-path: /file
  auth: false
  endpoints:
    download:
      path: /{filename}/download
      method: GET
      path-parameters:
        filename: string
      response: file
