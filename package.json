{
  "name": "birch-api",
  "version": "1.0.0",
  "description": "Birch API",
  "repository": "https://github.com/birch-solutions/birch-api",
  "author": "Birch Solutions, Inc.",
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "clean": "yarn workspaces foreach --verbose --parallel run clean",
    "compile": "yarn workspaces foreach --verbose --parallel --topological run compile",
    "test": "yarn workspaces foreach --verbose --parallel --topological run test",
    "lint:eslint": "eslint . --max-warnings 0",
    "lint:eslint:fix": "eslint . --fix --max-warnings 0",
    "lint:style": "yarn workspaces foreach --verbose --parallel run lint:style",
    "lint:style:fix": "yarn workspaces foreach --verbose --parallel run lint:style:fix",
    "lint:monorepo": "mrlint lint",
    "lint:monorepo:fix": "mrlint lint --fix",
    "format": "prettier --write --ignore-unknown --ignore-path ./shared/.prettierignore \"**\"",
    "format:check": "prettier --check --ignore-unknown --ignore-path ./shared/.prettierignore \"**\"",
    "depcheck": "yarn workspaces foreach --verbose --parallel run depcheck",
    "prepare": "husky install",
    "generate-fern-api-ir": "fern generate packages/api/src packages/api/ir.json && run fern-typescript model ../api/ir.json src/generated",
    "prepublish": "run generate-fern-api-ir",
    "publish": "yarn workspaces foreach --no-private --parallel --verbose npm publish --access public"
  },
  "devDependencies": {
    "@fernapi/fern": "0.1.2",
    "@fernapi/fern-typescript": "^0.1.4",
    "@fernapi/mrlint": "^0.1.3",
    "@typescript-eslint/eslint-plugin": "^5.17.0",
    "@typescript-eslint/parser": "^5.17.0",
    "depcheck": "^1.4.3",
    "eslint": "^8.12.0",
    "eslint-plugin-react": "^7.29.4",
    "eslint-plugin-react-hooks": "^4.4.0",
    "husky": "^7.0.4",
    "jest": "^27.5.1",
    "lint-staged": "^12.3.7",
    "prettier": "^2.6.1",
    "react": "^18.0.0",
    "stylelint": "^14.6.1"
  },
  "packageManager": "yarn@3.2.0"
}
